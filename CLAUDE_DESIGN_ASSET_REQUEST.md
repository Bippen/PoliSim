# Claude Design asset request — PoliSim

> **This file holds the LIVE ask and what stands beside it** - the current status, D21, the notice to Design, the count rule, what is costed and not yet requestable (§4), the standing conventions (§5), and the generated asset inventory. **Every answered ask moved VERBATIM on 2026-09-23 to `docs/archive/DESIGN_REQUESTS.md`** (`COMPLETED.md` §582): the earlier STATUS heads this one stacked, the ninth to eleventh requests with their annexes, §0's hand-derived delivered set, §E5, and D9 to D20 with D18's prose. Their request rows went with them, and `DesignNotificationCheck` reads the rows from both files.

## ⭐ STATUS 2026-09-21 (night) — **D21 ROW 1 IS INSTALLED (your 16a: the bolt and the olive ink - the bar reads 44 of 44 for the first time since the tab was born), YOUR 16b–16e ARE READ AND NOT YET BUILT, AND THE LIVE ASK IS A SITTING: THE WHOLE GAME, FILMED, FOR A READING OF THE DESK AS ONE - NOTHING ASKED AS A REDRAW** (`COMPLETED.md` §556, §560; the archive `SITTING_2026-09-21.zip`, E-42)

**16a, installed as delivered.** `send/icon_area_energy.png` verified by content (your MANIFEST's digest; 256 × 256 RGBA, one white tone on alpha, the 214 box on the 24-grid with its 21 px air, seated on the lower edge) and imported beside its ten siblings. The ink `#566217` and the lift `#8A9648` are in the palette; the electricity-tax law's area flipped to Energy in the same cut. Your two bars recompute here from the hexes and reproduce: 5.03:1 on the active tongue, 4.96:1 on the rail's desk ground; Political the nearest area ink. **Four things back from installing it, none a defect of the file:** (1) the README's split of the inked pixels is thirty off the file's on the same total, and the file carries a content-credentials chunk its ten siblings do not - both harmless; (2) the board's list of area hues is not this repo's on-paper inks' in OKLCH by ten to twenty degrees in several places - Political is the nearest neighbour either way; (3) **16a delivers no inactive tab-swatch tint** and says it derives *as the other ten do* - the theme's table holds six SNAPPED tints from pass 3 under a stated rule (the ink at oklch chroma ×0.78, lightness ×0.97), so Energy's was computed by that rule (`#535D28`), and the rule does not reproduce your six to the unit; (4) **your first rail flag measures a rail we did not build** - 1n-r3 as built draws every cell as a PAPER tongue, so the inactive glyph is that tint on paper, not the lift at 0.72 on the desk ground. (3) and (4) ride in the sitting with your second flag.

**16b, 16c, 16d - BUILT 2026-09-23 (`COMPLETED.md` §589); 16e - read, recorded, not built.** On the People page's five family plates: each family's declared key row stands in its header lane (Health · coverage, Education · attainment, Infrastructure · road quality, Environment · emissions per person, Immigration · poverty gap); a row opens in place on a click of its name, its state the sitting's; the supporting readouts rest as one caption line and open to the built five columns. Two things said rather than asked: the data rows have no 12 px strip of their own, so the dagger stands in the row's left margin before the name; and the desk has no row hover wash, so the dagger alone is the hover tell and the name's hairline is struck, as your board provides.

**The sitting.** One archive: every surface a player can reach, at 1280 and at 2560, from one real game - the play protocol's save loaded through the game's own Load and played forward by its own day path - each frame by digest with one line naming the screen and its state, and each proving the screen it claims. **What is asked is a reading of the desk as one: where it reads well, where it reads badly, what has drifted between boards built weeks apart. Nothing is asked as a redraw and no board is owed.** The head names what is known on the frames and ours, so you need not report it, and carries the composition ask's three rows so this is one paste: the Energy tab composed with its decisions as its spine; C2's three density rows with your 16b–16d standing as their answers; the rail's two token flags.


## ⭐ D21 — THE ASK (2026-09-18), IN TWO PARTS; PART 2 RE-CUT 2026-09-21

**Read against D20 first.** D20's three boards are answered and built (15a the energy page's plates against 14a; 15b the schedule row's grammar; 15c the statutory marks) and none of that is re-asked: the plates, the ledger, the instruments and the marks stand as you drew them. Part 1 is what the pass measured and could not draw; Part 2 says what is coming and gives you the page as it stands, so the ask that follows is drawn against real parts.

### Part 1 — asked now

| # | the row | what is asked | the measurement behind it |
|---|---|---|---|
| **1** | **`icon_area_energy` and the area's ink** | The rail has a ninth cell, ENERGY, and no art for it: `AreaIconCoverageCheck` reports `icon_area_energy` MISSING on every bar - by ruling, until yours lands (every cheap bar reads 42 of 43 meanwhile) - and the area has no ink of its own, so the cell and the page borrow the Sectors indigo as a stated stand-in. Asked: the icon in the `icon_area_*` idiom (white on alpha, RGBA32, the 24-grid the other ten sit on) and the area's ink pair (the paper ink at 4.5:1 on the card, and the lifted desk weight). | The rail's missing-icon fallback draws the caption's initial in the stand-in ink - a bare E fitted to the slot - which navigates but does not read as the others do. The rail at nine cells is filmed at 1280 and 2560, active and inactive (`COMPLETED.md` §536). |
| **2** | **The family's key figure in its header** | The People page's family header carries the name and, behind the tab, the publisher and vintage; it has no figure cell. Asked: a header form with the family's headline figure, so a family reads before its rows do. | §531, off the label tables and the pixels: a data row at rest is about 5 % ink; **the figure is what a player reads first** - the largest type in the row (17 units, bold) and the darkest ink in every data row (38, the page's black), in one column with one left edge - and today the first one a family shows is a row's, not the family's. Rows per viewport at 1280×699: 17 row names under 4 family headers, the pitch 36 px; at 2560×1419, 18 under 4, the pitch 66. |
| **3** | **A per-row disclosure** | A row's detail - the reach chips, the unit and source lines, the rank numeral, the honesty column, the seed foot - opens with the desk-global `†` and sits closed without it: every row at once, and the state persists across sessions (§9.1). Asked: whether a row's own detail should open in place, on that row, beside the global tab - and its form if so. | §532: at rest a row is one glance - 3 to 4 text draws and three graphics (the band with its tick and peer ticks, the pips, the sparkline); with the tab open every row carries its apparatus on every visit until the tab is closed. |
| **4** | **The extra row's form** | The health family's *Supporting readouts* row carries fifteen draws at rest - five sub-figures, each with a caption above and a unit line below - against three to four on any other row. Asked: what that row should carry at rest. | §531's ink table: the row's figure column reads 8.1 % ink against 3.1–5.6 % on the family's data rows, its band column 7.7 % against 0.0–4.5 %. |
| **5** | **The pie below its size, and the bar it becomes** | The distribution pie is a 120 px disc at every window, and 8a's inside-label rule (a share printed where the wedge clears 45°) is over its own chord at every filmed width, so the renderer draws the distribution as the plate grammar's stacked bar there - at every width this project films. Asked: whether the disc should scale with the window so the pie holds at size, or the bar is the form - and if the bar, its board (the legend's place, the OTHER's hatch on a bar). | §535: the inside share label needs 32.1 px at 1280 against a 28.5 px chord, and 53.6 px at 2560 against the same 28.5; both forms filmed at both widths. |

**Held back, not asked this round:** the selector card at the smallest window (§527's floor holds every Canvas line at the desk's caption floor and flattens two levels of the card's hierarchy at 1280) - a finding kept, to be asked against a played card if Elias's play says so.

### Part 2 — ARRIVING: the Energy tab's composition *(re-cut 2026-09-21 against the tab as it now stands; the text of 2026-09-18 described a page with one decision and three promises, and all three have landed)*

- **The tab exists and is reachable.** Energy is the rail's ninth cell (DS-4b overruled, §536) and the page moved whole to it: the layer's readouts - the plates you answered in D20/15a - under its own header, at both widths.
- **Its first decision is on it, and it now works in play.** Build and retire (§539, re-founded in §544): a line per technology with the fleet it has and its lead time (EIA AEO2023 Table 1: coal 4 y, gas 3, nuclear 6, wind 3, solar 2), a step down and a step up, the connection queue with each order's year placed and the year it connects or leaves. **The queue refuses what would not move, and each refusal is a sentence on the technology's own line with its steppers drawn disabled** - this is new since the first cut, and in Sweden four of the plate's seven lines are refusals: hydro (*THE WATER, NOT THE TURBINE, IS THE LIMIT*) and other (*NO SINGLE TECHNOLOGY, NO LEAD-TIME ROW*) everywhere; nuclear where the law has closed it (Germany: *CLOSED BY LAW · A BUILT MW WOULD NOT RUN*); coal and gas where the record holds no such fleet (France's and Italy's coal: *THE RECORD'S FUEL GROUPS DO NOT HOLD THESE PLANTS*) or where the zones clear without one (Sweden: *THE ZONES CLEAR WITHOUT A FOSSIL FLEET · NOTHING WOULD MOVE*). The cost row still reads **BILLED** - IRENA's 2024 costs are the CAPEX source and they are owed, so nothing is priced and the page says so.
- **The mandate stands under the queue** (§544): the country's own energy statute read for the player - a citation line (*THE MANDATE · BET. 2022/23:FIU21 · RIKSDAGEN*; Germany's reads *EEG 2023 § 4 · WINDSEEG § 1 · KVBG § 4*), the statute's figures with what the fleet and the queue reach against them (*WIND 145 GW · SOLAR 215 GW IN 2030 · COAL OUT BY 2038 · WITH THE QUEUE: …*), and a closing line saying that an AI state's ministry answers its own statute with orders like these and that this is HELD. Poland's reads BILLED (its plan was reached through secondary reports only) and the USA's says there is no federal statutory target to answer. **The AI ministry is built and held: no order of its is on any page yet.**
- **The four instruments are dials now** (§542) - *Retail intervention*, *Market liberalisation*, *Investment planning*, *State ownership*: the Energy sector's own four dials under the names the spec-let's S9 gave them, in the slider-row family you drew for D13 (end names under the track, the figure at the right, the range caption in the band while a knob is held), sharing one draft with the Sectors page and one bill, with the bill's status and its one button beneath them. They sit at the page's foot, under the readout rows that have carried the same four names since EN-6; two of the four (*Investment planning*, *State ownership*) reach nothing in the energy layer and their readout rows say so as ABSENT, in place. Where the retail stack carries no policy levy (the USA) the first dial keeps the sector's own name, *Subsidy*.
- **The law category is reachable from the tab** (§543): one faced chip between the readout rows and the dials - *THE ELECTRICITY TAX'S LAWS ▸* - with a line beside it saying what it opens and what it will find there (*OPENS LAWS ON THIS CATEGORY · 10 OFFERED · 0 ENACTED · 0 BEFORE PARLIAMENT*); drawn disabled with its reason where the country has no national statute (the USA). Filmed at rest and arrived (`filmpa3_1280_08_energy_laws_link`, `…_laws_link_arrived`).
- **What is still to come before the composition is asked:** a capacity on the connection queue (the ministry's first prerequisite, its own item - today an order of any size is accepted), the capital-cost row when IRENA's costs land, and the ministry's orders on an AI country's page once it is live.
- **The composition ask follows the D19 way** - against the built page with the decisions on it, real parts, not against the spec-let's text. Until then nothing in Part 2 is asked. **One fact about the page as it stands, for when you draw against it:** its order is the build order, not a composed one - the rule row and the stacks, the fleet and the zones, *Build and retire* (the order, the queue, the mandate, the BILLED cost), the water, the system cost and the incidence bridges, then the instruments' readout rows, the laws chip, the four dials and the bill. The decisions sit mid-page between readouts and the dials at its foot. The page is in the archive with its ABSENT rows honest: `Load growth · ABSENT · THE LOAD DOES NOT GROW WITH GDP OR ELECTRIFICATION · STATED, NOT MODELLED`, and the *Investment and retirement* row that read ABSENT since EN-6 is retired, because the decision exists.

**What is not asked.** The energy plates (D20/15a), the schedule row (15b), the statutory marks (15c), the plate grammar (9c/10a), the eight party inks (D17), the mark inventory (D18) - all answered and built; the pass touched none of their forms.

## ⚠ TO DESIGN, WITH THE NEXT RETURN — one screen you were asked to draw has been CUT

**Row `E17`, the *"as published"* band on Statistics › Domestic — GDP / unemployment / inflation as
published, with the date axis, release markers, the PRELIMINARY/FINAL badge, the dashed revision frame and
the 1yr/5yr/All pager. It is not being built. Please do not draw it.**

It was removed on **2026-08-29** as a display cut after a playtest, and **Elias ruled the cut permanent on
2026-09-01**. E17's row now says so. ⚠ **You were not told at the time, and the row went on asking for
three days.** That is our failure, not a change of mind late in the day — and it is recorded as a finding
of its own (`S-39`) rather than as a note on the row, because the row was one instance of it.

**Nothing you have drawn is wasted and nothing else in §E moves.** The publication MECHANISM is
load-bearing and untouched: the model still publishes lagged, revisable figures, the election model reads
the published series rather than the true state, and **B6's honesty conventions — the badge, the revision
frame — are alive on the main graphs.** They moved; they were not lost. If anything, the honesty channel
you drew for is now carried in more places than the band would have carried it.

---
**Standing rule: a count in prose is a cached value with no expiry** (working-discipline rule 12).
Before trusting any number in this document, re-derive it: sprites with
`find Assets/Resources/Art/UI -name '*.png'`, chrome coverage with `ChromeV2CoverageCheck`, stat icons
with `StatIconCoverageCheck`, portraits with `PortraitCoverageCheck`, deliveries with
`DeliveredAssetCheck`, importer state with `ImporterSettingsCheck`, screen inventories from the enums
(`StatNodeId`, `UiPalette.SystemArea`, `ConsolidatedTab`, `CabinetPortfolio` ×
`CabinetSystem.CandidatePool`, `CountryId`, and `PartySystems.BuildParties()` - `PartyArchetype` was RETIRED at W-G1 2026-08-30 and replaced by 53 real parties). This document has been the failure that
rule exists to catch at least four times.

---

## 4. Costed, NOT requestable yet — the next ask starts here (not sent)

Derived from the screens that exist and the one that does not (`COMPLETED.md` §187 §A.14;
`COMPLETED.md` §198 §D/§E):

- **Election night, Canvas screen 1h (item-10-gated, 13 Sept 2026).** Against the delivered set: paper,
  the ornate frame, the masthead, the `SEATS DECLARED` chip (`ui_chip`) and the party-ink swing figures
  (procedural, rule 10) all exist. **Two things do not:** one identity mark per SEATED party for the
  legend row — count unknown until the party seeds land (five `mark_party_*` on disk, rule 9a's
  original-art-never-the-registered-mark rule applies to every one) — and the verdict stamp, which is
  §A.11's generic procedural treatment unless Design asks for baked art at the 1h board. The R5 hex
  exchange (an ink-safe LP `DisplayColor`, Sweden's set) travels with the same gate.
- **Two `StatNodeId` icons** — youth unemployment and life expectancy — when those Society rows are
  promoted to nodes (`COMPLETED.md` §198 §E4); the promotion is ours and comes first.
- **A portrait for the sitting turn-0 Fed chair** (Harriet Ellsworth) only if the roadmap's
  sitting-chair-row question resolves toward one — the same envelope as the batch (512×640 @2×, the
  Portraits class, `portrait_fedchair_harriet_ellsworth`).
- **The roster beyond eighteen** — flagged 2026-08-25, not requested: more portfolios means more
  candidates and a larger batch later; a future ask for "twelve" or "fifteen" is a fresh request, not
  scope creep against the one just delivered.
- **The rail's six navigation glyphs redrawn small-size-first** — refused by board 1n-r2 (2026-08-28) as a
  costed follow-up: the captions under the cells answered the sitting's legibility finding; a redraw of
  the delivered `icon_nav_*` / `icon_area_*` set at 22 / 26 / 31 / 36 px is a fresh ask if the captioned
  rail is still not enough at the next sitting. Same envelope as the delivered set (24-unit grid, SVG).

Nothing else. The two built Canvas screens (1f the selector, 1g signing) needed nine pass-3 sprites and
consumed six of them; the three paper-canvas button states are delivered and unwired. Boards 1k and 1l
asked for no art.

---

## 5. Conventions — standing, unchanged; read before producing anything

### 5.1 Explicitly OUT of scope — please do not produce these

- **Anything the seven data renderers already draw**: axes, gridlines, tick marks, plot lines, threshold
  lines, bars, area fills, legends, sparklines, map shapes, policy-web nodes and edges, hemicycle seats,
  pie wedges, compass dots. All procedural, per working-discipline rule 10 — these render real tracked
  simulation data rather than a picture, and that is exactly what rule 10 protects. **Frames, plates and
  paper AROUND them are in scope; the data marks inside are not.**
- **Any sprite already delivered** — §0 lists the held stock. Check `Assets/Resources/Art/UI/` before
  producing anything; re-derive the count from the filesystem (rule 12).
- **Typefaces.** Already chosen, open-licensed and imported: TeX Gyre Pagella (display + body) and
  Courier Prime (document artifacts). Do not propose or supply fonts.
- **Cabinet portraits beyond the eighteen.** A larger roster later is a fresh request (§4).
- **`menu_pattern_tile.png`** — delivered, imported and wired (2026-08-02, `DrawCountrySelector` and the
  Canvas ground). **Do not re-request it.**
- **Pre-coloured trend arrows or badges.** Colour is applied at runtime; see §5.2.
- **Real-world agency logos, national statistics-office branding, or any real party's registered mark.**
  Rule 9 (split 2026-08-11) lets real INSTITUTIONS be named — parties, chambers, formulas — but people
  stay fictional, and rule 9a makes every party mark ORIGINAL ART, recognisable by silhouette and real
  colour, never the organisation's own mark (the S banner-not-rose and the Democrats' torch-not-donkey
  precedents). Someone else's trademark is not ours to draw.

### 5.2 Format & technical spec

**Tintable art — the default, and still binding.** PNG 256×256, 8-bit RGBA, transparent background,
**authored pure white** with all shape information in the alpha channel; tinted at draw time via
`UiPalette.DrawTintedIcon`; **no pre-coloured variants** — colours live in `UiPalette`/`PoliSimTheme` and
must stay there. The inks are the v2.0 aged set in `PoliSimTheme.cs` and `COMPLETED.md` §187
§A.3; re-read them from the code, never from a table in this document.

⚠ **THREE CATEGORIES ARE EXEMPT, and this is not a lapse in the convention.** **Country flags**,
**party emblems** (`emblem_party_*`) and **portraits** are authored in their own real colours — a flag is
not tintable, and a portrait is a painted bust (full-colour, opaque background, 512×640 @2× since the
batch of nine; the sixteen older ones are 256×256 squares). Any new art in those categories stays
full-colour; everything else stays white-on-alpha, **including `mark_party_*`**, which is tinted from
seed data at draw time (a rebrand is a data edit, never a redelivery). Getting this backwards in either
direction produces art that cannot be used.

**Icon authoring.** Renders small — a stat icon draws at **22px** on the contextual stat row: avoid thin
strokes and interior detail that will disappear. SVG source 24×24 geometry, `currentColor` fill, simple
primitives, mirroring the existing packs.

**Portrait authoring.** 1 source → 2 files (`.png` @2× + `.svg`); the roster frame is `ui_portrait_frame`
(RECT brass over the art) and the hero oval lives in `ui_portrait_frame_oval`'s alpha — **bake the art,
never the cutout**; no baked vignette. The register is Design's planar gouache (the PoC passed Elias's
register side-by-side 2026-08-26).

**9-slice frames (the IMGUI path).** Ornate frames, folders, plates and paper panels are drawn with
`GUIStyle.border`, a proven mechanism here. Deliver the **exact border inset in px per edge** with each
frame; corners must not stretch and the centre must tile or stretch cleanly. Author at **2× the largest
size it will render at** — every style rescales with `Screen.height`, so there is no single fixed render
size. **Non-rectangular edges must be baked into the alpha** — runtime masking in IMGUI is rectangles
only: a torn edge, a deckle, an oval portrait vignette, all baked, none masked. **No effect can be
applied at runtime** — shadows, grain, glow, blur and paper texture bake into the sprite; tint and
opacity are the only runtime adjustments.

**The Canvas path (narrative screens only).** Per-state single sprites (`normal` / `hover` / `pressed` /
`disabled` / `selected` — the scrollbar precedent; never a multi-cell strip, which `Resources.Load<Sprite>`
returns null for), 9-slice borders stated per sprite, transition timings per component; effects *can*
be runtime parameters here rather than baked. Everything in the tintable rule still applies. The
`canvas_*` namespace is retired — one `ui_*` namespace inside `Chrome/`.

### 5.3 Unity import settings — the defaults are actively harmful here

| Setting | Value | Why |
|---|---|---|
| Texture Type | `Default` | |
| `nPOTScale` | **None** (`0`) | **Matters most.** The default resamples a non-power-of-two sprite to the nearest power of two, silently altering the artwork |
| `alphaIsTransparency` | **On** (`1`) for white-on-alpha; `0` on the opaque portraits | Shape lives in alpha |
| sRGB | On (`1`) | |
| Filter Mode | `Bilinear` (`1`) | |
| Compression | **None** (`textureCompression: 0`) for white-on-alpha and tiling art; kept (`1`) for full-colour flags/portraits/emblems by ruling | Block compression mangles white-on-alpha at icon sizes; on full-colour art it showed no visible damage at display size |
| Mipmaps | **Off** (`enableMipMap: 0`) | UI sprites never minify — `ImporterSettingsCheck` errors on any |
| Wrap Mode | **Clamp** (`wrapU/V/W: 1`) | Correct for every sprite drawn once. **Tiling art needs `Repeat`** |
| `isReadable` | `1` for chrome (`UiPalette.GetTintedChrome` reads pixels back), `0` for icons and portraits | copied across the class boundary it cost an entire UI |

**COPY THE META FROM WITHIN THE SAME RENDERING CLASS, never from the nearest filename.** Three classes,
and the only thing that matters is what happens to the pixels between the file and the screen:

| Class | Members | Compression |
|---|---|---|
| **White-on-alpha, tinted** | `Chrome/`, `Icons/`, `Stats/`, **`mark_party_*`** | None |
| **Full-colour, untinted** | `Flags/`, `Portraits/`, **`emblem_party_*`** | kept, by ruling |
| **Tiling** | `Textures/menu_pattern_tile` | None, wrap Repeat |

`Emblems/` STRADDLES TWO CLASSES — `emblem_party_*` full-colour, `mark_party_*` white-on-alpha —
filename-adjacent and treatment-opposite; four marks once imported DXT5 from the wrong neighbour. **The
test is never "which file is next to it": does this art get tinted at draw time?** If yes, copy from
`Chrome/`; if no, from `Flags/` or `Portraits/`; if it tiles, the tile. A new portrait's meta is the
PoC's (`portrait_cabinet_defense_katarzyna_ekelund.png.meta`) with a fresh guid — the eight of
Progress5 differ from it in that line alone. `ImporterSettingsCheck` enumerates every PNG under
`Assets/Resources/Art/UI/`, classifies by treatment, and asserts against the **imported texture** — the
meta is the claim, the texture is the fact. The rulings behind these rows are `COMPLETED.md` §33.

### 5.4 Filename manifest and the naming rule

**Every filename derives from a real enum value in the code.** The game resolves art at runtime by
building the string from the enum, so a filename that does not match an enum resolves to null and draws
nothing.

| Pattern | Derived from | Example |
|---|---|---|
| `icon_area_<systemarea>` | `UiPalette.SystemArea`, lowercased | `icon_area_sovereignwealth` |
| `icon_stat_<statname>` | the displayable stat (`StatNodeId` via `GetIconName`), lowercased | `icon_stat_laborforceparticipationrate` |
| `icon_nav_<tab>` | `ConsolidatedTab`, lowercased — four of six; Budget and Politics reuse area icons by design | `icon_nav_policylaws` |
| `portrait_cabinet_<portfolio>_<name_slug>` | `CabinetPortfolio` + `IconLibrary.Slug(name)` | `portrait_cabinet_foreignaffairs_zofia_nakamura` |
| `portrait_fedchair_<name_slug>` | `Slug(name)` | `portrait_fedchair_priya_anand` |
| ~~`emblem_party_<archetype>`~~ | ⚠ `PartyArchetype` is RETIRED (W-G1); the four sprites are orphaned and their disposition is D8-1's | `emblem_party_centristcoalition` |
| `mark_party_<country>_<party>` | the party seed's mark name | `mark_party_us_lib` |
| `flag_country_<countryid>` | `CountryId`, lowercased | `flag_country_poland` |
| `ui_<control>_<state>` | control + state, one sprite per state | `ui_btn_brass_canvas_hover` |

`Slug()` = lowercase, drop every non-letter, spaces → underscores. "Wei-Lin Tanaka" → `weilin_tanaka`;
"Amara Osei-Bonsu" → `amara_oseibonsu`. **The check for a portrait batch is programmatic:** derive the
expected stems from `CabinetSystem.CandidatePool` by this rule and `diff` them against the delivered
stems — 0 missing, 0 unexpected, spelling exact — because a rename that looks like an omission has
happened before (`icon_crime` → `icon_area_crimejustice`).

⚠ **Enumerate the DISPLAY enum, not the storage struct.** The macro pack derived its stat list from
`EconomyState`'s fields — the right instinct — and still missed `InterestRate`, which lives on
`CurrencyZone`. It was structurally invisible to that derivation while being a headline figure on two
screens.

⚠ **Everything must live under `Assets/Resources/`.** `IconLibrary` uses `Resources.Load`, not
`AssetDatabase`. The flags and emblems once sat outside `Resources/` for weeks, fully delivered and
**unreachable by the game the entire time**. An asset's status has two parts, **delivered** and
**reachable**, and only the first is visible from the inbox — which is why every import ends with a
load through the game's own accessor, never with a directory listing.

**1 source asset → 2 files** (`.png` + `.svg`), as in every previous pack. No zip at the project root
means every delivery is imported and archived — a zip appearing there is the signal something is
unfinished, and `DeliveredAssetCheck` enforces it.

## ⭐ THE GENERATED ASSET INVENTORY - D18's block, written and read by `D18Inventory` (its ask and prose: `docs/archive/DESIGN_REQUESTS.md`)

<!-- GENERATED by D18Inventory.Emit (Tools/d18_inventory.sh). DO NOT EDIT BY HAND. source-digest: c0bd8ca3db0ab8b0ba5dae8af9230cd4fe3a5899a478a3c5ecbd30d13778660c -->
*Every figure in this section is EMITTED, never typed: `D18Inventory.Emit` runs the coverage
checks in-process and writes their own lines here. Regenerate with `Tools/d18_inventory.sh`.
`D18InventoryCheck` runs in the cheap bar and fails it when this block and the repo disagree,
so a figure here cannot go stale without the build going red.*

#### 1 · Party identity marks — `PartyMarkCoverageCheck`

```
  permanent mark_party_us_lib - ⚠ ARITHMETIC, not an omission. `PartySystem` seeds the USA by SEATS in the House, whose SOURCED size is 435 - and it seeds REP 220 + DEM 215 = 435, the WHOLE chamber. There is no seat for a third party to hold, so a Libertarian party cannot be seeded without taking a seat from one of the two that hold them, which would be inventing a result. The file is kept rather than retired: it is delivered art, and the day this model gains a vote-share dimension the USA's third parties live in, it is already drawn.
=== Party marks: 53 seeded part(ies), 53 with a resolving mark, 0 without one, 0 delivered-but-unconsumed (ceiling 0), 0 error(s) ===
```

**By chamber** (seeded parties, the marks that resolve, and what is left to cut; the rule
`mark_party_<iso2>_<abbrev>` re-derived against all 53 delivered marks on this run):

| country | chamber seats | parties seeded | marks resolving | to cut |
|---|--:|--:|--:|--:|
| USA | 435 | 2 | 2 | 0 |
| Sweden | 349 | 8 | 8 | 0 |
| Germany | 630 | 9 | 9 | 0 |
| France | 577 | 15 | 15 | 0 |
| Italy | 400 | 14 | 14 | 0 |
| Poland | 460 | 5 | 5 | 0 |
| **total** | | **53** | **53** | **0** |

**The batch, party by party** — seats are the seed's own, so the largest are the ones a
chamber shows most; the stem is the file the game will load once the seed names it:

| country | abbrev | party | seats at seed | stem |
|---|---|---|--:|---|

#### 2 · Portraits — `PortraitCoverageCheck`

```
  sitting   USA                  Harriet Ellsworth        no portrait (none requested; not counted)
  sitting   Sweden               Ingrid Hallstrom         no portrait (none requested; not counted)
  sitting   Poland               Marek Zawadzki           no portrait (none requested; not counted)
=== Portrait coverage: 25 of 25 pool members resolve through Resources.Load (18 ministers across 6 portfolios + 7 Fed chairs; 3 sitting chair(s) reported, not counted; NOT Portraits/ on disk) ===
```

#### 3 · Stat icons — `StatIconCoverageCheck`

```
=== UI art coverage: 21 of 21 names resolve, 0 reported GAP(s) (every StatNodeId icon + menu_pattern_tile; NOT chrome, emblems, marks or portraits) ===
```

#### 4 · Area icons — `AreaIconCoverageCheck`

```
=== Area icons: 11 of 11 resolve (SystemArea members as RGBA32), 0 missing, 0 damaged ===
```

#### 5 · Audio cues — `AudioCueCoverageCheck`

```
=== AudioCueCoverageCheck (P4-2): the cue catalog against the files, both directions ===
    cue                    file             state        note
    ButtonPress            Click1           ok           the pack's first click
    SliderStep             Click2           stand-in     the pack's Slider_Click_Sound is an AAC stream in an MP4 container, which Unity's importer does not read; SET ASIDE by ruling (E-10, Elias 2026-09-03) - the second click stands in, and stays a stand-in
    FolderSwitch           Folderswitch1    ok           the pack's first folder switch
    FolderReturn           Folderswitch2    ok           the pack's second folder switch
    BillPasses             ApprovalStamp    ok           the pack's approval stamp
    BillFails              -                NO FILE      the pack's RejectedBillTear is an AAC stream in an MP4 container, which Unity's importer does not read; SET ASIDE by ruling (E-10, Elias 2026-09-03) - no stand-in, a stamp or a click would say the wrong thing; the cue is wired and silent
    InterruptRaised        Folderswitch2    stand-in     the pack holds no interrupt sound; a folder landing on the Desk stands in, and stays a stand-in (E-10, closed by ruling 2026-09-03)
    ConstituencyDeclares   Click2           stand-in     the pack holds no declaration sound; a click stands in, and stays a stand-in (E-10, closed by ruling 2026-09-03)
    8 cue(s), 5 file(s) under Assets/Resources/Audio/Cues, 5 named by a cue.
    3 stand-in(s) [SliderStep (Click2), InterruptRaised (Folderswitch2), ConstituencyDeclares (Click2)] and 1 cue(s) without a file [BillFails] - reported as such by ruling (E-10, 2026-09-03), not accepted as covered.
=== AudioCueCoverageCheck: ALL ASSERTIONS PASS ===
```

#### 6 · Delivered and held — nothing in `Assets/Scripts/` can load these

*198 sprite(s) under `Assets/Resources/Art/UI/`. A file counts as REACHED when an
accessor the game calls actually loads it on this run (the texture's own name is taken as the
key, so no naming rule is duplicated here), or when a source file names its stem as a whole
token. The rest are below: delivered work with no way in. **They are ours to wire, not
Design's to cut again.***

| stem | folder |
|---|---|
| `ui_frame_double` | Chrome |
| `ui_portrait_frame_oval` | Chrome |
| `ui_stamp_draft` | Chrome |
| `emblem_party_centristcoalition` | Emblems |
| `emblem_party_conservativeunion` | Emblems |
| `emblem_party_nationalistfront` | Emblems |
| `emblem_party_progressivealliance` | Emblems |
| `mark_party_us_lib` | Emblems |
| `badge_revised` | Stats |
| `icon_release_marker` | Stats |
| `icon_stat_birthrate` | Stats |
| `icon_stat_budget` | Stats |
| `icon_stat_consumption` | Stats |
| `icon_stat_creditrating` | Stats |
| `icon_stat_currencystrength` | Stats |
| `icon_stat_deathrate` | Stats |
| `icon_stat_governmentdebt` | Stats |
| `icon_stat_homeownership` | Stats |
| `icon_stat_housepriceindex` | Stats |
| `icon_stat_housingcost` | Stats |
| `icon_stat_inequality` | Stats |
| `icon_stat_inflationexpectations` | Stats |
| `icon_stat_investment` | Stats |
| `icon_stat_netmigrationrate` | Stats |
| `icon_stat_population` | Stats |
| `icon_stat_productivity` | Stats |
| `icon_stat_realwages` | Stats |
| `icon_trend_down` | Stats |
| `icon_trend_flat` | Stats |

29 held, 169 reached.

**The runs behind this block**: PartyMark 0 · Portrait 0 · StatIcon 0 · AreaIcon 0 · AudioCue 0 — 0 is a clean check; a non-zero code means that family's own bar is red and this
inventory is describing a broken build.
<!-- END GENERATED -->
