# The claim convention and the working discipline, in full, as they stood on 2026-09-22

> Moved out of `POLISIM_FEATURE_LIST.md` (`COMPLETED.md` §579), VERBATIM. Both are now CONDENSED IN `CLAUDE.md`, where a session reads them at its start - each
> rule one to three lines with its § pointer. This is the long form they were cut from, kept because nothing is deleted that has not first been migrated. ⚠ If the
> two ever disagree, `CLAUDE.md` is the rule and this is the history: their narratives are `COMPLETED.md` §35, §181 and §190, which is where *"rule N"* resolves.

---

# THE CLAIM CONVENTION — ruled 2026-09-01, governs every document AND every source comment

⚠ **This is the head of the governing document because it outranks everything below it.** It was ruled on
the measurement in `COMPLETED.md` §190: **1,009 of 6,209 live claim-lines asserted a fact about
the code, and 557 asserted nothing else.** Six of those were demonstrably false at the moment they were
measured, and **not one was catchable by any of the checks in the bar.**

## The test

> **The code can change freely and no document becomes wrong** — only *incomplete*, and only where
> TRACKING has genuinely moved.

## Every claim is one of three things

- **INSTRUCTION** — what to do, what is ruled, what is owed. Timeless; never stale.
- **TRACKING** — what is open, whose it is, what it unblocks. Stale only when the work moves.
- **DERIVED** — a fact about the code or the environment: a type or member name, a line number, a count, a
  measured figure, a commit hash, a file path, a build status, a *"there are N of X"*.

## A DERIVED claim has exactly three permitted destinations

1. **GENERATED** — emitted by a tool into a marked block, re-derivable by one command, so it cannot be
   wrong.
2. **REFERENCED** — a pointer to **where the fact lives**, never a transcription. *"the party count is
   whatever `BuildParties()` seeds"*, never *"53 parties"*.
3. **DELETED** — it was decoration. **Most counts in prose are.**

## ⚠ Nobody transcribes, anywhere, in any file

**Not a count. Not a line number. Not a file path. Not a status. Not an environment fact.**

⚠ **This binds SOURCE COMMENTS exactly as it binds markdown, and the proof is that the worst instance was
in code:** `CheckSuite.cs` opened its enumeration comment with *"TWENTY-ONE"* while `Suite` held
twenty-five — and `POLISIM_FEATURE_LIST.md`, a document, had the number right. **Coupling is a property of
transcribed numbers, not of markdown.** The fix for such a comment is a **reference or a generated block —
never writing today's number in place of yesterday's.**

⚠ **An assertion about the environment that is not computed from the environment is forbidden outright.**
It is the class this convention exists to end — and ⚠ **the worked example is this paragraph, which asserted
one and was wrong.**

It read: *"`ERRANDS.md`'s glance instruction asserted the working copy is LF when it was CRLF."* **The
working copy is LF.** Measured `tr -cd '\r' | wc -c` per file: **zero carriage returns, everywhere.** The
review that produced the claim had probed with `grep -c $'\r'`, an empty pattern that matches every line,
and read a count equal to the file's line count as evidence of CRLF. `core.autocrlf=true` is real but
governs a **fresh checkout**, not this tree.

⚠ **So the instruction was RIGHT and the review was wrong, and the correction came within one edit of
flipping a correct statement — which is the exact failure the last line of this section forbids.** The
fault was never the value: it is that **a line ending was written down at all instead of being computed
where it is used.** A true transcription and a false one fail identically the day the environment moves.
**Derive it at the moment it is used, or say nothing about it.**

## Where generated blocks go

The shape is the one `ElectionsDataCatalogGenerator` and `GeneratedCatalogCheck` already prove, unchanged:

```
<!-- GENERATED by <Tool>. DO NOT EDIT BY HAND. source-digest: <sha256> -->
… emitted lines …
<!-- END GENERATED -->
```

Three properties, all load-bearing: a **DO NOT EDIT** stamp, a **digest of the source**, and **drift
detected by comparing the digest, never by re-parsing** — a second parser is a second thing to keep true.

## How a session updates a record without transcribing a fact

- Move a row's **state**; never restate the evidence. `✅ CLOSED at <hash>` is a pointer, not a figure.
- **Re-derive a board; never edit it forward** (the standing rule). ⚠ The roadmap's document table was
  neither re-derived nor edited — it was simply left, and it went silently wrong.
- When a figure is genuinely wanted in prose, **name the command that produces it** instead of its result.
- ⚠ **Never repair a stale fact by flipping it.** Derive it or delete it.

## The two historical records are exempt, permanently

`COMPLETED.md` and `CLAUDE.md` exist to say what **was** true. Naming a since-deleted member is them
working correctly. This is already `DocumentClaimCheck`'s ruling and it now governs the convention too.
⚠ There were three until 2026-09-02: the elections prototype log was retired into `COMPLETED.md` §185, so
it is exempt by being *inside* an exempt record rather than by being named beside one.

## What this does NOT license

⚠ **No new check may be written to enforce this** (R-N5 stands: a defect must cost something twice). The
review measured that the checks in the bar are already at the sensible limit of what scanning prose can
prove — **the convention works by nobody writing the claim, not by something catching it.**

---

**The audit era is closed by ruling.** The checks enumerated in `CheckSuite.Suite` stand as the bar and
keep running; no new check is written unless a defect has actually cost something **twice**. Everything below is work a player can
see or feel. Work down in order, continuously, one commit per item, R-SP1 push per item or coherent batch,
one report at the end of budget.

⚠ **This file is the governing objective.** `POLISIM_MASTER_LIST.md` is deleted and
`InstructionResidueCheck` is retired with it — the residue number was the audit era's goal and the era is
over. ⚠ **`POLISIM_MASTER_ROADMAP.md` is deleted too, ruled 2026-09-01** — its working discipline and
standing constraints are migrated to the foot of this file, its history to `COMPLETED.md` §181, and its
trigger shelf needed no migration because `COMPLETED.md` §197 §5 and §7 already carried it.
⚠ **The register is retired (2026-09-02).** `POLISIM_BACKLOG.md` was migrated verbatim into `COMPLETED.md`
§197 and `MISSING_PREREQUISITES.md` into §198; their open rows are indexed by pointer in this file's
appendix. **This file is now the only live list.** A row's evidence and ruling are read in the record;
where a pointer here and the row disagree, the row wins on facts and this file wins on ORDER.

## The era's one inheritance

- **The bar** — the checks `CheckSuite.Suite` enumerates (`CheckSuite.RunAllBatch`) and the simulation
  group `CheckSuite.Simulation` enumerates (`RunSimulationBatch`). ⚠ **The arrays ARE the enumeration;
  neither list is counted here.** They keep running and they keep failing when they should.
- **The standing rules**: R-T1 (a trigger carries its defence clause) · R-T2 (a shell probe is the weakest
  evidence and may not carry a finding alone) · R-T4 ("verified by digest" is not verification - a digest over stored dumps proves the files, not the model; a no-trajectory claim is carried by a run: the dump diff, and `TrajectorySentinelCheck` at every simulation bar - §496, §501) · R-T3 (an instrument fix is finished when every other
  consumer is enumerated) · S-36 (when a comment says a thing is dead, that sentence is the last place its
  name appears) · S-37 (every bound is a direction as well as a number) · S-38 (a prose claim about
  behaviour should name the instrument that checks it) · **measure the premise before fixing it** · **open
  the source rather than recalling it** · **evidence binds where the change lives** · **never tune to
  pass** · **never invent a figure** · **never lower a ratchet you have not cleared**.
- **`ERRANDS.md`**, and the outward-facing discipline around it.

## R-N5 — a new check is written only after a defect has cost something TWICE

⚠ **Recorded 2026-09-01, and it is a reversal of how the last week worked.** Instrument work is now a
**consequence of a real failure, never a prophylactic**. One instance is a fix; two is a class, and only a
class earns a check. A guard written for a defect that has not happened twice is a guess about the future
wearing the costume of rigour — and every one of them costs a suite slot, a session, and a reader's
attention for as long as the project lives.

## The two process findings the era leaves behind

- ⚠ **Committing on a red bar happened TWICE in one session.** Both were caught by the next run and fixed,
  and both were avoidable by reading the exit code before typing the commit. **The rule is one green bar
  per commit, no exceptions** — not "green when I last looked", green for the tree being committed.
- ⚠ **A safety rule over-applied looks careful while doing nothing, and nothing in the bar catches it.**
  E-4: a standing convention (R-SP1: sessions push fast-forward-only) was filed as an errand needing
  permission, on the reasoning that pushing to clear a red check would be tuning to pass. **The tripwire is
  not the thing being satisfied; it is the thing asking.** The note stays in `ERRANDS.md` for its shape,
  because the failure mode is invisible from inside: refusing to act reads as prudence in every log line it
  produces.

## The mechanical process rules — measured, not exhorted (2026-09-01)

⚠ **These are RULES ABOUT MECHANISM, not requests for care.** The class they govern has now cost this
project at least nine Unity runs and put twenty-four corruptions into committed documents, and every
instance was produced by somebody trying to be careful. **Care is not the missing input.**

- **P1 — no shell state across tool calls, and every file edit is ONE self-contained operation.** A
  variable set in one call is gone by the next: this spliced a block to line 1 of a document because
  `$idx` was `$null` and `$null+1` is `1`. And an edit assembled by shell quoting carries the shell's
  escapes into the file — **24 `''` sequences reached `COMPLETED.md` and `COMPLETED.md` §197 this way,
  with no compile error, no red bar and no exit code to catch them.** ⚠ **That is the worst member of the
  class precisely because nothing failed.**
- **P2 — one green bar per commit, and the bar is for the tree being committed.** Not "green when I last
  looked". Broken twice in one session; both caught by the next run, which is luck rather than process.
  ⚠ **Precondition, measured:** Unity refuses to run against a project another Unity process holds — Editor
  **or** a background batch. **Unity work cannot be parallelised**, and a bar started in the background
  blocks the next one.
- **P3 — what a bar costs is read, never written down, and what runs is the commit's tier.** Every bar
  appends its stages to `Logs/bar_timing.tsv`; the cost fixes and their measurements are `COMPLETED.md`
  §524 (and the digest manifest before them, 2026-09-01); which bar a commit owes is rule 1's tiers, not
  one bar for everything.

---


---

# THE WORKING DISCIPLINE — migrated from `POLISIM_MASTER_ROADMAP.md` at its retirement (2026-09-01)

⚠ **This is INSTRUCTION and it is why the roadmap could be deleted.** The rules below bound every pass
while that document stood and they bind every pass now. **Their history — the embedded corrections, the
narratives, the instances that taught each one — is `COMPLETED.md` §35 and §181**, and this text points
there rather than restating it. Numbered references to *"rule N"* across the record resolve against §35.

1. **Truth = real Unity, explicit project path, validation scaled to risk - and the risk is read off what the
   commit touches** (the tiers, ruled 2026-09-17; the measurement behind them is `COMPLETED.md` §524).
   `Tools/bar_tier.ps1` prints the tiers a commit touches, the paths that decided each, and the runs it owes
   (`-Staged` for the index, `-Commit <rev>` for a commit made). **A commit owes every tier it touches.**

   | tier | the commit touches | per item | at the track's close |
   |---|---|---|---|
   | **DOCUMENTS** | documents only | `CheckSuite.RunDocumentBatch` - the checks that read documents | - |
   | **TOOLING** | an Editor tool or a cheap-group check, and nothing the model or the screens run | the cheap bar | - |
   | **UI** | the screens: `Assets/Scripts/UI`, `Assets/Scripts/Testing`, art, scenes, the capture tools | the cheap bar + **the dry film** of the touched screens (`UiScreenshotCapture.RunDry`) at every width and country the item names, iterated until its guards are silent + **ONE filmed width** at the item's end | the four-width film matrix |
   | **SIMULATION** | anything the model runs: `Assets/Scripts` Simulation, Data, Elections and Persistence; the data folders and their prep tools; a check only the simulation group registers, or a tool one of those calls | the cheap bar + the simulation bar (the sentinel runs first in it) | the full trajectory dump with its old-beside-new diffs; the four-width film matrix |

   - ⚠ **The full four-width matrix, the full trajectory dump and the old-beside-new diff run ONCE, at the
     close of a track - never per item.** This has drifted twice and been restored twice (2026-08-28, and
     2026-09-17, when every BASELINE item was found dumping twice per item); the tiers and their tool are
     the mechanism, so a third drift has something to contradict it.
   - ⚠ **A UI item iterates on the dry film, never on films.** A layout iteration's findings are rects, and
     the dry film measures the rects: the overflow and containment guards, the ledger reach, and the label
     table (every text draw of a captured frame against its rect and its clip; a film and a dry film of one
     tree write it byte-identically). Pixels stay the film's alone - `ScreenEdgeCheck`, the identity token,
     the frame size - and so does the play path the dry film cannot take (it always sets the screen size
     the film reads from the screen), which is why a UI item still films one width before it closes.
   - ⚠ **A BASELINE item's per-item evidence is the sentinel.** When the sentinel moves, the item sets its
     digests from the check's own failure line in the same commit and says so; the family is dumped and
     explained per country at the track's close.
   - **The adversarial review is conditional** (ruled 2026-09-17). It runs for a BASELINE family and for
     anything that touches the fiscal book or a money flow - a spending line, a tax, a transfer, the debt, a
     ledger, a bill that is booked. It is skipped for UI, records and documents, and for anything else that
     moves neither money nor the trajectory unless the item's own sheet asks for it. `Tools/bar_tier.ps1`
     names the money paths it sees; **the record says whether the review ran and why.**
   - ⚠ **Since 2026-09-21 the bar says so too** (`COMPLETED.md` §546; §539 shipped a defect because this rule was
     written and not enforced). `ReviewLedgerCheck`, in the cheap bar, fails where a money path stands in a state no
     review covers, where a commit since the guard left such a state behind, or where the sentinel's digest has no
     reviewed row. A row in `Tools/review_ledger.tsv` names the state, the record and the reviewer's report, which
     is committed under `Reviews/`; `Tools/review_row.ps1` writes it. There is no waiver row.
   - ⚠ **Review at TRACK boundaries, not item boundaries.** A session works across items without stopping
     and reports at a track close or when budget runs low. Pre-rulings exist so no session stalls
     mid-track.
   - **Records-only work batches** into ONE commit. **One commit per item remains the rule for anything
     touching `Assets/`.**
2. **Never invent a figure.** Every real-world number carries **source, vintage and basis**; the
   `[VERIFIED]`/`[PROVISIONAL]`/`[ESTIMATED]`/`[PLACEHOLDER]` tags stay in force; the API cross-check gate
   applies to anything sourced.
3. **Visual work is built-not-confirmed until Elias sees it.** Everything visual lands in
   `COMPLETED.md` §198 §V with its capture named. ⚠ **A capture is a harness film, not Elias's
   eyes** — closing a visual item on film alone is the conflation this rule exists to catch.
4. **Forks.** Pre-ruled forks in a kickoff are binding. A new reversible fork → make the call, log one
   line. Stop mid-pass ONLY for an irreversible or expensive fork, or a blocking validation failure.
   **A ruling given in chat and not written into the owning document did not happen.** Reversals of
   standing rules are recorded explicitly, never silently.
5. **Commits.** One unit, one commit, descriptive message; stage by explicit path; confirm the staged
   contents match the message. Verify Unity has exited before a batch run.
   **R-SP1 (standing): sessions push at pass end, FAST-FORWARD ONLY.** `git fetch origin`; if
   `origin/main` is an ancestor of HEAD, push; re-fetch and confirm. ⚠ **Any non-fast-forward state, any
   lease or credential surprise, anything wanting a force flag: stop and hand Elias the exact state.
   Force is exclusively his.** `UpstreamCheck` is the tripwire, and **the tripwire going red IS the
   instruction to push** — refusing to act on it is not caution.
6. **A check is evidence only for what it enumerates — name the enumeration when citing it.**
   ⚠ **Do not build a third site-specific guard**; the three guard scopes stand as defined.
7. **The load-bearing invariants may not regress.** The appearance may change completely; the FUNCTION may
   not. They are catalogued in `CLAUDE.md`, and **the catalogue is the list — this line does not restate
   it or count it.**
8. **A failing validation stops its DEPENDENCY CHAIN, not the pass.** Fix it, or shelve the item with one
   line and continue with independent tracks. **Never build on top of a failing step.**
9. **A status about the outside world is a cached value.** Re-derive from the filesystem, the callers and
   `git log` before reporting anything outstanding. ⚠ **Boards are re-derived, never edited forward** —
   and *"left alone"* is not re-derived either.
10. **Stop at coherent boundaries.** When budget runs low, drop whole units in the stated priority order.
    **Never thin rigor; never leave a unit half-validated.**

## The process rules — ruled standing 2026-09-01

⚠ **These are not carelessness to be fixed by care.** Each was bought by measured, repeated loss, and the
measurement is `COMPLETED.md` §190 §B.3.

- ⚠ **NO SHELL STATE ACROSS TOOL CALLS.** Every invocation is complete in itself: no variable set in one
  call and read in another, no reliance on a working directory, no chaining a cleanup to a run where a
  short-circuit can skip the run while reporting success.
- ⚠ **EVERY FILE EDIT IS ONE SELF-CONTAINED OPERATION, MATCHED ON TEXT — never on a line number or a
  walked position.** A walk-back that never matches runs to the top of the file.
- ⚠ **Prefer a direct file write to a shell heredoc for anything containing prose.** *(This rule caught
  its own twenty-fifth instance inside the review that proposed it: the report died on broken quoting as a
  heredoc and succeeded as a file write.)*
- ⚠ **ONE GREEN BAR PER COMMIT, NO EXCEPTIONS** — green for the tree being committed, not "green when I
  last looked". **Precondition, and it is why this rule gets skipped: the bar CANNOT run while the Unity
  Editor is open** — Unity refuses a second instance on the same project. A bar run means the Editor is
  closed first.
- ⚠ **The dominant failure mode is that a wrong invocation and a right one look identical in their
  output.** A bar can pass in seconds having run nothing; a capture can exit 0 having filmed the wrong
  screen. **Check what a run actually did, never that it merely exited.**

## Standing constraints — rules that bind every pass, not tasks

- **Numbering is stable.** Master Sequence items and Steps are cited throughout the record and the code;
  **never renumber.** ⚠ Two distinct items carry "9" — disambiguate by name when citing, never by number.
- ⚠ **THE CRITICAL CORRECTNESS RISK — published vs live.** The player-facing UI reads the **PUBLISHED**
  (lagged, possibly-revised) series; every internal system keeps reading **LIVE** values. A leak makes the
  model consume its own stale output, and **the effect may not appear for hundreds of turns.** The
  enforcement is the one-directional rule: `PublicationSystem` writes `Country.Published` and reads
  `Country.State`, **never the reverse.**
- **`[GAP]` figures are Elias's to source, never to invent.** The variant-axis rule governs any
  re-sourcing.
- **ONE NATURAL RATE** (ruled standing 2026-09-08, `COMPLETED.md` §399). Every reader of an unemployment gap reads
  `Country.EffectiveNaturalUnemploymentRate` - the seeded rate as the labour-institution laws compose it plus the labour
  force's demographic shift - never `NaturalUnemploymentRate` alone: Okun's reversion, the Phillips curve, the Taylor rule,
  the discouraged-worker, poverty, crime, approval, real-wage, hoarding and rating terms, the preview, the UI, the probes.
  ⚠ **A natural rate two readers disagree on is not one**: §398 measured what a split does - a 0.2-point composition
  shift read as tightness by one term and as nothing by the next became ±30 % of productivity in five centuries. A new
  gap reader takes the effective figure or does not land.
- **Calibration stays at turns 100–200; the long horizon is a diagnostic, never a target.** Judge a fix by
  whether the mechanism is present and correctly signed. ⚠ **The word "equilibrium" stays banned without a
  run that earns it.**
- ⚠ **SCREEN GRANULARITY, NEVER ELEMENT GRANULARITY.** IMGUI composites as one flat rectangle and no
  Canvas render mode draws above it; a screen is either Canvas or IMGUI, **never both interleaved.** Any
  request that violates this silently **is a request to migrate that screen wholesale to Canvas**, and
  should be recognised as such rather than hacked around.
- **Spec references:** anything row-shaped cites `COMPLETED.md` §187 by section. ⚠ **Every number
  that spec supplies is suspect until derived or explicitly confirmed** — its own banner says so.
  **Declare deviations from the boards rather than diverging silently.**
- **One row type per capture for NEW row types, never batched** — a re-capture of an already-shipped row
  may batch. The rule exists to catch first-build defects.
- **The guard scopes are fixed:** `UiOverflowGuard` (does text fit its rect) · `UiContainmentGuard` (does a
  child rect sit inside its container) · `ScreenEdgeCheck` (pixel lines per PNG, flushness not magnitude,
  at the captured resolutions only). ⚠ **Do not build a third site-specific guard** — a GUILayout-aware
  check needs IMGUI internals; the pixel check is cheaper, exists, and asks the question the player
  experiences.

## The three-way test every task gets

1. **Finished?** → `COMPLETED.md`, then delete from source.
2. **Waiting on a named party?** → `COMPLETED.md` §198, then delete from source.
3. **Neither?** → it stays live.

⚠ **"Built but unconfirmed" and "built but uncalled" are case 3, not case 1** — or case 2 when the only
thing missing is Elias's eyes, which is a named party. **These are the two states this project keeps
mistaking for done**, and every consolidation pass so far has found both again.

## The root documents, charted (2026-09-07, `COMPLETED.md` §356 - the retired roadmap's table, re-homed here as the governing document's own)

`ls *.md` at the root is this table, no more and no less; a file added at the root is added here in the same commit, and a file retired is migrated to `COMPLETED.md` verbatim first (C-G1's rule).

| document | job | grows or shrinks |
|---|---|---|
| `POLISIM_FEATURE_LIST.md` | the governing document: the working discipline, every open row, the fix tracks, this table | shrinks as rows close |
| `COMPLETED.md` | the record: finished work, rulings, findings, retired documents verbatim | grows |
| `CLAUDE.md` | the detailed technical record, never superseded | grows |
| `ERRANDS.md` | every outward-facing action waiting on Elias, one line each | shrinks |
| `CLAUDE_DESIGN_ASSET_REQUEST.md` | the single standing ask to Design (D15 live), earlier asks carried beneath | appended, emptied on delivery |
| `docs/outbound/SEND_PACKAGE.md` | the current paste-and-glance package for Design | regenerated |
| `docs/specs/ELECTIONS_CAMPAIGN_SPEC.md` | **REFERENCE** - the 44-section elections specification cited by §; says so at its head | frozen |
| `docs/specs/TAX_SPEC.md` | C-C12's spec-let: the tax instruments and the sourcing bill - RULED 2026-08-31 (D-3), its sheets §474 | F4-1, F4-2 and F4-4 built; F4-3 next |
| `docs/specs/ENERGY_SPEC.md` | **REFERENCE-AND-PLAN** - the external energy concept adapted to this game: the collisions, what is kept, the eight stages; its source docx is external reference material, not on disk (§451) | frozen; its provenance line changes if the docx lands |
| `docs/specs/ENERGY_SPEC.md` | stage 1 of the energy track: the collision map against `EconomyState` and the environment family, the load-block and zone form, the sourcing bill with every series named, the sizing, the design S1–S15 - **RULED 2026-09-10 as recommended (E-34, §454)**; Design's map prototype recorded in its §3.2 as evidence (§452) | frozen; its status head carries the ruling |
| `docs/generated/ENERGY_LAYER_PREMISE.md` | GENERATED by `EnergyLayerDump` (§457, §459): the physical layer at its seed - the six fleets with the definition named, the ten load folds, Sweden's zones and links with the flows, the single-book decomposition per country - and the market at its seed: every block's clearing, the adders, Sweden's zones against the NTCs, snitt 4's answer | regenerated |
| `EnergyData/` (nine CSVs + `Tools/energy_prep.pl`, `Tools/energy_market_prep.pl`) | the energy layer's derived data files - stage 2's six and stage 3's three (the dispatch levels per zone and block, the merit order's inputs, Sweden's interconnectors), generated by the prep scripts from the sources the spine lists, read by `EnergyCatalogGenerator` into `EnergyLayerData`, held by `GeneratedCatalogCheck` | regenerated when a source lands or changes |
| `docs/data/ENERGY_LAYER_SPINE.md` | stage 2's data spine (§455): generation, fleet, the load folded into the three blocks for six countries and SE1–SE4, Sweden's zones and links from the authorities, fuel and carbon prices, the retail stack, the HICP weights, the electrification stock, the EDGAR residual's first reading, the two BILLED rows, every file by digest | grows by section as stages 3–4 source their rows |
| `docs/data/SOCIETY_STATS.md` | P5-C1: Elias's metric list mapped to the six countries' sources | grows with families |
| `docs/data/SOCIETY_STATS.md`, `docs/data/SOCIETY_STATS.md`, `docs/data/SOCIETY_STATS.md`, `docs/data/SOCIETY_STATS.md`, `docs/data/SOCIETY_STATS.md` | the five families' data spines: seeds with their sources, couplings, fetch outcomes, feedback passes | grow by section |
| `docs/generated/BUDGET_PREMISE.md` | GENERATED by `BudgetPremiseDump` (P5-B1): what a line and a tax are, probed | regenerated |
| `docs/generated/POTENTIAL_PREMISE.md` | GENERATED by `PotentialPremiseDump`: the premise of potential output, measured before and after P5-B7, re-measured after §394 (`potential03`) and §522 (`potential04`) | regenerated |
| `docs/play/PLAY_PROTOCOL.md` | CL-3's play protocol (§523): the seed stated (777; the 2022 prior until K-1), the four saves and what each opens on, the play's five steps, the sheet, the record | frozen until a play changes it |
| `docs/play/PLAY_SHEET.md` | GENERATED by `PlaySheetGenerator` (§523): §346's twenty entries with each constant read off the code by reflection and its one thing to look for; the last column is Elias's and survives regeneration; `PlaySheetCheck` holds the rest | regenerated; the last column by hand |
| `docs/generated/LEVER_MAP.md` | GENERATED by `LeverMapDump`: every quantity and what reaches it; the unreached set is P4-C3's coverage plan | regenerated |
| `docs/data/COUNTRY_CENTROIDS.md` | the six countries' centroids and the map's adjacency (board 6b row 1) | frozen |
| `COMPLETED.md §580` | the four-item sequence (taxes by bracket, pension age and payment, the energy page, the campaign loop) and D20, with the ten sheets ruled (§474); re-read, not re-typed - a session that lands a row marks it there in the same commit | shrinks as rows land |

Retired 2026-09-07 into §356 and deleted: `CLAUDE_DESIGN_BOARD_1I_NOTE.md` (a courtesy note superseded by seven later packages), `E12_PARTYINK_PRINT.md` (the ink print, its return answered). Retired 2026-09-07 (afternoon) into §369a and deleted: `PLAYTEST5_ANNEX_E16.md` (Elias's Playtest 5 annex - input consumed by the catalog and the six families, so its job was history). Off the root and charted here so the code's citations resolve: `ElectionsData/DATA_BILL.md` (the elections data bill, cited by `CampaignAiHarness` and `LiveCampaignSetup`) lives beside the CSVs it bills. One name in a code comment matches no file and says so itself: `Country.cs` cites *"COMPLETED.md section 12 (was STEP_A_LIVE_VALUE_AUDIT.md, consolidated 2026-08-02)"* - history stated as history.

## Rules learned from consolidating the documents four times

*The instances that taught these are in `COMPLETED.md` §181 and the sections it points at. The lessons are
here; the evidence is there.*

- **Verify against the repo and the commit history, not against a summary.** A summary is exactly where a
  false "DONE" hides.
- **Check callers before believing a feature exists.** Code has shipped with zero callers more than once.
  `grep` for the call sites; do not assume the wiring landed with the code.
- **If removing finished items empties a document, delete it.** An empty shell drifts back into use.
- ⚠ **Do not duplicate a live list into another document.** Two copies of one list is the drift these
  passes exist to undo — and the roadmap was retired partly because its trigger shelf duplicated
  `COMPLETED.md` §197 §5.
- **Repoint references before deleting a file, and grep afterwards to prove nothing dangles** — source
  comments included.
- ⚠ **A document can assert two states of one task at once.** When a live document is edited, search it
  for the task's other mentions before saving.

## The closing section — what is left, whose it is, what each unblocks (2026-09-07, evening; `COMPLETED.md` §381)

**The residue reads zero** (`ResidueCheck`, re-read after the three rulings of §382: `bar228_close2_RunAllBatch` read one open row, FT-7 - the row the rulings themselves opened, ruling-first with no recorded recommendation - which is named in the exclusions as Elias's with its reason; `bar228b_close2_RunAllBatch`, 33 of 33, 384 files scanned, reads **0**, FT-6 and FT-7 printed as excluded and not counted): no CODE row a session could start. That is not "finished". Every row below is open and owned; the count excludes each by name with its reason, and this section is the list the count points at.

### OWNER ELIAS

| row | what it is | what it unblocks |
|---|---|---|
| **E-1** (`ERRANDS.md`) | ✅ DONE 2026-09-10 (§442–§445) - the waves landed; per-group loyalty built; the FdI test re-run, NOT REACHED | F3's per-group loyalty for Italy - the one input the elections model lacks |
| **E-3** (`ERRANDS.md`) | the RETURN paste to Design and the glance | the seven Swedish marks then the batch, the two Society icons, the valkrets cartogram - all consumed on landing, nothing queued behind them |
| **E-6** (`ERRANDS.md`) | Italy's IRPEF *scaglioni* as a text a fetch can read | the last open figure of F4's bill |
| **The §V sitting** and the two felt verdicts (E-2..E-4 of the register) | 54 rows read by a person; decision density and the Trade bill's costs, played | the judgements no harness film can give |
| **The 20 play-calibration entries** (E-5..E-24; `COMPLETED.md` §189) | constants judged by playing | the loop's feel - each entry says what must not be tuned meanwhile; two of its names are stale and recorded (§380) |
| **E-34** (`ERRANDS.md`, §451, ruled §454) | ✅ **RULED 2026-09-10** - S1–S15 as recommended; stage 2's sourcing landed the same night (`docs/data/ENERGY_LAYER_SPINE.md`, §455) with Sweden's links from Svenska kraftnät and the loads without an ENTSO-E account. Still Elias's: the source docx's whereabouts; optionally IRENA's 2024 costs PDF (403 to this machine). **Stage 2 BUILT the same night (§457)** - the physical layer as readouts, the five gates on the bar; **stage 3 BUILT 2026-09-11 (§458–§460)** - the dispatch writes the power figure; **stage 4 BUILT 2026-09-11 (§461–§463)** - the retail stack and the two ledgers, the confidence proxy retired | **EN-4c** (the carbon tax's unit, ruling-first), **EN-5** (the S8 pass-through), EN-3b; then stages 5–8 |
| **E-25..E-29** | C-C6's basis, C-C11's recalibration lines (FT-1 held, FT-4 fixed), the tax spec-let, C-D2's pool, C-D3's språkrör | F-7's instruments: the spec-let RULED (D-3) and built as F4-2 and F4-4, F4-3 next; the rest execute as written unless struck |
| **T-3** (§376) | the identity's government-consumption block - the six P3_S13 shares are in hand, the form of G in the identity is the ruling; **2026-09-15 (§503): "measure before I rule"** - both forms MEASURED (§505: each form's implied potential and year-100 path per country, both seeds), neither built; the tables are in front of Elias; **RULED form A 2026-09-15** with two conditions - the first, the preview path probed before it lands, TAKEN as its own item (§506: the clone audited by field and three preview defects fixed, the preview's identity G the boundary's in six); **✅ LANDED §507** (baseline `t3a`); ⚠ **The USA's reading is ONE source** (the ruling's condition): P3_S13 is GENERAL government (13.54 % of GDP, BEA NIPA A955RC ÷ A191RC, 2023) against the model's FEDERAL lines (6.04 % at the seed; BEA's federal consumption A957RC 4.82 %); the World Bank's NE.CON.GOVT.ZS is BEA's own ratio, so the second reading §376 counted is one source in two copies; BEA, billed at §376, is fetched now and is the reading, and no independent reading exists; the USA is the country the change moves most (k 2.2426) | P-N5b, Okun on the output gap (FT-2's fix) - next in the fixed order **→ measured and stopped (§508), RULED (d) hold (§513)**; the harness's impact reading RULED and re-based (§512) |
| **T-3's impact reading** (§507) | ✅ **RULED AND RE-BASED 2026-09-15 (`COMPLETED.md` §512): swept across k from 1 (the old dump exactly - 76 of 76 fields byte-identical, six comparisons) to the seed, the impact curve is CONTINUOUS on all three dials (Sweden's k 1.0000 → 1.2526: −10 % 0.5079 → 0.4984), the mechanism real; the ratchet re-based to 0.4984 with the curve in its doc, the harness exits 0.** As sheeted: the block lowers the harness's landing-year spending multiplier on its own purchases: form A reads 0.4984 at impact on the weakest dial against the ratchet 0.5074, the tree before the block (k = 1, the same instrument) 0.5079; not the AI ministry (off, the same gap), not the basis (on the book's lines the band breaks at L+4 instead); every cumulative L+1 and L+4 reading inside Ramey's band. **The ratchet is not lowered** and the harness exits 1 on this tree (it is on no bar). Whether form A's reading becomes the ratchet by ruling, or the mechanism is found first | the harness green again; a ratchet that means what it says |
| **P-N5b, the gap Okun reads** (§508) | ✅ **RULED (d) HOLD 2026-09-15 (`COMPLETED.md` §513): the growth form stays; the reason is the circularity - Okun would read a gap whose construction includes employment (potential's labour input is 1 − U, FT-7's second seat), so unemployment would answer to a quantity built partly out of unemployment; FT-7's formula stands; (a), the identity anchored to potential, is the next pass's question.** As sheeted: Okun on the output gap MEASURED AND STOPPED: after T-3 the level gap is still the identity's settling point against a factor potential, never within 2 points of zero in 5 of six countries, and co-moving with the unemployment gap with the wrong sign; at the sourced range the level form turns it into unemployment (1.6 to 11.8 points above today's at year 100 outside Sweden) and inflation at its floor. The sheet: (a) the identity anchored to potential (a macro-core family), (b) Okun on a cyclical gap (a second potential), (c) the deviation form (does not remove the drift), (d) hold - recommended (d) now with (a) as the deciding question | P-N5b; the AI ministry's output-gap-adjusted balance (§390); the central bank's gap lane weighed |
| **D16 board 10d** (§409, built §426) | ✅ **BUILT 2026-09-09, on the RECORD's palette.** The row opened because the bundle asserted a palette ruling that is not in the record; it closes because §423's bloc fence shut the collision the board was drawn to answer **inside §256**, which un-stopped the geometry without taking the assignment. The chamber now draws as the board drew it - nine rings r 110–220, Ø 9 on every seat, party gap 1, bloc gap 3, the bloc arc at r 238, the majority tick between the seat that carries the chamber and the one before - and the caption under it is derived every frame from the drawn order. ⚠ The eight assigned colours are still **not taken**: they are D17's item 2 and Elias's | nothing - the last D16 board is drawn; what remains of D17 is one ruling, and of D18 one copy (E-31) |
| **The AI finance ministry, what still waits** (§387, §388, §390) | built and ruled; two things wait on sources: a US figure for a sequester's size as a share of GDP (the 0.5 % is tagged borrowed), and T-3's output-gap-adjusted balance for the safeguards' structural reading | the sequester's size struck on a source; the structural reading, with T-3 |
| ~~**Laws for the dials-only set** (§378)~~ | ANSWERED by §474 - stage 5's category its own item, the wider set one category a session | EN-7b |
| ~~**SC-1** (§498)~~ | **RULED 2026-09-15 (§503), BUILT (§504)** - the Business-and-industry line; the seed-relative clamp bounds the line's own path, the applied dial cost outside it | done |
| **The infrastructure level form** (§374) | λ billed - IMF WP/22/95's coefficient, unreachable from a session | the quality-to-output-level feedback, if wanted back |
| **PISA, the graduation rate, congestion** (§373) | three series billed with the reason a session cannot reach them | three plate rows printing figures instead of BILLED |

### DESIGN

| row | what it is | what it unblocks |
|---|---|---|
| **D-7** | board 2b, the Policy Web drawn to be read | the web as a picture rather than a list |
| **D-8.1** → **D18** (§419, §420, landed §429) | ✅ **LANDED 2026-09-10 - 53 of 53 parties resolve a mark** (E-31 closed, §429). The row as it stood: the party identity marks, **carried into D18 and re-cut as four runs by chamber** - the count is no longer written down anywhere: it is emitted from the seeds into D18's generated block and guarded by `D18InventoryCheck`. Sweden's eight and the USA's two resolve; the batch is what is left | four chambers - the seat arc, the legend, the stance rows and the campaign masthead draw a party without an identity in each of them |
| **D-8.2..D-8.6, D-E4** | five countries' party colours (a ruling), the valkrets map (✅ **D-8.3 DRAWN 2026-09-10**, board 4a on election night, §438), election night's paper, the verdict stamp, modal-or-stage, the two Society icons | each its screen's finished face; all eleven D9 rows are answered and the batch waits on E-3 |
| **D-1 portraits** | ⚠ **MEASURED CLOSED 2026-09-09 (§420)**: an appointed minister renders a DELIVERED face - `PortraitCoverageCheck` reads 18 of 18 in the cabinet pool and 7 of 7 chair candidates resolving, and the procedural silhouette is reached only for a name added to a pool without art. The row read the other way for as long as it was true and then some | nothing - the faces are on disk; the three sitting turn-0 chairs have no portrait and no surface draws one |
| **D18** (§419, §420, answered §424, landed §429) | ✅ **LANDED 2026-09-10 - E-31 closed.** Design's board 11b made the batch a seed table over fifty neutral cells already cut: 43 rows, 15 distinct cells, the hatched half unspent for D8-2. The fifteen arrived as a delivered pack and were copied to the 43 stems disk to disk; every destination is byte-identical to the cell the rules assign it (checked against the rules and against Design's own table, all 43), every PNG passes the CRC walk. The seeds are wired and `D18MarkAssignment` asserts the wiring against the derived rules; coverage 53 of 53 both ways; filmed in all four chambers at 1280 and 2560. §430 fixed the two bloc devices a chamber with no blocs exposed | four chambers' identity - drawn now |
| **D17** (§416, answered §423, closed §447) | ✅ **CLOSED 2026-09-10.** Item 1 BUILT (§423): the bloc fence inside §256, 12 of 12, floor raised to match. Item 2 - the eight colours as a proposal - **RULED NOT TAKEN**: Elias saw the laddered hemicycle and ruled the published-hue-plus-lightness-rule palette the permanent answer (§256/§279/§423's lineage, dated); the collision the assignment was drawn to answer is resolved inside the standing ruling, and the proposal is filed in §447 as the alternative it always was, with its source and its cost. Design learns it in the E-32 return, with thanks - the mechanism they proposed on 11a is what shipped. The hatched half of the fifty-cell sheet stays unspent and waits on nothing. | nothing - the palette is settled; D19 is the only open Design row |
| **D20** (§474 opened, assembled §483, the paste E-35, answered 2026-09-13) | ✅ **ANSWERED 2026-09-13 (boards 15a–15c); ALL THREE BUILT - 15a §486, 15b §490, 15c §491.** **The return (E-36) answered 2026-09-15 as board 15c-r2, BUILT §509; `sweden_outline.svg`'s digest question closed (held with 12f). The 15c-r2 return re-cut as ONE archive with both flags (E-37, §511); the archive LANDED WHOLE and came back the same night as board 15c-r3 - READ and BUILT 2026-09-16 (§515), four marks re-cut and eleven flags back. The 15c-r3 return (E-38, §516) READ BY DESIGN 2026-09-16: 27 of 27 reachable, the eleven flags corrected on the board in place, nothing asked; read back and verified §521 - the driver's landing (§520) moves the override state's precondition to the dial.** As assembled: Batch 1 the energy page composed over the built page (EN-6, twelve frames; the rule row's two forms a composition question; the absent rows content); batch 2 the schedule row's grammar on the sourced shapes; batch 3 the statutory mark. Boards 12a–12f read the D11 way: the four areas, the three cuts and the interconnectors verify against the spine, 13b's one-Sweden ruling and 4a's geometry hold, snitt 4's 5 400 does not (the spine's 5 600 stands), a Poland link and seven energy statutes are not in the game; every board rests on an instruction the record does not hold and stops as a board - no instruction recorded as Elias's. Nothing asked twice; no art asked in a file. | the energy page's second pass, F4-4's row, PN-1's screen |
| **D19** (§440, answered on boards 13a/13b, read §449, built §450) | ✅ **ANSWERED 2026-09-10.** Item 2 - the ledger as an instrument - BUILT: the bridge in the board's fixed order at 1 pp = 20 px, the identity asserted (`AttributionBridge`), the rows beneath as the read; and the campaign map moved to 4a's arrangement on the board's note 2. Item 1 - the page composed (13a) - **STOPPED on this side**: three of its citations are not in the record and one of them overturns B8 (the hold banner survives the takeover, ruled); its composition checks out and it builds the day Elias rules on the banner and the stamp (§449). Boards 12a–12f (Energy) are a redirect no record here holds - stopped whole. | the night reads as an instrument where it read as a footnote; one Sweden of seats on both screens; 13a waits on two rulings that are Elias's |

### CALENDAR

| row | what it is | what it unblocks |
|---|---|---|
| **K-1** | the seed refresh from Sweden's real result | **13 September 2026** - the game's Sweden after the election. **Read 2026-09-16 (§523): Valmyndigheten's feed is on the FINAL count and 27 % in (1815 of 6626 districts); the refresh lands when it completes** |
| **K-2** | Svenska kraftnät's report on Sweden's bidding-zone division - **reporting is not enacting; only an enacted change moves the seed** (§456) | **29 January 2027** - the day to read whether SE1–SE4 and their links change, and nothing before a decision is in force |

Nothing else is open. The strikeable decisions of this run are marked in §380 (HOLDS / RECONSIDER / NEEDS ELIAS); the fix-track table carries FT-1..FT-9 and RF-1..RF-2 each with its state (FT-8 landed §398, FT-9 landed §404); the shelf's last cycle is §473 (2026-09-12, with T-8 added to the deferral register; §469, §436 and §380 before it).

