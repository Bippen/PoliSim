# The send package — 2026-08-28, regenerated at the close of the Policy Web micro-pass (late night; paste-and-glance; sending is Elias's)

**This paste supersedes every earlier package** (the v3.1 Phase B close's paste of the same
day and everything before it). **It carries ONE LIVE ASK: the tenth request — D7, board 2b,
"the Policy Web, drawn to be read"** (the request doc's §2, drawn against the new Annex G) —
plus the standing readback (Annex C's dead-space re-measure and paper-box correction, Annex F's
two D6 misses, §E5 now CLOSED end-to-end after Elias's same-night ruling). Rows are numbered
**n of N** so a partial paste is visible at the sending glance.

**Artifact 1 of 2** — `CLAUDE_DESIGN_ASSET_REQUEST.md` (the request doc as it now stands): §2
the tenth request D7/board 2b with R-W2's constraints; Annex G (the Policy Web measured —
occupancy before/after the live interim build, the ring's arithmetic, the label ladder, the
census, the readout contents, the film crops named); §1 answered-and-built with Annexes A–F as
corrected; §E5 closed in the status line; §4's costed list; §5 conventions.
- SHA-256 (as on disk, CRLF): `85690abf952dd9d3c2fdf256ef6b4a25c381f76d8322c9342f3be422ab81a795`
- bytes: 65 004
- goes to: `uploads/CLAUDE_DESIGN_ASSET_REQUEST.md` AND a new dated copy at
  `send/design_request_2026-08-28f/CLAUDE_DESIGN_ASSET_REQUEST.md` (the `…-28f` path continues
  the R-PC4a hygiene; the app also names duplicates by content digest).

**Artifact 2 of 2** — `CLAUDE_DESIGN_BOARD_1I_NOTE.md` (the courtesy note, 1i–1n),
**unchanged** — listed so the readback can be checked, not because it is new.
- SHA-256: `26892355f4ff6bba1639c89382aa8b41f9272a3c4e5a34dbd9fb94597da4b8ad`
- bytes: 11 959
- goes: nowhere new (already in Design's `uploads/`).

**The glance, after the paste:** read artifact 1 back (`get_file`) and hash the readback; the
digest above is what it must equal (`sha256sum` in Git Bash on the CRLF file — an
LF-normalized readback hashes differently). Both rows accounted for = the paste was whole.
Then mark §S in `MISSING_PREREQUISITES.md` SENT with the date, the way the earlier pastes are
marked.

**What comes back:** board 2b (the one ask). If Design also acts on the filed notes, the
shapes are unchanged from the last package: a re-composition board each for Demographics /
Decisions / the short Politics screens; a second Caution value; the §4 rail-glyph redraw —
none started before its board. **Elias's, not Design's:** §P's three felt verdicts, the
sitting on the `v31bf_*` film and the new `pweb_*` frames (§V — finding 3's row included),
13 September.

**Not in this package, by design:** the capture films (out of tree, named in Annex G.6 and
§V); the request doc's §0/§5 context Design already holds; the answered asks' boards
(Design's own); `ElectionsData/` and everything of the overnight elections track (nothing
there is Design's).
