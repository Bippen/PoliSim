# PoliSim v2.0 chrome manifest — the SPECIFIED set.
#
# Read by ChromeV2CoverageCheck. This file is the answer to "what is supposed to be there", which is
# the one question a folder listing can never answer. Enumerating the disk tells you whether what is
# PRESENT resolves; only a manifest tells you whether what is SPECIFIED is present. The check needs
# both directions, because until 2026-08-10 it had only the first and would have reported a clean
# 52/52 with four specified assets missing entirely.
#
# Source of truth: assets/polisim_ui_v2/MANIFEST.md in the Claude Design project, reproduced in
# CLAUDE_DESIGN_ASSET_REQUEST.md §1C. Update this file in the same commit that imports a pass.
#
# Syntax: one sprite name per line, no extension. `#` comments. A leading `!` marks a sprite that is
# present on disk but deliberately NOT specified — superseded art kept until its replacement is wired.

# ---- pass 1, 2026-08-03 (30) ----
ui_panel_paper
ui_frame_double
ui_frame_ornate
ui_tab_folder_on
ui_tab_folder_off
ui_tab_folder_hover
ui_tab_spine
ui_subtab_on
ui_subtab_off
ui_btn_brass
ui_btn_paper
ui_btn_disabled
ui_slider_track
ui_slider_tick
ui_slider_knob
ui_slider_knob_disabled
ui_hatch_draft
ui_stamp_carried
ui_stamp_rejected
ui_stamp_draft
ui_seal_official
ui_folder_dossier
ui_plate_tile
ui_portrait_frame
ui_portrait_frame_oval
ui_banner_hold
ui_calendar_pad
ui_scrim_takeover
ui_grain_tile
ui_pixel

# ---- pass 2, 2026-08-03 (11) ----
ui_scrollbar_track_v
ui_scrollbar_thumb_v
ui_scrollbar_thumb_v_hover
ui_scrollbar_thumb_v_pressed
ui_scrollbar_track_h
ui_scrollbar_thumb_h
ui_scrollbar_thumb_h_hover
ui_scrollbar_thumb_h_pressed
ui_scrollbar_button_none
ui_chip
ui_chip_outline

# ---- pass 3, IMPORTED 2026-08-10 (9) ----
# Delivered and imported. The four names that stood here — canvas_folder_country, canvas_btn_brass,
# canvas_btn_paper, emblem_state_seal — were SUPERSEDED rather than merely fulfilled, so they are
# gone rather than ticked off: Design retired the canvas_* namespace, renamed the seal to match what
# §3.1's emblem_ prefix actually means, and split each three-cell button strip into per-state
# sprites following the scrollbar precedent. That last one turned two open questions into nine
# single-sprite files, which is why this section grew from four entries to nine.
#
# ⚠ Retiring the old names is PART OF the import. Leaving them would have kept the check failing
# against assets that no longer exist under those names — a red that means nothing, which is worse
# than the honest red it replaced.
ui_folder_country
ui_seal_state
icon_pencil_draft
ui_btn_brass_canvas
ui_btn_brass_canvas_hover
ui_btn_brass_canvas_pressed
ui_btn_paper_canvas
ui_btn_paper_canvas_hover
ui_btn_paper_canvas_pressed

# ---- superseded pre-v2.0 chrome (11) ----
# Present on disk, fully superseded by the passes above, removable in one pass once the v2.0 wiring
# is confirmed live. Listed so they read as a known state rather than as unexpected files.
!ui_button_normal
!ui_button_hover
!ui_button_pressed
!ui_button_disabled
!ui_panel
!ui_scrollbar_track
!ui_scrollbar_thumb
!ui_scrollbar_track_vertical
!ui_scrollbar_thumb_vertical
!ui_slider_fill
!ui_slider_thumb
