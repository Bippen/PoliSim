# Review: §626 - boards 17a-17c and 18a-18b built, two guards widened (2026-09-25, s626)

**What was reviewed.** The staged change to `Assets/Scripts/UI/GameController.cs` (the settings sheet, `DrawSettingsScreen` and helpers; the verdict string), `GameController.Desk.cs` (the masthead), `MainMenuScreen.cs`, `CountrySelectorScreen.cs` (the start sheet), `Elections/StartPoints.cs`, `Elections/StartBrief.cs`, `Editor/InkContrastCheck.cs`, `Editor/MetaTextCheck.cs`, and the six player-surface strings MetaTextCheck's new rule caught - `Assets/Scripts/Data/TaxSchedule.cs` (one citation string, *BILLED (E-6)* reworded without the register id; no figure moved) and `Assets/Scripts/UI/AudioDirector.cs` (four cue notes). One independent read-only reader, asked to refute the preference semantics, the IMGUI balance, the hit rects, the layout arithmetic, the sheet/panel state machine, the regexes and the sprite-face measurement. Its report verbatim, then what was done.

---

## The reader

Adversarial read-only review of the 17a–17c / 18a–18b change (8 files, +1010/−217). Nothing edited, no Unity, no git state changes. I measured the two chrome strips with my own PNG decoder (perl) to check the new InkContrastCheck pairs, and ran a comment-stripped string-literal scan of MetaTextCheck's three roots against the new regex.

FINDINGS

1. DEFECT — Assets/Editor/InkContrastCheck.cs:100-104. Two of the four new pairs are asserted below their floor, so the check (and the cheap bar) goes RED on the next run. Line 102 `MenuTextOnBrass`: 0xF0E7D8 on the measured strip face at BodyFloor 4.5. The strip's centre-patch mean (ui_btn_brass_canvas.png, 256×128 RGBA8, alpha≥250, middle half) is #A68B52 (n=8192); F0E7D8 on it = 2.64:1. This pair documents the regression the board named, but Run() (line 147-152) counts every pair below floor as a failure. It cannot pass by construction: the light ink is what D6 rejected. Line 105 `TextPrimary` on `BrassFace(fallback)` 0x8A6B2F at CaptionFloor: CaptionFloor is 3.5 (line 35), and the ratio is 3.01 (the diff's own comment says "3.02"). Fails. The other two pass: TextPrimary on #A68B52 = 4.63 (clears 4.5 by 0.13 — a hair; see 2), 0x4A3A22 on paper #E8DCC3 = 8.08. Fix options are the author's: drop the two documentary pairs, or print them as REPORTED lines (the §576 idiom at lines 158-169) rather than asserted pairs.

2. RISK — InkContrastCheck.cs:103. TextPrimary on the strip face clears the body floor at 4.63 against a mean of a 9-sliced strip's centre patch; a re-export of the art that lightens the face by ~2 % flips the bar red. Fine as a ratchet, but state it. SpriteFace itself is sound: LoadImage replaces the 2×2 RGBA32 placeholder with the decoded 256×128; GetPixels32 row order is irrelevant for a mean; alpha threshold 250 excludes the strip's feathered edges; DestroyImmediate in finally is the right editor-side release; a missing/undecodable file falls back. Note the comment at line 93 quotes Design's ≈#A88E55 — the measured face is #A68B52 (close, not equal).

3. NOTE (no defect) — MetaTextCheck.cs:92 new regex. Scanned Assets/Scripts/UI/*.cs, Assets/Scripts/Data/*.cs and Assets/Scripts/Simulation/LawCatalog.cs with block and line comments stripped, every string literal tested: ZERO matches today. The one candidate ("PF-5's" in UiPalette.cs:768) is inside a // comment and is stripped. The verdict text at GameController.cs:6634 had "(PS-3)" removed in this same diff, which is exactly what the new pattern would have caught — consistent. Latent false-positive classes to know about (none present now): `\bS\d+\b` will hit any future "S1"/"S2" section label, `\bD\d+[a-z]?\b` any "D3"/"D5a" stamp, `\bT-\d+\b` a "T-3" chip. Banned patterns run on diagnostic lines too (line 176-181 apply Banned before the DiagnosticLine skip), so a Debug.Log carrying "PF-16" inside a root would also hit — acceptable, but it is stricter than the widened five.

4. NOTE — StartPointsDiagnostic.cs:66 `HasRegister` = `(^|[^A-Z])[A-Z]{1,2}-?[0-9]{1,3}([^0-9]|$)`. Tested mentally against every string the prompt named: "18 MAY 2025" (Y then space, no), "7 JUL 2024" (no), "RUN-UP · OPENS 18 JAN 2026" (UN-U has no digit after the hyphen; no), "SEJM ELECTION" (no digits), "2 STARTS · 1 LOCKED" (no letter-digit adjacency), the playable Lines from WorldClock.StartLine all clean; "(S8)", "E-49" match; "R-EL10" does NOT match (three letters "REL" exceed {1,2} and "EL10" is preceded by a letter) — the diagnostic is weaker than its doc-comment claims, but the two old offenders (S7/S8, E-49) match. Also "COVID-19"-class words are safe.

5. NOTE — StartBrief.cs Rows (lines 118-173): no exception path for a country with no government of record — the else at line 147 emits "— NONE OF RECORD" and the Since/Seats/Support rows are simply not added; Support is added only when non-null and non-empty (line 137). "Last polling day" (line 152): `PollingDay < opens` — France legislative PollingDay is 7 Jul 2024 (the SECOND round, despite StartPoint's doc "the first round for a two-round election" at StartPoints.cs:26) vs Opens 18 Jul 2024 → "Last polling day", correct; the other five → "Polling day". Rows is only reached for the selected playable card, so the `ChamberAt(id, DateTime.MinValue)` case for a locked card never runs. Two behavioural differences from Clauses worth naming: Rows omits the K-1 SeatingDeviation clause (Clauses line 92-93 appends it), and ChamberName France changed "Assemblée nationale" → "Assemblée" (line 27), which also changes the existing SP-2 paragraph text — any film/assert keyed on it moves.

6. NOTE — StartPoints.cs:47 `Presidential` = Kind starts with "PRESIDENTIAL": the USA's playable card is a presidential election, so it draws NO seat bar (CountrySelectorScreen.cs:604-607) even though its start seats the House. Consistent with 18b's letter; say so if Design expects a chamber on the USA card.

7. RISK (low) — GameController.cs DrawSettingsScreen: the Time-holds row and the Resolution row are the widest. At 1280×720 the holds row is ≈ 380 px in a ≈ 475 px control lane and the resolution row with its readout ≈ 450 px; fits, with ~25 px to spare on the readout row, and the readout is a MeasuredLabel that shrinks rather than overflows. No scroll view now (line 1444); the sheet is ≈ 260 px tall at 720, so vertical fit is not in doubt. Film it at 1280.

8. NOTE (verified OK) — GameController.cs preferences, each read/write the same key with the same semantics as HEAD lines 1429-1545: Window mode, Resolution, Speed, the two holds, Provenance, Autosave cadence, Slots (ONE intended semantic change: while AutosaveDays==0 the strip is reduced so the click is swallowed and the assignment is guarded; the old code let you change slots while OFF), Mute, Volume (LedgerRow.Draw draft vs saved, now on DeskBody(11) copies). ELECTION NIGHT (1514): selected:true, disabled:true → brass fill, TextMuted ink, GUI.enabled=false around the invisible GUI.Button → inert. `_settingsOpenedFromDesk` is stamped in OpenSettings() (1386), the ONLY setter of _settingsOpen=true (the menu's SETTINGS at 1334, the desk chip at Desk.cs:312); no load path opens the screen. Edge: if the harness seats a player while the menu-opened settings sheet is up, the foot still says BACK TO THE MENU while CloseSettings lands on the desk — cosmetic, harness-only. IMGUI: BeginHorizontal/EndHorizontal and the two BeginVertical/EndVertical are balanced; every GetRect result is consumed only on Repaint or by GUI.Button(rect); no shared style is mutated (SettingsNameStyle copies _labelStyle; DeskCaption/DeskBody return fresh copies; LedgerRow's name cell is a private copy). Screen.fullScreenMode (1671) is a plain property read, safe in batch (reports Windowed).

9. NOTE (verified OK) — GameController.Desk.cs:305-343 masthead: each chip's hit rect is the same Rect it is drawn with; the ordering right-to-left is SETTINGS, gap, SAVES, a SAVES-width pitch with the hairline at its centre (Repaint only), then the 1×/2×/3× cells sharing edges. GUI.enabled is forced true only around SAVES/SETTINGS and restored before the strip, as before. LIVE (345-351) is unchanged code; nothing draws outside r; at 1280 the whole cluster is ≈ 240 px of a ≈ 1240 px masthead.

10. NOTE (verified OK) — MainMenuScreen.cs: title block top-anchored at 40 units, height 71 → bottom at 111 canvas units. Column centred at y = 520 with 460 units of height; with CONTINUE the content is 405-419 units → top ≈ 310; without CONTINUE ≈ 342. Gap to the title ≥ ~180 units (~120 px at 1280). Overlap would need a canvas under ~680 units tall; with ScalerMatch 0.5 and reference 1920×1080 no window geometry gets there. hasContinue=false: NEW GAME takes Face.Brass, no Continue item, column one item shorter. Brass ink is PoliSimTheme.TextPrimary via FacedButton's new FontStyle parameter — compiles.

11. NOTE (state machine, one design point) — CountrySelectorScreen.cs. ShowStartPanel → HidePartyPanel (closes panel, destroys sheet, re-activates the grid) → BuildSheet (destroys any sheet, deactivates the grid) — re-selecting a card calls BuildSheet again, which destroys the old sheet before building, so no duplicate; both `_sheet` and `_partyPanel` are single-slot fields whose every assignment is preceded by a Destroy, and both live under Root. The harness pair ShowStartPanel(country,null) → HidePartyPanel() restores the folders. The one thing to name: ShowPartyPanel calls only ClosePartyPanel, so the sheet stays UNDER the party panel with the folders hidden, and the panel's BACK (line 316 → HidePartyPanel) destroys the sheet and returns to the COUNTRIES, skipping the start sheet. Not a leak, but "BACK" from the party step does not return to the step it came from. The seat bar is correct: both HorizontalLayoutGroups set childControlWidth=true and childForceExpandWidth=true so the LayoutElement.flexibleWidth=seats on each cell is honoured; `PoliSimTheme.PartyLaddered(CountryId, string)` is called as (id, party.Abbrev) — a two-argument key. Card content fits: 196−32 = 164 units against ≈ 142 of stacked children; the stamp box (102 units) holds "13 SEP 2026" at Document 12 bold.

VERDICT. The UI work is sound: every preference keeps its key and semantics (the one deliberate change is slots being inert while autosave is OFF, which the board asked for), the settings sheet's IMGUI is balanced and mutates no shared style, the masthead's hit rects are its drawn rects, the menu's title block and item column cannot meet, the selector's sheet/panel fields cannot leak or duplicate, the seat bar's layout flags are right, and the HasRegister/MetaTextCheck regexes produce no false positive on any literal in the roots today. The change should NOT be committed as-is because of finding 1: InkContrastCheck.cs asserts two pairs that are below their floors by construction (2.64 vs 4.5; 3.01 vs 3.5), so the cheap bar goes red the moment it runs — those two pairs must become REPORTED lines or be dropped. Secondary items to decide, not block on: the party panel's BACK skips the start sheet (11), the USA card draws no chamber bar (6), Rows drops the SeatingDeviation clause and "Assemblée" shortens the existing brief text (5), and the settings sheet's widest rows should be filmed once at 1280 (7).

---

## What was done

1 FIXED: the two documentary pairs are REPORTED under the table (the §576 idiom), never asserted; the strip's face is measured from its own pixels (#A68B52 brass, #E8DCC3 paper - an independent decoder agrees). 2 stated in the check's comment (a ratchet on the strip's face). 3, 4 noted (R-EL10 is caught by MetaTextCheck's own pattern; the diagnostic's is a second net). 5 noted: `Rows` is the board's ledger of the spec's slots and the deviation clause stays in `Clauses`; "Assemblée" is the board's word. 6 stated in the record: the USA's presidential card draws no chamber bar by 18b's letter - a question for Design's next sitting, not invented here. 7 filmed at 1280 (`mm3_1280_00a_settings`): the sheet fits, no scroll. 11 FIXED: the party panel's BACK returns to the start sheet where one is open, else to the countries. 8-10 verified by the reader, no change.
