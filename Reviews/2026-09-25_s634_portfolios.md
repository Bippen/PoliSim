# Review: PS-3g - portfolios by Gamson's law, the gate's portfolio leg, the junior partner's desk (2026-09-25, s634)

**What was reviewed.** The working-tree change (read-only, `git diff` against HEAD a3b8ad7; no Unity run, no git write) to `Assets/Scripts/Elections/GovernmentRecord.cs` (`Portfolios` and its doc 49-59, `HoldsPortfolio` 61, `PortfoliosOf` 64-70, `AllocatePortfolios` 73-108, the calls in `AtStart` 137/143, `WhatIfGoverning` 158, `FromView` 176), `Assets/Scripts/Simulation/SimulationManager.cs` (the meeting roll 298, the five gated introducers 1382/1441/1589/1642/2102, the two-argument `PlayerMayIntroduce` 2431-2452, `PortfolioOfLaw` 2454-2465, the cabinet-decision roll 3312), `Assets/Scripts/UI/GameController.cs` (`DrawLeverLock` 11960-11963 and the eight sites 9160, 10088, 10134, 10572, 10618, 11427, 11767; the seven sites left at null: 3995, 4020, 4237, 4712, 10664, 11069, 11944), `Assets/Scripts/UI/GameController.Desk.cs` (`RoleTitleSuffix` 260-270), `Assets/Scripts/Persistence/SaveGameService.cs` (`CurrentSaveVersion` 31 at 53), `Assets/Editor/CheckSuite.cs` (the row 186), `Assets/Editor/PortfolioAllocationDiagnostic.cs` (new, whole, with its `.meta`), `docs/reference/GAMSON_PORTFOLIOS.md` (whole) and `ElectionsData/sweden/portfolios.md` (whole; the raw folders `docs/reference/raw/gamson/` and `ElectionsData/sweden/raw/portfolios/` are listed by the two documents, not opened). Read for the paths around the change: `SimulationManager.cs` (`PlayerGoverns` 2403-2413, the one-argument `PlayerMayIntroduce` 2421-2430, `TableShadowBudget`'s junior refusal 1187, `AdvanceTaxProgramBillsDay` 1399-1429, the boundary's ministry block 3026-3049), `Assets/Scripts/Simulation/AiFinanceMinistry.cs` (`Decide`/`Apply`/`Withdraw` 130-159, the tax leg 240-247), `Assets/Scripts/Simulation/ParliamentSystem.cs` (`ApplyTaxProgramBillResult` 540-553, `ApplyWelfareProgramBillResult` 570-583, the seat fill 75-77), `Assets/Scripts/Simulation/CabinetSystem.cs` (`TryRollDecisions` 553-572), `Assets/Scripts/Data/WorldFactory.cs` (the seats 1032, the records 1092), `Assets/Scripts/Data/CabinetPortfolio.cs` (the enum 29-40), `Assets/Scripts/Data/PortfolioEffectiveness.cs` (`Effectiveness.ShortName` 75-87), `Assets/Scripts/Data/LawDefinition.cs` (`LawCategory` 9-21), `Assets/Scripts/Simulation/LawCatalog.cs` (`cash_bail_reform_act`'s category), `GameController.cs` (`DrawCabinetPortfolioPanel` 10046-10090, `DrawCabinetCandidateButton` 10125-10135, the record sites 2175/2218/2222/6622), `GameController.Desk.cs` (the masthead's title and LIVE placement 287-364), `SaveGameService.cs` (`BuildSettings` 71-79, the refusal 161), `Assets/Editor/MetaTextCheck.cs` (the roots 61, the patterns 63-93), `Assets/Editor/PlayerRoleDiagnostic.cs` (55-70), `Assets/Scripts/Testing/UiScreenshotDriver.cs` (the dense staging 1505-1535), `Tools/review_ledger.tsv` (the s633 rows), the play save on disk (`C:/Users/elias/AppData/LocalLow/DefaultCompany/PoliSim/Saves/playtest_4_precampaign_day1.json`, "SaveVersion": 30, 2026-09-25 11:50), `ElectionsData/sweden/records_by_date.md` (row 40, the 2026 chamber), and `Reviews/2026-09-25_s633_budget_contest.md` and `Reviews/2026-09-25_s630_role_gate.md` (the shape, the 62-character measurement, the candidate-list note).

---

## The reader

Adversarial review of §634 - PS-3g, part 4 of the roles stage: the six portfolios apportioned to the cabinet's parties by seat share (Gamson's law, sourced first as ruled), the gate's portfolio leg for a junior partner, the desk's locks by lever, the masthead's suffix, format 31, the diagnostic. The allocation is right in its arithmetic and its edges (findings 1-3), the doc comment quotes the reference verbatim (finding 4), the gate's portfolio leg is sound and the partner's bills do not collide with the ministry's fields (findings 5-6). Three things stand against it: the one-argument gate's junior-partner sentence now says a false thing on every lever without a portfolio (finding 7), the cabinet-decision roll now consumes the RNG for a country the player does not govern (finding 8), and the play save is v30 against a v31 build (finding 10). The rest are notes.

FINDINGS

1. OK - `AllocatePortfolios` line by line (`GovernmentRecord.cs:73-108`). (a) THE QUOTAS: `total` sums the cabinet parties' `ParliamentSeats` (a missing key reads 0, `:80`); `quota = 6 × seats/total`, or `6/Cabinet.Count` when `total == 0` (`:86`), so a cabinet whose parties hold no seats splits equally rather than dividing by zero; `floor` and `remainder` per party (`:87-89`). (b) THE LARGEST REMAINDER: the sort (`:91`) is by remainder descending, a tie to the larger party; the loop `for (i = 0; given < all.Length && remainder.Count > 0; i = (i + 1) % remainder.Count)` (`:92`) hands one extra to each party in that order and wraps - the wrap can only be reached when the shortfall exceeds the party count, which the quotas' sum of exactly 6 rules out in exact arithmetic (shortfall = 6 − Σfloor < Cabinet.Count); with floating point the sum is 6 ± an ulp and a party's quota of 3 could read 2.9999999 (floor 2, remainder 0.9999999) - it then heads the sort and takes its own portfolio back; nothing can be handed out twice because `given` climbs by one per iteration and stops at 6. Every iteration terminates. (c) THE PM GUARANTEE (`:93-98`): only when `PmParty` is a cabinet key with `count == 0`; one is taken from the richest party. `richest` is found by strict `>` over `count`, whose keys were inserted in `Cabinet` order (`:88`) - a `Dictionary<,>` with no removals enumerates in insertion order in every .NET runtime the project can build on, so a tie goes to the earlier cabinet entry; deterministic in practice, but a guarantee the BCL does not document (NOTE, not a defect - `order` at `:99-100` is the sorted list the code could have searched instead). (d) THE HAND-OUT (`:99-107`): every cabinet party gets an entry (`:101`), so a party with `count == 0` is present with an empty list and `PortfoliosOf` prints "NONE" (`:66`); the PM's party takes `FinanceTreasury` from the pool first (`:103`, guarded by the same `TryGetValue` so a `PmParty` outside the cabinet cannot throw at `Portfolios[PmParty]`); the rest go in `Enum.GetValues` order (Finance, Interior, Health, Defense, Foreign, Education - `CabinetPortfolio.cs:31-39`, by underlying value) to the parties by size (`:100`, `List.Sort` on seats - an unstable sort, so two cabinet parties with EQUAL seats have an unspecified but run-to-run stable order; NOTE). `while (Portfolios[party].Count < count[party] && pool.Count > 0)` counts the PM's Finance toward its `count`, so M with `count` 4 takes three more. (e) THE CASES ASKED: a single-party cabinet - quota 6, floor 6, the loop is not entered, Finance then the five (the diagnostic's France what-if, `:69`); seven parties over six portfolios - every floor 0, `given` 0, the six largest remainders (ties to seats) take one each, the seventh is present with "NONE"; a PM's party with 0 seats - quota 0, the guarantee gives it one from the richest, which could leave a two-party cabinet's other member at 0 only if it had exactly 1 (i.e. a total of 1 seat between them - not a chamber). No RNG, no clock, no dictionary of the world beyond `ParliamentSeats`.

2. OK - Sweden, by hand and against the record. The diagnostic's chamber is the 2026 Riksdag (`records_by_date.md:40`: M 70, KD 22, L 19 = 111, seeded by `WorldFactory.cs:1032` BEFORE the records at `:1092`, so the seats are never empty when `AtStart` allocates): quotas 3.78 / 1.19 / 1.03, floors 3 / 1 / 1, one extra to M's 0.78 → M 4, KD 1, L 1; on the 2022 chamber the task names (68 / 19 / 16 = 103): 3.96 / 1.11 / 0.93, floors 3 / 1 / 0, extras to M (0.96) then L (0.93) → the same 4 / 1 / 1. The hand-out: M TREASURY (first), then INTERIOR, HEALTH, DEFENCE; KD FOREIGN; L EDUCATION - exactly the check's printout. THE PREMISE, STATED WHERE IT SHOULD BE: the real cabinet (`sweden/portfolios.md`, the table at 58-83) has KD at Socialdepartementet (Forssmed, Lann) and Klimat- och näringsliv (Busch), L at Utbildning (Mohamsson, Edholm) and Arbetsmarknad - the model's enum order gives KD Foreign and L Education, one of two right. The doc comment says so in its own words (`:57`: "Which portfolio a party takes follows its manifesto's emphasis in the literature [BDD11] - unsourced per party here, so the enum's order stands as the premise"), and the reference's element 6 (`GAMSON_PORTFOLIOS.md:23`) carries the BDD11 sentence it rests on. The deviation the literature states (large underpaid, small overpaid) is declared UNSIZED and unpaid (`:54-56`), which matches GAPS 1-2 of the reference (`:114-117`) and the Sweden document's own ratios (0.82 / 1.36 / 1.34, `portfolios.md:109-116`). One thing NOT said anywhere: the installed record is the 2022 CABINET allocated on the 2026 CHAMBER (`AtStart` reads today's `ParliamentSeats`, `:137`), so the shares are those of a chamber elected after the cabinet formed - harmless here (4/1/1 either way) and defensible as "the portfolios as the sitting chamber would apportion them", but a reader of the record comment would take the shares to be the 2022 election's. NOTE, one clause in the comment.

3. OK - the throws asked about. `Portfolios[PmParty]` (`:103`) and `count[PmParty]` (`:97`) are reached only through `count.TryGetValue(PmParty, …)` succeeding, and `count` has exactly the cabinet's keys - so a `PmParty` outside `Cabinet` allocates with no guarantee and no throw. CAN it be outside? `FromView` (`:171-175`) takes a candidacy only where `record.Cabinet.Contains(abbrev)`, else `Largest` over the cabinet - always inside. `WhatIfGoverning` (`:157`) adds the party it names - inside. `AtStart` (`:136`) takes `HeadParty` - the party in the record's "(M)" parenthesis if it is a party of the system - WITHOUT checking it against `record.Cabinet`; `Largest` is the fallback only when the head names none. So an installed record whose head's party is not in its cabinet is representable (a mis-keyed `WorldClock` head against a `SeatedGovernment` cabinet) and would allocate silently with the PM's party at NONE and `RoleOf(PmParty)` still `PrimeMinister`. Not the case for any record now (Sweden's "Ulf Kristersson (M)" against `{M, KD, L}`, s633 finding 5) - NOTE, the class of check `PlayerRoleDiagnostic` could carry for every country. `seats[a]`/`seats[b]` in the sort (`:100`) and `count[remainder[i].Party]` (`:92`) are cabinet keys - safe. `PortfoliosOf` and `HoldsPortfolio` guard null/missing/empty. A `Cabinet` with a DUPLICATE entry would double-count `given` and `total` - no record produces one (grep of the record sources is outside this change; NOTE only).

4. OK - the doc comment's quotations against `docs/reference/GAMSON_PORTFOLIOS.md`, verbatim. "a share of the payoff proportional to the amount of resources which they contribute to a coalition" [BF73] - the reference's BF73 abstract at `:45-46` (Gamson's proposition as Browne & Franklin quote it; the reference's GAP 4 says this is the only reachable text of it, and the comment attributes it to BF73, not G61- right). "one-to-one proportion" [WD01] - `:57` ("receive portfolios in one-to-one proportion to the amount of legislative support"). "near-perfect relationship" [WD06] - `:70`. "no formateur premium [WD06]" is a paraphrase of `:77` ("contra proposer models and any other models based on bargaining power") and element 4 (`:21`); "the large party underpaid, the small overpaid [BF73] [WD01]" paraphrases `:49-50` and `:57-58` - both cited where the reference cites them. The manifesto clause [BDD11] paraphrases `:86-88`. The seat shares "0.66/0.18/0.16" round the Sweden document's 0.6602 / 0.1845 / 0.1553 (`portfolios.md:111-113`) - 0.1553 → 0.16 is a correct rounding. The reference's GAP 3 (the prime ministership's weight unsourced) is answered by the comment's "keeps the head of government and takes Finance first (the premise)" - the PM's office is counted as none of the six and Finance is the premium in kind, stated as a premise. Nothing typed from memory that the reference does not carry.

5. OK - the gate's portfolio leg (`SimulationManager.cs:2441-2452`). The two-argument overload returns the one-argument answer where that is true (the PM's party, an AI country, a tool's un-seated country), else returns false for every role but `JuniorPartner` WITHOUT touching `lockedBecause` - so the opposition's "IN OPPOSITION · THE GOVERNMENT INTRODUCES BILLS · YOUR PARTY VOTES ON THEM" survives (`PlayerRoleDiagnostic.cs:63-64` still passes; the diagnostic's own `:63` asserts the prefix). For a junior partner: a held portfolio passes with `lockedBecause = null` (the diagnostic asserts `why == null`, `:56`); a null portfolio reads "THE PRIME MINISTER'S LEVER"; another's reads "THE <SHORT NAME> MINISTER'S LEVER"; both end "YOURS: <PortfoliosOf>" (`:2448-2450`). `PortfolioOfLaw` (`:2455-2465`): an unknown law → null → the PM's; `cash_bail_reform_act` is `CrimeJustice` (`LawCatalog.cs:162`) → Interior, as the diagnostic asserts. The lever-to-portfolio map in the comment (`:2434-2439`) matches the callers: tax programme Finance (`:1382`), welfare Health (`:1441`), crime Interior (`:1589` - a path with no desk button since the sliders' ruling, `GameController.cs:4336-4350`, so the gate is reached by no player action; consistent, unreachable), trade Foreign (`:2102`), law by category (`:1642`), the meeting Foreign (`:298`), a cabinet decision its own portfolio (`:3312`); the budget, labour, sector, SWF, fleet, chair and rate stay the one-argument gate's - stated in the comment as the PM's.

6. OK - "two hands" with the portfolio leg: the FIELDS do not overlap. A junior partner's passed bills write `TaxLine.IsImplemented` (`ParliamentSystem.cs:551`), `WelfareProgram.IsImplemented` (`:581`), the tariff table (the trade bill), a law's structural parameters and the crime dials - none gated on `PlayerGoverns`, right: the chamber passed them. The AI ministry for the PLAYER'S country goes by bill since §632 (`SimulationManager.cs:3036-3040`: `ministryByBill` skips `Apply`; the rule reaches the book only as the government's budget bill), and what its rule writes is `SpendingLineChanges` (line amounts) and `TaxRateOverrides` for `IncomeTax`/`VAT` only, and only where the line `IsImplemented` (`AiFinanceMinistry.cs:246-247`); `Withdraw` (`:154-159`) removes exactly its own keys from the decision. So no ministry write can undo a partner's flag, tariff or law, and no partner lever writes a line amount or a rate. THE INTERACTION THAT REMAINS is the coalition's, not a defect: a partner that turns VAT OFF removes it from the ministry's rate leg (`:246`'s `!line.IsImplemented` skip), one that turns income tax ON hands the ministry a rate to raise on its next bill - the junior partner moving a Finance flag while the PM's ministry sets the rate. That the FINANCE flags (`:1382`) are a junior partner's at all is the question: a partner holding Finance is the finance minister, so the flag is its lever by the premise, and Sweden's KD/L never hold it under the enum order (finding 2). Consistent with the spec's "the budget is the coalition agreement's" (`TableShadowBudget`'s refusal at `:1187` still stands beside the leg). Not read: whether a welfare programme's cost sits inside a `SpendingLine` the ministry's percentages scale - if it does, the partner's programme is turned on and the ministry's next bill trims the line it lands in by the rule's percentage, which is the rule acting on the book, one hand.

7. DEFECT - the one-argument gate's junior-partner sentence is now false on every lever it still guards. `SimulationManager.cs:2428` reads "JUNIOR PARTNER · THE PRIME MINISTER'S PARTY INTRODUCES BILLS · YOUR PORTFOLIOS' LEVERS ARE NOT YET BUILT", and that is what a junior partner is shown (through `DrawLeverLock()` with no portfolio, `GameController.cs:11963` → the two-argument overload → the one-argument reason for a null portfolio? NO - a null portfolio reaches `:2448-2450` and reads "THE PRIME MINISTER'S LEVER · YOURS: …", fine) on the desk's seven null sites. But the one-argument overload is still called DIRECTLY for the labour bill (`:1500`), the sector bill (`:1949`), the SWF drawdown (`:2013`), the budget bill (`:1290`), the chair (`GameController.cs:3825`), the budget page's status sentence (`:11047`, `:11108`) and the fleet (`GameController.Energy.cs:149`), and each of those writes or shows `lockedBecause` from `:2428`: the fleet's head caption prints it (`Energy.cs:149-150`, per s630's second reading), and the LEVERS log line carries it on every refused introduction. Scenario: KD, junior partner holding Foreign, opens the Energy page: "JUNIOR PARTNER · … · YOUR PORTFOLIOS' LEVERS ARE NOT YET BUILT" - built this session. Fix: cut the sentence at `:2428` to the same register as the leg ("JUNIOR PARTNER · THE PRIME MINISTER'S LEVER · YOURS: <PortfoliosOf>" - the country is in hand there), or route the one-argument callers through the two-argument overload with `null`. The two doc comments that say the leg is "not yet built" (`:2417-2418`, `:2400-2401`) are comment-only.

8. RISK - the cabinet-decision roll now draws the RNG for a country the player does not govern. `SimulationManager.cs:3312` was `if (PlayerMayIntroduce(country.Id, out _)) { pendingDecisions.AddRange(CabinetSystem.TryRollDecisions(country)); }` - the roll skipped whole for an opposition/support/junior player - and is now `foreach (… in CabinetSystem.TryRollDecisions(country)) { if (PlayerMayIntroduce(country.Id, portfolio, out _)) … }`: `TryRollDecisions` draws `RandomSource.NextDouble()` per seated minister and `Next(pool.Count)` per hit (`CabinetSystem.cs:558, 568`) BEFORE the gate sees the portfolio. (a) THE DUMP (no player): `PlayerMayIntroduce` is true for every country, the roll ran before and runs now, the AI countries seat no ministers - byte-identical, no family to re-run (`AllocatePortfolios` itself draws nothing, finding 1). (b) THE PLAY: an opposition player cannot appoint (locked), the play save carries no minister, the roll draws nothing - unaffected. (c) THE HARNESS: the dense staging appoints all six ministers while re-seated (`UiScreenshotDriver.cs:1505-1516`) and restores S afterwards (s630 second reading, item 7); from the restore on, every day tick of that run now draws six `NextDouble()`s the s633 run did not, so every RNG-dependent frame AFTER the dense staging shifts against the film on disk. The det_* byte-stability the memory records is a per-frame claim on the same seed; this change moves the stream for that run. Either keep the whole-roll skip for the roles that hold no portfolio (`if (role is PrimeMinister or JuniorPartner)` around the call - the junior partner's own portfolios still roll) or accept and re-baseline the dense film. Also a semantic note: a decision rolled for the PM's portfolio in a junior partner's country is DROPPED - no one answers it (the AI government answers no decisions either) - the same as §630's whole-skip for the opposition, stated nowhere new.

9. OK - the desk's sites, the Cabinet tab, the masthead. The eight portfolio sites match their introducers (finding 5): the law pane passes `PortfolioOfLaw(new LawBill { LawId = law.Id })` (`:9160`) - the same category read the gate makes at `:1642`; the trade bill and the partner override Foreign (`:10618`, `:10572`); the tax and welfare toggles Finance/Health (`:11427`, `:11767`). THE CABINET TAB: `portfolio` is the parameter of both `DrawCabinetPortfolioPanel(CabinetPortfolio portfolio)` (`:10046`) and `DrawCabinetCandidateButton(CabinetPortfolio portfolio, …)` (`:10125`), in scope at Reshuffle (`:10088`) and Appoint (`:10134`) - a junior partner reshuffles and appoints its own portfolios' ministers and sees the lock on the PM's; the write is to the country's one `CabinetMinisters` (`Country.cs:855`), the government's shared cabinet, which is the ruling. THE MASTHEAD: `RoleTitleSuffix` (`Desk.cs:262`) returns empty on a null `Government` before the switch, so `:267`'s dereference is guarded; a junior partner reads "SWEDEN · YEAR 1 · JUNIOR PARTNER · YOUR PORTFOLIOS: FOREIGN" (61 characters), drawn through `MeasuredLabel` into a rect clamped to `liveLeft − gap − titleLeft` (`:362-364`), with the LIVE caption pushed right to `Max(title's end, liveRight − liveWidth)` - the title can only clip, never widen. At 1280 (bold Document 10.5 px, ~8.2 px per character by s630's 7.06 at 9 px) that is ~500 px of a masthead whose right cluster and LIVE take ~450 - it fits, unfilmed. A junior partner holding two portfolios ("YOURS: INTERIOR, HEALTH") adds 16 characters.

10. DEFECT (procedural, expected) - the play save is v30 against a v31 build. `CurrentSaveVersion = 31` (`SaveGameService.cs:53`) and the loader refuses any other version (`:161-164`); the file the protocol names, `C:/Users/elias/AppData/LocalLow/DefaultCompany/PoliSim/Saves/playtest_4_precampaign_day1.json`, reads `"SaveVersion": 30` (written 2026-09-25 11:50, s633's re-cut). `PlayProtocolStaging.Run` is owed before the play; `PlayProtocolCheck` cuts its own to a temp path and passes. The three `Sweden_turnN.json` beside it are older still. Not new to this change - every bump since 27 has said the same - named because the record calls the protocol's save "the record".

11. OK - the save's shape. `Portfolios` is `Dictionary<string, List<CabinetPortfolio>>`, a public field initialised at declaration; Json.NET under `BuildSettings` (`SaveGameService.cs:71-79`: `PreserveReferencesHandling.Objects`, `MissingMemberHandling.Ignore`, `TypeNameHandling.None`, no `StringEnumConverter`) writes the enum as its integer and the dictionary as an object keyed by party - the same form `CabinetMinisters` (`Dictionary<CabinetPortfolio, CabinetMinister>`) has ridden for months, and `CabinetPortfolio` is append-only by its own comment (`CabinetPortfolio.cs:35-36`). `ObjectCreationHandling.Auto` populates the initialised dictionary in place, which is fine for a fresh record. The diagnostic's round trip (`:72-75`) asserts three keys, M's Finance and KD's held portfolio - a real serializer pass, not an in-memory read (s633 finding 7's gap, closed for this field). `Breaks` still has no round-trip assertion anywhere.

12. OK - the diagnostic (`PortfolioAllocationDiagnostic.cs`). It seeds 777, resets the energy calibration, builds the default world (which allocates every country at the factory, `WorldFactory.cs:1092`), and asserts: 3 keys with 4/1/1; M holds Finance; six distinct of six held; SD and S read "NONE"; KD passes its own portfolio with a null reason and is refused Finance and the null portfolio with the exact strings; KD does not govern; M passes both; S is refused with the "IN OPPOSITION" prefix; the law's category; France's what-if with all six; the round trip. `Effectiveness` is the static class at `PortfolioEffectiveness.cs:31` - compiles. `g.Portfolios["KD"][0]` throws `ArgumentOutOfRangeException` if KD holds nothing - caught at `:77` as a failure, not a crash; a `KeyNotFoundException` on `ParliamentSeats["L"]` (`:42`) likewise. The `CheckSuite` row is in the `Suite` array (`:186`, the cheap bar by `bar_tier.ps1:29`'s parse). Not asserted: that the six go out in the enum's order (the check prints them, `:44`, and the record comment states the premise; a sort of the pool would pass this check unseen), and any country but Sweden and France (the USA's presidency record allocates all six to the president's party - `Cabinet` is one party - unasserted).

13. OK - `MetaTextCheck`, the culture, the dump. The new player-facing literals - " · JUNIOR PARTNER · YOUR PORTFOLIOS: " (`Desk.cs:267`, a root) and the two reasons in `SimulationManager.cs` (outside the roots) - match none of the banned patterns (`MetaTextCheck.cs:63-93`; "PS-3g" and "§634" appear in comments only, the comment-only exemption). `ShortName(...).ToUpperInvariant()` and `string.Join(", ", …)` carry no culture; the diagnostic's `F` is Invariant; nothing formats a number. The dump: `AllocatePortfolios` reads `ParliamentSeats` and `Enum.GetValues` only (finding 1), and the meeting roll's gate for an AI country is unchanged (the one-argument leg returns true at `PlayerCountryId.Value != countryId`) - no player, no change. `WhatIfGoverning` calls `AtStart` and so allocates the of-record cabinet it then discards (`:154`, `:158`) - wasted, harmless.

14. NOTE - the ledger. `Tools/review_ledger.tsv` carries s633 rows for `SimulationManager.cs`, `UiScreenshotDriver.cs` and `SigningScreen.cs` (139-141) and none yet for this state; `SimulationManager.cs` and `GovernmentRecord.cs` are on the gate's path and `ReviewLedgerCheck` will want this review's row before the cheap bar.

VERDICT: READY WITH ONE STRING AND ONE DECISION. The allocation is correct by hand on both chambers, terminates on every input, throws on none, states its two premises (Finance first; the enum's order for who takes what, KD's Foreign against the real Socialdepartementet) where the record is read, and quotes the reference verbatim; the gate's portfolio leg is sound and its fields do not collide with the ministry's; the desk's sites, the Cabinet tab's scope and the masthead's guard hold; the diagnostic asserts the shape and a real round trip. Before the commit: cut the one-argument gate's "YOUR PORTFOLIOS' LEVERS ARE NOT YET BUILT" at `SimulationManager.cs:2428` (finding 7 - it is now shown to a junior partner on the fleet, the budget and the chair), and decide the cabinet-decision roll's RNG (finding 8 - keep the whole-roll skip for the roles that hold no portfolio, or re-baseline the dense film). Then as notes: the play save's re-cut to v31 (10), the record comment's clause that the shares are today's chamber's (2), the head-party-outside-cabinet class for `PlayerRoleDiagnostic` (3), the unstable sort on equal seats and the `richest` tie (1), the order assertion and the USA in the diagnostic (12), the ledger row (14).
