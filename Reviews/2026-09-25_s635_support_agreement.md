# Review: PS-3h - the support agreement as data, its items owed, delivered, broken; the withdrawal (2026-09-25, s635)

**What was reviewed.** The working-tree change (read-only, `git diff` against HEAD 2b7b602; no Unity run, no git write) to `Assets/Scripts/Elections/SupportAgreement.cs` (new, whole, as it stands after the cache was added mid-review: `SupportAgreement` with the `Templates` cache 50 and `Demand` 52-57, `EnactedKey`/`SeatsKey` 59-60, `Copy` 63-68, `Score` 70-112, `Alignments` 114-123, `Track` 126-146, `DialValue`/`DialName` 148-149, the three enums, `AgreementItem`; with its `.meta`; findings 3-7 cite the pre-cache line numbers of `Demand`, which are `Score`'s now, +26), `Assets/Scripts/Simulation/StanceModel.cs` (the four-argument wrapper 246-250, the seven-argument body 256 on), `Assets/Scripts/Elections/GovernmentRecord.cs` (`Agreements` 61, `AgreementOf` 64, `FormAgreements` 67-71, the calls in `AtStart` 150/157 and `FromView` 191, the `World world = null` parameter 137), `Assets/Scripts/Data/WorldFactory.cs` (the world passed at 1092), `Assets/Scripts/Simulation/SimulationManager.cs` (`TrackAgreements` 2469-2480 and its two call sites 1764 and 3088, `WithdrawSupport` 2487-2498), `Assets/Scripts/UI/GameController.cs` (the call 9809, `DrawSupportAgreements` 11087-11104), `Assets/Scripts/UI/GameController.Desk.cs` (the Support case of `RoleTitleSuffix` 266-270), `Assets/Scripts/Persistence/SaveGameService.cs` (`CurrentSaveVersion` 32 at 53), `Assets/Editor/CheckSuite.cs` (the row 187), `Assets/Editor/SupportAgreementDiagnostic.cs` (new, whole, with its `.meta`), `ElectionsData/sweden/tido_agreement.md` (new, the header, the identity quotations and the table; the item quotations sampled) and the untracked raw folder `ElectionsData/sweden/raw/tido/` (its four files listed, the two PDFs not opened). Read for the paths around the change: `StanceModel.cs` (the body 256-388 diffed against HEAD's, `Stances` 234-235, the constants 119-125, `BillConcern.Add`/`IsEmpty`/`Loaded` 45-63), `Assets/Scripts/Elections/GovernmentFormation.cs` (`TryGovernment` 135-152, `ViewOf`/`Describe` 181-253, `TryFormChamber` 256-278, `TryFormSeats` 285-297), `Assets/Scripts/Elections/SeatedGovernment.cs` (`TryAt` 56-77, `TryInstalled` 90-96, `TryAsResult` 103-125), `Assets/Scripts/Elections/WorldClock.cs` (`StartDate` 104-112, the governments of record 235-278), `Assets/Scripts/Simulation/ParliamentSystem.cs` (`ApplyLawBillResult` 726-735, `LawDialAxes` 1045-1049, `GetLawBillConcern` 1051-1068, the crime bill's concern 1035/1040, `RandomSource` 51), `Assets/Scripts/Simulation/LawCatalog.cs` (`IsWithinCompetence` 2513-2516, the border laws' ids and names 171/387/536/767/786, the ten `RequiresOwnCurrency` laws), `SimulationManager.cs` (`ApplyLawBillEffects` 1733-1772, the crime recompute 1855-1866, the dial writes 4121/4282, the law bill's resolution 1686-1705, the boundary's per-country loop 3040-3088, `IntroduceLawBill`'s gate 1642, the budget vote's roles 1228-1241, `_world` 483), `Assets/Scripts/Data/PartySystem.cs` (SD's and M's rows 363-366), `Assets/Scripts/Data/Country.cs` (the two dials 633/739), `GameController.cs` (`DrawParliamentTab` 9792-9815 and its switch 9368-9370, `DrawSentenceAction` 12008-12030, `_removeButtonStyle` 3296, the `PlainName` sites 8747/9009/9118, the post-election `FromView` call 6622), `GameController.Desk.cs` (`DeskCaption` 99-110, the masthead's title 291-364), `SaveGameService.cs` (`BuildSettings` 71-79), `Assets/Scripts/Data/World.cs` (`OwnsCurrencyZone` 14-22), `ProjectSettings/EditorSettings.asset` (the play-mode options 29-30), `Assets/Editor/MetaTextCheck.cs` (the roots 61, the patterns 63-93), `Assets/Editor/PlayerRoleDiagnostic.cs` (48-50), `docs/specs/POLITICAL_SYSTEM_SPEC.md` (§5.2-§5.4, 82-121), `Tools/review_ledger.tsv` (the s632 `StanceModel.cs` row 130, the s634 rows 142-145), the play save on disk (`C:/Users/elias/AppData/LocalLow/DefaultCompany/PoliSim/Saves/playtest_4_precampaign_day1.json`, `"SaveVersion": 31`, 2026-09-25 13:01), and `Reviews/2026-09-25_s634_portfolios.md` (the shape, the masthead measurement).

---

## The reader

Adversarial review of §635 - PS-3h, part 5 of the roles stage: the support agreement as data per the spec's §5.3 - the supporter's demands chosen by the stance model from the catalogue and two dials, the formateur's compatibility as the filter, each item tracked owed / delivered / broken at the boundary and the day a law applies, a withdrawal recorded, format 32, the diagnostic. The refactor of the stance model is exact (finding 1), the demand path draws no RNG and enumerates nothing unordered (finding 2), the tracking's placement is right (finding 5) and the save's shape is sound (finding 11). Four things stand against it: the demands depend on WHO THE PLAYER IS through the stance model's government-bill term (finding 3), the agreement formed after the game's own election excludes every own-currency law because `FromView` has no world (finding 4), a stand-in record forms its agreements twice at the factory (finding 4 too), and the withdrawal strikes the party from a list the chamber's votes never read (finding 8). The rest are notes.

FINDINGS

1. OK - the `StancesOver` split (`StanceModel.cs:246-256`). Diffed against HEAD's file: after the signature line the seven-argument body is BYTE-IDENTICAL to the old four-argument body minus its one line `bool government = GovernmentFormation.TryGovernment(country, out … cabinet, out … support);` (HEAD 254), which now sits in the wrapper (`:248`) and passes the three values in the same names and types (`bool`, `IReadOnlyList<string>` ×2). Every existing caller (`Stances` at `:235`, the diagnostics' synthetic-party calls) takes the four-argument form and so computes `TryGovernment` exactly as before, once per call; the chamber's votes are unchanged. `governmentBill` at `:264` reads the passed `government` and `cabinet` as it read the locals. No behaviour change for any caller outside `SupportAgreement.Demand`.

2. OK - the cost, the RNG, the order at the factory. (a) WHO FORMS AGREEMENTS AT THE EPOCH: Sweden's start is the run-up before 13 September 2026 (`WorldClock.cs:112`, `PreCampaignStart`), inside the Tidö record's window `2022-10-18 → 2026-09-17` (`:236`, cabinet sourced, support `{ "SD" }`) - INSTALLED, so `AtStart` takes the installed path and `FormAgreements` forms SD's (`PlayerRoleDiagnostic.cs:48` asserts exactly "M+KD+L, support SD, of record"). No other government of record carries a support array (`:242-278` are all `null`): Germany (Merz, installed), Poland (Tusk, installed), Italy (Meloni, installed) and the USA (a presidency) form NONE; France's stand-in (`:278`, unsourced → provisional → the formation on the 2024 Assembly) forms one agreement per party the formation puts in support, if its outcome is `ConfidenceAndSupply` - not verifiable by reading (it depends on `CoalitionFormation.Form`'s result); at most a few. (b) THE COST per supporter: one `TryGovernment` (`SupportAgreement.cs:48`), then for each of the 140 laws within competence and not enacted: `GetLawBillConcern` (a loop over 12 deltas plus the structural list, no formation) and one seven-argument `StancesOver` over the party system (8 parties for Sweden, term 1 only - see finding 3), then two more for the dials. Linear, small; the "wedged the warm host" cost was the per-law `TryGovernment`, and it is gone. (c) RNG: `TryGovernment` → `TryFormChamber` → `SeatedGovernment.TryAsResult` (Sweden, installed) or `TryFormSeats` → `CoalitionFormation.Form` - a grep of `GovernmentFormation.cs`, `CoalitionFormation.cs`, `SeatedGovernment.cs` and `Compatibility.cs` for `Random`/`Shuffle` finds nothing; `GetLawBillConcern`/`GetLawBillDirection` touch no `RandomSource` (`ParliamentSystem.cs:51` is the only stream and it is read by the vote, not the concern); `StancesOver`'s body draws nothing; `Track` draws nothing. The dumps' RNG streams are untouched. (d) ORDER: `LawCatalog.All` is a list in file order; `PartySystems.For` a list; `country.EnactedLaws` a list; `Support` a list; the sort at `:61` is total (score descending, then `CompareOrdinal` on the id) so `List.Sort`'s instability cannot show. No dictionary is enumerated on the demand path (the `Portfolios` dictionary of s634 is not read here). Run-to-run identical.

3. DEFECT - the demands depend on who the player is. `Alignments` (`:88-97`) calls the seven-argument `StancesOver` with the real government context, and the body's `governmentBill = government && (concern.GovernmentAuthored || (!IsNullOrEmpty(country.PlayerPartyAbbrev) && cabinet.Contains(country.PlayerPartyAbbrev)))` (`StanceModel.cs:264`) is therefore TRUE for every law scored whenever the player's party sits in the cabinet, and FALSE otherwise. With it true, term 2 fires: the supporter (in `support`) takes the cohesion pull `0.6 × 0.5 × (1 − distance)` toward +1 (`:291-296`), the formateur (in `cabinet`) takes the full cohesion pull, and every law's `wants`/`allows` are inflated relative to the pure position; with it false, term 1 alone. Scenario: the same 2026 chamber, the same formation M+KD+L with SD - a player seated as M (the play protocol's staging, s633) gets SD's agreement scored as government bills, a player seated as S or SD gets it scored on positions; the two agreements can differ in their fourth law and in whether a dial passes `allows >= 0`. At the FACTORY `PlayerPartyAbbrev` is null (`WorldFactory.cs` never sets it; grep), so the diagnostic and the dumps see the position-only scoring - the defect appears only in `FromView` after the game's election (`GameController.cs:6622`, the party set) and in any load-then-re-form path. The spec's sentence is "chooses from its own positions": the government's cohesion is not a position. Fix: pass `government: false, cabinet: empty, support: empty` to the seven-argument call (term 1 only - the whole reason for the split; `TryGovernment` at `:48` then goes too), or set a `BillConcern` flag the body honours. Either keeps the "compatibility" reading (the formateur's own alignment) intact.

4. DEFECT - `FromView` forms with no world, and the stand-in forms twice. (a) `FromView` (`GovernmentRecord.cs:191`) calls `record.FormAgreements(country, formedOn)` with the default `world = null`; `Demand` passes it to `LawCatalog.IsWithinCompetence(world, country, law)` (`SupportAgreement.cs:53`), which for a law with `RequiresOwnCurrency` reads `(world != null && world.OwnsCurrencyZone(country))` → FALSE (`LawCatalog.cs:2515`) - so the ten own-currency laws are outside competence for EVERY country whenever the agreement is formed from a view. The post-election call (`GameController.cs:6622`) has no world to pass because `FromView`'s signature has none. Scenario: Sweden after its 13 September election - the first agreement the player actually plays under is formed by this path; a monetary-regime law SD aligns with cannot be demanded, while the factory's agreement (world passed, `:150`) could have carried it. The competence test the browser and the bill apply is the world's; the agreement's must be the same one. (b) `AtStart`'s stand-in path builds the record through `FromView` (`:154`), which forms the agreements at `:191` with `world = null`, and then forms them AGAIN at `:157` with the world - the first pass is thrown away (`Agreements.Clear()` at `:69`), so the result is right for the stand-in but the cost is doubled (two `TryGovernment` and two catalogue passes per supporter) on exactly the path (France's stand-in, and any provisional country) where the factory's time is spent. Fix both at once: give `FromView` a `World world = null` parameter, pass it from `AtStart` (`:154`) and from the post-election site (`_world` is in `GameController`'s reach through the manager), and drop the second call at `:157`. The `WhatIfGoverning` path (`:166-173`) forms none (its cabinet is the player's party, no support) - right.

5. OK - the tracking's placement and its cases. `TrackAgreements(country)` sits in `ApplyLawBillEffects` AFTER the `if (bill.IsRepeal) … else …` (`SimulationManager.cs:1755-1764`), so a repeal is tracked the day it applies as an enactment is; it is reached only through `ApplyLawBillResult`'s `applyEffects` on a PASSED bill (`ParliamentSystem.cs:726-735`), so a failed vote tracks nothing (nothing changed). It runs BEFORE the three recomputes (`:1769-1771`), so the dial consequence of the law just applied (the crime recompute writes `BorderEnforcementOverride = 50 + Σ deltas` through `ApplyCrimePolicyChanges`, `:1855-1865` → `:4282`) is read at the next turn boundary (`:3088`, after `UpdateSeats`, inside the loop over every country, `:3040-3088`), not the same day - a one-boundary lag on dial items, NOTE. The cases asked: a law item never delivered and not enacted stays OWED for the term - right, the spec's "owed"; a law delivered then repealed → BROKEN and the break recorded (`:2476`), by ANY tabler: the supporter cannot table (the role gate refuses `Support`, `:1642` → `PlayerMayIntroduce`), the AI government tables no law bills (the six `IntroduceLawBill` callers are the desk, the driver and diagnostics), so the repeal is the player's government's act and "the government broke" is the true sentence; a Broken item is never re-read (`:105`), so re-enacting after a break stays BROKEN - a design choice, stated nowhere (NOTE). `Track` runs at every country's boundary: agreements exist only where `Support` is non-empty (Sweden, France's stand-in at most), an AI record's `Breaks` can only grow through its own dial moves, and the AI countries move these dials through no path (the ministry writes lines and rates only; the crime recompute needs a law bill) - harmless.

6. RISK - the dial item: double delivery, double break, the wrong-field question answered. `BorderEnforcementLevel` and `PoliceFundingLevel` (`Country.cs:739/633`, default 50, clamped to `MinPolicyDialLevel..MaxPolicyDialLevel` at every write, `:4121/:4282`) are the two dials the crime bill's own concern loads (`ParliamentSystem.cs:1035/1040`, `Galtan` and `ImmigratePolicy`, positive toward 10) - the right fields, the right axes, and the sign convention of `concern.Add(axis, +DialStep)` (`SupportAgreement.cs:76`) matches the bill's. Two consequences the record does not state: (a) the dials are RECOMPUTED FROM THE ENACTED LAWS - `RecomputeCrimeJusticeDialsFromEnactedLaws` writes `border = 50 + Σ BorderEnforcementDelta` over the book (`:1852-1862`) and applies it as an override on every law application; so enacting `border_security_act` (+20) both delivers the LAW item and moves the dial from 50 to 70 ≥ 60 and delivers the DIAL item ("Border enforcement to 60") at the next boundary - one act, two deliveries; and repealing it later breaks the law item AND, if the dial falls below 45, the dial item - one act, two breaks. Four of SD's five items can therefore ride one law. (b) A dial item is BROKEN while still OWED whenever `value < StartValue − 5` (`:116`) - a crime bill the government tables lowering border enforcement by 6 points, or a repeal of some earlier border law, breaks a demand the government never accepted as delivered; that reads as the spec intends ("a dial target to reach … broken"), and the tolerance is stated `[AUTHORED-DRAFT]`. A dial delivered at 60 and later moved to 50 (above 45) stays DELIVERED - the target is not held, only the start defended; NOTE. The AI ministry (`AiFinanceMinistry`) never writes these dials.

7. RISK - the saturated alignment, chosen by id. SD's `immigratePolicy` is 9.95 (`PartySystem.cs:363`), so on a concern that loads the immigration axis alone the position term is `(9.95 − 5) / 5 = +0.99`, and `border_security_act`, `immigration_detention_expansion_act`, `frontex_border_cooperation_agreement`, `physical_border_barrier_construction`, `schengen_border_reimposition` (`LawCatalog.cs:171/387/536/767/786`) all load `LawDialAxes[5]` (`ImmigratePolicy`, toward 10) - those with other deltas take a weighted mean with the `Galtan` axis where SD sits at 9.00 (+0.80), so a pure border law reads +0.99 and a mixed one a little under. Among the pure ones the score ties exactly and the sort's second key `CompareOrdinal(a.Law.Id, b.Law.Id)` (`:61`) chooses: `border_…` before `frontex_…` before `immigration_…` before `physical_…` before `schengen_…`. So the diagnostic's "four at +0.99" is the alphabet's choice among a saturated set, not the party's - a real reading of "chooses from its own positions" would need a second key that is a position (the law's own weight - `Direction`, the sum of its dial moves - or the manifesto's emphasis, both in hand). M's `immigratePolicy` 8.47 gives `allows` +0.69 on the same laws, comfortably ≥ 0. The filter `wants <= 0.05f` (`:58`) reads the `UndecidedBand` idea but at half its size; `allows < 0f` lets a formateur with NO measured axis (alignment 0, `Measured` false) "not oppose" - fine as compatibility, NOTE. The Basis strings carry "+0.99" through `{0:+0.00;-0.00}` Invariant (`:64`), not drawn.

8. DEFECT - the withdrawal strikes a list the chamber never reads. `WithdrawSupport` removes the party from `GovernmentRecord.Support` (`SimulationManager.cs:2493`) and records the break. What reads that list: `RoleOf` (`GovernmentRecord.cs:127`) - so the masthead reads IN OPPOSITION, the budget vote's `governmentParty` (`:1231`) no longer counts the party as the government's, and `breakBy` (`:1241`) no longer records an own alternative as a break. What does NOT read it: `GovernmentFormation.TryGovernment` (`GovernmentFormation.cs:135-152`), which the stance model's every vote takes through the four-argument wrapper - it re-forms from `SeatedGovernment.TryInstalled` (Sweden before its election: the Tidö record's `{ "SD" }`, `WorldClock.cs:236`) or from `TryFormSeats` (after it: the formation's own result on the chamber) - and so, for every government bill after a withdrawal, SD is still `inSupport` (`StanceModel.cs:291`), still takes the `SupportPull` cohesion toward the government's bill and still carries no populism term (`:326`). The two views disagree: the record says opposition, the chamber votes it as a supporter. `SeatedGovernment.TryInstalled` still says INSTALLED with SD for the of-record window - as the task expects - and the formation's stand-in after the election re-derives SD's support from compatibility and declarations, which the withdrawal does not touch either. Scenario: SD withdraws on day 10 over a broken item; the government's next budget bill is voted with SD pulled +0.3 toward it by cohesion, so the government it left still passes its frames on SD's votes. The record's own comment says "the government stands on without the supporter" (`:2483`) - it stands on WITH it in the division lobby. Fix: `TryGovernment` should read `country.Government` (the stored record, which is what PS-3a introduced it for) when one exists - the same disagreement class s628's record was built to end - or, at minimum, the record should state that the chamber's cohesion term is part 6's. Also unstated: the withdrawn party's agreement stays in `Agreements` and keeps being tracked (breaks keep accruing on a government it no longer supports), and the tab keeps drawing it without its button (`GameController.cs:11096` guards on `Support.Contains`) - acceptable as "the record", but say so.

9. OK - the desk. `DrawSupportAgreements` (`GameController.cs:11087-11104`) draws inside `DrawParliamentTab`'s scroll view after `DrawPendingLegislation` and before `DrawRecentDivisions` (`:9808-9812`), for EVERY role (`DrawParliamentTab` is reached from the politics switch with no role gate, `:9368-9370`), so an M player sees SD's agreement - right, the government's obligations; the WITHDRAW sentence appears only when the player's party is the supporter and still in `Support` (`:11096`). The caption is `DeskCaption(9, TextPrimary, bold)` - the mono Courier caption, upper-case by convention, and the line "SUPPORT AGREEMENT · SD · 4 OWED · 1 DELIVERED · 0 BROKEN" is upper-case; the items in `_labelStyle` read "Border Security Act · OWED" - the FORMAL name (`AgreementItem.Name = law.Name`, `SupportAgreement.cs:64`), where the desk's law pane shows the PLAIN name at rest and the formal name as an 8 px caption (`:8747`, `:9009`, `:9118`, §415) - the agreement should carry `PlainName` (RISK, one line: `law.PlainName ?? law.Name`, with `Name` kept in the record). The sentence action rides `DrawSentenceAction` with `_removeButtonStyle` (`UiPalette.ButtonKind.Remove`, `:3296`) - the destructive face, right for a withdrawal; the sentence wraps to the row's two lines and shrinks past that (`:12017-12021`); `enabled: true` with no confirmation - a one-click irreversible act on the same face the desk uses to remove a line, NOTE (the spec's "threaten" is not built; the record says part 6). A refused withdrawal logs and shows nothing (`:11100`) - unreachable through the guard. THE MASTHEAD: "SWEDEN · YEAR 1 · IN SUPPORT · AGREEMENT: 4 OWED · 1 DELIVERED · 0 BROKEN" is 72 characters against s634's 61 - at ~8.2 px per character (bold Document 10.5 px at 1280, s630's measurement) about 590 px in a title rect clamped to `liveLeft − gap − titleLeft` (`Desk.cs:363`) and drawn through `MeasuredLabel`, which shrinks before it clips; the s634 reading put the right cluster and LIVE at ~450 px, leaving ~770 px - it fits, unfilmed, and it is the longest suffix the masthead has carried; "10 OWED" adds one. NOTE.

10. OK - the diagnostic (`SupportAgreementDiagnostic.cs`). It seeds 777 after `ApplyStart(Sweden)` (the epoch scope disposed at the end), builds the default world (SD's agreement formed at the factory with the world, finding 2), and asserts: one agreement, SD's; ≥ 2 items all OWED; at least one law; every law item re-scored through the four-argument `Stances` reads `wants > 0` and `allows >= 0` for SD and M (with `PlayerPartyAbbrev` still null at that point, so the same term-1 scoring `Demand` used - the check would NOT catch finding 3, whose trigger is a seated cabinet party); then with M seated, `IntroduceLawBill` (M is the PM, the portfolio leg passes), up to 25 day ticks until enacted, DELIVERED asserted on the item; the repeal exercised by `EnactedLaws.RemoveAll` + `TrackAgreements` (`:74-75`) - the chamber may refuse a repeal SD's bloc opposes, so the book is edited directly: the code path `ApplyLawBillEffects → TrackAgreements` after the `RemoveAll` branch is the same call the check makes, so the exercised code is the tracker, not the placement (finding 5 reads the placement). The dial set to its target directly and DELIVERED asserted; S refused with the exact string; KD refused (the reason unasserted - it is "YOUR PARTY IS NOT A SUPPORT PARTY", fine); SD withdraws, struck and recorded; a REAL serializer round trip (`Serialize`/`Deserialize`) asserting the count, the item count and `Items[0].State` (BROKEN after the repeal, so a non-default enum value is round-tripped). `sd.Items.Count` at `:101` dereferences `sd` after `Check(sd != null …)` - a null `sd` reaches it and throws into the `catch` as a counted failure, not a crash. Not asserted: the four laws' identity (printed, `:44`), the alphabet's role among ties (finding 7), the France stand-in, a dial BREAK (only delivery), and that `TrackAgreements` at the boundary (`:3088`) is reached - the 25-day loop reaches it implicitly if the bill resolves inside it. The `CheckSuite` row is in the `Suite` array (`:187`).

11. OK - the save. `Agreements` is `List<SupportAgreement>`, a public field initialised at declaration; `SupportAgreement` and `AgreementItem` are plain classes with public fields, three enums serialised as integers (no `StringEnumConverter` in `BuildSettings`, `SaveGameService.cs:71-79`), `DateTime`s under `RoundtripKind`, no cycles (`Track`'s returned list is not stored). `ObjectCreationHandling.Auto` appends to the initialised lists - fine for a fresh record. `CurrentSaveVersion = 32` (`:53`) and the loader refuses every other version; the play save on disk reads `"SaveVersion": 31` (written 2026-09-25 13:01, s634's re-cut) - refused by this build, the re-stage owed as every bump since 27 (DEFECT, procedural, expected). A v-old record cannot reach `country.Government.Agreements` because the version gate is before the graph; in the factory `country.Government` is assigned from `AtStart`'s return, so no path reads `Agreements` on a null record; `TrackAgreements` and `WithdrawSupport` guard `country?.Government == null`; `DrawSupportAgreements` guards `_playerCountry?.Government`.

12. OK - `MetaTextCheck`, the culture, the log. The roots are `Assets/Scripts/UI`, `LawCatalog.cs` and `Assets/Scripts/Data` (`MetaTextCheck.cs:61`); the new UI literals - "SUPPORT AGREEMENT · ", the withdrawal sentence, "Withdraw support", " · IN SUPPORT · AGREEMENT: " - match none of the banned patterns (no §, no register id - `SD` has no digit for `S\d+`, no "the spec", no backtick); `SupportAgreement.cs` is OUTSIDE the roots though its strings reach the screen: `Tally()` ("OWED · DELIVERED · BROKEN"), `Line()`, the dial's "Border enforcement to 60" - all clean by reading; "AUTHORED-DRAFT" and "§" appear in its comments only (and the file is not scanned). `Tally` and the dial's `ToString("0", …)` are Invariant; `State.ToString().ToUpperInvariant()` culture-free; `$"{CurrentDate:yyyy-MM-dd}"` in the break lines is the file's standing idiom. The `Debug.Log("AGREEMENT: …")` lines are new log prefixes, greppable.

13. NOTE - the reference and the raw folder. `tido_agreement.md` is a reading of the two PDFs (byte-exact copies under `raw/tido/`, `SHA256SUMS` and `fetch_log.txt` present, the 404 page kept) with its counting rule stated and 191 items counted; the record says it is the comparison, not the data, and `SupportAgreement.cs:14-15` says the same - consistent with the spec's "chooses from its own positions". The raw folder and the document are untracked and unstaged: the commit must add them (and `.gitattributes` already marks raw pages `-text`, per s607). Not compared: the model's four border laws against the Tidö migration project's 33 items - the record names the comparison as the reference's purpose but makes none; a one-line "the model's SD demands read against Tidö: migration 33 of 191, the model's four of four" would close the loop.

14. NOTE - the ledger. `Tools/review_ledger.tsv` carries s634 rows for `SimulationManager.cs`, `GovernmentRecord.cs`, `GameController.cs` and `SaveGameService.cs` (142-145) and an s632 row for `StanceModel.cs` (130) - all five change here, and `WorldFactory.cs` (118, s629) too; `ReviewLedgerCheck` will want this review's rows before the cheap bar.

15. DEFECT - the `Templates` cache (added after this review's launch; `SupportAgreement.cs:50-68`, the scoring moved to `Score` at 70-112). The key (`:54`) is `Id | supporter | formateur | yyyyMMdd | sorted enacted ids | BorderEnforcementLevel | PoliceFundingLevel | sorted seats`, and the result is `Copy()`'d. WHAT THE SCORING READS THAT THE KEY OMITS: (a) THE WORLD - `IsWithinCompetence(world, …)` (`:79`) reads `world.OwnsCurrencyZone(country)` (reference identity over the world's countries, `World.cs:14-22`) and is FALSE for the ten own-currency laws when `world` is null; the key does not carry whether a world was passed, so the first caller's competence is every later caller's. Scenario, in the code as it stands: a provisional country's `AtStart` calls `FromView` (`GovernmentRecord.cs:154` → `:191`, `world = null`) FIRST and caches the null-world template; the second call at `:157` with the world hits the SAME key (same id, supporter, formateur, date, laws, dials, seats) and returns the null-world agreement - the fix of finding 4(b) by "drop the second call" is now the ONLY fix that works, and `FromView` must carry the world (finding 4(a)) or the key must carry `world != null` (worse: two templates for one state). (b) `PlayerPartyAbbrev` - the government-bill term (`StanceModel.cs:264`) reads it and the key does not; in one process the factory scores with the player null (term 1), and a later `FromView` with the player seated in the cabinet keys the same state (same date only when re-formed on the epoch day - the diagnostics' and the driver's re-seatings do exactly that) and returns the term-1 template; a different order returns the term-2 one. Finding 3's fix (score on positions alone) removes the read and the omission together; without it the key must carry the party. (c) `SeatedGovernment.HeldElection(country)` (`ElectionHistory`) decides whether `TryGovernment`'s context is the installed record or the formation (`GovernmentFormation.cs:272`) - two worlds with the same seats, one before and one after a held election, take different cabinets/support into term 2; again moot under finding 3's fix, unkeyed until then. (d) `MinimumWageImplemented` (`ParliamentSystem.cs:1060`, the law concern skips the wage delta without it) - seeded per country and never written in play (grep: `WorldFactory.cs:467-479` only), so the country id keys it today; unkeyed against a future writer. NOT read by the scoring, so rightly absent: approval, the campaign trail, the budget, the ministry - `StancesOver` reads `ParliamentSeats`, `PlayerPartyAbbrev`, `TradeAxisAvailable` (the party system by id), the voter profile and salience by id, and the concern; `GetLawBillConcern` reads the law, `MinimumWageImplemented` and the structural grid; positions are the static party system. The dials go into the key through `{5}`/`{6}` under `InvariantCulture` - "50", not "50,0" - fine. THE COPY (`:63-68`): all three agreement fields and all ten `AgreementItem` fields are copied field by field into new objects; the template's items are never handed out, so tracking on the copy cannot reach the cache - faithful. THE STATIC ACROSS RUNS: `ProjectSettings/EditorSettings.asset` has `m_EnterPlayModeOptionsEnabled: 1` with `m_EnterPlayModeOptions: 0` (no flag set), so entering play mode still reloads the domain and clears the dictionary; a `-batchmode -executeMethod` bar is a fresh process. The case that persists: the WARM EDITOR's bar command (s575's no-recompile rule) runs `CheckSuite` in one domain across successive bar runs, so a template scored under one build of the code survives an edit to `LawCatalog`, `PartySystem` or the scoring itself until the next recompile - a check that reads "the four laws" after a catalogue edit can read the previous run's four. Within one `CheckSuite` run every world-building check shares the cache, which is the intent; a harness that changes `EnactedLaws` between worlds is keyed by them. Fix: carry `world != null` (or, better, land findings 3 and 4 so the scoring reads nothing the key omits), and clear `Templates` in `CheckExit.ArmLogFold()` or at the suite's start so a warm domain cannot serve a stale catalogue.

VERDICT: NOT READY - TWO DEFECTS IN THE DEMAND, ONE IN THE WITHDRAWAL, AND THE CACHE'S KEY INCOMPLETE. The refactor is exact and every existing vote unchanged (1); the demand path is deterministic, RNG-free and cheap (2); the tracker's placement is right and its law cases read as the spec says (5); the save's shape and the diagnostic's round trip are real (10-11). Before the commit: (3) score the demands on POSITIONS ALONE - pass `government: false` (and empty lists) to the seven-argument `StancesOver`, or the agreement changes with the player's seat after the election; (4) give `FromView` the world and drop `AtStart`'s second `FormAgreements` - as built, every post-election agreement excludes the ten own-currency laws and every stand-in forms twice; (8) either make `TryGovernment` read the stored record or state in the record that a withdrawn supporter still votes with the government's cohesion until part 6 - the masthead and the chamber disagree today; (15) the cache's key omits the world (so the stand-in's second, world-bearing call returns the first, null-world template), the player's party and the held-election state - landing 3 and 4 removes the reads, and the warm Editor needs the dictionary cleared at the suite's start. Then as notes: the play save's re-cut to v32 (11), `PlainName` on the item line (9), the tie among saturated laws broken by the alphabet (7), the one-law-two-items coupling of the dial demand (6), the Broken-is-final and the delivered-then-lowered dial (5, 6), the withdrawn agreement still tracked (8), the untracked reference files to add (13), the ledger rows (14).

---

## Second reading

**Re-read (read-only, the working tree after the fixes).** `Assets/Scripts/Elections/SupportAgreement.cs` (`Templates`/`Demand` 50-57, `Copy` 63-68, `Score` 70-112 - the empty context at 75, the weight at 85, the sort at 87, the item at 90 - `Alignments` 114-123), `Assets/Scripts/Elections/GovernmentFormation.cs` (`TryGovernment` 135-161, the stored-record branch 141-149; `ViewOf`/`Describe`/`TryFormChamber` unchanged), `Assets/Scripts/Elections/GovernmentRecord.cs` (`AtStart` 137-158 - `FromView(…, world: world)` at 154, no second `FormAgreements`; `FromView` 176-192 with `World world = null` and `:190`; `WhatIfGoverning` 166-173 unchanged), `Assets/Scripts/Simulation/SimulationManager.cs` (`TrackAgreements` 2469-2481 with the withdrawn skip at 2474; `WithdrawSupport` unchanged; the `WouldBillPass`/`Stances` callers 1218-2134), `Assets/Scripts/UI/GameController.cs` (`ResolveElectionVerdict` 6605-6624, `world: _world` at 6622; the what-if store at 2222; `DrawSupportAgreements` unchanged), `Assets/Scripts/Simulation/StanceModel.cs` (the 4-arg wrapper 246-250, `CabinetPositions` 490-509, the bloc loop 268-276, term 2's branches 288-333), `Assets/Scripts/Simulation/ParliamentSystem.cs` (`GetLawBillDirection` 692-716, `WouldBillPass` 332-338), `Assets/Scripts/Simulation/LawCatalog.cs` (140 `PlainName` literals over 140 ids, none empty), `Assets/Scripts/Data/PartySystem.cs` (the twelve cabinet keys of the installed records all seeded), `Assets/Editor/SupportAgreementDiagnostic.cs` (the new assertion at 96), `Assets/Editor/StanceModelDiagnostic.cs` (44-63) and `Assets/Editor/OfficeTestDiagnostic.cs` (248-256) as the only other `TryGovernment` callers, `Assets/Editor/PlayerRoleDiagnostic.cs` (88-124) and `Assets/Editor/PortfolioAllocationDiagnostic.cs` (68) for the what-if callers.

S1. OK - (3) fixed. `Score` passes `(false, empty, empty)` (`:75`), so `governmentBill` is false for every law and dial scored and term 1 alone reads (`StanceModel.cs:264`); the `TryGovernment` call is gone from the scoring; `PlayerPartyAbbrev` and the held-election state are no longer read by anything under the cache key (finding 15 (b) and (c) closed by construction). The diagnostic's own re-check (`:56`, the 4-arg `Stances` with the real context) asserts only signs, and with the player null at that point it reads term 1 too - consistent.

S2. OK, one NOTE - (7) the positional second key. `Weight = |concern.Direction|` (`:85`), sorted after the score and before the id (`:87`), named in the Basis with `{1:0.#}` Invariant (`:90`). `Direction` is `GetLawBillDirection`'s SIGNED NET of the dial deltas under `LawDialSigns` plus the structural grid (`ParliamentSystem.cs:700-716`) - a law with opposing deltas (a border cut beside a police rise) nets, so the "weight" is the law's net lean, not the size of its movement; among the five saturated border laws this is a real positional key (their border deltas 20 / 16 / 12 / 26 / 14 with their other deltas net differently), and the comment at `:85` says "how far the law moves" - one word off. NOTE. Ties in the weight still fall to the alphabet; stated by the sort.

S3. OK - (9) `Name = law.PlainName ?? law.Name` (`:90`); every one of the 140 laws sets a non-empty `PlainName`, so the item line reads the plain name as the law pane does.

S4. OK - (4) fixed on both counts. `AtStart`'s stand-in passes its world through `FromView` (`GovernmentRecord.cs:154`) and the second `FormAgreements` is gone (`:156` allocates only); `FromView` forms once with the world it is given (`:190`); `ResolveElectionVerdict` passes `_world` (`GameController.cs:6622`). The one `FromView`/`AtStart` caller left without a world is `WhatIfGoverning` (`:168`, `AtStart(country, start)`) - it forms the of-record agreements with `world = null` and discards them, and under the cache that null-world scoring would be stored under the same key as the factory's; the factory always runs first in a process (the what-if needs a built world, `GameController.cs:2222`, the diagnostics at `PlayerRoleDiagnostic.cs:124`/`PortfolioAllocationDiagnostic.cs:68` after `CreateDefault`), so the cache serves the world-bearing template and the null call scores nothing. Residual, NOTE: the key still omits `world != null` (finding 15 (a)); harmless while every scoring caller passes a world or runs after one that did.

S5. OK with two things STATED - (8) `TryGovernment` reads the stored record. (a) THE CALLERS: the 4-arg `StancesOver` wrapper (`StanceModel.cs:248`, every chamber vote through `WouldBillPass`/`SeatSides`/the budget contest) and three diagnostics (`StanceModelDiagnostic.cs:44/56`, `OfficeTestDiagnostic.cs:250`, the new `SupportAgreementDiagnostic.cs:96`) - NOTHING ELSE; election night, the pickers' "IN THE CABINET", `GovernmentFormation.IsProvisional`, the reference view and `ResolveElectionVerdict` all go through `ViewOf`/`Form`/`SeatedGovernment`, which are untouched, so no display of a hypothetical reads the stored record. Between the count and the verdict: `ElectionHistory.Add` (`GameController.cs:6830/6843`) makes `HeldElection` true before `ResolveElectionVerdict` replaces the record - the OLD path would have re-formed on the new chamber for any vote in that window, the NEW returns the sitting record; the verdict's own rule ("you stay in office until one can", `:6621`) is the record's, so the new reading is the ruled one. (b) THE DUMP: by reading, every `WouldBillPass` and `Stances` call is on a bill the manager's `Introduce*`/budget-contest paths table (`SimulationManager.cs:1218-2134`), the AI countries apply their budgets without a vote (`ministryByBill` only for the player's country, s634 finding 6), and the no-player dump tables nothing - so `TryGovernment` is not reached and the trajectories are byte-identical by construction (the factory's `Demand` draws nothing, finding 2). Should a vote be reached, the record at each epoch EQUALS the old path's result: Sweden, Germany, Poland, Italy and the USA are INSTALLED (`WorldClock.cs:236/244/250/256/264`), the old `TryAsResult` masked the same `Cabinet`/`Support` arrays over the party system (`SeatedGovernment.cs:103-125`; all twelve cabinet keys are seeded, so its "names a party the chamber does not seat" failure was not live), and the new branch copies the same arrays; France's stand-in is `FromView(ViewOf(country))` (`GovernmentRecord.cs:153-154`), the same `TryFormChamber` → `TryFormSeats` the old `TryGovernment` ran, at the same seeds. TWO DIFFERENCES that remain, stated: (i) ORDER - the old lists were in party-system order, the new in the record's (the installed array's M, KD, L; the view's by seats descending); `CabinetPositions` iterates the party system and tests `cabinet.Contains` (`StanceModel.cs:497-499`), so it is order-free, but the `governmentBloc` loop (`:268-276`) takes the FIRST strict maximum over `cabinet` - two largest cabinet parties with EQUAL seats in different blocs would now pick by the record's order instead of the roster's; no seeded cabinet ties (Sweden 70/22/19, Poland KO over TD/NL, Italy FdI over the rest, one-party USA, Germany's CDU/CSU/SPD unequal). (ii) DRIFT - `UpdateSeats` moves the chamber every turn; the old path re-formed a PROVISIONAL country's government on the drifted seats at every vote (France), the new holds the factory's record until an election replaces it - the ruled behaviour (§628: who governs is stored), and a change only for a France player, whose record is the what-if anyway. (c) THE WHAT-IF: with the stored record read, France's chamber now votes the player's UG as the cabinet (`WhatIfGoverning`, cabinet = the party alone, no support): the player's bills are government bills, UG takes cohesion, the rest the bloc line or the opposition line - BEFORE, those same votes read the formation's stand-in as the government while the record said the player governed. The new reading is the §631 ruling made whole ("the player's party governs on the chamber of record"), but it is a BEHAVIOUR CHANGE for every France what-if vote: `GovernmentBudgetBillDiagnostic` (player path) and any France film would move; unmeasured here, to be run. (d) WITHDRAWN SD's votes: with SD in neither list, term 2 reads it as a bloc party of M's bloc (`:323`, `BlocPull 0.3 × (1 − distance)`) plus its populism term (5.33 > 5, small) - pulled toward government bills at half the support pull, not against; the model's own 2b, stated here so the record does not claim it votes as opposition.

S6. STATED - (b) the `Cabinet.Count > 0` guard. A record with `Outcome = "none"` (`FromView:179`, an empty cabinet) arises only from `AtStart`'s stand-in on a seeded chamber that forms nothing (`ResolveElectionVerdict` keeps the previous record when none forms, `:6621`). For it `TryGovernment` falls to `TryFormChamber` on TODAY's seats: at the factory the same formation returns `NewElection`/`Collapse` again and `TryGovernment` is false - consistent with the record; after seats have DRIFTED under `UpdateSeats`, the formation may form a government on the drifted chamber while the record still says none - a government the record does not hold, exactly the class §628 stored the record to end. No seeded country produces a "none" record today (the five installed; France's 2024 Assembly forms a stand-in, `PlayerRoleDiagnostic.cs:109-111` asserts it), so the class is latent. The right guard is on the record's existence, not its cabinet: `country.Government != null` → return its lists (empty cabinet → false), and the formation path only for a world with no record at all (tools' un-seated countries).

S7. OK - the withdrawn skip (`SimulationManager.cs:2474`) leaves the agreement as the record (drawn with its frozen tally, no button, `GameController.cs:11096`) and stops its tracking; `TryGovernment` after the withdrawal reads M+KD+L with no SD and the diagnostic asserts it (`:96`).

S8. STILL OPEN - (15) the cache's warm-domain life. No `Templates.Clear()` exists (grep); the key omits `world != null` (S4) and `MinimumWageImplemented` (seeded only). Under the warm Editor's no-recompile bar a template scored under one catalogue survives an edit to `LawCatalog`/`PartySystem` until the next recompile - unchanged from the first reading; a clear at the suite's start (or in `CheckExit.ArmLogFold()`) closes it in one line.

S9. NOTE - the `TryGovernment` doc comment (`GovernmentFormation.cs:141-143`) says "a world with no withdrawal reads exactly what the formation below would form" - true at the factory (S5 (b)), not after seat drift for a provisional country or a "none" record (S5 (b)(ii), S6); say "at the record's formation".

SECOND VERDICT: READY WITH ONE GUARD AND ONE CLEAR. Findings 3, 4, 7, 8 and 9 are fixed as described and verified by reading: the demands are positions alone, formed once with the world on every live path, the item carries the plain name and a positional tie-break, and the withdrawal reaches every vote through the one caller that matters. Before the commit: (S6) guard `TryGovernment` on the record's existence rather than its cabinet's count, so a "none" record cannot be out-formed by drifted seats; (S8) clear `Templates` at the suite's start. Then as notes: the France what-if's votes now read UG as the cabinet - a behaviour change to run (S5 (c)); the record-order bloc tie and the drift difference (S5 (b)); the withdrawn party votes as a bloc party (S5 (d)); the "weight" is the net lean (S2); the `WhatIfGoverning` null-world call and the key's `world != null` (S4); the comment at `:141` (S9); the v31 play save, the untracked reference files and the ledger rows from the first reading.
