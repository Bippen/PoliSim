# Review: PS-3a - who governs, stored and read as the player's role; the family `ps3a` (2026-09-25, s628)

**What was reviewed.** The staged change to the money paths `Assets/Scripts/Simulation/SimulationManager.cs` (`PlayerGoverns`, the four gates and the budget-process gate), `Assets/Scripts/Data/Country.cs` (`Government`), `Assets/Scripts/Persistence/SaveGameService.cs` (format 27) and `Assets/Scripts/UI/GameController.cs` (the decision routing, the record written at the start and after an election), with `Assets/Scripts/Elections/GovernmentRecord.cs` (new), `GameController.Desk.cs` (the masthead's role), `Assets/Editor/PlayerRoleDiagnostic.cs` (new) and `PlayProtocolStaging.cs`. One independent read-only reader, asked to refute the save path, the default, the seeded starts by country, the role ordering, the bill hole, the masthead's width and the diagnostic. Its report verbatim, then what was done.

---

## The reader

Adversarial review of PS-3a (working tree at G:/UNITY/Projects/PoliSim, read-only). Note first: the diff I was handed and the file on disk differ at ResolveElectionVerdict - the working tree ALREADY carries the "keep the previous record when no government forms" form (`if (formedView != null && formedView.HasGovernment) { _playerCountry.Government = FromView(...) }`, GameController.cs:6606-6608), so item 4's contradiction is resolved in the current file, but it left a new null dereference (finding 2).

FINDINGS

1. DEFECT - the government of record is written only when a party was pre-seated; the harness, the fallback seat and a second game in one process get NO record, so the player governs regardless of party. GameController.cs:2171-2178: `if (after != null && !string.IsNullOrEmpty(seatedParty)) { ... after.Government = AtStart(...) }` where `seatedParty = before?.PlayerPartyAbbrev` (line 2156). The one-argument SelectPlayerCountry (the harness's, UiScreenshotDriver.cs:393/3567, and StartScenario's line 2035) reaches OpenWorldAt with no party seated, so the block is skipped; SelectPlayerCountry then seats the largest party (lines 2200-2213: S for Sweden) WITHOUT a Government. Result: every film and every harness run opens Sweden with S as prime minister of M+KD+L's world - Government null - and PlayerGoverns is true (SimulationManager.cs:2265). The masthead suffix never appears on film. The picker path (SelectPlayerCountryAndParty -> SeatPlayerParty first) is the only one that stores a record. Same skip, a second way: OpenWorldAt's `if (sameEpoch) { return; }` (line 2159) returns before any seat when EpochDate already equals the country's start and the clock sits on it - a country re-chosen at turn 0 after ResetPlayerCountrySelection (the driver's path, 2244-2248) or any future "new game in the same process" keeps the old world's country and never writes a Government. Fix: move the AtStart write out of the seatedParty guard (write it whenever `after != null`, PlayerPartyAbbrev may be filled later) and also write it in SelectPlayerCountry after the largest-party fallback, or make the fallback go through one seat routine that stores the record.

2. DEFECT - NullReferenceException on election night when no government forms and no record is stored. GameController.cs:6608: the ROLE log's second interpolation `_playerCountry.Government.RoleOf(...)` is unguarded; the first interpolation guards `Government != null` but the second does not. With finding 1 (Government null on every harness/scenario/fallback path) and a formation that returns NewElection/Collapse (GovernmentFormation.cs:222-228), FromView is skipped and the log throws inside ResolveElectionVerdict - on polling day, in the day loop. Guard it (`Government?.RoleOf(...) ?? PlayerRole.None`).

3. RISK - the "none" case is now handled by keeping the old record, but the old record's meaning drifts: after the game's election a stale record (e.g. the 2022 M+KD+L Tidö cabinet, FormedOn 2022-10-18, Outcome "of record") keeps standing over a chamber that may no longer seat those parties, and RoleOf still reads it. The verdict text "You stay in office until one can" now matches PlayerGoverns only if the player was PM before; a player who was in opposition before stays in opposition with the AI governing - also consistent. No contradiction left, but nothing marks the record as carried over (Provisional stays false, FormedOn stays old). NOTE-level fix: set Provisional=true / append to Basis when carrying it.

4. Save/load - OK, with one caveat. SaveGameService.cs:71-80: PreserveReferencesHandling.Objects, MissingMemberHandling.Ignore, TypeNameHandling.None; GovernmentRecord is a plain sealed class with public fields and two Lists initialised at declaration - Json.NET serialises public fields, ObjectCreationHandling.Auto populates the fresh instance's empty lists (no doubling), $id on the object is harmless. RestoreFromSave (GameController.cs:899-935) adopts save.World wholesale and never calls OpenWorldAt ("A load never comes here", 2141-2142; the early return at 2148 also fires because CurrentTurn/_selectedPlayerCountryId are set after RestoreInto). SimulationManager.RestoreSaveState (2628) only replaces the world. The record survives; the diagnostic's round trip (PlayerRoleDiagnostic.cs:62-67) is a real test of this. Version 27 refusal at SaveGameService.cs:161. Caveat (NOTE): SeatedGovernment.TryInstalled/IsProvisional read EpochDate (SeatedGovernment.cs:50) - fine on load because RestoreInto sets the epoch (PS-1), but any future reader of Government that also asks the formation is mixing a stored record with a live re-derivation; only PlayerGoverns and the masthead read the record today.

5. The Editor paths (what each sees) - the family is unchanged, and the record's claim "the AI governs Sweden in the dump" is TRUE for the wrong reason: TrajectoryBaselineDump.cs:97-110 never sets sim.PlayerCountryId -> PlayerGoverns false for every country (the `!PlayerCountryId.HasValue` leg, SimulationManager.cs:2264) -> AI ministry governs Sweden exactly as before. Government null. Unchanged. NoPolicyCentury.cs:61-68: sets PlayerCountryId=player, no party, no Government -> PlayerGoverns TRUE for the player's country (Government==null leg) -> as before. Unchanged. PlayProtocolStaging.cs:59-69: sets PlayerCountryId=Sweden and PlayerPartyAbbrev = largest (S) directly, never AtStart -> the staged save (format 27) carries Government: null -> on load S governs Sweden as PM while a picker-started game seats S in OPPOSITION under M+KD+L. DEFECT-adjacent inconsistency with the play protocol's stated purpose ("a played run, a filmed one and this save open as one party in one world", line 62): the staged save is not the game a player would start. Add `player.Government = GovernmentRecord.AtStart(player, WorldClock.StartDate(Sweden))` there, and PlayProtocolCheck should assert it. The harness (UiScreenshotDriver 393): see finding 1 - Government null, S governs.

6. Seeded starts, by country (GovernmentRecord.AtStart, lines 54-66; WorldClock.Governments 208-241): Sweden 2026-01-18: Kristersson row holds 2022-10-18 <= d < 2026-09-17, CabinetSourced true -> Installed, Cabinet M,KD,L, Support SD; HeadParty parses "Ulf Kristersson (M)" -> "M" (roster check passes). Correct; the diagnostic's assertion (line 46) holds. Germany 2024-11-06: the FIRST Scholz row (SPD,Grune,FDP; Until 2024-11-07, Holds is `date < Until`) -> Installed with FDP still in; PmParty "SPD". The assertion `PmParty=="SPD" && Cabinet.Contains("SPD")` (diagnostic line 77) is true either way, but the diagnostic's summary comment (line 18: "SPD+Grüne after the FDP left") is wrong for the ruled start date - NOTE, fix the comment or assert the FDP is present. Poland 2023-02-19: Morawiecki row CabinetSourced=false -> SeatedGovernment.TryAt returns Provisional with Cabinet null (SeatedGovernment.cs:66-74) -> AtStart falls to ViewOf(country) (vintage Seated; TryInstalled false since the standing is Provisional) -> the formation on the 2019 Sejm with derived lines only, Provisional=true, PM = Largest (no candidacies outside Sweden, DeclaredRedLines.cs:311). Plausibly PiS; the player's default (largest = PiS) governs. RoleOf is meaningful. Acceptable given §605, but note the AtStart guard `record.Cabinet != null` is redundant with Standing==Installed (Record's ctor forbids Installed with an empty cabinet, SeatedGovernment.cs:44). Italy 2022-07-21: Draghi row cabinet null -> same stand-in on whatever chamber WorldFactory seats (Italy2018 is NOT seat-sourced, PartySystem.cs:622-628, so the seated table is the latest sourced vintage - 2022's - under a 2022-07 date). The formation with derived lines may well exclude the largest party (the player's default), so an Italy player can open IN OPPOSITION with the AI governing - RISK, unmeasured; the reality (Draghi's national-unity cabinet) held nearly everyone. France 2024-07-18: Governments(France) is empty (line 239) -> TryGovernmentAt false -> the formation stand-in on the 2024 Assemblée, positive investiture, UG's LrGen NaN (compat 0). France is "GoverningModeOnly - its government of record, no election" (WorldClock.cs:87), yet its government now comes from the formation model the record says France does not run, and if the formation seats a cabinet without the player's default party (UG, 178, the largest) the player opens France in opposition, budget process closed, AI governing - a contradiction with §8. RISK/DEFECT depending on what the formation forms; needs a measured run. Suggest: where Governments(id) is empty, store no record (Government stays null -> the player governs) or a record with PmParty = the player's party, explicitly Provisional. USA: same fall-through, ViewOf on the House (UsaParties, seats from InitialSeats). Two parties, one holding the majority -> a single-party majority cabinet; PM = Largest = the majority party = the player's default (largest). PlayerGoverns stays true for the default party; a player who picks the minority party opens in opposition, which is arguably wrong for a presidential system (the House is not the government). RISK - same fix as France.

7. RoleOf ordering (GovernmentRecord.cs:44-51): PmParty checked before Cabinet - correct. A party in both Support and Cabinet cannot arise from the record (Describe's `supports = !inCabinet && ...`, GovernmentFormation.cs:240) nor from WorldClock's rows. PmParty null with empty Cabinet -> everyone Opposition -> PlayerGoverns false: that record is only produced by FromView on a no-government view, which ResolveElectionVerdict no longer stores; AtStart can still produce it when the stand-in formation fails (e.g. "the chamber holds no seats") - then the player opens in opposition of a "none" government. Minor; RISK. Also `Largest` returns null on an empty cabinet, so a "none" record has PmParty null (fine).

8. The bill hole - confirmed. AdvanceTurn routes PolicyDecision.None() for a non-governing player (GameController.cs:6311), and TryOpenBudgetProcess now returns early (SimulationManager.cs:2067-2069), which disables the "Introduce Budget Bill" button (GameController.cs:11031, gated on budgetProcessOpen). But every other tier is introduced straight from the tabs with no role gate: IntroduceLawBill (9149), IntroduceLaborBill (4701), IntroduceTaxProgramBill (11385), plus the welfare/crime/sector/SWF tiers (SimulationManager.cs:1260-1885). They resolve by ParliamentSystem.WouldBillPass on the chamber and ApplyBillResult writes the country's dials on PASS regardless of who governs (e.g. AdvanceBudgetBillDay pattern, SimulationManager.cs:1195-1213). So an opposition player still enacts/repeals laws, tax programs, labour and crime dials through the chamber - and since the AI ministry then writes only "where the decision carries nothing of its own", these land on top of the AI's book. Whether an opposition motion CAN pass is the spec's question (a private member's bill), but today a passed one applies as if the government moved it. RISK (stated deviation at best; the PS-3 record should say so). Also the desk's lever drafts (sliders) remain editable and previewed while in opposition - the preview (line 3153 path) still applies the player's draft to the clone, so the readouts show a budget the player cannot lay.

9. Masthead width at 1280 - fits. DrawDeskMasthead (GameController.Desk.cs:275-362): title mono 10.5 bold; the suffix makes "SWEDEN · YEAR 0 · IN OPPOSITION · THE GOVERNMENT IS THE AI'S" 60 chars, roughly 400-430 px at 1280. Right cluster (SETTINGS + SAVES + a SAVES-width divider + the joined 1×2×3× strip + 14 px) is about 240-260 px, the LIVE caption about 120 px, flag+gap 36 px, out of 1156 inner: ~700 px available. The layout also degrades safely: liveLeft = max(title end + gap, liveRight - liveWidth) so LIVE is squeezed first, and the title is clipped by MeasuredLabel to `liveLeft - gap - titleLeft`. NOTE only; the canvas-film rule means a real 1280 frame is still owed for the suffix, and finding 1 means the harness cannot produce one today (no Government on the harness path).

10. Diagnostic (PlayerRoleDiagnostic.cs): `using` declaration after the local function, before try (line 29): legal C#; it disposes at method end. CheckExit.Finish may terminate the process before disposal - same as the other diagnostics. CreateSaveGame(sim, world, CountryId.Sweden, null) matches the signature (SaveGameService.cs:87); it stamps CurrentSaveVersion so Deserialize's version gate (line 161) passes. ResetTurnState/ResetCalibration exist (EnergyMarket.cs:509, 721). The formation assertion (line 70-72): ViewOf(sweden, Sweden2026) on the seated chamber - `vintage != Seated` skips TryInstalled, forms on 2026's declarations; §607 measured SD+M+KD+L; CandidaciesAt(Sweden, 2026-09-13) returns the standing candidacies in timeline order and the loop takes the first IN the cabinet - S not in cabinet, M is -> "M". Holds as long as §607's result stands; the assertion depends on the formation output, which is the check's purpose. Line 43 asserts `sim.PlayerGoverns(sweden)` with PlayerPartyAbbrev null: PlayerGoverns short-circuits on Government==null before RoleOf, so true - but that is exactly the harness state of finding 1, which the diagnostic blesses as "as before" rather than flagging. Missing coverage: nothing exercises OpenWorldAt's guard (finding 1), the no-government election log (finding 2), or a France/USA/Italy AtStart (finding 6). Add at least `AtStart(france, ...)`, `AtStart(usa, ...)` prints so the stand-in's cabinet is visible in the log.

VERDICT: NOT READY as it stands. Two defects: (1) the record is written only on the picker path - every harness film, scenario start, fallback seat and staged save open with Government null, so the whole role mechanism is invisible on film and the play-protocol save contradicts a picked game; (2) an unguarded `Government.RoleOf` in the election-night log throws when no government forms on a record-less country. Save/load and the seeded Sweden/Germany reads are sound; France (governing-mode-only) and the USA falling to the formation stand-in is a design risk to measure before the record claims the four-role reading for them; the non-budget bill tiers remain an unstated hole for an opposition player.

---

## What was done

1 FIXED: the record is written on every path that opens a world - `OpenWorldAt` writes it whenever the country exists (the party may be seated after), and `SelectPlayerCountry` writes it after the largest-party fallback where the world was not rebuilt (the same-epoch return); the harness, the scenarios and the fallback seat all read the role now. 2 FIXED: the log reads `Government?.RoleOf(...) ?? None`. 3 noted (a carried record keeps its date; its basis is the record's own). 4 verified. 5 FIXED for the staged save (`PlayProtocolStaging` stores the government of record, so the save is the game a player would start: S in opposition); the dumps are as the reader says - the trajectory dump sets no player and the no-policy century no party, so both read as before and the family `ps3a` is byte-identical to `ps2cal` on both seeds, stated in the record. 6 FIXED for France and the USA: where the record names no cabinet on the date, NO record is stored and the player governs, as before (a stand-in formed on a chamber whose record names no government would seat a cabinet the record never held); Poland's and Italy's stand-ins stay provisional under §605 and are printed by the diagnostic; the Germany comment corrected and the FDP's presence asserted. 7 noted. 8 STATED in the record as the deviation it is: an opposition player's non-budget bills still go to the chamber and apply if passed, and the drafts preview - role-gating what may be introduced is part 3. 9 filmed at 1280 after the fix. 10 the diagnostic prints every start's record.
