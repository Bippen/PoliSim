# Review — §639 K-1h and K-1g (2026-09-25)

One independent reader, read-only (no build, no Unity run), on the working-tree diff of `CoalitionFormation.cs`, `DeclaredRedLines.cs`,
`Formation2026Diagnostic.cs`, `DeclarationDatesDiagnostic.cs`, `OfficeTestDiagnostic.cs`, `StanceModelDiagnostic.cs` and the records
(`coalition_declarations_2026.md`, `coalition_declarations_2022.md`, `POLISIM_FEATURE_LIST.md`, `CLAUDE.md`, `docs/play/PLAY_PROTOCOL.md`).

## Checked and found correct

- `passesOnLines` in `CoalitionFormation.Form` is pass 2 minus the hold-out term exactly: the same supporters (`SupportersOf` with the
  no-support mask), the same skip of cabinet members and supporters, the same opposition (`SupportBlocked` or a vote-against rule), the same
  wins formula; computed unconditionally, and the hold-out filters on it.
- The two masks are not swapped: the no-support mask (every rule) is read only by `SupportersOf`; the vote-against mask by both opposition
  loops and `RedLinedMask`. SD with `VotesAgainst` false is never a supporter and never red-lined by its rule.
- `RedLinedMask` takes vote-against rules only; its one runtime caller, `GovernmentFormation.RedLinedFrom`, passes none, so the confidence
  motion reads the party rules as before.
- No remaining use of the removed measurement switch. Every consumer of `InOrAgainst`, `InOrAgainstFor`, `InOrAgainstAt` and `RedLinedMask`
  read; none prints "votes against" for SD.
- `DeclarationDatesDiagnostic`'s expected strings, ordinal-sorted, match the data at every date asserted; no overlap on the timeline.
- The dates' sources match the citations in the declarations file.
- The measured table's arithmetic agrees with the lines and rules row by row.
- KD → S is one way, support-blocking, and cabinet-blocking through the internal-line test. The SD-form probe in `OfficeTestDiagnostic` is sound.
  The stance diagnostic's partner fallback is null-safe.

## Findings, and what was done

1. **stale-record** — `PLAY_PROTOCOL.md` said the game starts on 1 October 2026 and called the 2026 chamber's formation the day-one
   government; the start was reversed (PS-1) and day one is the government of record. **Fixed.**
2. **defect (record honesty)** — MP's rule was dated 2026-08-10 by sources that do not carry the vote against; only TT's 2026-04-04 report
   does, and MP has no own record, so any date deviates from §621. **Fixed:** dated 2026-04-04, the deviation stated in code, the file and
   §639, and put to Elias (K-1i); the timeline check moved with it.
3. **defect (record honesty)** — KD's date (spoken words in SVT's live report) was called §621's precedent, which was a leader's own post.
   **Fixed:** stated as an extension, put to Elias (K-1i).
4. **defect (record honesty)** — SD's reading was presented as the source's own, and [L-C2]'s "full opposition" was discounted while the same
   quotation dated the rule; K-1g closed with no open question. **Fixed:** "the builder's reading, open for Elias"; [L-C2] treated the same
   way for both; row K-1i opened; the outcome sentence taken out of the source comment.
5. **stale code docs** — the candidacy strength "a reading" (now ruled), the §621 block's change points, `DatedFact`'s kinds, the timeline
   check's summary, the office test's "V the only party", its error text's chamber, `vRuleWired`'s name, the stance diagnostic's "forms no
   government", `RedLinedMask`'s doc. **Fixed.** `CountrySelectorScreen`'s two comments: **deferred** to the formateur's UI commit, which films
   the picker (a comment in a UI file owes the UI tier's film).
6. **stale-record** — the declarations file's §603 paragraph, the K-1f section's KD sentence, the SD row's primary GAP. **Fixed** with pointers.
7. **unstated consequence** — KD's support-blocking line also counts KD for a motion of no confidence in a cabinet holding S. **Stated** in code,
   the file and §639, and put to Elias (K-1i).
8. **the coalition screen shows no party rule.** **Filed** as K-1j.
9. **the K-1h (i) guard is weak** — it reads the real chamber. **Stated** in the check; not replaced: a probe that discriminates the rule
   needs the rule it replaced, which is removed.
10. **the change is global; no re-baselining shown.** **Answered by the bars** (§639): the trajectory sentinel held on `ps3k`.
11. **the combined result against the ruling's own words** — (i) named S with MP's support; with K-1g the chamber forms S alone. **Stated** in
    the file and §639.
12. **claim convention** — figures in source comments and the feature row. **Fixed:** references to `Formation2026Diagnostic` instead; the
    figures stay in the dated MEASURED tables and §639.
13. **`CLAUDE.md`'s example contradicted its sentence.** **Fixed.**
14. **latent** — SD's rule has no "after the next election" scope; `For(Sweden2018)` falls into the 2026 branch. The scope is **stated** in the
    basis; the 2018 branch is pre-existing, nothing seats 2018, and it is **stated** in §639, not changed.
15. **row labels** in `Formation2026Diagnostic`. **Fixed.**
