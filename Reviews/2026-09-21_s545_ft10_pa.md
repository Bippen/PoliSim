# Review evidence — §545 (2026-09-21): FT-10 · P-A, the levy scale on the support line's share of its own path

**What was reviewed.** The uncommitted change that became FT-10 · P-A: `EnergyLedger.cs` (the levy scale's new form, K captured at the seed), `EnvironmentFamily.cs` (the seed field), `SaveGameService.cs` (format 21), and the BASELINE family it makes - `traj_p6pa`, the sentinel's digests `57f1e5cc…` (seed 777) and `cd39dfbe…` (seed 424242). `EnergyLedger.cs` is a money path, and a family is reviewed whatever it touches.

**The form.** One independent read-only reader on the change, and a second independent read-only reader on the rework. Neither ran anything; both read the code and the two trajectory dumps.

**What the first reader changed.** It found no defect in the formula, in K's capture, in save and load, or in how K travels between the worlds the game holds. It found that the change's first cut had re-worded two player-facing sentences from *one for one* to a share-of-path form, and that for the country the player governs *one for one* is still exactly true (only an AI ministry's path rides real growth), so the new wording was wrong by the factor K: the three UI files went back to their committed text. It corrected the record's *"the USA and Germany cannot move"* (their energy prices cannot; their other rows move by propagation from turn 3), the stated bound (1 + K, and 1 + 0.8 K on the own path alone), and it proposed the assertion that K is live, which was added to `EnergyLayerCheck`. Its finding 4 - the *Retail intervention* captions promise a levy floor the dial cannot reach in two countries - predates this change (§542) and is filed as PF-4. What was done is told in `COMPLETED.md` §545.

**What the second reader changed.** No blocking defect; it confirmed that `EnergyLedger.cs`'s code lines are exactly five things (K's capture in `Seed`, the two helpers, the path share and the scale) and that the three UI files are untouched. Taken from it: probe 3b in `EnergyLedgerDiagnostic` (the player's *one for one* asserted ten years from the seed, and a ministry's book required to read the two forms APART so the probe discriminates), the class doc's sentence set down where it belongs, *at the boundary* on the equality's comment, *fifth decimal place*, and Italy's row's instruction.

**After the second reader, and read by no one but the author:** `EnergyLedger.cs` changed in COMMENTS only (the class doc's paragraph and the comment above the scale - the second reader's findings 3 and 5); its code lines are the five the second reader lists. `EnergyLedgerDiagnostic.cs` gained probe 3b and `TrajectorySentinelCheck.cs` two comment edits; neither is a money path. The ledger's row is for the file as committed.

---

## The first reader - on the change

*The reviewer's report, verbatim.*

# Adversarial review of FT-10 · P-A (uncommitted)

The formula, the K capture, save/load and the several-worlds travel are sound; I found no defect that freezes or corrupts a levy. What is wrong is in the record and the player-facing texts: one false statement about which countries move, two UI strings that replaced a true sentence with a less true one, and a bound stated too tightly.

I did not run Unity or any bar. The sentinel patch described as "not yet applied" landed in the working tree while I was reading, so I reviewed it too. Two directory listings of the captures folder timed out and finished in the background; I used nothing from them.

## Findings, most severe first

### 1. "The USA and Germany cannot move" is false for their trajectory rows — CONFIRMED

- **Where:** `TrajectorySentinelCheck.cs`, the `BaselineLabel` doc (about lines 38–39): "Poland, France, Sweden and Italy move, the USA (no levy) and Germany (no line) cannot". The task brief and presumably §545 say the same.
- **Evidence:** I diffed `PoliSim-captures/trajectories/traj_p6e1_s777_t100.csv` against `traj_p6pa_s777_t100.csv`. I first recomputed the twenty-turn SHA-256 of each:
  - p6e1 gives `59c67cd4…`, the old sentinel digest;
  - p6pa gives `57f1e5cc…`, and s424242 gives `cd39dfbe…`, so both new digests match their dumps.
- **What moves:**
  - Germany: 4,798 rows differ. `Zone.InterestRate` moves first at t3, because the ECB's blended rate reads France's and Italy's inflation, which now carry a different pass-through. From t4 almost everything follows. Max relative differences are 3.5e-4 on the rate, 4e-3 on `PotentialGrowthRate` and 2.6e-2 on `Taylor.GapTermPp`.
  - USA: 2,404 rows differ. `CurrencyStrength` moves at t3, GDP from t14 through trade, and the nominal energy fields from t37 through its price level. The largest relative difference in the fields I listed is 2.3e-4, on `CurrencyStrength`.
- **What does not move:** their levy scale (0 and 1) and `EnergyHouseholdPriceReal`, which reads 0.16 and 0.44062975 at every checkpoint to t1000.
- **Why it matters:** the family rule is "explain it per country" (§402). A record that says two countries cannot move while 7,202 of their rows differ is a wrong explanation.
- **Smallest fix:** reword to "their levy scale and real energy prices cannot move; their rows move from turn 3 by propagation — Germany through the zone's blended rate, the USA through currency strength and trade". Say the same in §545.

### 2. The two player-facing texts replace a statement that is still exactly true with one that is wrong by the factor K — CONFIRMED from code

- **Where:** `GameController.cs:11202`, `GameController.Energy.cs:510`, the comments at `RangeCaptions.cs:303` and the `SimulationManager.cs` ~4397 doc.
- **Defect:** for the country the player governs, P-A is arithmetically the old rule.
  - `IndexSpendingLines` sets `realGrowth = aiBudget ? … : 1f` (`SimulationManager.cs` ~4175), and Energy has no driver (`SpendingDrivers.Of` default), so the player's path is S = S₀ × P.
  - `PriceLevelAtLastIndex` is captured at the seed (`Country.cs:229`) with `PriceLevel = 1f` (`EconomyState.cs:19`).
  - Then levy = L₀P − L₀P × (S₀⁄L₀) × (A − S)⁄(S₀P) = L₀P − (A − S): one for one in current billions, as before.
  - `EnergyLedgerDiagnostic` (3) asserts a billion for a billion within 1e-6 and (7) still asserts `levyOneForOne`. Both pass under P-A by my arithmetic: K as a float adds at most 6e-8 relative, inside the 1e-6 tolerances, and France's ±1 on 20.228 is exact in float.
- **Scenario:** player Sweden adds 2.5 % to the line's path. Read literally, "displacing the policy levy by the share it adds to the line's path" says the levy falls 2.5 %; it falls 100 % (K 40.198). The literal reading is wrong by ×13.4 for France, ×0.51 for Poland and ×0.07 for Italy.
- **Smallest fix:** keep "one for one" in both strings, or "one for one at the year's prices", and keep the K form in the code docs where AI books are described.
- **Worth saying in §545:** P-A changes AI-governed books only. It also changes the from-seed `ShadowBaseline`, which seats no player, so the player's country is ministry-run there. The played country's levy arithmetic is unchanged to float rounding.

### 3. The stated bound "1 + 0.8 K, by the line's own clamp band" is wrong where a subsidy dial stands below neutral; the true bound is 1 + K — CONFIRMED, low

- **Where:** the `EnergyLedger.cs` ~249 comment ("bounded by the line's own clamp band") and the sentinel's `RuleAnswer` string.
- **Defect:** since SC-1, `ComposeLine` (`SimulationManager.cs` ~4249) clamps only the own path. The dial cost sits outside the band and meets only zero.
- **Scenario — player Sweden, Energy subsidy at 0:**
  - the cost is −0.003 % × 50 × 620 = −0.93 bn against S₀ 1.0718;
  - so A⁄S = 0.13 and the scale is 1 + 40.198 × 0.87 ≈ 35.9, over the stated 33.2;
  - with the line also at its 0.2 floor the total is held at 0 and the scale is 41.2.
  - The same construction takes France to 14.4 against the stated 11.7.
- **Not a regression:** the old rule gave the same for the player. AI rows do hold 1 + 0.8 K, because no AI moves the dial (`Sector.SubsidyLevel` seeds at 50 and the only writer is the player's decision), so the sentinel's cells are right.
- **Smallest fix:** comment wording — "at most 1 + K (a line cannot go below zero); 1 + 0.8 K on the own path alone".

### 4. Retail-intervention captions: the top bands are false for Italy and Poland, and late for Sweden and France — CONFIRMED, pre-existing (§542), not caused by P-A

- **Where:** `RangeCaptions.cs:306–317`.
- **What I can establish:** the dial's maximum cost is 0.15 % of nominal GDP (`SectorCouplings`), and the seed levies follow from the catalog.

| Country | Seed levy (bn) | Cost at dial 100 (bn) | Scale at dial 100 | Floor reached at |
|---|--:|--:|--:|---|
| Italy | 11.70 | 3.45 | 0.705 | never; even with the line at 3× its path the scale is about 0.56 |
| Poland | 3.19 | 1.26 | 0.605 | never by the dial alone |
| France | 1.51 | 4.8 | 0 | dial ≈ 66 |
| Sweden | 0.0267 | 0.93 | 0 | dial ≈ 52 |

- **Consequence:** "On the house — the levy at its floor" and "Lavish — the levy nears nothing" are false for Italy and Poland. For Sweden and France, "Generous" and "Lavish" describe a levy that is already gone. `RangeCaptionCheck` tests only the sign, on Poland.
- P-A does not change the dial's reach for the player (finding 2).
- **Fix:** outside this commit — per-country bands, or captions that do not promise the floor.

### 5. Housekeeping — CONFIRMED, low

- `CLAUDE.md:195` still says "thirteen cells … Proposed in §541, NOT applied".
- §545 is cited in the code comments of six files but is not yet in `COMPLETED.md`.
- All four staged saves in `LocalLow/DefaultCompany/PoliSim/saves` are format 19, already refused since v20. The bump to 21 newly breaks nothing, but CL-3's `playtest_4_precampaign_day1.json` needs `PlayProtocolStaging.Run` before Elias plays.

### 6. No guard that K is live — a suggestion, not a defect

- A K of 0 on a country with a line and a levy freezes the scale at 1, which the bounds pass reads as a clean 0.000 %.
- Today that is caught only by `EnergyLedgerDiagnostic` (3) (`down.LevyScale > 1.0`, fresh world) and by the digests.
- **Suggested fix, one assertion in `EnergyLayerCheck` gate 8:**
  - where the country has a policy levy and an energy line, K > 0 and K ≈ SeedAmount ⁄ `SeedLevyBillions`;
  - otherwise K == 0.

## Questions (not tied to a defect)

- The Energy tab subtitle grew by five characters ("… CUT BY THE LINE'S RISE OVER ITS PATH"), and the Sectors sentence is longer too. I could not run a film to see whether either clips or wraps.
- Italy's industry LEVEL cell measures 1.003 against a slack threshold of 1.0. A 0.3 % relative fall flips the sentinel to SLACK → FAIL. P-B's cross-effects measured about 1e-5, so this is probably safe, but it is the tightest cell.

## Checked and found sound

- **Formula and edge cases (`EnergyLedger.cs`):**
  - the scale is exactly 1 on the path (float by itself), for the AI's no-policy lines too, since Amount and SeedAmount take one float factor (`IndexSpendingLines`, `ComposeLine`);
  - no line gives `pathShare` 0 and scale 1;
  - USA reads 0 and Germany reads 1, bit-identical to the old expressions;
  - the `SeedLevyBillions > 0` guard matches the old `levyNominal > 0` for every reachable price index (≥ 0.0001);
  - the floor holds;
  - `pa_measure.log` reproduces 1 − K(A⁄S − 1) at every checkpoint: France t100 9.523, Poland t1000 1.2835, K 40.198 / 13.396 / 0.07349 / 0.50668 / 0 / 0;
  - the path at t1000 (1.5e22) is far from float overflow, and the 1000-turn dump holds no NaN.
- **No reader of the old arithmetic:**
  - `SupportDeviation` is read only by `EnergyRunawayDiagnostic.cs`;
  - the bridges (`GameController.Energy.cs:845`) read book fields, and the identity closes by construction;
  - `EnergyPassThrough.cs`, `EnergyPassThroughDiagnostic.cs`, `RangeCaptionCheck.cs` (sign only), `EnergyLayerCheck.cs:230` and `AppliedCostIdiomDiagnostic.cs` are all compatible;
  - `EnergyRunawayDiagnostic.cs` compiles (10 arguments, indices 0–9).
- **K's capture:**
  - `SeedAmount` is written only by the constructor, `Clone` and the index (`SpendingLine.cs`, `SimulationManager.cs:4207`), and Amount only by `ComposeLine`;
  - `WorldFactory.CreateDefault` is the one factory, and `EnvironmentFamily.SeedAll` runs last (`WorldFactory.cs:1085`);
  - the other `Seed` callers are editor-only and guarded by `!Seeded` on fresh worlds (`EnergyLayerCheck.cs:67` and `:209`, `EnergyLayerDump.cs:39`);
  - `ScenarioDefinition`, `ScenarioLibrary` and `ScenarioEvaluator` do not touch spending lines, and `SelectPlayerCountry` seats the player before any turn.
- **Several worlds:**
  - the preview clone shares `Environment` and nothing writes K after the seed;
  - the preview indexes its own cloned lines under the same `PlayerCountryId`;
  - `ShadowBaseline(int)` seeds its own world with the same K;
  - the ledger forks and the forked shadow travel through the save round trip, so K rides the JSON (`ShadowBaseline.cs`, `PolicyImpactLedger.cs`);
  - the market's static water value is re-begun after the shadow (`GameController.cs:5637`) and on load (`:895`);
  - I found no path to K = 0 with a live line and levy.
- **Save/load:**
  - `EnvironmentSeeds`' public floats serialise — an existing save shows its siblings at nine digits;
  - the v21 gate refuses older saves in `Deserialize` and in the header read;
  - `RestoreSaveState` and `RestoreFromSave` re-seed nothing;
  - `SaveLoadRoundTripDiagnostic.cs` covers K by string equality, and its tamper test uses `CurrentSaveVersion + 1`;
  - `PlayProtocolStaging.cs` and `PlayProtocolCheck.cs` cut and load in one build.
- **Subsidy path:** `ApplyCostOnLine` resets the tracker to the target each boundary, so nothing is counted twice or lost. For the player the displacement is unchanged; the dial's bite on a static levy still grows with real GDP, as it did before.
- **Boundary order (`SimulationManager.cs`):** index → costs → line changes → families → `EnergyLedger.AdvanceYear`. The ledger reads Amount and SeedAmount after the same index, and the new scale no longer drifts with mid-year P on the page.

**Files read:**

- Scripts: `EnergyLedger.cs`, `EnvironmentFamily.cs`, `SaveGameService.cs`, `SimulationManager.cs`, `SpendingLine.cs`, `SpendingDrivers.cs`, `SectorCouplings.cs`, `Sector.cs`, `Country.cs`, `EconomyState.cs`, `WorldFactory.cs`, `EnergyLayerData.cs`, `EnergyPassThrough.cs`, `EnergyMarket.cs`, `ShadowBaseline.cs`, `PolicyImpactLedger.cs`, `GameController.cs`, `GameController.Energy.cs`, `RangeCaptions.cs`.
- Editor: `TrajectorySentinelCheck.cs`, `EnergyRunawayDiagnostic.cs`, `EnergyLedgerDiagnostic.cs`, `EnergyLayerCheck.cs`, `EnergyPassThroughDiagnostic.cs`, `RangeCaptionCheck.cs`, `AppliedCostIdiomDiagnostic.cs`, `SaveLoadRoundTripDiagnostic.cs`, `PlayProtocolStaging.cs`.
- Docs and data: `COMPLETED.md` §541, `CLAUDE.md:195`, the trajectory dumps named in finding 1, `ft10_measure.log`, `pa_measure.log`.

---

## The second reader - on the rework

*The reviewer's report, verbatim.*

# Second read of FT-10 · P-A (uncommitted, HEAD 3d92516)

**Verdict: no blocking defect.** The money path is what the brief describes, the reworked statements hold against the code, and the sentinel's numbers reproduce from the dumps. The findings below are doc, record and coverage items.

I did not run the bars. The post-rework sources (14:57:33) did compile: both ScriptAssemblies DLLs are dated 14:58 and contain the new strings.

## Findings

### 1. Nothing asserts the player's one-for-one away from the seed — PLAUSIBLE (coverage gap, not a defect)
- **Where:** `EnergyLedgerDiagnostic.cs:90-112` (France, line ±1 bn) and `:185-229` (subsidy at 80).
- **Defect:** both probes run on a fresh seed world, where P = 1 and SeedAmount = S0. There the old and new expressions are identical by construction. So "THE PLAYER'S BOOK IS UNCHANGED" (`EnergyLedger.cs:253`) rests on reading alone, which CLAUDE.md §402 says never proves inertness.
- **Scenario:** a later change gives the player's driverless lines a driver or a growth term. The page keeps saying "one for one", the arithmetic quietly stops being that, and every probe stays green.
- **Smallest fix:** a 3b in that diagnostic. Set `PlayerCountryId` to France, put +10 % on the energy line, advance about ten boundaries. Assert `|LevyScale − max(0, 1 − SupportDeviation ⁄ (SeedLevyBillions × P))| ≤ 1e-5`. Run the same world with no player and require the two to differ, so the probe discriminates.

### 2. The records lag the code — CONFIRMED
- **Where:** `COMPLETED.md` ends at §544, and no `.md` is modified in the tree.
- **Defect:** the code cites §545 in ten places, including `SaveGameService.cs:52` and the sentinel. `CLAUDE.md:195` still says "FT-10's thirteen cells … Proposed in §541, NOT applied". `POLISIM_FEATURE_LIST.md:1224` still says "PROPOSED, NOTHING APPLIED".
- **Fix:** land §545 and both lines in the same commit.
- Because the record is not in the tree, I could not read the reworded "cannot move" sentence. I only saw its code twin, the `BaselineLabel` doc.

### 3. The class-doc splice dangles — CONFIRMED (doc only)
- **Where:** `EnergyLedger.cs:23-27`.
- **Defect:** the insert landed mid-sentence. "(the EEG's own history …), spread over every kWh in proportion to the seed's levies by class" now hangs off the AI-book sentence, as if the EEG history explained indexed-with-indexed.
- **Fix:** move the P-A insert to after "…taxpayer-funded support with no retail effect."

### 4. `PoliSim.slnx` is modified — CONFIRMED (commit hygiene)
- **Defect:** only the project order is swapped (1+/1−); it is a regeneration artefact. The brief scopes the change to `Assets`.
- **Fix:** do not stage it.

### 5. "to float rounding" is exact at the boundary only — REFUTED as a defect
- **Where:** `EnergyLedger.cs:253`.
- At the boundary, `IndexSpendingLines` (via `ResolveSpendingForTurn`) and `EnergyLedger.AdvanceYear` run in one call with no daily step between them, so both read the same `PriceLevel`. The booked state is unchanged.
- The live page (`GameController.Energy.cs:64`) recomputes with the day's price level, which `MacroSystem.cs:580` moves daily. The old scale divided the deviation by seedLevy × P_today. The new scale is constant within a year.
- **Scenario:** a raised dial at scale about 0.50 with 3 % inflation read 0.51 by December under the old rule and reads 0.50 now. A film of the Energy tab with a dial raised may move in the second decimal.
- **Fix:** add "at the boundary" to the comment, or accept it as is.

### 6. "1 + 0.8 K on the own path alone" has a pinned-line exception — REFUTED as a defect
- **Where:** `SimulationManager.cs:4207-4208`.
- The index does `SeedAmount *= factor`, then skips a pinned line before the clamp. So a player's pinned line can drift below 0.2 × path.
- **Scenario:** at 3 % inflation this takes about 54 years.
- The 1 + K bound holds, and the old rule behaved identically. The AI never pins, so "which is all an AI book moves" stands.

### 7. Italy's cell sits 0.003 over the band — by design, but fragile
- The cell reads 1.003 against a ceiling of 2.0. At 0.999 it reports SLACK.
- The instruction "lower it" then means deleting the row and lowering the count to 10. It should not be re-cut.

### 8. "from turn 3 in the fifth digit" — nit
- It is the fifth decimal place, not the fifth digit.
- Germany's first mover is `Zone.InterestRate` 3.07836366 → 3.07833719.
- The USA's are `CurrencyStrength` 100.325516 → 100.325546 and `TradeBalance` at relative 2e-7.

### Side note, pre-existing
The staged `playtest_4_precampaign_day1.json` on disk is save version 19. It has been refused since §538's bump, and CL-4 waits on it. Re-run `PlayProtocolStaging.Run` after the commit.

## Checked and found sound

**A. UI untouched.**
- `git diff --stat -- Assets/Scripts/UI` is empty.
- "share it adds" and "RISE OVER ITS PATH" occur nowhere in `.cs`, `.md`, `.json` or `.txt` files.

**B. `EnergyLedger.cs` code lines are exactly the expected set.**
- The K capture in `Seed`.
- `SeedLevyBillions` and `SupportLineToLevyAtSeed`.
- `pathShare` and `b.LevyScale`, replacing the three old lines.
- Everything else in that file's diff is comments. The guard is equivalent to the old `levyNominal > 0`.

**C(i). The player's driverless lines ride prices alone.**
- Energy falls to `SpendingDriver.None` in `SpendingDrivers.cs`, whose level is 1f.
- For the player, `realGrowth` and `wages` are both 1f.
- `PriceLevelAtLastIndex` is seeded to `State.PriceLevel`, which is 1 (`Country.cs:229`).
- `SeedAmount` has only two writers: the `SpendingLine` constructor and the index.
- K cannot be missing where it matters:
  - older saves are refused;
  - the preview clone shares `Environment` by reference (`SimulationManager.cs:3456`);
  - the seed runs once, after the lines exist (`WorldFactory.cs:1085`).
- No static state was added.

**C(ii). `Amount` cannot go negative.**
- `ComposeLine` (`SimulationManager.cs:4254`) is the only runtime writer of `Amount`. It composes own path plus dial costs and floors the total at zero.
- So 1 + K is right.

**C(iii). An AI book moves only its own path.**
- No AI path touches the subsidy dial; only the player's bills set it.
- No seed sets the dial off 50.
- `AiFinanceMinistry` uses percent changes only, with no nominal targets and no pins.
- `AiEnergyMinistry.Live` is false, and that class does not touch the subsidy.
- The energy line is nobody's fallback cost line.

**C(iv). The sentinel doc's turn claims hold; I diffed both dumps in full.**
- France, Italy, Poland and Sweden first differ at turn 2, on their own `Energy*` rows.
- Germany and the USA first differ at turn 3.
- `EnergyHouseholdPriceReal` never differs for Germany or the USA across 100 turns.

**D. The K assertion is at the seed and is not vacuous.**
- Gates 1–7 mutate nothing, so gate 8's world is at the seed.
- The tolerance is 1e-5 relative against a float error of about 6e-8.
- Types and `F(...)` are in scope. The sibling-scope `k` is legal.
- A K of 0 would fail Poland, France, Sweden and Italy.
- The shared-helper vacuity is covered by the gate's stack-total assertion.
- Cosmetic: a line with a zero seed would print "no energy line".

**E. The sentinel patch is complete.**
- The five-element tuple is applied at both uses.
- `ConstantProvenanceCheck.cs` scans only `Assets/Scripts/Simulation` and only float, double and int. The string consts are out of scope on both counts, and `EnergyLedger.cs` gained no const.
- I recomputed all eleven measurements from `traj_p6pa_s777_t100.csv`, and the p6e1 figures in parentheses from the other dump. All match to three decimals.
- The table has eleven rows against `EnergyBreachCeiling = 11`. Exactly eleven cells exceed 1.0.
- Every ceiling is its measurement rounded up to the next whole point. The slack under each ceiling is at most 0.997 (Italy).
- Both SHA-256 digests equal the dumps' first-20-turn prefixes.

**F. Nothing else is broken by the rework.**
- `EnergyRunawayDiagnostic.cs` uses placeholders {0}–{9} with ten arguments in matching order.
- The `SaveGameService.cs` version-21 comment is accurate.
- No backticked `Type.Member` was added, so `CommentClaimCheck` sees nothing new.
- All seven changed files are CRLF throughout, with no BOM and no mojibake.

**Not verified:**
- K's values of 13.4 and 40.2, and Poland's "levy scale 1.28 to t1000". Both need the check's or the diagnostic's output.
- That the bars are green on the post-rework tree. The dumps date from 14:25 and the sources from 14:57. The sentinel's live digest is the proof.

**Files read:**
- EnergyLedger.cs
- SimulationManager.cs
- EnvironmentFamily.cs
- SaveGameService.cs
- SectorCouplings.cs
- SpendingDrivers.cs
- EnergyPassThrough.cs
- WorldFactory.cs
- EnergyLayerData.cs
- EnergyLayerCheck.cs
- EnergyRunawayDiagnostic.cs
- TrajectorySentinelCheck.cs
- ConstantProvenanceCheck.cs
- EnergyLedgerDiagnostic.cs
- RangeCaptionCheck.cs
- PlayProtocolStaging.cs
- CommentClaimCheck.cs, CommentImmunityCheck.cs and DocumentClaimCheck.cs (headers only)
- CLAUDE.md
- both trajectory CSVs

I wrote two scratch perl scripts to the session scratchpad and touched nothing in the repository.
