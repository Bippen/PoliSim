# Review evidence — §552 (2026-09-21): FT-10 · P-B, Sweden's water value in the seed's prices

**What was reviewed.** The uncommitted change that became FT-10 · P-B: `EnergyMarket.cs` (each neighbour's block price divided by the price index it was cleared at before the 615/600 weighting; the zone price carried by Sweden's level once; `Result.PriceIndex`, stamped by `ClearAt`), `SimulationManager.cs` (one comment), and the BASELINE family it makes - `traj_p6pb`, the sentinel's digests `798bfbee…` (seed 777) and `a0bc788f…` (seed 424242) - with the sentinel's re-cut (eleven recorded cells to six) and `EnergyPassThroughDiagnostic.cs`. `EnergyMarket.cs` and `SimulationManager.cs` are money paths by the tier tool's pattern, and a family is reviewed whatever it touches.

**The form.** One independent read-only reader on the change, and a second independent read-only reader on the rework. Neither ran anything in Unity; both read the code and the trajectory dumps, and both recomputed the two digests from the dumps' files.

**What the first reader changed.** It found no defect in the substance: at the seed every figure is bit-for-bit what it was, the deflation is exact on the standing fleets, the several-worlds class gets smaller, and the family's claims reproduce. It found the RECORD wrong in three places - the sentinel's label doc said Sweden moves from turn 2 (it moves from turn 1), that the other five move by propagation (the USA and Poland do, from turn 2 through `CurrencyStrength`; Germany, France and Italy do not move at all), and named the old dump in the table's doc. It found the comment on what the price level carries overstated (ACER's ceiling is nominal by EN-5's convention), the deflator a second copy of `Clear`'s clamp (so `Result.PriceIndex` was added and `WaterValueOf` reads the clearing's own stamp - numerically inert, the digests re-read unchanged), three stale comments, and a slack bound in `EnergyPassThroughDiagnostic` (0.05 to 0.005). Filed from it and not fixed here: the unserved branch's scarcity going negative once cost times the price level passes the nominal ceiling; the Energy page's peer ticks and its cached clearing read at the live price level; the unweighted partner-rate average in `CurrencySystem`.

**What the second reader changed.** No defect in the arithmetic; six low findings. (1) **The two B6 probes could not tell P-B from the code before it** - a level doubled everywhere cancels in the water value's old form too - so both probes (`EnergyPassThroughDiagnostic` 5, `ReservoirDispatchDiagnostic` 5) now double Poland's level TWICE, and the mutation was run: with the deflation dropped both fail, restored both pass. (2) The bound's rationale cited §465's world; the tree just before P-B read 0.0080 pp, so the bound went to 0.001 and cites that. (5) The unreachable guard in `WaterValueOf` that would have read an unstamped clearing at 1 - silently the removed defect - is a throw. (6) Comment residue: `ClearSweden`'s summary, the diagnostic's class doc, *alone* in `BeginTurn`'s comment. (3) is a note on the file's line endings, (4) the page's `split` test re-deriving the zone price in the old association under an absolute tolerance (unreachable in play; filed with the page's display item, whose cure - reading `Result.PriceIndex` - this change makes possible). It also named two effects the dumps cannot show, both for the better: a Swedish player's preview no longer loses up to a year of inflation against the standing water value, and the null-state fallback reads the zone's seed price times the level.

**After the second reader, and read by no one but the author:** in `EnergyMarket.cs`, ONE code line - the guard in `WaterValueOf` became a throw (the second reader's own recommendation; unreachable, both call sites take results from `ClearAt`) - and two comments (`BeginTurn`, `ClearSweden`'s summary); the two diagnostics' probes, bound and docs as told above. `SimulationManager.cs` is as the second reader read it. The ledger's rows are for the files as committed; the states the second reader names in its report are the states BEFORE these edits.

---

## The first reader - on the change

*The reviewer's report, verbatim.*

# Adversarial review: FT-10 · P-B (Sweden's water value in the seed's prices)

**Verdict: I found no defect in the substance of the change.** At the seed the change leaves every figure bit-for-bit unchanged. On the standing fleets the deflation is exact. The several-worlds class gets smaller. The family claims and both digests reproduce from the dumps. What is wrong is in the record (the sentinel's doc comment), in one overstated code comment, and in stale diagnostic documentation.

State reviewed (SHA-256 of the file's bytes with carriage returns removed), at HEAD 6d7ec53 plus the working tree:
- `EnergyMarket.cs`: `1361383509a298063f9df4acbe480a7d8a98248f329b497e5cbe66b54003a0d6`
- `TrajectorySentinelCheck.cs`: `a06d315c28152743807c14cfcfa7201ec705086280b9e5942483ab293dd1b1ff`

The measurement ran on this code: `EnergyMarket.cs` was saved at 18:03:44, Unity started at 18:03:45, and `Assembly-CSharp.dll` was built at 18:03:58.

## Findings, most severe first

### 1. The sentinel's BaselineLabel doc comment states three things the dumps contradict — CONFIRMED (the record is wrong; the code is unaffected)

**Where:** `Assets/Editor/TrajectorySentinelCheck.cs` lines 42-44, plus line 90.

**The defect:**
- **"Sweden moves on its own clearing from turn 2."** Sweden moves from turn 1. Eight energy fields differ at turn 1 on both seeds, for example `EnergyCongestionRent` 1.01266348 → 1.00501764 and `EnergyHouseholdPriceRealChange` 0.113 → 0. Nothing else differs on turn 1.
- **"The other five move by propagation alone."** Only the USA and Poland move. Germany, France and Italy have 0 differing rows in s777 t100, s424242 t100 and s777 t1000.
- **"Their own energy prices in the seed's prices byte-identical."** This is false for Poland. `EnergyHouseholdPriceReal` differs in 406 of 1000 rows from t5, by at most 1e-5 %. Its nominal energy fields differ from t4 and the USA's from t14 to t20. Only the USA's real household price is byte-identical.
- **Line 90** still says the ceilings are "the measurement on `traj_p6pa`". The five surviving rows' figures are unchanged on p6pb (27.176, 7.075, 1.003, 2.797, 2.851), so only the label is stale.

**Smallest fix:** reword to "Sweden moves from turn 1 on its own clearing; the USA and Poland from turn 2 through `CurrencyStrength`, in the fifth to eighth digit; Germany, France and Italy do not move at all". Change line 90 to `traj_p6pb`.

### 2. "Clear carries every cost by the country's price level" is overstated: the ACER ceiling is nominal — CONFIRMED in code, pre-existing, not exercised by any measured run; REFUTED as a defect of P-B

**Where:** `EnergyMarket.cs` lines 271 and 280 (`MaxClearingPrice = 4000f` inside `ClearBlock`), line 568 (Sweden's own chain), and P-B's new comment in `BeginTurn`.

**What is carried by the price index:** fuel, VOM, the ETS price including `ProbeEtsRisePerT`, the adders, the tranche factors and the floor price (the cheapest floor tranche, `mc × (1 − FleetSpread)`). `CurtailmentOffer` is 0. The hydro shift compares `spread / priceIndex`.

**What is not carried:** the scarcity term `max(0, 4000 − price) × x²` and the unserved price of 4000. `EnergyPassThroughDiagnostic` test (5) states this as a convention ("ACER's 4 000 is a nominal legal figure").

**Measured:** Germany and Poland show `scarcity/P 0` and `unserved 0` at every checkpoint to t1000. Their price/P is constant to five digits (81.708 / 90.38 / 101.37 and 68.219 / 82.204 / 97.106). On the standing fleets the deflation is exact.

**Scenario:** a fleet change in Germany puts a block at ratio 0.95 (x = 0.5). Examples are a player's coal retirement, or the held energy ministry once released (§544 measured coal at zero from 2039).
- The block's real price is c + 0.25 × (4000 ⁄ P − c).
- That is about 1076 at P = 1, 576 at P = 2, and 205 at P = 7.7.
- Sweden's water value falls with Germany's price level.
- The old code had the same erosion. P-B inherits Germany's own book and does not add a term.

**Separately, a pre-existing bug in the unserved branch:** `r.Scarcity = MaxClearingPrice − price` goes negative and `r.Price = 4000` sits below the marginal cost once c × P > 4000 (P_de around 40, roughly year 185). The same applies to `price[z] = MaxClearingPrice` in Sweden's chain.

**Smallest fix:** reword the P-B comment to "every cost; the nominal ceiling excepted, by EN-5's convention". For the branch bug, use `Math.Max(MaxClearingPrice, price)` or carry the ceiling; that is its own item.

### 3. The deflator is a second copy of `Clear`'s clamp — PLAUSIBLE (robustness)

**Where:** `EnergyMarket.cs`, `BeginTurn`, which calls `Math.Max(0.0001f, de.State.PriceLevel)` beside `Clear(de)`.

Today the two are the same float at the same instant. `Clear` is pure, arguments evaluate left to right, and the float converts to double identically. Exactness then rests on two expressions staying equal.

**Smallest fix:** stamp `Result.PriceIndex` in `ClearAt`, the way `EtsRisePerT` already is, and divide by it in `WaterValueOf`.

### 4. Stale diagnostic documentation now calls the removed defect "the mechanism working" — CONFIRMED (docs and a slack bound)

**Where:**
- `EnergyPassThroughDiagnostic.cs` lines 32-36: the rationale for `NoPolicyBoundPp = 0.05f`.
- `EnergyPassThroughDiagnostic.cs` line 114: the comment "the water value is Germany's and Poland's clearing at THEIR price level".
- `SimulationManager.cs` line 2698: the comment "Germany's and Poland's block prices at their current rates".

The checks still pass. The B6 probe is exact because (x × 2) ⁄ 2 = x. The bound was sized to admit Sweden's 0.0204 pp from the very channel P-B removes, so the bound is now slack.

**Smallest fix:** re-cut the three comments, and re-measure and lower the bound.

### 5. Siblings of the same class on the Energy page, outside the diff — CONFIRMED by reading, display-only, pre-existing

**Where:** `GameController.Energy.cs` lines 61-64, 73-74, 415, 427, 439 and 621-625.

- **Peer ticks.** They are each peer's nominal wholesale at its own price level. They are drawn on the player's row on a fixed 0–250 scale. By t100 Poland's tick sits about 65 % too high against Sweden's for an unchanged real price, and everything is off the scale by about t50.
- **Cached clearing against a live index.** The page caches its clearing per turn but decomposes it with the live `s.PriceLevel`. As days tick, `own ≠ block.Price`, so the rule row can read "SPLIT BY A BINDING LINK" with a spurious AT THE CUT term. Under P-B the WATER term becomes β × seedProxy × (P_cached − P_live). The old code had the same mismatch, so P-B neither fixes nor worsens it.
- **`r.WaterValue × P`.** This is algebraically right for the page's formula at cache time.

### 6. A save from before P-B — PLAUSIBLE, negligible at realistic horizons

`State.EnergyHouseholdPriceReal` and `State.EnergyCongestionRent` in an old save carry the old formula. The first boundary after load books a one-off negative real change. That is about −0.1 % over years 1 to 10, or roughly −0.002 pp of inflation. No water value is persisted, so no save-version bump is needed.

### 7. Model finding (the item 5 question): why Germany, France and Italy do not move at all

There is no channel to them. It is not a matter of float resolution.

**Where:** `TradeSystem.ApplyTradeEffects` and `CurrencySystem.ApplyCurrencyStrength`.

- **Trade.** Net exports are Σ ExportVolume × (1 − tariff) × the exporter's own currency factor, less a static ImportVolume. There is no partner-demand term and no importer-currency term. The doc states "no import elasticity".
- **Euro members.** They take currency factor 1 and are skipped by the currency rule. The ECB reads only its members.
- **The USA and Poland.** They move only because `ApplyCurrencyStrength` averages partner policy rates unweighted. Sweden's Riksbank therefore counts one fifth in the dollar's target, the same as Germany, and the ECB's rate is counted three times. A Swedish rate move of −0.0017 pp becomes +0.00026 on the USA's `CurrencyStrength` at turn 2. This arithmetic is consistent with the dumps.
- **Size of the propagation over 1000 turns.** GDP moves by at most 4e-5 % (USA) and 4e-4 % (Poland). Trade balance moves by at most 0.04 % and 0.17 %.
- If trade-weighting of the partner-rate average was intended, that is a separate ruling.

## Checked and found sound

1. **Exactness.**
   - Every component scales with the price index except the nominal ceiling.
   - A curtailed block prices at 0. A block running only fossil floors, such as Poland's base (residual 0), prices at the cheapest floor × 0.75 = 68.2. Both are homogeneous in P.
   - The hydro shift compares the spread in seed prices.
   - (c × P) ⁄ P is exact to 1 ulp, invisible at float32.
2. **Which price level, and the several worlds.**
   - The same float is read at the same instant. The price level is written only by `MacroSystem.ApplyPriceLevelDaily`, never inside the boundary.
   - `ShadowBaseline` runs its own days and boundary. The ledger forks run before the real advance.
   - No daily step reads a clearing.
   - `GameController` lines 895 and 5658 re-set the turn state after a load and after the shadow.
   - The class is strictly smaller. A foreign `_waterValue` now differs only through fleet, ETS or scarcity differences in Germany or Poland, and no longer through price-level paths.
   - The preview (`EnergyPassThrough.PlannedForPreview`), a mid-year load and the first year before any boundary all improve. The old code measured a boundary-dated nominal proxy against a live P_se.
3. **Callers and readers.**
   - Every `ClearAt` caller passes P = 1, or P = 2 for the B6 probe, which is exact.
   - `ReservoirDispatchDiagnostic`'s coupling assertion holds: move = β × Δw × P, and Δ`WaterValue` = Δw × P. Its year-2 residual formula is unchanged, as is the deficit-factor assertion.
   - `EnergyLayerCheck`, `EnergyLayerDump`, `EnergyMarketDiagnostic` and `EnergyPassThroughDiagnostic` only print the water value or compare differences.
   - `EnergyLedger` never reads `WaterValue`.
4. **Identity at the seed.**
   - `x ⁄ 1.0` is exact.
   - `ClearAtSeed` passes the same array as `seedProxy`, so the move is exactly 0 and the zone price is bitwise the seed's.
   - `FitMargins`, the seed book and the rent's seed read `ClearAtSeed`.
   - The t0 blocks of `pa_measure.log` and `pb_measure.log` are identical (34 lines).
   - Turn 1 is explained. At the first boundary P_se is 1.01965, P_de 1.0293 and P_pl 1.01813. The old nominal proxy was 76.87 against 75.047 × 1.01965 = 76.52. That is +0.35 €/MWh, which shows as +0.76 % on the rent.
5. **The family.**
   - (a) and (b) are confirmed as above. The USA's and Poland's first differing fields are `CurrencyStrength` and `TradeBalance` at turn 2.
   - (c) Sweden's household real price runs 0.258431 → 0.259836, at most +0.607 % at t93. Industry over P runs 0.105210 → 0.106335, at most +1.193 % at t93. Rent over P is 0.985650, flat. The "third digit" claim is loose; these two figures are the measured moves.
   - The log shows wholesale, margin and network over P constant to t1000. The levy is the only mover (0.000216 → 0.00134), so the `LargeK` cause is right.
   - Seed 424242's century reads Sweden's industry cell at 1.464 %.
6. **The sentinel.**
   - Both digests recompute from the dumps: `798bfbee…` and `a0bc788f…`.
   - My re-run of the bounds arithmetic gives exactly 6 cells over the band, which is the table's 6 rows and `EnergyBreachCeiling`.
   - Sweden's 1.193 against a ceiling of 2.0 is not slack.
   - No reference to the retired cause constant remains.
   - The Editor assembly built at 18:20, after the edit at 18:18.
7. **Other.**
   - The deficit multiplier's placement is unchanged.
   - `Response`'s second leg is unchanged.
   - With no Germany or Poland in the world, Sweden now holds a flat real price. The old code eroded it against seedProxy × P_se.
   - With no Sweden the change is inert.

`Tools/review_ledger.tsv` has no row yet for this `EnergyMarket.cs` state or for the two p6pb digests. They are owed with this review.

## Files read

- Simulation: `EnergyMarket.cs` (whole), `SimulationManager.cs`, `MacroSystem.cs`, `CurrencySystem.cs`, `TradeSystem.cs`, `EnergyLedger.cs`, `EnergyPassThrough.cs`, `ShadowBaseline.cs`, `PolicyImpactLedger.cs`, `EurozoneRateSystem.cs` (greps)
- UI: `GameController.cs`, `GameController.Energy.cs`
- Editor: `TrajectorySentinelCheck.cs`, `EnergyRunawayDiagnostic.cs`, `ReservoirDispatchDiagnostic.cs`, `EnergyPassThroughDiagnostic.cs`, `EnergyLayerCheck.cs`, `EnergyLayerDump.cs`, `EnergyMarketDiagnostic.cs`, `EnergyLedgerDiagnostic.cs` (greps)
- Records: `COMPLETED.md` §541, `ENERGY_LAYER_PREMISE.md`, `Tools/review_ledger.tsv`
- Captures: `logs/pb_measure.log`, `logs/pa_measure.log`, and the dumps `traj_p6pa` / `traj_p6pb` at s777 t100, s424242 t100 and s777 t1000

I changed nothing in the repository. Two helper scripts, `cmp.pl` and `bounds.pl`, sit in the scratchpad at `C:\Users\elias\AppData\Local\Temp\claude\C--Users-elias\e2343ca0-830e-4b66-89ed-89bd33443c7e\scratchpad\`.

---

## The second reader - on the rework

*The reviewer's report, verbatim.*

# Second read of FT-10 · P-B (uncommitted): the substance is sound, the rework is complete, and there are six low findings, none in the arithmetic

Nothing in the repository was edited and nothing was run. I wrote two comment-stripped copies of `EnergyMarket.cs` to the session scratchpad for a diff. One over-broad `grep -r` of mine timed out and I stopped it; it was read-only.

## Findings, most severe first (all LOW)

**1. The two B6 probes cannot tell P-B from HEAD, and the new comment says otherwise — CONFIRMED**
- Where: `EnergyPassThroughDiagnostic.cs:115-118`, and its sibling in `ReservoirDispatchDiagnostic.cs:22,117-125`.
- Defect: the re-cut comment says the probe "now also holds that a level doubled everywhere moves Sweden's real price by nothing".
  - HEAD held that identically. With every level at 2, the old water value is exactly 2 × seedProxy and `own = S×2 + β×(2s − s×2) = 2S`.
  - A uniform doubling cancels in both forms, so probe 5 passes on the defective code too.
  - What actually excludes HEAD's code is check (2)'s year-1 bound and the sentinel's cells.
- Smallest fix: make the doubling non-uniform.
  - After the `PriceLevel = 2f` loop, set Poland's `PriceLevel = 4f`. Powers of two keep the arithmetic exact, and each country's own book is computed at the explicit `2.0`, which `EnergyLedger.Compute` takes as a parameter.
  - Sweden's 0.000 then is P-B's property. HEAD fails it, because the old water value would be about 2.99 × seed.
  - The same fix applies to `ReservoirDispatchDiagnostic` (5). Otherwise re-word the comment.

**2. The bound's rationale cites a stale figure, and the new bound sits only 1.6× under the removed defect — CONFIRMED**
- Where: `EnergyPassThroughDiagnostic.cs:32-37`.
- Defect: "(0.0204 pp in year 1)" is EN-5-day's reading (§465). On the tree just before P-B the same cell read **0.0080 pp**.
  - Source: `traj_p6pa_s777_t100.csv`, Sweden turn 1, `EnergyHouseholdPriceRealChange` 0.113175 % × 70.5 ‰.
  - So 0.005 pp excludes HEAD's code by a factor of 1.6. A regression in only one of the two neighbours' prices could pass under it.
- The new value is an exact float `0` for all six: `traj_p6pb` turn-1 rows read `0`, not a small number.
  - The float noise ceiling is about 8e-7 pp: one float ulp on the ratio, × 100, × Sweden's weight.
- Smallest fix: lower the bound to 0.001, which is still about 1000× the noise and 8× under the defect, and cite 0.0080. Otherwise keep 0.005 and attribute 0.0204 to §465's world.

**3. The working copy of `EnergyMarket.cs` is LF-only — CONFIRMED, informational**
- It has 0 CR and 721 LF, and `git ls-files --eol` gives `i/lf w/lf`. The other three changed files and the file's siblings are `w/crlf`.
- There is no effect on the commit: `autocrlf=true`, the blob is LF either way, and `ReviewLedgerCheck` hashes with CRs stripped.
- Consequence: any apply script that matches `\r\n` in this file will miss silently.
- Note that `grep -c $'\r$'` reported "721 CR" here. The CRLF test lied again; `tr -cd '\r' | wc -c` is the honest count.

**4. The Energy page re-derives `own` with the old association under an absolute 1e-6 test — PLAUSIBLE, unreachable in play**
- Where: `GameController.Energy.cs:621-625`.
- At HEAD the page's expression and the market's were the same arithmetic, so they agreed exactly. After P-B they agree to about 3e-14 × P.
  - `split` would turn falsely true only past P ≈ 3e7, about year 870 at 2 % inflation.
- Pre-existing and adjacent, in case the filed display item does not cover it:
  - The cache is keyed per turn, but `priceIndex` (line 415) is read live from a `PriceLevel` that moves daily (`MacroSystem.cs:580`).
  - So `block.Price` at the cached level against `own` at today's level trips "SPLIT BY A BINDING LINK".
  - `r.PriceIndex` now exists and would cure both.

**5. The `PriceIndex > 0 ? … : 1.0` guard is unreachable, and if ever reached it silently reproduces the defect — REFUTED-ON-INSPECTION as a defect; a recommendation only**
- Where: `EnergyMarket.cs:490`.
- `WaterValueOf` is private with two call sites. Both take results from the private `ClearAt`, which stamps 1.0 or a value ≥ 0.0001.
  - A NaN level gives NaN prices either way.
- A `throw` would say so where the guard stays silent.

**6. Comment residue in touched files — CONFIRMED, nits**
- `EnergyMarket.cs:512-518`: the `ClearSweden` summary still describes the old shape.
  - It reads "carried by the price level, plus … the proxy's move". Now the sum is carried by the level and the move is in seed prices.
- `EnergyPassThroughDiagnostic.cs:15-17, :22`: "only the fitted adders' real erosion" has been stale since EN-5. Section 5's own header says the ceiling.
- `BeginTurn`: "ACER's ceiling *alone*" is true only while `CurtailmentOffer` is 0.
- Any edit to `EnergyMarket.cs` or `SimulationManager.cs` changes its review-ledger state hash. The states I read:
  - `EnergyMarket.cs`: `173e8de082e29f7ee11c79a2007b38ff968b61c303cf4bff1f186bfd6cea8be6`
  - `SimulationManager.cs`: `c85a649618fbbd06bc160a361dfc2d45c3cf0391933a2108d74bdf94b89262ee`

**Not in the four files**
- `PoliSim.slnx` is modified: two project lines are swapped. Keep it out of the commit.
- Records still owed in the commit:
  - `CLAUDE.md:195` still says "eleven since P-A", "P-B next", and "reads its neighbours' prices at THEIR price levels".
  - The FT-10 row at `POLISIM_FEATURE_LIST.md:1226` is unchanged.
  - §552 does not exist yet.
  - `Tools/review_ledger.tsv` has no rows yet for the two file states or the two new digests.

## Checked and found sound

**A. Stamping and caches**
- The only `EnergyMarket.Result` constructions are `NewResult` (`EnergyMarket.cs:399`) and one hand-built Sweden proxy.
  - The proxy is at `ReservoirDispatchDiagnostic.cs:46` and feeds only `LoadWeightedPricePerMwh`.
- Every public entry (`Clear`, `ClearAt`, `ClearAtSeed`, `Response`) goes through the private `ClearAt`.
  - It stamps after `ClearAtCore`, so all three return paths (Sweden, `unshifted`, `best`) are covered.
  - `SeedSpread`'s unstamped result never leaves the method.
- No `Result` is cached inside the market. Only doubles, `double[]` and the adders are.
- The one field-level cache is `_energyResult` in `GameController.Energy.cs:49`: a single call, stamped once, never re-stamped.
- The stamp is numerically inert by construction: it is the same float-to-double value the earlier code read twice.

**B. The non-comment changes against HEAD are exactly six**
- The six:
  - the `PriceIndex` field;
  - the stamp in `ClearAt`;
  - the `priceLevelDe` / `priceLevelPl` line in `WaterValueOf`;
  - the `water[b]` expression;
  - `result.WaterValue[b] = water * priceIndex`;
  - the `own[z]` expression.
- `BeginTurn`'s call and `WaterValueAtSeed`'s call are byte-identical to HEAD, because the rework restored the two-`Result` signature. The brief's list names them as changed; they are not.
- Insertions 20 and deletions 6 reconcile with the comment lines.

**C. `own[z]`**
- Old and new are algebraically equal whenever the three price indices are equal, because the old water value is then the new one times P. This holds even with scarcity.
- At P = 1 with `w == s` it is bit-for-bit the same double:
  - `w − s` is +0.0, and β × 0 is ±0.0;
  - S + ±0.0 is S, and S × 1.0 is S;
  - both forms end at S × k.

**D. The lowered bound**
- 0.005 pp is about 6000× above the noise for both uses. Both are exact zeros: check (2) reads `0` in the dump, and probe 5 uses exact power-of-two scaling.
- The year-1 zero is **structural, not luck**:
  - `AiFinanceMinistry.Apply` returns empty on a null report (`AiFinanceMinistry.cs:122`).
  - The first report is written inside boundary 1's `ApplyDomesticPolicy` (`SimulationManager.cs:2864`), after that boundary's `Apply` (`:2747`).
  - So `LevyScale` is exactly 1 at boundary 1.
  - The dials move only by decision, and the electricity tax moves only by a law.
- Scarcity is 0.0 on every seed block (the `ENERGY_LAYER_PREMISE.md` table). With load and fleet static it stays 0 on the standing fleets.

**E.** The `SimulationManager.cs` diff is that one comment line (numstat 1/1), and the comment is true.

**F. The sentinel doc, checked against the dumps**
- I recomputed both digests from `traj_p6pb_s{777,424242}_t100.csv`; they match.
- Row-by-row against `traj_p6pa`, on both seeds over 100 turns:
  - Sweden moves from turn 1.
  - The USA and Poland move from turn 2, first in `CurrencyStrength` and `TradeBalance`, by 3e-6 to 8e-6 relative.
  - Germany, France and Italy have zero differing rows.
- `CurrencySystem.cs:110` confirms a shared-currency country returns early.
- The bounds pass, recomputed:
  - Six cells stand over the band.
  - Sweden's industry LEVEL is 1.193 at t93, its rent 0.000 / 0.000, its tails 0.114 and 0.223.
  - The five untouched rows read the same figures as before.
- The ×1.65 is the Poland-to-Sweden level ratio at t100: 13.25 over 8.04.
- Two things P-B changes that the dumps cannot show, both for the better and worth a line in §552:
  - A Swedish player's **preview**: the game never calls `EndTurn`, so the standing water value from the last boundary is now real. Before, it lost up to a year of inflation against `seedProxy × P_today`.
  - The null-state fallback now gives `S × P`, not `S×P + β×s×(1−P)`.

**Other diagnostics that read Sweden, by inspection (I did not run them)**
- `ReservoirDispatchDiagnostic`'s coupling, reservoir and B6 identities hold under `WaterValue = water × P_se`.
- `EnergyLedgerDiagnostic`'s single-book and credit checks are consistent with the new form.

## Files read
`EnergyMarket.cs`, `SimulationManager.cs`, `TrajectorySentinelCheck.cs`, `EnergyPassThroughDiagnostic.cs`, `EnergyLedger.cs`, `EnergyPassThrough.cs`, `AiFinanceMinistry.cs`, `CurrencySystem.cs`, `GameController.Energy.cs`, `ReservoirDispatchDiagnostic.cs`, `EnergyRunawayDiagnostic.cs`, `EnergyMarketDiagnostic.cs`, `ReviewLedgerCheck.cs` (head), `EnergyLayerDump.cs`, `CarbonTaxUnitDiagnostic.cs`, `EnergyLedgerDiagnostic.cs`, `TrajectoryBaselineDump.cs`, `MacroSystem.cs` (the last five in part or by grep), `ENERGY_LAYER_PREMISE.md`, `COMPLETED.md` §541, `CLAUDE.md:195`, `Tools/review_ledger.tsv`, and the four named dumps under `G:/UNITY/Projects/PoliSim-captures/trajectories/`.
