# Review: PS-3b - the government of record gains its executives; the USA's two presidencies (2026-09-25, s629)

**What was reviewed.** The working-tree change (read-only, `git diff` against HEAD) to `Assets/Scripts/Elections/WorldClock.cs` (`ExecutiveKind`, `GovernmentOfRecord.Kind`/`.President`, the USA's two rows, the `//FRANCE-ROWS` marker), `Assets/Scripts/Elections/GovernmentRecord.cs` (`Kind`, `Executive`, `AtStart`'s throw, `Describe`), `Assets/Scripts/Simulation/SimulationManager.cs` (`PlayerGoverns` and its four gates), `Assets/Scripts/Elections/StartBrief.cs` (the presidency clause and rows, `Majority()`), `Assets/Scripts/Persistence/SaveGameService.cs` (format 28, `BuildSettings`, `Deserialize`), `Assets/Editor/PlayerRoleDiagnostic.cs`, `Assets/Editor/StartBriefDiagnostic.cs`, `Assets/Scripts/UI/GameController.cs` (`OpenWorldAt`, `SelectPlayerCountry`, `RestoreFromSave`, the decision routing at 6318, `ResolveElectionVerdict`, the election-night formation read at 6752, the IMGUI pickers at 2342/2400) and `ElectionsData/usa/records_by_date.md` §5, with the evidence file `Reviews/evidence/2026-09-25_s629_probe_red.txt`. Read for the callers and the surfaces: `Assets/Scripts/Elections/SeatedGovernment.cs`, `Assets/Scripts/Elections/GovernmentFormation.cs` (`ViewOf`, `Cabinet`, `TryFormChamber`), `Assets/Scripts/Elections/StartPoints.cs`, `Assets/Scripts/Elections/CampaignClock.cs` (the run-up arithmetic), `Assets/Scripts/Elections/CompassPositions.cs`, `Assets/Scripts/Data/PartySystem.cs` (`FranceParties`, `UsaParties`, `SeatsAt`, `InitialSeats`, `SeatsSourced`), `Assets/Scripts/Data/Country.cs` (`Government`), `Assets/Scripts/UI/GameController.Desk.cs` (`RoleTitleSuffix`), `Assets/Scripts/UI/GameController.CampaignCoalition.cs`, `Assets/Scripts/UI/ElectionNightScreen.cs` (the outcome names), `Assets/Scripts/UI/PoliticalCompassRenderer.cs`, `Assets/Scripts/UI/CountrySelectorScreen.cs` (the brief's ledger, `PartyLine`), `Assets/Scripts/Testing/UiScreenshotDriver.cs` (the harness's `SelectPlayerCountry` calls), `Assets/Editor/WorldClockDiagnostic.cs`, `Assets/Editor/PlayProtocolStaging.cs`, `Assets/Editor/MetaTextCheck.cs`, and the 32 Editor tools that set `sim.PlayerCountryId` without storing a `Government` (listed under finding 2). One independent read-only reader, asked to refute the throws, the formation on a presidency, France's start, the brief, the save, the masthead and the meta-text rule.

---

## The reader

Adversarial review of PS-3b (working tree at G:/UNITY/Projects/PoliSim, read-only). Note first: the diff I was handed does NOT do what the brief says in one place - `SimulationManager.PlayerGoverns` was not changed (only its comment was), so "a null Government now THROWS" is not in the code, and the diagnostic that asserts it will be red (finding 1). The USA's dates and ids match `records_by_date.md` §5 exactly (finding 10).

FINDINGS

1. DEFECT - `PlayerGoverns` does not throw; the diagnostic asserts that it does. `Assets/Scripts/Simulation/SimulationManager.cs:2265` still reads `if (country.Government == null) { return true; }` - the diff touched only the comment block above it (lines 2257-2258, "FAILS LOUDLY (PS-3b, §629)"). `Assets/Editor/PlayerRoleDiagnostic.cs:107-109` sets `sweden.Government = null`, calls `sim.PlayerGoverns(sweden)` with `PlayerPartyAbbrev = "S"` and `PlayerCountryId = Sweden`, expects `InvalidOperationException` - the call returns `true`, `threw` stays false, `Check(threw, ...)` FAILS. This is exactly the last FAIL line of the red probe (`Reviews/evidence/2026-09-25_s629_probe_red.txt`, "a country whose government was never stored: PlayerGoverns THROWS rather than defaulting the player to governing", stack at PlayerRoleDiagnostic.cs:108) and it will print the same on this build: the probe is red for the SAME reason on both builds, so the green half of "proved both ways" has not happened. The comment and the code contradict each other; either implement the throw (and see finding 2 first) or revert the comment and the assertion.

2. RISK (blocking IF finding 1 is fixed by adding the throw) - 32 Editor tools run turns with a player country and no stored government. `PlayerGoverns` is called for EVERY country at the turn boundary (`SimulationManager.cs:2834` the AI finance ministry, `:2843` the AI energy ministry, `:4268` `IndexSpendingLines`, `:2069` `TryOpenBudgetProcess`): for the player's own country the null test at 2265 is reached (the order is `country == null || !PlayerCountryId.HasValue || PlayerCountryId.Value != country.Id` FIRST - another country returns false before the null test, confirmed). Every tool that sets `sim.PlayerCountryId` and never writes `Government` would throw on its first `AdvanceTurn`: `AiFinanceMinistryDiagnostic.cs:111`, `CarbonTaxUnitDiagnostic.cs:212/245`, `DebtPathProbe.cs:32`, `EducationFamilyDiagnostic.cs:70`, `EducationFeedbackDiagnostic.cs:64`, `EffectivenessDiagnostic.cs:78`, `EnergyLedgerDiagnostic.cs:427/557/588`, `EnergyMarketDiagnostic.cs:137`, `EnergyPassThroughDiagnostic.cs:188/228/257`, `EnvironmentFamilyDiagnostic.cs:76`, `EnvironmentFeedbackDiagnostic.cs:67`, `EventShockPathDiagnostic.cs:92`, `HealthFamilyDiagnostic.cs:82`, `HealthFeedbackDiagnostic.cs:66`, `HealthTrendDiagnostic.cs:78`, `HouseholdBurdenAnchorDiagnostic.cs:56`, `ImmigrationPovertyFamilyDiagnostic.cs:73`, `InfrastructureDriftProbe.cs:27`, `InfrastructureFamilyDiagnostic.cs:60`, `InfrastructureReadoutDiagnostic.cs:76`, `JobsLagDiagnostic.cs:95`, `LaborTaxParticipationDiagnostic.cs:71`, `LineIndexationDiagnostic.cs:32`, `LineScalingProbe.cs:28`, `NoPolicyCentury.cs:68`, `PollingDayDiagnostic.cs`, `PotentialEmploymentDiagnostic.cs`, `PriceLevelDiagnostic.cs`, `ReservoirDispatchDiagnostic.cs`, `SaveLoadRoundTripDiagnostic.cs`, `SpendingIndexationDiagnostic.cs`, `TaxScheduleDiagnostic.cs` (grep: files with `PlayerCountryId =` and no `.Government =`). `TrajectoryBaselineDump` sets no player at all (the `!PlayerCountryId.HasValue` leg, unaffected); `PlayProtocolStaging.cs:71` stores the record (safe). The intended semantics "the role is never defaulted" therefore needs either a one-line seat in each tool (`c.Government = GovernmentRecord.AtStart(c, EpochDate)`) or a shared helper on `SimulationManager` that the tools call - otherwise the cheap bar goes red across the board the moment the throw lands. The game's own paths are covered: `OpenWorldAt` (GameController.cs:2175) writes on every rebuild, `SelectPlayerCountry` (:2218) writes on the same-epoch return, both the harness entries (`UiScreenshotDriver.cs:393`, `:3567`) go through `SelectPlayerCountry`, `RestoreFromSave` (:899-935) adopts the saved world whose record rides `Country.Government` (`Country.cs:925`), and the loader refuses anything but format 28 (`SaveGameService.cs:161`, strict `!=`), so no v27 `null` can arrive by load. `GameController.PlayerCountryId => _selectedPlayerCountryId ?? CountryId.USA` (:106) is the controller's own; `SimulationManager.PlayerCountryId` is nullable and set only at :2195 and :912, so nothing reads the USA as the player before its record is stored.

3. DEFECT - `PlayerRoleDiagnostic` asserts the WRONG president for the USA's start. `Assets/Editor/PlayerRoleDiagnostic.cs:96-97` (diff lines 61-62): `Check(usa != null && usa.PmParty == "REP" && ... && usa.Executive.Contains("Trump"), F("the USA's start ({0:yyyy-MM-dd}) ...", WorldClock.StartDate(CountryId.USA), ...))`. `WorldClock.StartDate(USA)` is the standard run-up (`WorldClock.cs:102-110`, default branch): `new CampaignCalendar(2024-11-05).PreCampaignStart` = 2024-11-05 − 8×7 days (`CampaignClock.cs:97`) − 26×7 days (`:99`; `DefaultCampaignWeeks = 8`, `DefaultPreCampaignWeeks = 26`, `:72/:75`) = **2024-03-12**. On that date `TryGovernmentAt` (`WorldClock.cs:260`) returns the BIDEN row (2021-01-20 ≤ d < 2025-01-20), `SeatedGovernment.TryAt` installs `{DEM}` and `HeadParty` parses "(DEM)" from "Joseph R. Biden Jr. (DEM), president" (`GovernmentRecord.cs:101-110`) → `PmParty = "DEM"`, `Executive = "Joseph R. Biden Jr. (DEM)"`. The check fails on both halves. `StartBriefDiagnostic.cs:55-58` asserts the same start as "Biden (DEM) since 20 Jan 2021" on "12 March 2024" - the two diagnostics contradict each other, and the brief's is the right one. Fix: `PmParty == "DEM"` and `Contains("Biden")` (or assert Trump against `AtStart(usa, 2025-01-20)` as a second, dated check - that exercises the DERIVED boundary too).

4. OK, with a wording RISK - the formation on a presidency. `SeatedGovernment.TryAt(USA, 2024-03-12)` (`SeatedGovernment.cs:60-64`) marks the Biden row INSTALLED with `Cabinet = {DEM}` (`CabinetSourced` true, non-empty). `GovernmentFormation.TryFormChamber` (`GovernmentFormation.cs:272-276`) then calls `SeatedGovernment.TryInstalled` → `TryAsResult` over the House: `Mask` finds "DEM" in `UsaParties` (`PartySystem.cs:475-483`, keys REP and DEM), seats from the Usa2022 table (REP 222, DEM 213; `PartySystem.cs:665`), majority 218 → `cabinetSeats 213 < majority`, `support == 0` → `CoalitionOutcomeKind.MinorityGovernment` (`SeatedGovernment.cs:119-121`). `TryAsResult` FAILS only for a key the chamber does not seat (`:107`), which DEM is not. `WorldClockDiagnostic.cs:85-96`: `want.ContainsKey("DEM")` true (InitialSeats is keyed by the roster), `TryInstalled` true (epoch = `ApplyStart(USA)` at :78), `GovernmentFormation.Cabinet(country)` = ["DEM"] equals the record's - PASSES. Where the label lands: the OUTCOME word "A MINORITY GOVERNMENT" (`ElectionNightScreen.cs:506`, `GameController.CampaignCoalition.cs:82`) is reached only from election-night and coalition-page snapshots, and both are built for the player's country's OWN election (`GameController.cs:6752` on `PollingDayToday`; the coalition page's snapshots are Sweden fixtures, `UiScreenshotDriver.cs:4566-4594`) - the USA offers no polling day (`WorldClock.TryNextPollingDay`, `:311` Sweden only), so no USA screen prints "minority government" today. What IS shown for the USA: the IMGUI pickers (`GameController.cs:2342/2400` via `PickerViewOf`, `WorldClock.cs:426-428`: Installed → `view.Cabinet = {DEM}`) print "DEM — Democratic Party · 213 SEATS · IN THE CABINET" (`CountrySelectorScreen.cs:206-211`), and the political compass legend (`PoliticalCompassRenderer.cs:100-108`) prints "UNITED STATES'S CABINET · SEAT-WEIGHTED MEAN / DEM · 213 seats". "IN THE CABINET" beside a House minority, for a president's party, is the presidency wearing a cabinet's word - not false in the model's terms (the record's `Cabinet` holds the one party by design, `WorldClock.cs:64`) but a reading Design has not ruled; the brief itself gets it right ("The president is ... ; the House majority is REP"). NOTE: PartyLine/legend could read `Kind` and say "THE PRESIDENT'S PARTY" where `Kind == Presidency`.

5. OK - France at 2024-07-18 is on the boundary and INSIDE. `ChamberOfRecord.Holds` is `date >= Convened && date < Until` (`WorldClock.cs:49`); France2024 is `Convened = D(2024, 7, 18)` (`:165`), so `ChamberAt(France, 2024-07-18)` returns it and never reaches the gap throw (`:184`); the gap is 2024-06-09 ≤ d < 2024-07-18 exclusive of the start. RISK for the parallel task at `//FRANCE-ROWS` (`WorldClock.cs:287`): `AtStart`'s INSTALLED branch (`GovernmentRecord.cs:69-75`) copies the record's cabinet WITHOUT going through `TryAsResult`, so a cabinet key that is not a `FranceParties` key (`PartySystem.cs:424-441`: UG, ENS, RN, LR, DVD, UXD, DVG, REG, HOR, DVC, UDI, SOC, ECO, DIV, EXD, DIV - nuances, not parties; "RE", "MoDem", "LIOT" would NOT be keys) installs silently in the record, while every formation reader (`GovernmentFormation.Cabinet`, the compass, the pickers, `WorldClockDiagnostic.cs:93-95`) goes through `TryInstalled → TryAsResult`, which returns false with "the installed cabinet names 'X', which this chamber does not seat" → `Cabinet()` returns EMPTY (`GovernmentFormation.cs:118-121`), the compass says "no government is formed from this chamber", and WorldClockDiagnostic FAILS "the formation reads the cabinet as [], the record says [...]". Also `HeadParty` for "Gabriel Attal (RE)" returns null (RE not a roster key, `:108-109`) and `PmParty` falls to `Largest` = ENS by seats (150) - fine as a premise, but stated nowhere. Recommend: the France rows use roster keys only, and `AtStart`'s installed branch asserts each cabinet key is in `PartySystems.For(id)` (throw, as for a missing record) so the record and the formation cannot disagree.

6. OK - the brief. `StartBrief.Clauses(USA)` (`StartBrief.cs:55-115`): country (61), the presidency clause (71-78), "Polling day is 5 November 2024." (`start.PollingDay` = `LatestElectionDay` via `Ruled`, `StartPoints.cs:93`; 100-103), the parties clause (111-113) - exactly four; `StartBriefDiagnostic.cs:35` holds. `SeatedVintage(USA, 2024-03-12)` (`WorldClock.cs:192-196`): `ChamberAt` → Usa2022 (2023-01-03 ≤ d < 2025-01-03, `:157`), `SeatsSourced(Usa2022)` true (`PartySystem.cs:627-636`, default branch) → Usa2022 → `InitialSeats` = REP 222, DEM 213 (`:665`), size 435. `Majority()` (`StartBrief.cs:44-49`) → ("REP", 222); the text is "The president is Joseph R. Biden Jr. (DEM), in office since 20 January 2021; the House majority is REP, with 222 of 435 seats." - matches the assertion at `StartBriefDiagnostic.cs:57`. The rows: President / Since 20 JAN 2021 / House "REP · 222 OF 435" / Polling day 5 NOV 2024 / In the House "2 PARTIES · 435 SEATS · ELECTED 8 NOV 2022" - the ledger draws Name and Figure only (`CountrySelectorScreen.cs:753-778`), never `Basis`, so the "§1" inside the USA row's Basis ("Twentieth Amendment §1 [EX-AM]") reaches no screen. NOTE on `Majority()` ties: `kv.Value > bestSeats` keeps the FIRST key on a tie, silently naming the roster's first (REP) a "majority" at 217-217 or on an all-zero table ("-", 0 seats) - no start hits it today (the three USA tables are 222/212, 222/213, 220/215), but a tie reads as a majority with no word for it.

7. OK - the save. `GovernmentRecord.Kind` is a plain public enum field on a sealed class with public fields; Json.NET's default serialises an enum as its integer and reads it back (no `StringEnumConverter` anywhere in `Assets/Scripts`: grep), `MissingMemberHandling.Ignore` / `TypeNameHandling.None` / `PreserveReferencesHandling.Objects` (`SaveGameService.cs:71-80`) unchanged; `Executive` is a nullable string. Format 28 is gated by strict equality on the raw parse (`:161`), so a v27 save (Kind absent → would read `Cabinet` = 0, `Executive` null) is refused before deserialisation, as the version note says (`:302`). `PlayerRoleDiagnostic.cs:62-67` round-trips Sweden's record at format 28; nothing round-trips a Presidency record - NOTE, add the USA to the round-trip assertion (Kind == Presidency after load) since that is the field the version was bumped for.

8. RISK (pre-existing, sharpened) - `AtStart`'s stand-in branch reads the EPOCH, not `start`. `GovernmentRecord.cs:77` calls `GovernmentFormation.ViewOf(country)` with `vintage = Seated`, whose `TryInstalled` reads `SeatedGovernment.TryFor(id)` = `TryAt(id, EpochDate)` (`SeatedGovernment.cs:50`, `:93`) while `AtStart`'s own record test at `:69` used `start`. In the game they coincide (`OpenWorldAt` sets the epoch before `AtStart`, `GameController.cs:2160/2175`). In `PlayerRoleDiagnostic` the epoch is Sweden's 2026-01-18 (`:34`) while Poland is asked at 2023-02-19: `TryAt(Poland, 2023-02-19)` → Morawiecki, unsourced → falls to the stand-in; the stand-in's `TryInstalled` reads `TryAt(Poland, 2026-01-18)` → Tusk INSTALLED → `TryAsResult` on the world's chamber (seated at the epoch: Poland2023) → "KO+TD+NL led by KO (provisional)" - exactly the probe's ROLE line. That is a 2023 date's record answered by a 2026 government, labelled provisional; the diagnostic prints it as the stand-in without noticing. The new "Kind/Executive" fields make this visible when France's rows land (a France stand-in would carry the 2026 epoch's president). NOTE-level fix: `ViewOf(country, WorldClock.SeatedVintage(country.Id, start))` or an `AtStart` that scopes the epoch (`SimulationManager.EpochScope` exists, `PlayerRoleDiagnostic.cs:30`).

9. OK, with a wording RISK - the masthead. `RoleTitleSuffix` (`GameController.Desk.cs:260-270`): a USA player seated as REP at 2024-03-12 reads `RoleOf("REP")` → not PmParty (DEM), not in Cabinet, not in Support → `Opposition` → " · IN OPPOSITION · THE GOVERNMENT IS THE AI'S"; `PlayerGoverns` false → the AI finance ministry writes the USA's book (`:2834`), the budget process never opens (`:2069`), decisions route `None` (`GameController.cs:6318`). Internally consistent: the model has one governing party and the AI holds it. The words are the risk: REP holds the House majority (222) on that date; "IN OPPOSITION" is true of the presidency and false of the chamber, and `JuniorPartner`/`Support` can never arise for a presidency (Cabinet holds one party, Support null). A `Kind`-aware suffix ("THE PRESIDENCY IS THE AI'S") would say what the record says. Divided government (§6 of the spec) is not modelled by this change and is not claimed to be; the record should say so.

10. OK - the USA's dates and ids against `ElectionsData/usa/records_by_date.md` §5. Biden `From 2021-01-20`, `Until 2025-01-20`, DEM, basis ids [EX-EC20] [EX-BIDEN] [EX-BIDEN-INAUG] [EX-AM] - the table row at `records_by_date.md:350`; Trump `From 2025-01-20`, `Open`, REP, [EX-EC24] [EX-DATES] [EX-TRUMP-INAUG] [EX-TRUMP] - row `:351`; all nine ids exist in the §5 register (`:385-395`), "306 to 232", "312 to 226" and the quoted "January 20, 2025 at Noon—Inauguration Day" match (the code's basis writes a hyphen for the record's em dash; harmless). The DERIVED end date: the record marks Biden's last day as the Amendment applied (G11), and the code's `Holds` = `date >= From && date < Until` gives 2025-01-20 to Trump - the noon boundary rounded to the day in Trump's favour, consistent with the code's convention for every other row (Sweden 2022-10-18, Germany 2024-11-07) and with the record's "the terms of their successors shall then begin". The code's basis text says "the term ends at noon 2025-01-20" but does not say DERIVED where the record does; NOTE only (the `[EX-AM]` id carries it).

11. OK - meta-text and culture. `MetaTextCheck` scans `Assets/Scripts/UI`, `LawCatalog.cs` and `Assets/Scripts/Data` only (`MetaTextCheck.cs:61`) - `Assets/Scripts/Elections` (StartBrief, GovernmentRecord, WorldClock) is OUTSIDE its roots, so the check is not tripped and would not be by anything there. By hand: the new user-facing strings ("The president is ... in office since ... ; the ... majority is ..., with N of M seats.", the rows "President", "Since", the chamber row "REP · 222 OF 435", ", under the president ...") carry no `§`, no register id, no backtick. The `§629`/`PS-3b` mentions are in comments, the exception message (`GovernmentRecord.cs:67`, a `throw` line) and `Describe`'s log line (`:116`, Debug.Log only). `Long()`/`Stamp()` are `CultureInfo.InvariantCulture` (`StartBrief.cs:36-38`), seat figures `ToString(InvariantCulture)`. Stale comment: `WorldClock.cs:259` still says `TryGovernmentAt` is "false for the two countries outside the formation model" - the USA now has rows and France is about to; NOTE.

12. NOTE - `PlayerRoleDiagnostic.cs:70-72` still names the save "format 27" in its Check string (`:67`) after the bump to 28; cosmetic.

VERDICT: NOT READY as it stands. Two defects, both in what the change claims of itself: (1) `PlayerGoverns` still returns true on a null government - the comment says it throws, the diagnostic asserts it throws, the code does not, so the diagnostic is red on this build for the same reason it was red on the last; (3) the diagnostic asserts Trump/REP at the USA's start, which is 12 March 2024 under Biden/DEM - the brief's own diagnostic asserts the opposite and is right. Before adding the throw, seat a record in the 32 Editor tools that run turns with a player country (finding 2) or the cheap bar goes red across the board. The USA rows are exact against the record, the formation on the presidency passes its diagnostic, France's boundary date is inside its chamber, the brief is four clauses with the right figures, and the save posture is sound; the open wording risks are the presidency wearing a cabinet's words on the pickers, the compass and the masthead (findings 4, 9), and the France rows must use roster keys or the record and the formation will disagree (finding 5).

---

## Second reading

**Re-read (read-only).** `Assets/Scripts/Simulation/SimulationManager.cs` (`PlayerGoverns`, 2262-2273), `Assets/Scripts/Data/WorldFactory.cs` (the seat seeding at 1032 and the new store at 1088-1092), `Assets/Scripts/Elections/WorldClock.cs` (`FrancePresident` at 79, the seven France rows at 261-267), `Assets/Scripts/Elections/GovernmentRecord.cs`, `Assets/Scripts/Elections/SeatedGovernment.cs` (`TryAt`), `Assets/Scripts/Elections/GovernmentFormation.cs` (`TryFormChamber`, `TryFormSeats`), `Assets/Scripts/Elections/DeclaredRedLines.cs` (`IsSourced`), `Assets/Scripts/Elections/StartBrief.cs` (the diff), `Assets/Editor/PlayerRoleDiagnostic.cs` (the diff), `Assets/Editor/StartBriefDiagnostic.cs` (the diff), `Assets/Editor/NoPolicyCentury.cs` (58-72), `Assets/Editor/PollingDayDiagnostic.cs` (74-100), `Assets/Editor/SaveLoadRoundTripDiagnostic.cs` (195-202), `Assets/Editor/ChamberVerdictCacheCheck.cs`, `Assets/Editor/ReBacktest.cs` (the hand-built countries), `Assets/Editor/WorldClockDiagnostic.cs` (the epochs at 74/123/137), `Assets/Scripts/Persistence/SaveGameService.cs` (`RestoreInto`'s `SetEpoch` at 232), `ElectionsData/france/records_by_date.md` Table E2 (549-560).

1. Finding 1 CLOSED. `SimulationManager.cs:2267` throws `InvalidOperationException` on `country.Government == null`, after the country/player test (order confirmed: another country still returns false at 2264 before the null test), and `:2270` returns true for a player country whose `PlayerPartyAbbrev` is null or empty, before `RoleOf`. The no-party rule keeps every one of the 32 tools of finding 2 on their old path: they set `sim.PlayerCountryId` and seat no party, so the second test answers as the null test used to - and the factory now stores a record for all six countries (`WorldFactory.cs:1092`), so the throw is not reached by them either way. `NoPolicyCentury.cs:61-68`: `ApplyStart(player)` then `CreateDefault()` (records stored at the player's own start), no party, true - the same path as before. `TrajectoryBaselineDump` sets no player (unchanged). The game never reaches the no-party leg: `SelectPlayerCountry` (GameController.cs:2200-2213) seats the largest party before anything runs, the picker seats first, and a load carries its party. Finding 2 CLOSED structurally: the only `Country` objects built outside the factory are `ReBacktest.cs:217-296`, which never sets a `PlayerCountryId` or a world on a manager, so `PlayerGoverns` is never asked of them.

2. RISK (a behaviour change, not a defect) - the tools that seat a party now read the ROLE where they read "the player governs" before. `PollingDayDiagnostic.cs:78-82` seats Sweden's largest (S) on Sweden's start epoch (2026-01-18) and runs 365 days of turns (`:91`): the factory's record is Kristersson's M+KD+L, so `RoleOf("S")` = Opposition, `PlayerGoverns` false, and the AI finance ministry writes Sweden's book in that run (`:2834`) where §628's build let the player's `None` stand. Its assertions are about the run-up and polling day, so it should still pass, but it was not among the three run in the warm host - run it. `SaveLoadRoundTripDiagnostic.cs:195-200` seats `roster[1]` and compares two determinism paths that both take the new branch - consistent with itself, the AI ministry now on the book in both. `ChamberVerdictCacheCheck`, `OfficeTestDiagnostic` and `StanceModelDiagnostic` seat parties but never run a turn or ask `PlayerGoverns`. NoPolicyCentury's family (ps3a byte-identical to ps2cal) is unaffected: no party is seated there.

3. OK - the factory's store. `WorldFactory.cs:1092` runs after `ParliamentSeats = InitialSeats(id, Seated)` (`:1032`) and after every family seed; `AtStart` reads `WorldClock` (static rows), `SeatedGovernment.TryAt(id, EpochDate)`, `PartySystems.For`, `country.ParliamentSeats`, `DeclaredRedLines` (static lists; a grep of `GovernmentFormation.cs` and `DeclaredRedLines.cs` finds no static mutable state or cache) and `ElectionHistory` (empty, so `HeldElection` is false); `CoalitionFormation.Form` is a pure function of seats, compatibility and lines. No side effect and no ordering hazard. Coverage by epoch: every country's own `StartDate` (Sweden 2026-01-18 Kristersson; Germany 2024-11-06 Scholz I; Poland 2023-02-19 Morawiecki; Italy 2022-07-21 Draghi; USA 2024-03-12 Biden; France 2024-07-18 Attal caretaker) and the K-1 `DefaultEpoch` 2026-10-01 (`SimulationManager.cs:231`; `EpochScope` restores whatever held, which is `DefaultEpoch` unless a tool set one - the 32 tools and `WorldClockDiagnostic.cs:123`): Sweden caretaker `Open`, Merz `Open`, Tusk `Open`, Meloni `Open`, Trump `Open`, Lecornu II `Open` - all six hold. The earliest row per country is Sweden 2021-11-30, Germany 2021-12-08, Poland 2019-11-15, Italy 2021-02-13, USA 2021-01-20, France 2022-05-16, so an epoch before 2022-05-16 would now THROW inside `CreateDefault` (France) - no tool or path sets one (grep of `SetEpoch(` / `ApplyStart(`: the six starts, `DefaultEpoch`, a save's own epoch at `SaveGameService.cs:232`). The two `AtStart` calls left in `GameController.cs:2175` and `:2218` are now redundant with the factory (the second is null-guarded and can no longer fire); harmless, NOTE. Finding 8 (the stand-in reads the epoch, not `start`) is moot in the factory, where they coincide, and stays as stated for the diagnostic's per-date calls.

4. OK - finding 3 CLOSED: `PlayerRoleDiagnostic` asserts `PmParty == "DEM"` and `Executive.Contains("Biden")` at `StartDate(USA)`, agreeing with `StartBriefDiagnostic`. The new factory and no-party assertions (`sweden.Government != null` after `CreateDefault`; `PlayerGoverns(sweden)` true with no party seated) test the two new rules directly, and the `Government = null` throw check is now a real red-to-green.

5. OK - France's seven rows against Table E2. Borne 2022-05-16 to 2024-01-09 [JD-B16] [EL-B16] [EL-B24] [JD-B09]; Attal 2024-01-09 to 2024-07-16 [JD-A09] [EL-A09]; Attal caretaker 2024-07-16 to 2024-09-05 [JD-A16] [EL-A16] [JD-BA05]; Barnier 2024-09-05 to 2024-12-13 [JD-BA05] [AN-SCR519] [JD-BY13]; Bayrou 2024-12-13 to 2025-09-09 [JD-BY13] [EL-BY13] [AN-CONF] [JO-0210]; Lecornu I 2025-09-09 to 2025-10-10 [JO-0210] [EL-L09] [EL-L06]; Lecornu II 2025-10-10 to Open [EL-L10] [IG-COMP] [EL-SMP]. Every date is E2's (E2 carries Attal as one row 2024-01-09 to 2024-09-05 with "resigned 2024-07-16, current business to 2024-09-05"; the code's split at the cessation decree of 16 July [JD-A16] is a faithful re-cut in the same shape as Sweden's caretaker row) and every id is in E2's basis column. The window is contiguous from 2022-05-16 to Open (each `Until` is the next `From`), so `TryGovernmentAt` cannot fall through inside it. `President = "Emmanuel Macron (ENS)"`: ENS is E2's own DERIVED mapping of Renaissance to the 2024 nuance, stated in the record above the table. `SeatedGovernment.TryAt` on a France row: `CabinetSourced` false, so the generic PROVISIONAL record with `Cabinet` null (`SeatedGovernment.cs:66-74`); `AtStart` then takes the stand-in branch with `Kind = Cabinet`, `Executive = Macron`, `Provisional = true` - what the diagnostic asserts. `HeadParty("Gabriel Attal, caretaker")` is null (no parenthesis), so `PmParty` comes from `FromView`: no France candidacies, `Largest` by seats.

6. RISK (pre-existing formation, newly STORED and now the player's role) - France's stand-in "ENS+RN+LR led by ENS with UG+...". That is `TryFormSeats` on the 2024 Assembly with derived lines only (`DeclaredRedLines.IsSourced` is Sweden-only, `:35`) - exactly the formation §628's reader described for France in its finding 6, so nothing is new in the formation; what is new is that the record now STORES it and reads the player's role off it. Consequence: France's default party (UG, the largest - `SelectPlayerCountry`'s fallback and the harness's seat) reads Support, so `PlayerGoverns` is FALSE: a player who opens France under "GOVERNING MODE · NO ELECTION" (`WorldClock.StartLine`) opens NOT governing, with the AI ministry on the book and the budget process closed, and the cabinet the record marks PROVISIONAL is one no French government has held (RN in cabinet beside ENS). Both are the risk §628's reader named and the "What was done" answered by storing NO record; PS-3b reverses that answer by design (no null records), so it should be ruled rather than inherited: either the stand-in's role is not read for a `GoverningModeOnly` country while G4 is open (the player governs), or France opens in support of an ENS-led stand-in and the picker says so. NOTE on the brief's words for that day: "Gabriel Attal, caretaker has governed since 16 July 2024, under the president Emmanuel Macron (ENS); the record does not name..." - the caretaker row's `From` is the resignation date, so "has governed since" reads as if Attal took office on 16 July.

VERDICT: READY on the two defects and the structural risk - `PlayerGoverns` throws on a null and passes a no-party player country through, the factory stores every country's record at every epoch in use, the USA assertion reads Biden/DEM, and France's rows are E2's dates and ids with gap-free coverage. Two things to carry into the record before the commit: PollingDayDiagnostic (and the other party-seating tools that run turns) now put the AI ministry on the player's book and should be run once; and France's stand-in is now the default party's role - a design consequence §628 avoided by storing nothing, which needs Elias's ruling rather than a quiet inheritance.
