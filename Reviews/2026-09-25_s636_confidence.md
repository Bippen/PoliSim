# Review: PS-3i - confidence and collapse as data; the motion, the week, the discharge, the Speaker's round, the extra election (2026-09-25, s636)

**What was reviewed.** The working-tree change (read-only, `git diff` against HEAD; no Unity run, no git write) to `Assets/Scripts/Elections/ConfidenceProcedure.cs` (new, whole: `Rules`/`RulesOf` 23-25, the two constants 28/30, `MotionVote` 33-47, `Vote` 54-75, `CanBeTakenUp` 78-85, `ExtraElectionDay` 88-92; with its `.meta`), `Assets/Scripts/Elections/CoalitionFormation.cs` (`RedLinedMask` 533-545, beside `SupportBlocked` 547-561), `Assets/Scripts/Elections/GovernmentFormation.cs` (`RedLinedFrom` 297-310, `ViewOfSitting` 315-326, `IndexOf` 329-335, `TryFormSeats`' `extraLines` 342/350), `Assets/Scripts/Elections/GovernmentRecord.cs` (`NoConfidenceOn`/`NoConfidenceMover`/`Caretaker`/`CaretakerSince` 62-67), `Assets/Scripts/Simulation/SimulationManager.cs` (the tick's call 309; the lost-budget break 1245; the capture 2283; `MoveNoConfidence` 2516-2544, `LeaveGovernment` 2547-2558, `OrderExtraElection` 2561-2577, `ScheduleExtraElection` 2579-2585, `AdvanceConfidenceDay` 2587-2595, `DischargeAndRound` 2598-2629, `TryPlayerPollingDay` 2632-2639, `CurrentCampaignCalendar` 2642-2652; the restore 3069), `Assets/Scripts/Persistence/SaveGame.cs` (`ExtraElectionDate`/`ExtraElectionOrderedOn` 88-90), `Assets/Scripts/Persistence/SaveGameService.cs` (`CurrentSaveVersion = 33` at 53), `Assets/Scripts/UI/GameController.cs` (the call 9810, `DrawConfidence` 11089-11127), `Assets/Editor/ConfidenceDiagnostic.cs` (new, whole, with its `.meta`), `Assets/Editor/CheckSuite.cs` (the row 188), `ElectionsData/sweden/confidence_rules.md` (new, whole) and the untracked raw folder `ElectionsData/sweden/raw/confidence/` (its nine files listed, the PDFs not opened). Read for the paths around the change: `CoalitionFormation.cs` (`Form` 265-400, `SupportersOf` 470-525, `DerivedRedLines` 631-667, `CoalitionMath.Majority` 156-160), `GovernmentFormation.cs` (`Cabinet` 115-128, `TryGovernment` 135-168, `ViewOf`/`Describe`/`TryFormChamber` 190-293), `Assets/Scripts/Elections/DeclaredRedLines.cs` (whole: `For` 40-108, the candidacies 135-184, `InOrAgainstFor` 203-216, the timeline 257-286, `CandidaciesAt` 308-314), `Assets/Scripts/Elections/SeatedGovernment.cs` (`HeldElection`/`IsProvisional`/`TryInstalled` 78-95), `Assets/Scripts/Elections/WorldClock.cs` (`LatestElectionDay`/`StartDate` 90-114, `Chambers` 130-176, `ChamberAt` 179-187, `Resolve`/`SeatedVintage` 190-197, `TryNextPollingDay` 335-346, `VintageOfElection` 361-373, `TryReference` 389-405), `Assets/Scripts/Elections/CampaignClock.cs` (`CampaignCalendar` 69-125), `Assets/Scripts/Data/PartySystem.cs` (`ElectionVintage` 272-292, Sweden's eight parties 359-377, the seat tables 647-649), `SimulationManager.cs` (`AdvanceCountryDayTick` 296-310, `AdvanceDay`'s polling-day flag 477, `ResetArrivalBudgetWindow` 1197, `ResolveGovernmentBudget` 1203-1250, `IntroduceLaborBill` 1499-1510, `TryOpenBudgetProcess` 2191-2243, `PlayerGoverns` 2405-2415, `AdvanceCampaign` 2661-2725, `AdvancePreCampaign` 2732-2762, the restore's clears 3024-3036), `GameController.cs` (the `Update` day loop 775-822, the choice-time rebuild 2150-2170, `QueueNewlyResolvedDivisions` 2268-2288, the pickers 2340-2410, `_effectsAtTurnEnd` 6335, `CheckElection`/`ResolveElectionVerdict` 6560-6650, `ShowElectionNight` 6690-6790, `RunNationalElection` 6795-6840), `Assets/Scripts/UI/SigningScreen.cs` (the title and provenance 155-175, the vote/citation/stances/estimate plates 330-440), `Assets/Scripts/UI/PoliticalCompassRenderer.cs` (98-110), `Assets/Scripts/Elections/CompassPositions.cs` (52-57), `Assets/Scripts/Data/DivisionRecord.cs` (`Append` 93-110), `Assets/Editor/SaveLoadRoundTripDiagnostic.cs` (the player's party 200, `IntroduceCoverageBills` 396-407, the pending snapshot 603-610), `Assets/Editor/MetaTextCheck.cs` (the roots and patterns 61-93), `ElectionsData/sweden/budget_procedure.md` (row 14 and [BA-3], 29/124), `ElectionsData/sweden/2026/coalition_declarations_2026.md` (the V rows 35/444, [C-I9]), the play save on disk (`C:/Users/elias/AppData/LocalLow/DefaultCompany/PoliSim/Saves/playtest_4_precampaign_day1.json`, `"SaveVersion": 33`, written 2026-09-25 16:02), and `Reviews/2026-09-25_s635_support_agreement.md` (the shape).

---

## The reader

Adversarial review of §636 - PS-3i, part 6 of the roles stage: confidence and collapse as data, the spec's stage-3 done-when "a supporter's withdrawal can bring a government down by the country's own procedure". The done-when is met on the seeded chamber and asserted (the diagnostic's 222 of 349, and by reading it carries on S and SD alone, 180); the statute's arithmetic is right (finding 2), election night on an extra date claims no real result (finding 13), and the whole feature is player-path only, so the dumps are untouched by construction (finding 18). Against it: a line-join in the edit COMMENTED OUT the pending labour bill's save capture and restore (finding 1 - every save drops it, and `SaveLoadRoundTripDiagnostic` should be red); the vote and the round read the EPOCH's declarations forever, not the sitting chamber's (finding 7); a carried motion rides the signing ceremony as a fiscal bill and can become election night's "standing budget" (finding 10); an extra election inside the ordinary run-up leaves the campaign record on the wrong polling day, so the ordinary election counts the prediction (finding 12); and the vote's plate tells the player that V and MP hold DECLARED lines they never declared (finding 5). The rest are risks and notes.

FINDINGS

1. DEFECT - two joined lines comment out the pending LABOUR bill's save. The edit fused each new statement onto the following line with no newline between them (the file is CRLF throughout and holds no lone CR - checked byte-wise - so the compiler sees one line): `SimulationManager.cs:2283` reads `ExtraElectionDate = _extraElectionDate, ExtraElectionOrderedOn = _extraElectionOrderedOn,   // PS-3i (§636)                PendingLaborBills = new Dictionary<CountryId, LaborPolicyBill>(_pendingLaborBillByCountry),` - the `PendingLaborBills` initializer is INSIDE the `//` comment, so the capture never writes it; `:3069` reads `_extraElectionDate = state.ExtraElectionDate; …;   // PS-3i (§636)            CopyInto(state.PendingLaborBills, _pendingLaborBillByCountry);` - the restore's `CopyInto` is commented out too, and `_pendingLaborBillByCountry.Clear()` (`:3031`) still runs before it. Scenario: the player tables a labour bill, saves on day 5 of its 21, loads - the bill is gone, the countdown and its vote never happen, and the saved game diverges from the unsaved one. `SaveLoadRoundTripDiagnostic` introduces exactly this bill as the PM's party (`:200`, `:399`) and snapshots `Pending.LaborBillDays` (`:605`), so it should FAIL on this tree - the run reported only `ConfidenceDiagnostic`'s print. Two more fused lines are harmless to behaviour but read as if they were commented: `:1245` (`if (!governmentAdopted) { …Breaks.Add(…); }                ParliamentSystem.ApplyBillResult(…)` - the apply EXECUTES, there is no `//` before it) and `SaveGame.cs:90` (`ExtraElectionOrderedOn … ;        public Dictionary<…> PendingTaxProgramBills` - a second field on the same line). Fix: split all four lines (the class of the Edit tool's escapes-as-raw-characters and the CRLF hunk-split facts in the memory); run the round-trip check. The play save on disk was re-staged by THIS build (v33, 16:02, CurrentDate 2026-01-18) - its `PendingLaborBills` is `{}` at day one, so nothing was lost there, but say so when re-staging after the fix.

2. OK, two NOTES - the statute's arithmetic. `Needed = Members / 2 + 1` (`ConfidenceProcedure.cs:73`) is 175 of 349 - "mer än hälften av riksdagens ledamöter" [RF-R:13:4], [RD-MF-3]; for an even chamber (350 → 176) still "more than half". `Carried = For >= Needed` (`:42`) and abstentions count as not-for (`:69`-`:70`) - right by 13:4, which counts yes-votes against the members, not against those voting. The tenth `(members + 9) / 10` (`:82`) is 35 [RD-MF-2]. `ExtraElectionDay` (`:88-92`) is the Sunday on or before three months, asserted on 4 March → 31 May (`ConfidenceDiagnostic.cs:96`). The 6:7 week: the government may order on days D … D+6 (`SimulationManager.cs:2572`) and the tick discharges on D+7 (`:2593`) - the desk's "DISCHARGES THE GOVERNMENT ON" D+7 agrees; the count of "en vecka" is a premise the GAPS section states. NOTES: (a) THE BASE - `Members` is the seated party system's sum (`:59-63`), a 0-seat party skipped (no effect); vacancies are not modelled, so the base is 349 exactly while the chamber sums to 349 (it always does - the seed tables and the allocator); `CanBeTakenUp` sums EVERY `ParliamentSeats` key (`:81`) while `Vote` sums only the party system's - two bases, equal for Sweden; the constitution's base is the fixed membership (`PartySystem.cs:324`, 349) and reading it once would make both exact. (b) `CanBeTakenUp` dereferences `country.ParliamentSeats` unguarded (`:81`, `:83`) where `Vote` guards it (`:61`) - no seeded country has a null chamber.

3. RISK - 13:4's ORDINARY-election bar is not built. `MoveNoConfidence` refuses between an EXTRA election's decision and its polling day (`:2530`), but 13:4's second paragraph bars a motion "från det att ordinarie val har ägt rum … till dess den genom valet utsedda riksdagen samlas" as well, and in both cases until the new Riksdag CONVENES, not until polling day. Scenario: the game's 13 September 2026 election forms its government at the count (`GameController.cs:6620-6621`); a player in opposition moves on 14 September - taken up; by the statute not before the 28 September upprop (`confidence_rules.md`'s DERIVED live window). The same one day after an extra election: `AdvanceConfidenceDay` clears the date the day after polling (`:2589`). Fix: refuse while `CurrentDate` is before the latest held election's convening - the chamber of record's `Convened` where the held election is the record's, else polling day + 15 days (RF 3:10, the derivation `WorldClock.cs:137` already uses for 2018).

4. OK with one RISK - 6:4, 6:5, 6:9, 6:11 and 3:11 as coded. (a) 6:9/6:11: `DischargeAndRound` marks the fallen record `Caretaker` and dates it (`:2601-2603`), then runs the round the SAME day, so a caretaker that is replaced lasts zero days (2021's round ran nine; the summary says so). (b) 6:4: the round is `CoalitionFormation.Form` on today's seats (`ViewOfSitting`, `GovernmentFormation.cs:315-326`), whose test is `opposed < result.Majority` under the negative rule (`CoalitionFormation.cs:365`) with `Majority = total / 2 + 1` (`:160`) - "Om mer än hälften av riksdagens ledamöter röstar mot förslaget, är det förkastat" [RF-R:6:4], the same inequality. Two things are the model's, not the statute's: "opposed" includes the hold-out term (a party voting against for a better cabinet of its own), and the formation picks the best VIABLE cabinet instead of modelling the Speaker's successive proposals - "no viable cabinet" stands for four rejections (stated in the comment). (c) 6:5's exception: `ordinary <= CurrentDate.AddMonths(3)` (`:2620`) - the caretaker serves to an ordinary election within three months, else an extra election within three (`:2627`); since the extra's day is on or before the three-month limit and the ordinary is past it, an extra from this route can never fall after the ordinary one (no double election). (d) 3:11's first-sitting proxy (`:2569-2571`): the chamber of record's `Convened`, or the game's own latest polling day where the game held one - the earlier bound, so the ban ends about fifteen days early, stated. THE RISK: a caretaker whose extra election forms nothing is a caretaker for good. `ResolveElectionVerdict` keeps the previous record when no government forms (`GameController.cs:6620-6621`), and `AdvanceConfidenceDay` returns on `g.Caretaker` (`:2592`), so no round ever runs again; no motion is taken up against it (`:2529`) and it orders no election (`:2566`) - it governs to the next ordinary election. 6:5 says the procedure is broken off and "återupptas först sedan val till riksdagen har hållits" - it RESUMES after the election; here it never does. Say it, or re-run the round when an election leaves a caretaker record.

5. DEFECT - the vote tells the player that V and MP DECLARED a line they never declared. `RedLinedFrom` (`GovernmentFormation.cs:297-310`) reads `DeclaredRedLines.For`, which returns the DERIVED lines with the declared ones appended (`DeclaredRedLines.cs:51`), and `Vote` labels every red-lined party "its declared line against the cabinet" (`ConfidenceProcedure.cs:68`). On the diagnostic's chamber (cabinet M+KD+L, the 2022 set - finding 7): S is FOR by the 2022 CANDIDACY line S→M (`DeclaredRedLines.cs:140-146`, `167-184` - a ruled refusal, which `IsCandidacy` exists to tell apart); V and MP are FOR by DERIVED galtan lines to KD alone - KD 7.79 − V 2.42 = 5.37 and KD 7.79 − MP 1.95 = 5.84, both past `SocialGap` 5.0 (`CoalitionFormation.cs:637`, positions `PartySystem.cs:367/371/375`). ANSWER TO "which line is MP's?": NOT one against M - M 6.47 − MP 1.95 = 4.52 on galtan and 7.58 − 2.74 = 4.84 on lrgen, so M–MP is a DERIVED cabinet-only line (`BlocksSupport` false) and does not red-line; MP's yes is KD's derived line. The reason reaches the player: the motion's division goes to the signing ceremony (finding 10), whose STANCES plate draws each side's `Reason` (`SigningScreen.cs:414-416`). Fix: carry the line's own kind in the reason - "a derived line against KD (the social axis)", "its candidate's refusal of M's" - `RedLinedFrom` can return the first refusing line per party.

6. RISK - the rule does not reproduce 2021, and the done-when does not need it to. By reading (positions are CHES 2024, the model's only set, so this is the RULE applied to 2021's chamber, not a backtest): cabinet S+MP, SD moving, the 2018 chamber. `For(Sweden2018)` falls through to the 2026 branch (`DeclaredRedLines.cs:57` is the only other branch: C–SD and C→V, and `Candidacies` is empty for 2018, `:139`); `RedLinedMask` over {S, MP}: KD FOR by KD–MP (22); M abstains (no support-blocking line to S or MP - M–MP is lrgen-only); V abstains (no line to S or MP); C and L abstain; SD 62 - 84 FOR. Even with 2022's candidacy lines (M→S) it is 154 (SD 62 + M 70 + KD 22). The record: 181 - M 70, SD 62, V 27, KD 22 [RD-VOT-0621]; the carried motion turns on V's 27, a policy vote no red line expresses, so the one yes the Riksdag has ever given [RD-MF-5] is outside the rule. On TODAY's chamber the done-when stands without the mislabelled votes: S 107 + SD 73 = 180 ≥ 175 (V's 24 and MP's 18 are surplus). Latent the other way: on 2026's set V's in-or-against rule (`InOrAgainstFor`, `:203-216`) makes V vote FOR no confidence in every cabinet it is not in, including Andersson's - which [C-I9] reports Dadgostar denying on TV4 ("V inte kommer att fälla Magdalena Andersson"), the party then saying its line had not changed (`coalition_declarations_2026.md:561`). The investiture rule is not the confidence rule; the premise that one implies the other (`CoalitionFormation.cs:529-531`'s comment) wants a sentence and a source, or V's rule kept out of `RedLinedMask`.

7. DEFECT - the vote and the round read the EPOCH's declarations, never the sitting chamber's. `RedLinedFrom` and `ViewOfSitting` pass `ElectionVintage.Seated` (`GovernmentFormation.cs:307-308`, `:322`), and `Seated` resolves by `SimulationManager.EpochDate` (`WorldClock.cs:190-191`) - in a Sweden game (epoch 2026-01-18, `StartDate` → the run-up before 13 September, `:112`; the 2022 chamber sits to 2026-09-28, `:158`) that is `Sweden2022`, for the whole run. After the game's own election, `ResolveElectionVerdict` forms on `VintageOfElection` → `Sweden2026` (`GameController.cs:6617`). Scenario: 2027, the government the game's election formed on 2026's lines is (say) a cabinet with M and SD - admissible in 2026, where the M/KD/L–SD lines are lifted; the player moves no confidence; the round re-forms on 2022's set, where M–SD, KD–SD and L–SD cabinet-block (`DeclaredRedLines.cs:68-72`), so no cabinet with M and SD can be re-seated, V's in-or-against is absent and the candidacies are 2022's - the round and the verdict run two different party systems of refusals over one chamber. The doc comment promises "today's seats and declarations" (`:312-313`). ANSWER TO (3): at Sweden's start `Seated` → `Sweden2022`, the chamber's own election's - right for the start's chamber, though not the dated timeline §621 built (by April 2026 L and M had lifted their SD lines, `:269-270`; `ForDate` exists, no runtime reads it). Fix: resolve the sitting chamber's vintage - `VintageOfElection(country.Id, the latest held election's date)` where the game has held one, else `Seated` - in both `RedLinedFrom` and `ViewOfSitting` (and pass the matching `InOrAgainstFor`).

8. RISK - the round's "S led by S with MP" rests on the mover tolerating S. With the player's one-way line SD→M (`SimulationManager.cs:2610`) SD cannot support or sit with M; on a cabinet of S alone the model has no support-blocking S–SD line (S–SD is lrgen 4.79, cabinet-only; galtan 4.26), and SD holds out for nothing - every SD pair cabinet is inadmissible on 2022's set plus the derived lines, so SD's best own is SD alone, which scores under S alone. So SD abstains on S and S passes under 175 against; with SD's 73 against, M's 68 (M→S candidacy) and any hold-out among KD, L, V or C would reach it. 2021's precedent is the other way: SD voted Nej, 62, on Löfven's re-proposal [RD-VOT-0707]. The comment's own precedent - "a party that brought the prime minister down tolerated his re-election" - is V's (27 Avstår, [RD-VOT-0707]), not the mover's. Accept as what the declared and derived lines say, but state that the round's product depends on it. The player-only line is right, and trivially so: AI parties move no motions (`:2503-2504`), so every mover is the player.

9. OK - the new record and the player's role after it. `FromView(country, view, CurrentDate, basis: …, world: _world)` (`:2615`): the PM party by `CandidaciesAt(formedOn)` - S's 2022 candidacy stands 2022-08-04 → 2026-05-01 and its 2026 one after (`DeclaredRedLines.cs:275-278`), so S leads a cabinet holding S on any date of the run; portfolios allocated (`GovernmentRecord.cs:194`), agreements formed with the world (`:195`; MP's). `Kind`/`Executive` stay at their defaults (Cabinet, null) - right for Sweden, as in `ResolveElectionVerdict`. THE ROLE: SD is `Opposition` (`RoleOf`, `:127-134`), `PlayerGoverns` false (`:2414`). `ResetArrivalBudgetWindow` (`:2617`) is read by the NEXT day's `TryOpenBudgetProcess`, which runs before `AdvanceConfidenceDay` in the tick (`:308-309`): the AI government tables its arrival budget as a bill (`:2203-2210`, §632's path) - unless the fallen government's bill is still pending (`:2197`), in which case that bill resolves first, under the NEW record's roles (`breakBy` reads `country.Government`, `:1242`), and the arrival follows. Right. NOTE: nothing spaces motions - SD can move against S+MP the next day (160 by reading: SD 73, M 68 by M→S, KD 19 by KD–MP; not carried), each attempt a division.

10. DEFECT - the motion rides the signing ceremony as a fiscal bill, and can become election night's standing budget. `MoveNoConfidence` appends a division with `Axis = BillAxis.Fiscal` (`:2535`). `QueueNewlyResolvedDivisions` (`GameController.cs:2268-2288`) enqueues every new division for the ceremony and attaches the turn's preview arrows (`_effectsAtTurnEnd`, `:6335`) to any division with none (`:2276-2282`); the ceremony then draws the motion under "PARLIAMENT · SWEDEN", "alignment +1.00 · CARRIED", "on the fiscal axis" (`SigningScreen.cs:369-371`), the estimate plate titled AS ENACTED with the turn's economic arrows (`:424-433`), and fires `BillPasses`. Worse: election night's `standingBudget` is "the newest passed division not on the trade axis carrying effects" (`GameController.cs:6717-6722`) - a carried motion that picked up arrows is that division, and the night's plate cites "DIVISION No. N · … · Motion of no confidence in the prime minister (M): carried …" as the budget the new chamber inherits. Scenario: a preview is held on the turn SD moves; the motion carries; at the next election the budget plate names the motion. Fix: give the motion its own kind (a `BillAxis`/`DivisionKind` value, or a flag on the record) that the queue skips or the ceremony draws as a motion (no arrows, no "fiscal axis", no seal), and exclude it from the standing-budget scan. The ceremony is a Canvas surface: by the canvas film rule a real film is owed for whatever it draws.

11. RISK - surfaces that still name the INSTALLED cabinet. `GovernmentFormation.Cabinet` (`:115-128`) goes through `TryFormChamber`, which returns the installed Tidö record while the game has held no election (`SeatedGovernment.cs:90-95`). The political compass legend - "SWEDEN'S CABINET · SEAT-WEIGHTED MEAN" and its mean (`PoliticalCompassRenderer.cs:100-103`, `CompassPositions.cs:54`) - therefore draws M+KD+L after the round has seated S+MP, and KD after it has left (`LeaveGovernment`). s635 moved `TryGovernment` onto the stored record for the votes; this is the first change that REPLACES the government mid-term before an election, so the disagreement is new in kind. Fix: `Cabinet()` reads `country.Government` when one is stored, as `TryGovernment` does (`:144-156`).

12. DEFECT - an extra election inside the ordinary run-up leaves the campaign record on the wrong polling day. Sweden's start IS the run-up (epoch 2026-01-18 = the 13 September calendar's `PreCampaignStart`), and every later cycle's run-up opens 34 weeks before polling day. An extra election whose day falls inside that window (the 6:5 route: e.g. a motion on 1 March, discharge 8 March, no government - an extra on Sunday 7 June; the ordinary is past three months) switches `CurrentCampaignCalendar` to the extra's (`:2646-2650`). Then: the ordinary run-up freezes (the extra's window is closed, `:2667`); at the extra campaign's first day `PlayerPreCampaign`'s date is not the extra's (`:2684`), so a FRESH record replaces `CampaignRecord` (`:2693`) and the run-up's queued decisions go with it; after the extra's polling day the calendar returns to 13 September, and `AdvancePreCampaign` keeps the stale `PlayerPreCampaign` (its date matches, `:2734`) and steps its frozen days on the EXTRA's record's queue, writing `PreCampaignDaysStepped` into it (`:2756-2760`); on 19 July `brought` takes `record = CampaignRecord` (`:2684-2693`) - the extra's, whose `ElectionDate` is 7 June; on 13 September `RunNationalElection`'s `CampaignRecord.ElectionDate == CurrentDate` (`GameController.cs:6805`) is false and the ordinary election counts the PREDICTION, not the player's campaign (and the save-as-record's replay is of the wrong record). Reachability by reading: on the seeded chamber no carried motion reaches the 6:5 route (SD's motion forms S+MP; the next motions reach 160), so it waits on seat drift under `UpdateSeats` or a later chamber - but it is the code's own path. Fix: on a calendar switch, drop `PlayerPreCampaign`/`CampaignRecord` whose `ElectionDate` is not the new calendar's (or keep the ordinary's aside and restore it), and never adopt a record whose `ElectionDate` differs from the calendar's. ANSWER TO (4b): the extra's campaign always begins - both routes schedule three months out, so from decision to polling day is at least about 83 days (three months less six), more than eight weeks: `weeks` is always 8 (`:2648`) and `CampaignStart` is always in the future when the election is ordered; the `Min`/`Max` never binds on a reachable path. With `preCampaignWeeks` 0, `PreCampaignStart == CampaignStart` and `AdvancePreCampaign` is never entered for the extra.

13. OK - election night on an extra date. `AdvanceDay` sets `PollingDayToday` from `TryPlayerPollingDay` (`:477`), which returns the extra only where it falls before the ordinary (`:2637`); the date is cleared the day AFTER (`:2589`), so the day's `CheckElection` runs (`GameController.cs:802-805`). `ResolveElectionVerdict` reads `VintageOfElection(extra)` - the latest election of record before it (`WorldClock.cs:361-373`; `Sweden2022` before 13 September 2026), the declarations of the chamber elected before, which is reasonable and stated nowhere (one line). THE REFERENCE: `TryReference` matches an election of record's exact day only (`WorldClock.cs:394`), and an extra election always falls before the ordinary one, so it never lands on 13 September 2026 - election night claims no "AS IT HAPPENED" for an election that never happened (verified). The first-election comparison reads the seated 2022 table (`GameController.cs:6735-6741`) - right, that is the chamber the extra replaces; the ordinary election after an extra compares against the extra's record (`:6727-6731`). The four-year cycle carries on beside it (`TryNextPollingDay` is anchored on 2026, [RD-EV-3]). Nothing reads the campaign record's `ElectionDate` wrongly on the extra's own day (the fresh record's date is the extra's) - the defect is the ordinary's (finding 12).

14. NOTE - `OrderExtraElection` is unreachable in play. The only game writer of `NoConfidenceOn` is `MoveNoConfidence` (`:2538`; grep), which requires the player's party in OPPOSITION (`:2524-2529`), while `OrderExtraElection` requires `PlayerGoverns` (`:2565`), and AI parties move no motions - so the player's government never faces a declaration, and the desk's "Order an extra election" sentence (`GameController.cs:11100-11106`) never shows. The diagnostic reaches the path only by writing `NoConfidenceOn` by hand (`ConfidenceDiagnostic.cs:77`). Also a scope choice to state: 3:11 lets a government order an extra election at any time between ordinary elections (outside its limits); the model offers it only in 6:7's week.

15. RISK, accept with a cache owed - the desk. (a) COST: for an opposition player with the Parliament tab open, `DrawConfidence` calls `ConfidenceProcedure.Vote` on every IMGUI event (Layout and Repaint at least, each frame; `:11111`): `PartySystems.For`, a `HashSet`, `DeclaredRedLines.For` (`WorldClock.Resolve` → `SeatedVintage` → `Chambers(id)`, which allocates its array; `DerivedRedLines.From` over 28 pairs with one `string.Format` per line - nine lines on Sweden's positions; the 2022 declared lines and two candidacy lines of several hundred characters by concatenation), `InOrAgainstFor` (a second `Resolve`), and a `MotionVote` with eight `DivisionSide`s. Tens of microseconds and a few kilobytes of garbage per call - small, but steady GC churn at two calls a frame. A cache keyed on (`CurrentDate`, the `Government` reference, the seat total) removes it; accepting is defensible for a structural block. (b) The button is ENABLED below the tenth: V, C and MP (24, 24, 18 < 35) see the projected arithmetic and "Move no confidence", and the refusal goes to `Debug.Log` only (`:11115`) - draw it disabled with the reason ("A MOTION NEEDS A TENTH OF THE MEMBERS - 35 - YOUR PARTY HOLDS 24"). (c) "Move no confidence" and "Leave the government" are one-click and irreversible with no confirmation, on the destructive face (s635's note, again).

16. OK, one NOTE - the save. The four record fields are public and serialise (`DateTime` under `RoundtripKind`, `SaveGameService.cs:79`); the pending extra election's two dates ride `SimulationPendingState` (`SaveGame.cs:88-90`) through the capture (`:2283`) and restore (`:3069`) - both lines otherwise right (finding 1 is the labour bill beside them). `CurrentSaveVersion = 33` and the loader refuses 32. The check round-trips `Caretaker` and `ExtraElectionDate` through a real `Serialize`/`Deserialize` (`ConfidenceDiagnostic.cs:88-90`); NOTE: not `NoConfidenceOn`, `NoConfidenceMover`, `CaretakerSince` or `ExtraElectionOrderedOn` - the last feeds the extra campaign's weeks (`:2648`), so a silent drop would change the campaign's length on load; one more clause each. The play save already reads v33 (finding 1).

17. OK - `MetaTextCheck`, culture, throws. The new UI literals ("AN EXTRA ELECTION ON ", "A CARETAKER GOVERNMENT SINCE ", "NO CONFIDENCE DECLARED ", "THE SPEAKER DISCHARGES THE GOVERNMENT ON ", the three sentences, the three verbs) match none of the banned patterns (`MetaTextCheck.cs:63-93`); "PS-3i (§636)" is in comments only. Dates are `ToString("d MMM yyyy", CultureInfo.InvariantCulture).ToUpperInvariant()` (`:11095-11100`); `Title()` and the projected sentence are `string.Format(InvariantCulture, …)`. `ConfidenceProcedure.cs` is outside the scanned roots but its `Title()` and `Reason` strings reach the ceremony (finding 10) - clean by reading. The `Breaks` lines carry "RF 6 kap.", "budget_procedure.md [BA-3]" and "PS-3i §636", but `Breaks` has no reader outside the record (grep) - log-only. THROWS: `IndexOf` returns -1 for a null key (`Abbrev == null` is false) and the line is added only for `mover >= 0 && pm >= 0` (`:2609`); a null `NoConfidenceMover` adds nothing; `Government` null is guarded in `MoveNoConfidence` (`:2520`), `LeaveGovernment` (`:2550`), `OrderExtraElection` (`:2565`), `AdvanceConfidenceDay` (`:2592`) and `DrawConfidence` (`:11092`), and `PlayerGoverns`' throw on a null record is reached only after those guards; a null player party reads `RoleOf` → `None`, falls through the switch and is refused at the tenth (`mover ?? string.Empty`, `:83`). The lost-budget break's "the constitution requires no resignation" is a reading by absence (no quoted paragraph says it) - it says "a premise", fine.

18. OK - player path only; the dumps untouched. Every trigger is a player verb (`MoveNoConfidence`, `LeaveGovernment`, `OrderExtraElection` all refuse a country that is not the player's). `AdvanceCountryDayTick`'s callers are `GameController.cs:789` (the player's country), `UiScreenshotDriver` (its country) and Editor diagnostics; `GovernmentBudgetBillDiagnostic.cs:124` ticks every country, where `AdvanceConfidenceDay` returns at once (`NoConfidenceOn` is MinValue on every record but the player's) and clears the global `_extraElectionDate` only past its day. No dump calls the tick. The lost-budget break sits on the frame decision, which only the player's country reaches (s634). `TryFormSeats`' `extraLines` defaults to null (`GovernmentFormation.cs:350`), and `DeclaredRedLines.For` returns a fresh list each call, so `AddRange` mutates nothing shared. Trajectories byte-identical by construction.

19. NOTE - `LeaveGovernment` (`:2547-2558`). The record keeps `Outcome = "of record"` and its `Basis` for a cabinet the record never held, and its agreements unchanged; a junior partner may leave a CARETAKER (6:11's second sentence is about a minister discharged on request, not a party leaving) - unreachable today, since only a player-moved motion makes a caretaker and the mover is in opposition.

20. NOTE - what the diagnostic does not reach, and the housekeeping. Not exercised: the 6:5 route (no government forms; an ordinary election within three months), an extra election actually HELD (`CheckElection` on its date, the vintage, the reference, the campaign - finding 12 lives in this seam), a motion against the government the round formed, and the ordinary-election bar (finding 3, absent). `SaveLoadRoundTripDiagnostic` must be run (finding 1). `confidence_rules.md` and `raw/confidence/` are untracked and unstaged - the commit must add them; `Tools/review_ledger.tsv` wants this review's rows for `SimulationManager.cs`, `GameController.cs`, `GovernmentRecord.cs`, `GovernmentFormation.cs`, `CoalitionFormation.cs`, `SaveGame.cs` and `SaveGameService.cs` before the cheap bar.

VERDICT: NOT READY - ONE DEFECT THAT BREAKS EVERY SAVE, FOUR IN THE FEATURE. The statute's arithmetic is right (2), election night on an extra date is honest (13), the round's new record and the player's role after it are right (9), and the change is player-path only (18); the done-when is met and holds without the mislabelled votes (6). Before the commit: (1) split the four fused lines - the pending labour bill's capture and restore are commented out, and `SaveLoadRoundTripDiagnostic` should be red; (7) read the SITTING chamber's declarations in `RedLinedFrom` and `ViewOfSitting`, not the epoch's; (10) keep the motion off the signing ceremony's bill grammar and out of election night's standing-budget scan; (12) never let the campaign record carry another polling day across an extra election; (5) name the line a party votes by - derived, candidacy or declared - not "declared" for all. Then as risks and notes: 13:4's ordinary-election bar and the convening (3), the permanent caretaker after a fruitless extra election (4), the round resting on SD tolerating S (8), the compass still naming the installed cabinet (11), the rule's distance from 2021 and V's in-or-against as a confidence vote (6), the unreachable extra-election order (14), the desk's per-event cost, the enabled button below the tenth and the missing confirmations (15), the four unasserted save fields (16), `LeaveGovernment`'s stale outcome (19), the untracked files and the ledger rows (20).

---

## Second reading

**Re-read (read-only, the working tree after the fixes; `git diff HEAD -- '*.cs'` scanned whole).** `Assets/Scripts/Simulation/SimulationManager.cs` (the tick 309, the lost-budget line 1245-1246, the capture 2283-2285, `MoveNoConfidence` 2516-2544 with the `Motion` flag at 2538, `AdvanceConfidenceDay` 2590-2602 with the campaign clear 2593-2599, `DischargeAndRound`, `TryPlayerPollingDay`, `CurrentCampaignCalendar`, `AdvanceCampaign` 2671-2735 - the `brought` test 2694, `CampaignRecord = record` 2712 - `AdvancePreCampaign` 2742-2771, `PreCampaignDecisionsFor` 2775-2780, `AdvanceDay`'s `AdvanceCampaign` call 475 and the polling-day flag 477, the restore 3078-3080, the cabinet-event roll 3490-3510), `Assets/Scripts/Persistence/SaveGame.cs` (86-92), `Assets/Scripts/Data/DivisionRecord.cs` (`Direction` 37, `Motion` 38-39), `Assets/Scripts/Elections/GovernmentFormation.cs` (`SittingVintage` 296-301, `RedLinedFrom` 304-318, `ViewOfSitting` 324-335, `TryFormSeats` 347-363), `Assets/Scripts/Elections/ConfidenceProcedure.cs` (whole; the reasons 68-69), `Assets/Scripts/Elections/CoalitionFormation.cs` and `Assets/Scripts/Elections/GovernmentRecord.cs` (their hunks, unchanged since the first reading), `Assets/Scripts/Persistence/SaveGameService.cs` (53), `Assets/Scripts/UI/GameController.cs` (`QueueNewlyResolvedDivisions` 2268-2289, the standing-budget scan 6719-6723, `DrawConfidence` 11089-11129 with `takenUp` at 11113), `Assets/Editor/ConfidenceDiagnostic.cs` (whole; the save assertion 88-90), `Assets/Editor/CheckSuite.cs` (the row), `Assets/Editor/TrajectorySentinelCheck.cs` (`BaselineLabel` "ps3i", 97), `Tools/film_scope.tsv` (the `dryps3i` row). Read for the paths around them: `Assets/Scripts/Simulation/CabinetSystem.cs` (`UnderPressure` 472-477), `Assets/Scripts/UI/GameController.Statistics.cs` (the division markers 207-216), `GameController.cs` (the calendar sheet's divisions 3550-3554, `_seenDivisionNumber`'s other writers 1075/2236/2298), `Assets/Scripts/Elections/DeclaredRedLines.cs` (`InOrAgainstFor` 203-216).

S1. OK - (1) the fused lines, and the whole diff scanned for the slip. The four are split: the capture's `PendingLaborBills` initializer (`:2285`) and the restore's `CopyInto(state.PendingLaborBills, …)` (`:3080`) are live code on their own lines; the lost-budget break and `ApplyBillResult` are two lines (`:1245-1246`); `SaveGame.cs:89-91` one field a line. THE SCAN: the diff removes six lines and every one is an intended replacement (the sentinel's `BaselineLabel`, `TryFormSeats`' signature, `CurrentSaveVersion`, `TryPlayerPollingDay`'s return, `CurrentCampaignCalendar`'s expression body, the standing-budget test); no added line carries a statement after a `//` (a pattern scan of every added line for code following a comment and a run of spaces finds only the save version's own chained comment); no touched file, and neither new file, holds a lone CR; the second pass's restorations stand as unchanged context (`DivisionRecord.Direction` 37, the `IsProvisional` summary above `TryFormSeats`, the motion's `Append` 2537, the queue's `if (record.Number > _seenDivisionNumber)` 2273). NOTE: `CheckSuite.cs`'s working copy mixes 787 CRLF lines with 9 LF lines (git normalises on commit; its diff is one added line). `SaveLoadRoundTripDiagnostic`'s result is not in front of me - by reading its labour bill (introduced as the PM's party, `:200`/`:399`) now crosses the save.

S2. OK, and one RISK now LIVE - (2) `SittingVintage`. The latest held election's date through `WorldClock.VintageOfElection` - the resolver `ResolveElectionVerdict` uses (`GameController.cs:6617`) - else `Seated`; `RedLinedFrom` passes it to both `DeclaredRedLines.For` and `InOrAgainstFor` (`:313-315`), `ViewOfSitting` to `TryFormSeats` (`:330`), which reads its lines and its in-or-against rule at that vintage. Finding 7 is closed: after the game's 13 September 2026 election the vote and the round read 2026's set, as the verdict did; an extra election held before it reads 2022's, as its verdict did. THE CONSEQUENCE (finding 6's latent case, not on the carried list): on 2026's set `InOrAgainstFor` puts V in `RedLinedMask` for EVERY cabinet it is not in, so from the game's first election on V votes FOR no confidence in, say, an S minority it let through - [C-I9] reports Dadgostar saying V "inte kommer att fälla Magdalena Andersson", the party then saying its line was unchanged (`coalition_declarations_2026.md:561`). The investiture rule read as a confidence vote is a premise: state it in the record (one sentence), or keep the in-or-against mask out of `RedLinedMask`. Either is fine; silence is not.

S3. OK, one NOTE - (3) the motion flag. Set on the motion's own division (`:2538`); the ceremony queue skips it and advances `_seenDivisionNumber` (`GameController.cs:2272`) - entries are in ascending number, so a bill appended before the motion is already queued and none after it is skipped; the standing-budget scan skips it (`:6722`); the flag is a public field on a record in the world graph, so it saves. The motion still lists where a division should (the recent divisions, the calendar sheet's "Carried"/"Rejected", `:3550-3554`; the statistics' passed-division markers, `GameController.Statistics.cs:207-216`) - right. Finding 10 closed. NOTE: `CabinetSystem.UnderPressure` (`:472-477`) reads "the latest division lost today" as pressure on the government; a FAILED no-confidence motion is the government's win, read as its loss. In the day loop the turn boundary runs before any click on that day (`GameController.cs:785-795`), so a motion dated the boundary day cannot be the latest division when `AdvanceTurn` asks - not reachable through the clock by reading; skip `Motion` there anyway for a turn advanced another way.

S4. DEFECT - (4) the campaign clear throws on exactly the path it was written for. THE ORDER: `AdvanceDay` steps the campaign (`:475`) BEFORE the controller's `AdvanceCountryDayTick` reaches `AdvanceConfidenceDay` (`:309`). THE STATE: the ordinary run-up's `PlayerPreCampaign` is dated to the ORDINARY polling day - no run-up is ever begun for the extra (its calendar has no run-up weeks, so `AdvancePreCampaign` is never entered for it) - so the clear's second test (`:2597`, "dated to the extra") is never true on this path. THE SEQUENCE: an extra election held inside the ordinary run-up (e.g. 7 June 2026 before 13 September); on the day after, `AdvanceCampaign` reads the ordinary calendar, `AdvancePreCampaign` keeps the stale run-up (its date matches, `:2744`) and steps its frozen days on the EXTRA's `CampaignRecord`; the tick then nulls `CampaignRecord` (`:2596`); the next day `AdvancePreCampaign` again keeps the run-up, its loop steps one day, `PreCampaignDecisionsFor(null, …)` is guarded (`:2778`) but `CampaignRecord.PreCampaignDaysStepped = PlayerPreCampaign.Day;` (`:2769`) dereferences null - a `NullReferenceException` out of `AdvanceDay` after the date has moved and before the polling-day flag and the boundary's return (`:475-480`), on every day until the campaign opens on 19 July: the day ticks, the budget windows and every turn boundary in those weeks are skipped while the date runs on. On 19 July `brought` needs `CampaignRecord != null` (`:2694`), so the run-up's outcome (chest, offices, staff) is discarded and a fresh record opens. ANSWER TO THE QUESTION: `AdvancePreCampaign` re-begins cleanly only when `PlayerPreCampaign` is null - and on this path the clear never nulls it. Fix (one condition covers the throw, the stale adoption and the day after): re-begin when the record does not belong to this calendar - `if (PlayerPreCampaign == null || PlayerPreCampaign.Calendar.ElectionDate != calendar.ElectionDate || CampaignRecord == null || CampaignRecord.ElectionDate != calendar.ElectionDate)` at `:2744`; or null `PlayerPreCampaign` in the clear whenever an extra election was held AND move the clear to the top of `AdvanceCampaign`, so the day after the extra reads a clean state. With the re-begin the ordinary run-up starts correctly: a new record dated to the ordinary polling day, the run-up stepped from its first day through today at once with an empty queue (the player's run-up decisions queued before the extra are gone - say so), and on 19 July `brought` finds the run-up and a record dated to it. Also read: an extra election inside the ordinary's CAMPAIGN window (6:5 with the ordinary just past three months away - a discharge on 10 June 2026 puts the extra on Sunday 6 September, a week before 13 September) does not throw - the campaign branch assigns `CampaignRecord = record` before stepping (`:2712`) - but the tick then nulls the ordinary campaign it has just begun from the extra's record, and the next day re-begins it fresh with no run-up outcome; two elections a week apart is the statute's letter (6:5 with [RD-EV-3]), worth a sentence. Reachability as before (a round forming none while a run-up is live - not from the seeded chamber by reading); the diagnostic's hand-set extra lands on 12 April 2026 inside the run-up but advances no further - a leg that walks past the extra's day to the campaign's opening and asserts no throw and `CampaignRecord.ElectionDate` equal to the ordinary polling day would have caught it.

S5. OK - (5) the reasons (`ConfidenceProcedure.cs:68-69`): "a red line against a cabinet party" and "abstains - no red line against the cabinet" - true for a derived, a declared and a candidacy line alike, and the plate no longer claims a declaration. Finding 5 closed (a reason naming the line's kind and the party it runs to would say more - NOTE).

S6. OK - (6) the desk. `takenUp` from `CanBeTakenUp` (`GameController.cs:11113`); below the tenth the sentence reads "A motion needs a tenth of the members, 35, to be taken up; your party holds 24." and the button is drawn disabled; above it, the projected arithmetic as before. The new literal is clean against `MetaTextCheck`'s patterns and formatted Invariant. The per-event cost is unchanged but for one `CanBeTakenUp` (a dictionary sum).

S7. OK, one NOTE - (7) the save assertion (`ConfidenceDiagnostic.cs:90`). `ExtraElectionOrderedOn == sim.CurrentDate` is a real, non-default value crossing the serializer - right. The `Motion` clause is VACUOUS: in the second leg every `MoveNoConfidence` is refused (M, KD twice, S, the caretaker), so no motion division exists and `Exists(e => e.Motion)` is false on both sides; the first leg's carried motion is never saved. Save in the first leg after the motion, or assert `Exists(e => e.Motion)` is true before comparing. `NoConfidenceOn`, `NoConfidenceMover` and `CaretakerSince` remain unasserted.

S8. THE CARRIED ITEMS - none must be fixed; each wants its sentence in the record. (a) 13:4's bar after an ordinary election until the first sitting: acceptable as stated; a proxy is cheap (polling day + 15 days, RF 3:10 - the derivation `WorldClock.Chambers` already writes for 2018). (b) The caretaker after a fruitless extra election: acceptable, but the sentence should say what 6:5 says - "återupptas först sedan val … har hållits", the procedure resumes after the election and the model does not resume it; the verdict's "you stay in office until one can" is the player's office, not the caretaker's standing. (c) 2021 not reproduced: as stated. (d) The desk's per-event cost: accept. (e) "Order an extra election" unreachable while AI parties move no motions: as stated. (f) The compass legend reading the installed record: acceptable as a filed follow-up - after the round it names a government that has fallen, which no earlier change could produce.

S9. OK - the rest. The sentinel's label moves to `ps3i` with the byte-identical claim; by reading (finding 18) no dump path reaches the change - not run here. The `dryps3i` film-scope row names the Parliament tab's block with S in opposition - the motion's arithmetic and the enabled button; the disabled state (a party under the tenth) is not in it.

SECOND VERDICT: NOT READY - ONE DEFECT LEFT, IN THE FIX. Findings 1, 5, 7 and 10 are fixed and verified by reading, the whole diff is clean of the fused-line slip, and the desk and the save assertion are as described. Before the commit: (S4) re-begin the run-up whenever the campaign record does not belong to the calendar (or null the stale run-up and clear before `AdvanceCampaign`) - as built, the day after an extra election held inside a run-up leads to a throw out of `AdvanceDay` on every day to the campaign's opening, skipping ticks and turn boundaries; add a diagnostic leg that walks past an extra election to the campaign. Then as notes: state V's in-or-against as a confidence vote, now live on 2026's set (S2); make the `Motion` save clause non-vacuous (S7); skip motions in `UnderPressure` (S3); the carried items' sentences (S8); `SaveLoadRoundTripDiagnostic`'s result to be read (S1).
