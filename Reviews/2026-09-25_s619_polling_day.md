# Review: PS-2's mechanism - CL-4, polling day on the calendar's date; the family `ps2cal` (2026-09-25, s619)

**What was reviewed.** The staged change (the patch `git diff HEAD -- Assets/Scripts Assets/Editor` plus the new file `Assets/Editor/PollingDayDiagnostic.cs` whole and `Assets/Scripts/Elections/WorldClock.cs` 250-380) to the money paths `Assets/Scripts/Simulation/SimulationManager.cs` (`PollingDayToday`, `TryPlayerPollingDay`, `CurrentCampaignCalendar` on the polling day, `NextElectionTurnAfter` removed), `Assets/Scripts/Elections/WorldClock.cs` (`TryNextPollingDay`, `SecondSundayOfSeptember`, `VintageOfElection`, `TryReference`, `RecordDate`, `PollingDayBasis`), `Assets/Scripts/Elections/NationalElection.cs` (`ElectionRecord.Date`, `Run` takes the date), `Assets/Scripts/Elections/GovernmentFormation.cs` (`ViewOf(country, vintage)`), `Assets/Scripts/Persistence/SaveGame.cs` and `SaveGameService.cs` (`PendingElectionDate`, format 26), with `GameController.cs` (the day loop's election call, the date-gated `CheckElection`, the verdict, the night's date, vintage and reference, the desk marker, `HoldElectionWithoutTheNight`), `ElectionNightScreen.cs` (`Reference`, `BuildReference`), `UiScreenshotDriver.cs` (the loops on `PollingDayToday`, no warm-up for the interrupts film, the warm-up holding the election), `PlayProtocolStaging.cs` (the save cut at the run-up to the real polling day) and the Editor beside them; the family `ps2cal` is byte-identical to `ps1wc` on both seeds (the sentinel relabelled on the same digests).

**The form.** Two independent read-only readers, run in parallel: one on CORRECTNESS AND HAZARDS (asked to refute the claims, the window edges and the day loop's order first), one on THE DATA against the sources on disk (the two statute pages, the record's polling days, the government of record after 13 September 2026). Neither ran Unity. Both reports are below VERBATIM, then what was done with each finding.

---

## Reader 1 - correctness and hazards

Adversarial review of the PS-2 / CL-4 patch (read-only; diff of Assets/Scripts + Assets/Editor, PollingDayDiagnostic.cs whole, WorldClock.cs 1-380, plus the callers). Findings, numbered; severity DEFECT / RISK / NOTE.

1. RISK — Assets/Editor/PollingDayDiagnostic.cs:89-105 leaves EnergyMarket's process turn state behind. The 365-day loop crosses one boundary (day 365) and `sim.AdvanceTurn(none)` calls `EnergyMarket.BeginTurn(_world)` (Assets/Scripts/Simulation/SimulationManager.cs:2776); nothing calls `EndTurn`/`ResetTurnState`, so `_waterValue` (Assets/Scripts/Simulation/EnergyMarket.cs:147) and `_swedenDeficitShare` (:140, :467) stay set for the rest of the bar. `ClearAt` reads both — `waterValue ?? _waterValue ?? WaterValueAtSeed()` at :361 and `(1.0 + ReservoirDeficitSlope * _swedenDeficitShare)` at :542. In CheckSuite.cs the diagnostic (line 180) runs BEFORE `EnergyLayerCheck` (the same array, ~line 260), whose `ClearAt(CountryId.Sweden, 1.0, 0.0)` (Assets/Editor/EnergyLayerCheck.cs:165-169) would then dispatch Sweden on the diagnostic's year-end water value, not the seed's. Today EnergyLayerCheck only prints the Sweden lines (it asserts the calibration gap for `c.Id != Sweden`), so the bar stays green, but the leak is exactly the one PlayProtocolCheck names and puts back (Assets/Editor/PlayProtocolCheck.cs:55 `EnergyMarket.ResetTurnState()`), and PollingDayDiagnostic does not. Failing scenario: any later check that asserts Sweden's dispatch or zone prices reads a fitted 2027 water value. Fix: `EnergyMarket.ResetTurnState()` in the `finally` beside `DestroyImmediate(go)`. (The `using` EpochScope at line 37 before the `try` is correct: it disposes after `CheckExit.Finish`, which only records the code; `ResetCalibration` at :74 mirrors PlayProtocolStaging.)

2. NOTE (dead member + three stale comments) — `SimulationManager.NextElectionTurnAfter` (Assets/Scripts/Simulation/SimulationManager.cs:2231) has no reader left in Assets (only comment mentions in PlayProtocolStaging.cs:18/48). It is public static, so DeadStateCheck (private declarations) will not catch it. The block header at SimulationManager.cs:2186-2193 still states "The game's election is the turn boundary `ElectionSystem.IsElectionTurn` names … the campaign runs the eight weeks before that boundary … resolved when election night moves off the boundary" as present tense, directly above the new PS-2 header that says the opposite; the comment at :2279-2280 ("Strictly AFTER the boundary day: the election is counted on the boundary day's own AdvanceTurn") is likewise stale. `ScenarioEvaluator.cs:10` and `GameController.cs:1908` both say CheckScenarioObjectives is "called from the same post-turn site `CheckElection` occupies" — CheckElection no longer occupies it (GameController.cs:6185-6190 removed the call).

3. RISK (film semantics, not play) — the main sweep's warm-up and the Fed-chair eve drive the manager past Sweden's 2026-09-13 with no election held. `AdvanceDays(controller, _countryId)` (UiScreenshotDriver.cs:445; MinWarmupDays = 365*3 at :2453) and `CaptureHeldState` (:2708-2718, AdvanceDay/AdvanceTurn only) raise and drop `PollingDayToday` without ever invoking CheckElection, so the 88n pin (`AdvanceToElectionTurn` :3573-3587 → :3539) now films the 2030-09-08 night: no `TryReference` (2030 is not an election of record), a 2030 campaign result, and a chamber play never voted in on 2026-09-13. The harness comment at :440-443 acknowledges exactly this but only for `-shotinterrupts`; 88n's night silently changed from "the first election, with history beside it" to "the second". The new `BuildReference` block (ElectionNightScreen.cs:871-886) therefore appears on no film except e7c. Also `MaxStateSearchDays = 365*4 = 1460` (:2750): the gap between consecutive second Sundays can be 1462 days (2030-09-09 → 2034-09-10, 2032 leap), so `AdvanceToElectionTurn` started on 2030-09-09 would return false. Not reachable in the current film order, but the bound is now one day short of the statute's worst case.

4. NOTE (verified, no defect) — the window edges. Campaign last day = ElectionDate−1: `todayIndex = TotalDays−1` steps to `Finished`, `PlayerCampaignResult` is set (SimulationManager.cs:2334-2338). Polling day: `TryNextPollingDay(on or after)` returns 2026-09-13 itself (WorldClock.cs:165-167, diagnostic line 49), so `AdvanceCampaign` builds the 2026 calendar, `CurrentDate >= ElectionDate` puts it outside the window, and the drop test `CurrentDate > …ElectionDate` (:2281) is false — `PlayerCampaign` survives, so `RunNationalElection`'s three-way guard (`Result != null && CampaignRecord.ElectionDate == CurrentDate && PlayerCampaign != null`, GameController.cs:6613-6616) holds and the campaign's shares are counted. `PollingDayToday` is computed after `AdvanceCampaign` (:476). Day after: calendar → 2030, `CurrentDate < PreCampaignStart`, drop fires; `PlayerCampaignResult` lingers but can never be counted (its record's date ≠ today) and is cleared when the 2030 run-up begins (:2360). The diagnostic asserts all of this (lines 96, 102-104). A load on the last campaign day re-steps `DaysStepped` and re-derives `Result` when finished (:2586-2587). Boundary coincidence: with Sweden's epoch 2026-01-18 the polling days fall on day 238, 1694 (%365 = 234) and 3157 (%365 = 237) — never a boundary, so the boundary-day ordering question (scenario verdict before election, `!_isGameOver` gate on the night's canvas entry at GameController.cs:1680) cannot arise for the only modelled calendar.

5. NOTE (verified) — the key question: `CheckElection()` sits inside the `while (_daySpeedTimer >= secondsPerDay)` body after `AdvanceTurn()` (GameController.cs:795-805) and before `AutosaveIfDue`; the re-check at :815-822 breaks on `_electionNight != null`, so no second `AdvanceDay` can clear the flag in the same frame before the election is held. When the night has been dismissed, Update's next iteration starts with `AdvanceDay`, which recomputes the flag before any CheckElection, so a day never runs the block twice. If `ShowElectionNight` yields no board (Available false, the five AI countries — which now never reach here anyway, `TryPlayerPollingDay` is false for them) the loop continues and the next AdvanceDay clears the flag. The harness's `InvokeNoArg(controller,"CheckElection")` (:3539, :4173) runs while the flag is still true. Fine.

6. NOTE (verified, stated) — a save on polling day cannot lose the election. The day loop is atomic within a frame, so no manual save can fall between `AdvanceDay` and `CheckElection`; the autosave (:809) runs after it; a save under the night carries the verdict and `ElectionHistory`, and RestoreFromSave lands the verdict at once (:1051, P2-0.2's existing statement). `PollingDayToday` is not saved and is not needed after a load (it is only true between a day's AdvanceDay and its CheckElection). The staged protocol save is cut at day 0. The only way to reach polling day with the flag lost is the harness's manager-only loops in item 3.

7. NOTE — `TryNextPollingDay` (WorldClock.cs:157-168): both loops terminate for every `DateTime` a run can produce (forward loop steps +4 years until the second Sunday ≥ date; the back loop is guarded by `year − 4 >= 1`); `.Date` strips a time on the argument, and `CurrentDate` is midnight. The only throw is `SecondSundayOfSeptember(year > 9999)` for dates in the last four years of the type's range — unreachable. `SecondSundayOfSeptember` verified independently for 2018/2022/2026/2030/2034 (9, 11, 13, 8, 10). `VintageOfElection` (:183-195): for Sweden every date from 2018-09-09 on resolves to a dated vintage (2018/2022/2026 all carry `ElectionDay`), so the `Seated` fallback — which would route `TryFormChamber` into `SeatedGovernment.TryInstalled` (GovernmentFormation.cs:272) — is unreachable for the one modelled calendar; it is reachable only for a country with no dated chamber, none of which has a polling day.

8. NOTE — `TryReference` (WorldClock.cs:211-245): `after` is `GovernmentOfRecord?` and every deref is behind `HasValue`; `Support` is null-checked; `Cabinet` is dereferenced only under `CabinetSourced`, and no record with `CabinetSourced == true` has a null `Cabinet` (Governments(): Draghi has Cabinet null with sourced false). For Sweden 2026-09-13 the line reads "17 SEP 2026 · ULF KRISTERSSON (M), CARETAKER · THE CHAMBER HAD NOT CHOSEN A HEAD OF GOVERNMENT BY THE RECORD'S DATE, 24 SEP 2026" — correct. But the `else` branch's sentence is Sweden-2026-specific while the method is generic: `CabinetSourced == false` also marks Poland's Morawiecki (the head WAS chosen; the cabinet's parties are the GAP) and Italy's Draghi. Unreachable today (no polling day for either), wrong the day their calendars are modelled.

9. NOTE — the verdict (GameController.cs:6420-6470): `latest.Seats` is only read after the `NotImplemented` early return (:6427), and Sweden's `Run` always builds `Seats` (NationalElection.cs:312-322). A null `PlayerPartyAbbrev` cannot reach `latest.Seats.TryGetValue(null)` (which would throw) because `GovernmentFormation.Form` returns `Formed.None("the player holds no party")` first (GovernmentFormation.cs:83) and the `!HasGovernment` branch returns at :6436. `ShortName` tolerates null anyway (PartySystem.cs:314). The "out of parliament" test correctly precedes the "out of office" sentence; the in-cabinet branch is unreachable with zero seats. `_pendingElectionDate` is never cleared by `ApplyElectionVerdict` — harmless (used only in the sentence).

10. NOTE — save compatibility is moot: `SaveGameService.cs:161` refuses any `SaveVersion != 26`, so no v25 record (Date = MinValue, `PendingElectionTurn`) is ever loaded; the "MinValue on a record from … a v25 save" comment in NationalElection.cs:103 describes a save the loader rejects. `PendingElectionDate` round-trips as yyyy-MM-dd invariant both ways (:974, :1045); the `ElectionRecord.Date` DateTime field is compared to `CurrentDate` only on the day it is made, never after a JSON round trip.

11. NOTE — remaining `ElectionSystem.IsElectionTurn` readers, none gating an election: GameController.cs:3663 (Fed-chair candidates on the turn cadence) and :3980-3982 (the term label), UiScreenshotDriver.cs:2708/2718 (the Fed eve), ElectionDayReachDiagnostic.cs:75-81 (asserts turn 4 is an election turn and runs `NationalElection.Run` directly — its premise "advance past an election turn, exactly as AdvanceTurn does" is now historical, still passes). The player-facing label "TERM ENDS WITH THE ELECTION CYCLE · TURN N" (:3982) now names a cycle that holds no election; the USA is the only country with a chair and has no polling day at all after this change (it previously got a NotImplemented record every 4th turn; now none, and the desk marks no polling day — consistent with §618's ruling 4, but the label's wording is a small false claim).

12. NOTE — PlayProtocolStaging.cs:71 prints "polling day" as `loaded.CurrentDate.AddDays(7*(26+8))`, which equals the real polling day only because the save is cut on `PreCampaignStart`; the cut itself (:48-50) and the post-restore guard (:107-111, plus the new equality against `CampaignCalendar.Sweden2026`) are right. The WorldClock header stamps PS-2 "2026-09-25" while `RecordDate` is 2026-09-24 — cosmetic, matches PS-1's own labelling.

Verdict: I could not refute the play path. The polling-day edges are right on all three days (last campaign day, polling day, the day after), `CheckElection` is correctly inside the day loop after `AdvanceTurn` with the night's takeover breaking further iterations, the flag cannot be double-consumed or lost across a save, `TryNextPollingDay` terminates and reproduces the record's four dates, `VintageOfElection` never falls to `Seated` for Sweden, and `TryReference` dereferences nothing null. The actionable items are (1) the diagnostic's EnergyMarket turn-state leak into the bar (add `ResetTurnState()` in its finally, as PlayProtocolCheck does), (3) the harness's manager-only warm-up now walking past the first polling day so the 88n night is 2030's and the new reference block is filmed only by e7c, and (2) the dead `NextElectionTurnAfter` plus three comments that still describe the boundary rule as current.

---

## Reader 2 - the data against the sources on disk

Read-only review of PS-2 / CL-4 (WorldClock.cs 250-380, election_calendar.md, raw/calendar/). Nothing was edited or run that changes state.

1. **Statute clauses verbatim on the raw pages; checksums clean.**
   - `sha256sum -c SHA256SUMS` in `G:/UNITY/Projects/PoliSim/ElectionsData/sweden/raw/calendar/`: both files OK (`5c199d0d…ca66e` kungorelse…sfs-1974-152.html, 571 814 bytes; `ccbdc7fe…1386` vallag…sfs-2005-837.html, 635 986 bytes).
   - Regeringsformen page (og:url `https://www.riksdagen.se/sv/dokument-och-lagar/dokument/svensk-forfattningssamling/kungorelse-1974152-om-beslutad-ny-regeringsform_sfs-1974-152/`, consolidated "t.o.m. SFS 2022:1600"), under chapter heading "3 kap. Riksdagen", sub-heading `<h4 name="Ordinarie val">`, anchor `K3P3`: `3 §   Ordinarie val till riksdagen hålls vart fjärde år. Lag \r\n(2010:1408).` — matches election_calendar.md:8 except that the source has a CRLF between "Lag" and "(2010:1408)" where the file prints a space (whitespace only; the file also collapses the page's double spaces). Chapter/section reference 3 kap. 3 § is correct.
   - Vallagen page (og:url `…/svensk-forfattningssamling/vallag-2005837_sfs-2005-837/`, consolidated "t.o.m. SFS 2026:989"), under `1 kap. Inledande bestämmelser`, sub-heading "När val skall hållas", anchor `K1P3`: `3 §   Ordinarie val till riksdagen och ordinarie val till  region- och kommunfullmäktige ska hållas samma dag. Valdag ska  vara den andra söndagen i september. Val till  Europaparlamentet ska hållas i juni vart femte år. …` — the two sentences quoted at election_calendar.md:9 appear verbatim (double spaces collapsed). 1 kap. 3 § is correct. Note the preceding `2 §   Val skall alltid hållas på en söndag.` is not cited; not needed for the rule.
   - No discrepancy in wording or reference beyond whitespace.

2. **Polling days of record and the 2030 arithmetic.**
   - `ElectionsData/sweden/records_by_date.md:46` ([VAL-18], Valmyndigheten): "… Valdag den 9 september 2018."; line 38: "chamber elected 2018-09-09".
   - `ElectionsData/sweden/returns_2022.md:28`: "## SWEDEN — Riksdag election, 2022-09-11"; records_by_date.md:85 (talman page): "Söndagen den 11 september var det val till riksdagen."
   - `ElectionsData/sweden/2026/returns_2026.md:6`: "Election day 2026-09-13; **result fixed (fastställt) 2026-09-19"; line 47: "## SWEDEN — Riksdag election, 2026-09-13".
   - Calendar (GNU date): 1 Sep 2018 = Saturday → 2nd Sunday 9 Sep; 1 Sep 2022 = Thursday → 11 Sep; 1 Sep 2026 = Tuesday → 13 Sep; **1 Sep 2030 = Sunday, so the second Sunday is 8 Sep 2030** — election_calendar.md:14 is correct, and `SecondSundayOfSeptember` (WorldClock.cs:295-300: `toSunday + 7` from 1 Sep) yields exactly these four dates. The rule reproduces the record.

3. **The government after 2026-09-13.** The record (source ids as given there):
   - `2026/government_2026.md:25`: "Thu 17 Sep | PM Ulf Kristersson (M) asks to be dismissed. The talman dismisses him and every other minister, and they stay on as a caretaker government (*övergångsregering*) | **on record** | [RD-N17a] [RD-N17b] [RG-ART] [RG-START]". Quotes at :227 [RD-N17b] "Ulf Kristersson (M) begärde den 17 september sitt entledigande som statsminister och talmannen har beslutat om detta." and :233 [RG-ART] "Statsministern har i dag begärt sitt entledigande, och därför har talmannen entledigat statsministern och övriga statsråd. … Regeringen är därmed en övergångsregering."
   - `records_by_date.md:79`: "| 2026-09-17 | — (open on the run date) | Ulf Kristersson (M), **caretaker** | M + KD + L (… [RD-AKT]) | … | 2026-09-18 sounding mandate to Magdalena Andersson (S), to report to the new Riksdag's talman; no report, no proposal, no vote on record on 2026-09-24 (§2.4)".
   - `records_by_date.md:182-184` (§2.4, fetched 2026-09-24 16:13–16:17 UTC): "**Has the new Riksdag convened? No — it convenes on Monday 2026-09-28.**"; :222-223 "the government of record is the caretaker Kristersson cabinet, and the formation stands where it stood on 2026-09-23". `government_2026.md:32`: the prime-minister vote "**not held** as of 23 Sep … **no date on record**"; :261 [RG-ART] "Riksdagen kan tidigast välja en ny statsminister efter riksmötets öppnande under vecka 40."
   - So: dismissed at his own request 2026-09-17 — yes; caretaker — yes; no prime minister chosen by 2026-09-24 — yes. WorldClock.cs:217 (`"Ulf Kristersson (M), caretaker"`, from 2026-09-17, `CabinetSourced=false`) therefore takes the `else` branch at :360-364, and the line "17 SEP 2026 · ULF KRISTERSSON (M), CARETAKER · THE CHAMBER HAD NOT CHOSEN A HEAD OF GOVERNMENT BY THE RECORD'S DATE, 24 SEP 2026" is true to the record. One nuance worth knowing: the new chamber had not even convened by that date (upprop 2026-09-28), so "had not chosen" is true a fortiori. Minor: WorldClock.cs:217 says cabinet not sourced, while records_by_date.md:79 does name the caretaker's parties M+KD+L from [RD-AKT] (post-election EU-nämnden notices) — a conservative choice, not an error.

4. **RecordDate 2026-09-24.** `COMPLETED.md:33959` heading: "## 617. PS-1, THE SOURCING HALF - CHAMBERS, GOVERNMENTS AND LEADERS OF RECORD BY DATE FOR ALL SIX, JULY 2022 TO 2026-09-24, … (2026-09-24)"; the Sweden row says "the live state on the run date … riksdagen.se's notice of 2026-09-24 15.03". `WorldClock.RecordDate = D(2026, 9, 24)` (line 271) matches. Note §618 and the WorldClock comment (line 262) are dated 2026-09-25 — the code half's date, not the records'; consistent.

5. **Extra-election clauses present on the RF page.** Anchor `K3P11` under `<h4 name="Extra val">`: "11 §   Regeringen får besluta om extra val till riksdagen mellan  ordinarie val. Extra val ska hållas inom tre månader efter  beslutet. … Efter val till riksdagen får regeringen inte besluta om extra  val förrän tre månader har gått från den nyvalda riksdagens  första sammanträde. Regeringen får inte hell[er] …". Anchor `K6P5`: "5 §   Förkastar riksdagen talmannens förslag, ska förfarandet  enligt 4 § upprepas. Har riksdagen fyra gånger förkastat  talmannens förslag, ska förfarandet avbrytas och återupptas  först sedan val till riksdagen har hållits. Om inte ordinarie  val ändå ska hållas inom tre månader, ska extra val hållas  inom samma tid. Lag (2010:1408)." Both as the file claims (election_calendar.md:16-17).

6. **Other claims in election_calendar.md.**
   - :3 "Fetched 2026-09-24 from riksdagen.se, the Riksdag's own consolidated statute texts (Svensk författningssamling)" — supported (og:url paths under `/svensk-forfattningssamling/`; file mtimes 2026-09-24 20:56).
   - :14 "Asserted by `PollingDayDiagnostic` in the cheap bar" — `Assets/Editor/PollingDayDiagnostic.cs` exists and is referenced from `Assets/Editor/CheckSuite.cs`; not executed here.
   - :17 "the political-system spec's §5.4" — `docs/specs/POLITICAL_SYSTEM_SPEC.md:117` "### 5.4 Confidence and collapse" covers "the extra election after repeated failed nominations" and "an extra election if the country's rules call for one"; :193 names stage 3. Supported.
   - :18 "§618's ruling 4" — COMPLETED.md §618 ruling 4: "Every election on a country's calendar inside a run is simulated once that country's model exists; until then its chamber and head of state hold as of record". Supported.
   - :1 cites "`COMPLETED.md` §619" — **no "## 619." heading exists in COMPLETED.md yet** (only 617 and 618); a forward reference to a record not yet written.
   - Not stated in the file but true of the page: the vallagen text on disk is the consolidation "t.o.m. SFS 2026:989", the RF page "t.o.m. SFS 2022:1600" — the file could name the consolidation stamps for completeness.

**Verdict.** The change is true to its sources on disk. Both statute quotations appear verbatim (whitespace-normalised only) at the cited chapter and section — regeringsformen 3 kap. 3 § under "Ordinarie val" and vallagen 1 kap. 3 § under "När val skall hållas" — and the SHA256SUMS verify. The second-Sunday-of-September, every-fourth-year rule anchored on 2026-09-13 reproduces the record's 9 Sep 2018, 11 Sep 2022 and 13 Sep 2026 exactly, and 8 Sep 2030 is correct (1 Sep 2030 is a Sunday). The Reference's government line for Sweden 2026 matches the record: Kristersson dismissed at his own request on 2026-09-17 ([RD-N17a] [RD-N17b] [RG-ART]), a caretaker, and no prime minister chosen — indeed no new Riksdag convened — by 2026-09-24, the date §617 closed the records. The 3 kap. 11 § and 6 kap. 5 § extra-election clauses are on the page as claimed. The only defects found are cosmetic: the "§619" citation points at a COMPLETED.md section that does not yet exist, and the cabinet flagged "not sourced" at WorldClock.cs:217 could cite [RD-AKT] (M+KD+L) as records_by_date.md:79 does.

---

## What was done with each finding

**Reader 1.** 1 FIXED: `EnergyMarket.ResetTurnState()` in the diagnostic's `finally`, as PlayProtocolCheck does. 2 FIXED: `NextElectionTurnAfter` was already removed before the review's snapshot was taken; the four stale comments (SimulationManager's block header and its window note, ScenarioEvaluator, GameController's scenario hook) now say the boundary rule was the rule until §619. 3 FIXED, twice: the interrupts film opens on day 0 with no warm-up (the first film of §619 showed the 2030 night); and the warm-up HOLDS the election play would hold - `GameController.HoldElectionWithoutTheNight` (the count, the chamber, the verdict landed, no board), invoked by the warm-up on `PollingDayToday`, the warm-up stopping only if the run ended on the verdict. A first cut that stopped on the eve instead starved every history-dependent capture at turn 0 (the dry film `drycal`: 4 failed). The Fed-chair eve loop (`CaptureHeldState`) still drives the manager alone: it is reached only after the main captures, is skipped for a country without a chair before any capture is written, and the USA has no polling day - stated in the harness comment. `MaxStateSearchDays` raised to four years and seven days (1 462 between consecutive second Sundays across a leap year). 4-7, 9, 10 verified, no change; 10's comment corrected (a v25 save is refused by the loader). 8 FIXED: the reference's unsourced-cabinet sentence is generic now - a head that the record names as caretaker reads "a caretaker until the chamber chooses"; any other reads "its cabinet not named by the record". 11 FIXED: the chair's label reads *TERM ENDS ON THE CHAIR'S OWN CYCLE* - the cycle times no election. 12 cosmetic, left.

**Reader 2.** All verified. The consolidation stamps added to `election_calendar.md`; the "§619" citation is this record. The caretaker's parties from [RD-AKT]: left as the conservative `CabinetSourced = false` (a derived cabinet is `CabinetDerived`'s marking, §618's convention), noted for the record's next revision.

**Verdict after the fixes:** the play path stands as reviewed; the harness holds the election it used to walk past; the reference block is on the filmed 2026 night at both widths.
