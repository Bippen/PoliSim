# Review: PS-3d/PS-3e - France's what-if and the AI-government budget bill (2026-09-25, s632)

**What was reviewed.** The working-tree change (read-only, `git diff` against HEAD; no Unity run, no git write) to `Assets/Scripts/Elections/GovernmentRecord.cs` (`WhatIfGoverning` 88-95), `Assets/Scripts/Elections/StartPoints.cs` (`ModeLine` 117), `Assets/Scripts/Elections/WorldClock.cs` (`BudgetProcedure`/`BudgetProcedureOf` 216-226), `Assets/Scripts/Simulation/SimulationManager.cs` (`_pendingBudgetAlternativeByCountry` 988, `TableGovernmentBudget` 1166-1175, `TableShadowBudget` 1178-1192, `GetPendingBudgetAlternative` 1195, `ResolveGovernmentBudget` 1198-1249, `DescribeBudgetBill` 1252-1262, `AdvanceBudgetBillDay`'s first line 1316, the `TryOpenBudgetProcess` branch 2189-2199, `ministryByBill` at 2987-2996; and `CaptureSaveState` 2262-2279 / `RestoreSaveState` 2802-2851, which the change did NOT touch), `Assets/Scripts/Simulation/AiFinanceMinistry.cs` (`AsBudgetBill` 114-127, `Decide` 130-135, `Apply` 139-151), `Assets/Scripts/Data/BudgetBill.cs` (`GovernmentBill`/`TabledBy` 36-38), `Assets/Scripts/Simulation/StanceModel.cs` (`BillConcern.GovernmentAuthored` 46, the `governmentBill` line 255), `Assets/Scripts/Simulation/ParliamentSystem.cs` (`GetBudgetBillConcern` 906), `Assets/Scripts/Persistence/SaveGame.cs` (`PendingBudgetAlternatives` 87), `Assets/Scripts/Persistence/SaveGameService.cs` (`CurrentSaveVersion` 29 at 53), `Assets/Scripts/UI/GameController.cs` (the what-if seat 2219-2223, `DrawBudgetBillStatusAndIntroduce` 11037-11056, `DrawShadowBudgetAction` 11064-11077, `BuildBudgetBillStatusText` 11085-11113), `Assets/Editor/GovernmentBudgetBillDiagnostic.cs` (new), `Assets/Editor/CheckSuite.cs` (185), `Assets/Editor/PlayerRoleDiagnostic.cs` (121-126), `Assets/Editor/StartPointsDiagnostic.cs` (45), and the statute `ElectionsData/sweden/budget_procedure.md` (the steps table, [RO-9-11], [RO-9-12], [RO-11-10], [RO-11-10-1], [RO-11-11], [RO-11-12], [RO-11-18], [FIU1-22]). Read for the paths around the change: `SimulationManager.cs` (`AdvanceCountryDayTick` 296-309, `IntroduceBudgetBill` 1280-1293, the player's `AdvanceBudgetBillDay` tail 1317-1326, `ApplyBudgetBillSpendingAndSwf` 1336-1363, `IsFiscalYearStart` 2160, `PlayerGoverns` 2392-2403, `PlayerMayIntroduce` 2410-2421, the boundary loop 2977-3020, `_lastFiscalReports` written at 3110, `ApplyTaxRateChanges` 3951, `ApplySwfPolicyChanges` 4092-4130, `ResolveSpendingForTurn` 4342-4360, `IndexSpendingLines` 4425, `ApplySpendingLineChanges` 4677-4700, `DaysPerTurn` 216), `AiFinanceMinistry.cs` (`Observe` 73, `Withdraw` 154, `CutLinesUniformly` 201, `RaiseHouseholdRates` 237), `ParliamentSystem.cs` (`GetSeatWeightedAlignment` 309-322, `WouldBillPass` 332-343, `ApplyBillResult` 452-516, `RecordDivision` 1097-1111), `StanceModel.cs` (`StancesOver` 246-330), `Assets/Scripts/Data/DivisionRecord.cs` (`Append` 77-102, `DivisionSide` 107-120), `GameController.cs` (`SelectPlayerCountryAndParty`/`SeatPlayerParty`/`OpenWorldAt`/`SelectPlayerCountry` 2099-2224, the load path 904-912, `QueueNewlyResolvedDivisions` 2268-2288, `BudgetWindowHolds` 1396, `CtaWidth` 4609-4614, the division list 9859-9895, `BudgetProcessHeaderHeight` 10798, `_labelStyle` 2839), `GameController.Desk.cs` (`DeskCaption` 99), `Assets/Scripts/UI/SigningScreen.cs` (the seal and the stamp 212-252, the plate 328-372), `Assets/Editor/MetaTextCheck.cs` (the roots 61, the banned patterns 63-93, `Allow` 143-183, `IsAllowed` 307-315), `Assets/Editor/NoPolicyCentury.cs` (58-78), `Assets/Editor/BudgetWindowDiagnostic.cs` (97, 129), `Assets/Editor/SaveLoadRoundTripDiagnostic.cs` (143, 200), `Assets/Editor/PlayProtocolStaging.cs` (91), `Assets/Scripts/Testing/UiScreenshotDriver.cs` (1506-1529), `WorldClock.cs` (`GoverningModeOnly` 101, `StartDate` 110, `StartLine` 121, France's governments 266-278), `CLAUDE.md` (the number-locale rule, 86) and `Reviews/2026-09-25_s630_role_gate.md` for the shape.

---

## The reader

Adversarial review of §631 (France's what-if) and §632 (the AI government's budget as a bill), working tree at G:/UNITY/Projects/PoliSim, read-only. The what-if is small and sound (findings 12-13). The bill path is right in its skeleton - player path only (finding 1), the boundary's `Apply` skipped only for the player's own AI-governed country (finding 2), the frame decision's vote as the statute's practice (finding 5) - but four things stand between it and a commit: the alternative is NOT saved (the save field is declared, the version is bumped for it, and nothing writes or reads it - finding 3); the desk's new status sentence is unreachable (finding 8); the ceremony stamps an adopted alternative REJECTED (finding 6); and one new caption trips `MetaTextCheck` on the word SOURCED (finding 11). Beside those, the arrival window is now consumed by the AI government, so the player who wins office gets none (finding 4).

FINDINGS

1. OK - PLAYER PATH ONLY holds for every no-player instrument. `TableGovernmentBudget` has exactly one caller, the new branch of `TryOpenBudgetProcess` (`SimulationManager.cs:2199`), which returns at `:2191` unless `PlayerCountryId.HasValue && PlayerCountryId.Value == countryId`; `TryOpenBudgetProcess` itself is called only from `AdvanceCountryDayTick` (`:308`) and `BudgetWindowDiagnostic.cs:205`. `TrajectoryBaselineDump` and `DeadStateCheck` set no `PlayerCountryId` (grep of `Assets/Editor` for `PlayerCountryId`: only `BudgetWindowDiagnostic`, `SaveLoadRoundTripDiagnostic`, `NoPolicyCentury` name one), so `PlayerGoverns` is false for all six countries there, `aiGovernment` is true and `ministryByBill` (`:2990`) is false - the boundary's `Apply` runs exactly as before for every country. `NoPolicyCentury.cs:68` names a player with no party, so `PlayerGoverns` returns true at `:2402` for it, `aiGovernment` is false, and `TryOpenBudgetProcess` takes the OLD branch (the process-as-permission), unchanged. `BudgetWindowDiagnostic` likewise (no party seated). `SaveLoadRoundTripDiagnostic.cs:200` seats `Government?.PmParty`, so the player governs and the new branch is never reached. The new diagnostic's part 4 asserts the no-player case. `ps3c`'s family cannot move.

2. OK, with a stated difference - the ministry's rule reaches the player's AI-governed book ONLY through the bill now. `ministryByBill` (`:2990`) skips `Apply` at `:2994` and keeps `Observe` (`:2995`); `Withdraw` at `:3003` is moot for that country since nothing was written into the decision. Nothing the boundary's `Apply` did is lost that the bill does not do: the lines' percent changes land through the SAME `ApplySpendingLineChanges` (`:4685-4688`, via `ApplyBudgetBillSpendingAndSwf` `:1338`) and the two household rates through `ApplyBillResult`'s tax loop (`ParliamentSystem.cs:461-477`), which clamps to the line's range and charges `MacroSystem.TaxHikeApprovalSensitivity × hike` (`:514`) - the same sensitivity the boundary's `ApplyTaxRateChanges` (`:3951`) threads through. The DIFFERENCES, each true of the player's own budget bill already and so consistent, not defects: (a) THE BASE - the boundary applies the cut AFTER `IndexSpendingLines` (`ResolveSpendingForTurn` `:4346` then `:4355`), the bill applies it mid-turn to the line as it stands (un-indexed since the last boundary), and the next boundary indexes the cut amount; the percent's product is the same either way, but the percent itself was SIZED by `Decide` from `linesNow`/`NominalGdp` on the tabling day (`CutLinesUniformly` `:204`) against a different base than the boundary's, and `ApplySpendingLineChanges`' seed-band clamp (`ComposeLine`) is judged against the mid-turn amount. (b) THE TIMING - `Decide` reads `GetLastFiscalReport` (`:1168`), which is null until the first boundary writes it (`:3110`), and `DaysPerTurn` is 365 (`:216`): Sweden's day-one bill AND its 1 January 2027 bill (tick 92, before the first boundary on 1 October 2027) are both "the standing figures, unchanged" (`DescribeBudgetBill` `:1261`); the first bill with content is tabled 1 January 2028 and adopted about 22 January - whereas the boundary's `Apply` would first have acted on 1 October 2028 (the first boundary sees a null report too, `Apply` `:142`). So the bill path acts about nine months earlier than the boundary did, once a year on the fiscal-year date instead of once a year on the boundary. Neither path's cadence is the statute's (the frame decision is late November, [RB-3]); the record should say so. (c) The boundary's `ResolveSpendingForTurn` recorded the cut into `Effectiveness.Record` and the legacy category deltas for MacroSystem's approval formulas (`:4357`, `BuildEffectiveDecisionForDetailedSpending`); the bill path does neither - as the player's bill never has.

3. DEFECT - THE ALTERNATIVE IS NOT SAVED, and the restore does not clear it. `SaveGame.cs:87` declares `PendingBudgetAlternatives` and `SaveGameService.cs:53` bumps to format 29 for it, but `CaptureSaveState` (`SimulationManager.cs:2262-2279`) captures `PendingBudgetBills` at `:2269` and nothing of `_pendingBudgetAlternativeByCountry`; `RestoreSaveState` clears `_pendingBudgetBillByCountry` at `:2806` and copies it back at `:2845`, and neither clears nor restores the alternative (grep: the field's only uses are `:988, :1185, :1189, :1195, :1201, :1316`). Scenario A: S tables an alternative on day 5, saves on day 10, loads - the government's bill restores with 11 days left, `GetPendingBudgetAlternative` is null, the desk offers "Table an Alternative Budget" again, and if the player does not click it the government's bill is voted ALONE on day 21 (the `alternative == null` leg at `:1204`) - the tabled motion is silently gone. Scenario B: S tables an alternative, then loads an OLDER save in the same session - `RestoreSaveState` leaves `_pendingBudgetAlternativeByCountry[Sweden]` standing from the abandoned game; `TableShadowBudget` refuses with "YOUR ALTERNATIVE IS ALREADY TABLED" (`:1185`) and the next government bill's day sets the STALE draft against it. The bump to 29 also refuses every v28 save (`SaveGameService.cs:161`, `PlayProtocolStaging.cs:91` asserts the current format) - including the K-1 play save - for a field that is not written. Fix: capture at `:2269`'s side (`PendingBudgetAlternatives = new Dictionary<CountryId, BudgetBill>(_pendingBudgetAlternativeByCountry)`), `Clear()` at `:2806`'s side and `CopyInto(state.PendingBudgetAlternatives, ...)` at `:2845`'s; then the round-trip diagnostic or the new one should save mid-countdown with an alternative tabled and assert it comes back. (`BudgetBill.GovernmentBill`/`TabledBy` DO ride the save as public fields of the pending bill, so the government's bill itself round-trips; a v28 pending bill would load with `GovernmentBill = false`, harmless - the bump is justified only by the alternative, which is exactly the part missing.)

4. DEFECT - the arrival window is a COUNTRY's flag and the AI government spends it. The new branch marks `_incomingBudgetWindowUsed` at `:2196` on day one for the player's AI-governed country. When the player's party wins office - Sweden's 13 September 2026 election, `ResolveElectionVerdict` storing a new `Government` at `GameController.cs:6622` - `PlayerGoverns` turns true, `TryOpenBudgetProcess` falls through to the old path, and `:2219`'s `!_incomingBudgetWindowUsed.Contains(countryId)` is FALSE: the incoming government gets no arrival window and waits for 1 January, which is precisely what C-C2 (Playtest-1 finding 4) was built to prevent ("an incoming Swedish government presents an amending budget instead of governing a year on its predecessor's", `:2205-2208`). The mirror holds too: an AI government that takes office after the player's lays no arrival bill. The flag has to belong to the GOVERNMENT, not the country - reset it where the record changes (`GameController.cs:6622`, or a `SimulationManager` method the verdict calls), and the diagnostic should assert that a role flip re-opens the window. (Question (e)'s other half: `BudgetWindowHolds` (`GameController.cs:1396`) reads `GetPendingBudgetProcess`, and the new branch never adds to `_pendingBudgetProcessByCountry` - `TableGovernmentBudget` removes it at `:1172` - so the pause never holds for the opposition. Confirmed.)

5. OK, with three citation slips - the vote and the statute. The tally (`:1222-1225`): government parties by `RoleOf` always +1, others by the larger alignment or abstaining, ties to the government (`forG >= forA`, `:1229`). Against the record: the 2021 division was S/MP for the government's frames, M/SD/KD for theirs, C/L abstaining, 154-143-51 [FIU1-22] - the model's "abstentions count for neither" is the statute's "mer än hälften av de röstande" [RO-11-10] read on two proposals, right. The two stated deviations are TRUE statements: [RO-11-12] tables a tied main vote and goes to lot only after a second tie and a refused return to committee, so "a tie keeps the government's" is a deviation as stated; and [RO-9-12] gives fifteen days from the announcement, so "any day of the 21" is a deviation as stated. The slips: (i) the comment at `:1213` cites the fifteen days to [RO-9-11] - that is the GENERAL motion period; the fifteen-day rule is [RO-9-12]; (ii) `:1209` puts "som stöds av mer än hälften av de röstande" at [RO-11-10-1] - the quote is in 11 kap. 10 § itself [RO-11-10]; 11.10.1 is the voting mechanics; (iii) `:1212` cites the tie rule to "[RO-11-11] [RO-11-12]" - [RO-11-11] is the PREPARATORY vote's tie (lot), the main vote's is [RO-11-12] alone, and it is "tabled, then a return-to-committee question, then lot", not "tabled and a second tie by lot". `WorldClock.cs:217-221`'s summary is accurate. Also `country.Government` is null-guarded at `:1222` (the factory stores it; `PlayerGoverns` would have thrown first), and `alternative.TabledBy` is `country.PlayerPartyAbbrev` set at `:1186` - reachable only through `TableShadowBudget`, which is refused before any government bill exists for a party-less country (`PlayerGoverns` true → no government bill) - so nothing in `ResolveGovernmentBudget` throws (question (h)). One RISK inside: `TableShadowBudget` does not refuse a JUNIOR PARTNER or SUPPORT player (`PlayerMayIntroduce` is false for both, so the desk shows the button), and on the day their own party is forced to +1 for the government's frames (`:1223`) against the alternative they tabled - a partner tabling a motion against its own cabinet's budget and then voting it down. Refuse at `:1183`'s side for those two roles, with a reason.

6. DEFECT - the ceremony stamps an adopted alternative REJECTED. `:1231` appends the division with `passed = governmentAdopted`. When the alternative carries, the title says "S's alternative frames adopted, 180 to 169, over the government's" while `QueueNewlyResolvedDivisions` fires `AudioCue.BillFails` (`GameController.cs:2285`), the signing screen drops no seal and its button is the failed form (`SigningScreen.cs:223-252`), the citation reads "LOST" in `PoliSimTheme.Bad` (`:351`), the Parliament list stamps REJECTED (`GameController.cs:9883-9892`) and the calendar prints "(Rejected)" (`:3551`) - for a budget the chamber ADOPTED and the government now governs on [BA-3]. A frame decision always adopts one proposal; `passed` should be true in the frame branch, and the sides should be recorded toward the ADOPTED proposal (or the seat map reads AGAINST > FOR beside a seal). The `Sides` also carry `Alignment = g.Alignment − a` (`:1227`), a −2…+2 quantity where `DivisionSide.Alignment` is documented −1…+1 (`DivisionRecord.cs:115`), and no `ReasonShort`, so the plate groups on the long `Reason` (`SigningScreen.cs:369`) - "the government's frames (+0.32) over S's (−0.10)" per party, the length class P3-C1 cut because it pushed the arrows out of the 720 frame. The 80-character title on the Parliament list (`:9866`, `_labelStyle` wraps at 2839, `ExpandWidth`) wraps to a second line beside a fixed-height stamp slot; the signing screen's title is Canvas (`SigningScreen.cs:161`) - by the canvas film rule this surface owes a REAL film, 1280 and 2560, before the close.

7. OK - the empty bill's side effects (question (b)). `ApplyBillResult` on a bill with empty `TaxLines`/`BracketRates`/`WelfarePrograms`: `totalHike` stays 0, the penalty at `:514-515` is `Clamp(approval − 0)`, no change; `PensionAgeSet` false. `ApplyBudgetBillSpendingAndSwf` with empty dictionaries: `ApplySpendingLineChanges` skips every line (`:4684`, `:4692`, `:4696`). The fund: `AsBudgetBill` sets `SwfShouldExist = fund != null` and copies the six figures (`AiFinanceMinistry.cs:120-124`); with no fund `:1343` sets `SovereignWealthFund = null` on a null; with one, `:1348` does not recreate (not null), and `ApplySwfPolicyChanges` re-sets each figure to `Clamp(itself)` (`:4102-4129`) - `TotalAssets` is never touched. Only a standing figure OUTSIDE its clamp would move, and the fund's figures are written through that same clamp. `RecordEvent` files a zero-delta "The government's budget adopted" line each year - clutter on the approval ledger, not a defect. The Unsourced/no-alternative leg with an empty concern: `WouldBillPass` returns true at `ParliamentSystem.cs:334`, `RecordDivision` records it uncontested with NO sides (`:1099-1102`), and the ceremony's plate then says "no sides recorded for this division - it predates the map" (`SigningScreen.cs:343`) for a 2026 division - a false caption; RISK, pre-existing for any uncontested bill but newly reachable every year for the first two empty government bills (finding 2b).

8. DEFECT - the new status sentence is unreachable. `BuildBudgetBillStatusText` (`GameController.cs:11087-11091`) returns "An annual budget bill is before Parliament - resolves in N day(s)." whenever `GetPendingBudgetBill` is non-null - BEFORE the §632 branch at `:11103-11108`, whose condition requires that same bill to be non-null. The government's budget is therefore never named on the desk, the player never reads "Your draft below can be tabled as the alternative", and `DescribeBudgetBill`'s content never reaches a screen (it reaches only the log). Move the §632 test above `:11088`. (`BudgetProcessHeaderHeight` at `:10798` measures the same string through `CalcHeight`, so the ~200-character sentence would wrap correctly once reachable - `_labelStyle.wordWrap = true`, `:2839`.)

9. RISK - the shadow action's width, its `GUI.enabled`, and the enum on the desk. (a) "Table an Alternative Budget" (27 characters) is drawn at `CtaWidth()` (`:11072`), which is measured from "Pending (99d)"/"Implement"/"Remove" (`:4609-4614`) - the existing "Introduce Budget Bill" (21) already exceeds that measure and was accepted on film in §568; six more characters in the same width is unmeasured, and IMGUI clips a button's label rather than growing the button. A 1280 film of the Budget page with S in opposition and the government's bill standing settles it. (b) `DrawShadowBudgetAction` sets `GUI.enabled = true` (`:11066`) rather than the caller's `ambientEnabled`; today the caller's ambient is true on that page, so nothing is wrong, but the button escapes any future page-wide disable. (c) `DescribeBudgetBill` prints `{kv.Key}` - the `TaxType` enum's own name, "IncomeTax at 33.4 %" - where every desk row names a tax through the ledger's own name column; by §568 (`UiCulture.Install`, CLAUDE.md 86) the `:0.0` reads with a point on the UI thread, so no `InvariantCulture` is owed here (the Editor diagnostic's log is not a surface). Unreachable today by finding 8, but the string will be on the desk once that is fixed.

10. OK - the countdown and the day. `TableShadowBudget` copies `pending.DaysRemaining` (`:1188`) and nothing decrements the alternative's - the desk caption reads the government's (`:11070`), `AdvanceBudgetBillDay` removes both on the day (`:1316`), so the copy is dead data, harmless (it would only matter in a save, finding 3). `IntroduceBudgetBill`'s role gate (`:1282`) keeps the opposition from laying a bill of its own beside the government's; `TableGovernmentBudget`'s `ContainsKey` guard (`:1168`) and `:2191`'s keep two government bills from stacking, and a fiscal-year date that falls inside a 21-day countdown is skipped, not queued - fine on Sweden's calendar (1 January vs a day-one arrival).

11. DEFECT - `MetaTextCheck` will hit the new caption. `GameController.cs:11071` draws the literal "THIS COUNTRY'S BUDGET PROCEDURE IS NOT YET SOURCED · THE GOVERNMENT'S BILL IS VOTED ALONE"; the banned pattern `\bSOURCED\b` (`MetaTextCheck.cs:82`) matches it, the line is a `GUILayout.Label` (not a diagnostic line), and none of the four `GameController.cs` allowances (`:157-160`: "SOURCED · SCB 2014", "VOTING AGE · SOURCED · CONSTITUTION", "A DERIVATION FROM TWO SOURCED SERIES, NOT A FORECAST", "SOURCED — A PUBLISHED SERIES, WITH ITS YEAR") is a substring of it (`IsAllowed` `:311`). The cheap bar goes RED. Reword ("NOT YET MODELLED", as the locked cards say) or allow it by name with its reason. The same words in `SimulationManager.cs:1184` are outside the roots. The other UI literals are clean: "YOUR ALTERNATIVE IS TABLED · THE CHAMBER DECIDES IN {n} DAY(S)", "Table an Alternative Budget", the status sentence - no register id (`\bS\d+\b` needs digits after the S; "S's" is not one), no `§`, no backtick. The division titles and `DescribeBudgetBill` live in `SimulationManager.cs`, outside the roots, and carry no token by hand; the `§632` mentions are in comments and doc comments only. Question (g) answered: nothing else to register.

12. OK - France's what-if reaches every seat and survives a load. `SelectPlayerCountryAndParty` → `SeatPlayerParty` (the party) → `SelectPlayerCountry` (`:2099-2102`); `OpenWorldAt` rebuilds the world with `AtStart` at `:2175` and carries the party at `:2179`; then `:2220-2223` replaces the record with `WhatIfGoverning` when `GoverningModeOnly` and a party is seated and `Outcome != "what-if"`. The fallback seat (`:2203-2216`, the harness's one-argument path and `StartScenario` at `:2036`) sets the party BEFORE `:2220`, so it reaches the what-if too. A load never calls `SelectPlayerCountry` (`:904-912` set the fields directly; `OpenWorldAt`'s comment "A load never comes here"), and the save carries `Country.Government` since v27, so a France save restores its what-if record with `Outcome == "what-if"` intact - and were `SelectPlayerCountry` ever run on it, the guard keeps it. France holds no election (`GoverningModeOnly`), so no formation ever overwrites it. `PlayerGoverns` for the seated party: `RoleOf(party) == PrimeMinister` (`GovernmentRecord.cs:51`, `PmParty = party`) → true → `aiGovernment` false → the AI ministry keeps off France's book on the player path, and `IndexSpendingLines` reads it as the player's (`:4427`). The no-player world (`WorldFactory` → `AtStart`) keeps the provisional stand-in, as `PlayerRoleDiagnostic.cs:124` asserts. `WhatIfGoverning` calls `AtStart(country, start)` first, which THROWS on a date with no government of record - France's start 2024-07-18 sits inside "Gabriel Attal, caretaker" (`WorldClock.cs:274`), so it holds. `Basis` reaches no screen (grep of `.Basis` in `GameController.cs`: none; `Describe` prints the record to the log only) - the card's "WHAT-IF · YOUR PARTY GOVERNS" is the one player-facing statement.

13. RISK - the two card lines disagree. `StartPoints.ModeLine` now says "WHAT-IF · YOUR PARTY GOVERNS · OPENS 18 JUL 2024" (`StartPoints.cs:117`), but `WorldClock.StartLine` still says "OPENS 18 JUL 2024 · GOVERNING MODE · NO ELECTION" (`WorldClock.cs:121`) - whichever selector surface draws `StartLine` states the old mode beside the new. `StartPointsDiagnostic.cs:45`'s check DESCRIPTION still reads "GOVERNING · OPENS 18 JUL 2024" while asserting the what-if string - cosmetic. `PlayerRoleDiagnostic.cs:124-126` asserts the what-if both ways (UG prime minister, RN opposition, `Executive` carries Macron) - sufficient.

14. RISK - the harness's dense-state run now tables a government bill. `UiScreenshotDriver.cs:1508` re-seats M for the staging and `:1529` restores S; while M is seated the arrival window opens for M (the old path, the flag set), so after the restore the AI government's first bill comes on 1 January (the fiscal-year date), not on the next tick - any Sweden frame filmed on or after 1 January 2027 with S seated now carries a pending government bill and, 21 days on, a division and its ceremony. Expected diffs, to be named in the record; `Tools/film_scope.tsv` should carry the dry-film row for the Budget page in opposition (finding 9a).

15. OK - `GovernmentBudgetBillDiagnostic`. Part 1 proves the day-one tabling, one alternative, the refusal of a second, both cleared on the day, one division with sides; part 2 the fiscal-year re-tabling (`Month == 1 && Day <= 3`); part 4 the governing and no-player cases. It does NOT assert: the save round trip with an alternative (finding 3), the role flip's window (finding 4), the stamp's sense when the alternative carries (finding 6), or Germany's unsourced leg beyond the enum value (part 3 checks `BudgetProcedureOf` only - `TableShadowBudget`'s refusal for Germany is never exercised). The division printed is a good instrument for finding 6's fix.

VERDICT: NOT READY as it stands. The what-if (§631) is ready. The bill path (§632) is right in its gates - player path only, the boundary's `Apply` skipped for the one country the bill now serves, the frame decision voted as the record shows the 2021 division - but: the alternative does not ride the save and the restore leaves a stale one standing (3); the desk never names the government's budget because the generic sentence returns first (8); an adopted alternative is stamped REJECTED, sealed nothing and sounds the failure cue (6); and the "NOT YET SOURCED" caption trips `MetaTextCheck` (11). Close those four before the commit, and re-cut the arrival flag as the government's rather than the country's (4) - otherwise the player who wins office in September waits for January for the budget C-C2 promised them on arrival. Then the three citation ids (5), the junior-partner/support refusal (5), the shadow button's width on a 1280 film and the enum in `DescribeBudgetBill` (9), `StartLine`'s old words (13), and the record's own statement of the timing difference (2b) and the harness diffs (14).

---

## Second reading

**Re-read (read-only).** `Assets/Scripts/Simulation/SimulationManager.cs` (`TableShadowBudget`'s reworded refusal 1184, `ResetArrivalBudgetWindow` 1194-1195, `ResolveGovernmentBudget` 1201-1252 with the corrected citations at 1210-1215 and the sides at 1229-1237, `AdvanceBudgetBillDay`'s first line 1319, `CaptureSaveState` 2273, `RestoreSaveState` 2850, `CopyInto` 2899-2904), `Assets/Scripts/UI/GameController.cs` (`ResolveElectionVerdict` 6610-6628 with the reset at 6622, `DrawShadowBudgetAction`'s caption 11071, `BuildBudgetBillStatusText` 11085-11112, the four `Government =` sites 2175/2218/2222/6622), `Assets/Editor/GovernmentBudgetBillDiagnostic.cs` (48-62, the captured state at 56-57), `Assets/Editor/TrajectorySentinelCheck.cs` (`BaselineLabel` `ps3e`), `Assets/Editor/StartPointsDiagnostic.cs` (53), `Assets/Scripts/Elections/WorldClock.cs` (`StartLine` 121), `Assets/Editor/MetaTextCheck.cs` (the SOURCED pattern 82, unchanged), `Tools/film_scope.tsv` (the `dryps3e` row), and `ElectionsData/sweden/budget_procedure.md` for the three ids. Not re-run: no Unity; the status sentence's fit is the coordinator's dry film (03b/03c), admissible for an IMGUI label.

1. Finding 3 CLOSED. `CaptureSaveState` copies `_pendingBudgetAlternativeByCountry` one level deep at `:2273` beside the bills; `RestoreSaveState` clears the alternative FIRST and then `CopyInto`s it at `:2850` - `CopyInto` returns on a null source (`:2901`), so a v29 save with no alternative restores empty and no stale draft survives a load (both scenarios of the first reading's finding 3 are closed). The diagnostic (`:56-57`) asserts the CAPTURE carries both bills, not a round trip through the serializer and `RestoreSaveState`; the public fields serialize as `PendingBudgetBills`' do, so that holds, but a `RestoreSaveState(captured)` followed by `GetPendingBudgetAlternative != null` would be the whole proof in two lines. NOTE.

2. Finding 8 CLOSED. `BuildBudgetBillStatusText` tests `pendingBill.GovernmentBill` at `:11089` before the generic sentence at `:11093`; the dry film `dryps3e` reads the sentence on 03b/03c. One residue, unreachable today: the first branch does not ask the role, so a player who WON office while a government bill was still counting down (an AI bill tabled in the 21 days before polling day) would read "Your draft below can be tabled as the alternative" while `PlayerMayIntroduce` is true and the shadow action is not drawn; Sweden's dates (bills 18 January and 1 January, the election 13 September) cannot overlap, so it is a note for the AI elections. NOTE.

3. Finding 6 CLOSED. `:1237` appends the frame decision with `passed = true` either way, the title naming whose frames; the seal, the cue, the stamp and the calendar now all read an adopted budget. `DivisionSide.Alignment` at `:1232` is the party's own alignment toward the proposal it chose (`g.Alignment` for the government's, `a` for the alternative), 0 abstaining - inside −1…+1. The sides' `Side` stays toward the government's frames (+1/−1), so when the alternative carries the plate's seat map reads AGAINST > FOR beside CARRIED - true of the convention (the Riksdag's own ja/nej is toward the committee's proposal [BA-1]) but the caption at `SigningScreen.cs:339` prints FOR/UNDECIDED/AGAINST unqualified; a reader will want "toward the government's frames". RISK, cosmetic, for the Canvas film the close owes.

4. Finding 11 CLOSED. `GameController.cs:11071` and `SimulationManager.cs:1184` read "NOT YET MODELLED"; `\bSOURCED\b` no longer matches the UI literal, and "MODELLED" is in none of the banned patterns (`MetaTextCheck.cs:63-93`). The `Unsourced` title at `:1246` ("this country's procedure is not yet sourced") lives in `SimulationManager.cs`, outside the roots - allowed by the check, though it is the same word on the same class of surface (a division title on the Parliament list) and would read better as "modelled" for the one reason. NOTE.

5. Finding 4 CLOSED, the site is right, with one latent hazard. `ResetArrivalBudgetWindow` (`:1195`) removes the flag; `:6622` calls it exactly where the formation's record replaces the government, and NOT when no government formed (`:6624`, the old record stands - right: no new government, no new window). The four `Government =` sites: 2175/2218 are the epoch's seating (turn 0, no window spent yet), 2222 the France what-if (turn 0, same), 6622 the election - the only mid-game change; a load restores the flag from the save as the state it was saved in. Both directions then follow from `TryOpenBudgetProcess` unchanged: a player who LOST office → `PlayerGoverns` false → the new branch at `:2189-2199`, `arrival` true → the AI government tables on the next day tick (unless the player's own bill is still pending, `:2191`, in which case it tables the day that bill resolves); a player who WON → the old path, `:2219`'s `!Contains` true → the arrival window opens with its pause. THE LATENT HAZARD: were the AI's government bill pending at the flip, the winner's arrival window would open beside it, `IntroduceBudgetBill` refuses (`:1283`, one bill per country), the "Introduce" button is disabled (`pendingBill == null`), and `BudgetWindowHolds` (`GameController.cs:1396`) stops the day loop - in which `AdvanceBudgetBillDay` runs - so the government's bill never resolves and the hold never clears. Unreachable on Sweden's calendar (item 2) and the five AI countries hold no election, but it is a deadlock by construction: the old path should not open the process while `_pendingBudgetBillByCountry.ContainsKey(countryId)` (the guard the new branch already has at `:2191`) - one condition, before the AI elections arrive. RISK.

6. Finding 5's citations CLOSED. `:1215` cites the fifteen days to [RO-9-12]; `:1211` puts "som stöds av mer än hälften av de röstande" at [RO-11-10]; `:1214` splits the ties - a preparatory vote's by lot [RO-11-11], a main vote's tabled then returned or drawn [RO-11-12]. All three match the statute file. The junior-partner/support tabling stands as a stated risk.

7. Finding 13 CLOSED. `WorldClock.StartLine` (`:121`) reads "OPENS 18 JUL 2024 · A WHAT-IF · NO ELECTION" and `StartPointsDiagnostic.cs:53`'s description matches its assertion. The two card lines agree on the mode.

8. OK - `TrajectorySentinelCheck.cs` bumps `BaselineLabel` to `ps3e` and narrates it as byte-identical to `ps3c`: the dump sets no player, so neither `TableGovernmentBudget` nor `ministryByBill` can fire (first reading, finding 1). Consistent with s630's `ps3c` = `ps3a`.

VERDICT: READY. The five defects are closed at their sources - the alternative rides the save and the restore clears it, the desk names the government's budget first (filmed), a frame decision is recorded as the adopted budget it is with per-party alignments inside the record's range, the caption no longer trips `MetaTextCheck`, and the arrival window is the government's again with the reset at the one mid-game site where the government changes. Carry as notes: guard the old path's window-opening on a pending bill before AI elections (item 5); the round trip through `RestoreSaveState` in the diagnostic (item 1); the seat map's caption when the alternative carries and the Canvas film owed at the close (item 3); "not yet sourced" in the unsourced title (item 4); and the stated risks the coordinator lists (the empty concern's "predates the map" caption, junior partner/support tabling, no `ReasonShort`, the enum name in `DescribeBudgetBill`, the harness diffs).
