# Review: PS-3f - the budget contest on the signing screen, the junior partner and the support party's break, the play's first sitting (2026-09-25, s633)

**What was reviewed.** The working-tree change (read-only, `git diff` against HEAD; no Unity run, no git write) to `Assets/Scripts/Data/DivisionRecord.cs` (`DivisionRecord.Contest` 45, `DivisionContest` 48-60), `Assets/Scripts/Elections/GovernmentRecord.cs` (`Breaks` 47-48), `Assets/Scripts/Persistence/SaveGameService.cs` (`CurrentSaveVersion` 30 at 53), `Assets/Scripts/Simulation/SimulationManager.cs` (`TableShadowBudget`'s junior-partner refusal 1187, `ResolveGovernmentBudget`'s `ownAlternative`/`abstaining` 1220-1236, the `Contest` and the break 1241-1243), `Assets/Scripts/UI/SigningScreen.cs` (the vote panel 336-345, the citation 360-367, the stances' verdict and ink 398-402), `Assets/Scripts/Testing/UiScreenshotDriver.cs` (the 89f block 1374-1387, `StageBudgetContestDivision` 1390-1412), `Assets/Editor/GovernmentBudgetBillDiagnostic.cs` (the PS-3f block 79-91), `Assets/Editor/PlayProtocolStaging.cs` (the seat 66-70), `docs/play/PLAY_PROTOCOL.md` (row 24) and `CLAUDE_DESIGN_ASSET_REQUEST.md` (the note 5-7). Read for the paths around the change: `SigningScreen.cs` (`BuildDivisionPlate` 302-334, the grouping 375-395, `PlatePanel`/`PlateCaption`/`PlateReason`/`PlateBody` 441-500, `Shown` 59), `Assets/Scripts/UI/CanvasPaint.cs` (`SeatMap` 20-52, the three inks 31-33), `Assets/Scripts/UI/GameController.cs` (`BudgetWindowHolds` 1396, `QueueNewlyResolvedDivisions` 2270-2286, `TriggerSigningForNewestDivision` 2291-2299, the calendar's division markers 3549-3552, election night's standing budget act 6718-6722, `DrawRecentDivisions` 9847-9895), `Assets/Scripts/UI/GameController.Statistics.cs` (`BuildEnactmentPositions` 196-207), `SimulationManager.cs` (`TableGovernmentBudget` 1166-1175, the no-alternative leg 1250-1258, `TryOpenBudgetProcess` 2201-2236, `PlayerGoverns` 2403-2413, `PlayerMayIntroduce` 2425-2429), `Assets/Scripts/Simulation/StanceModel.cs` (`Stances`/`StancesOver` 232-256), `Assets/Scripts/Elections/WorldClock.cs` (`StartDate` 104-112, Sweden's governments of record 232-237, `BudgetProcedureOf` 225), `Assets/Scripts/Elections/SeatedGovernment.cs` (`TryAt` 57-72), `GovernmentRecord.cs` (`RoleOf` 50-57, `AtStart` 65-81, `WhatIfGoverning` 89-97, `FromView` 100-114), `SaveGameService.cs` (`BuildSettings` 57-77), `Assets/Editor/PlayProtocolCheck.cs` (whole), `PlayProtocolStaging.cs` (whole: the class doc 15-27, `CutAndVerify` 33-115), `Assets/Editor/MetaTextCheck.cs` (the roots 61, the banned patterns 63-93, `Allow` 143-183), `Assets/Editor/FilmDiffCheck.cs` (`Excluded` 49-54), `Assets/Editor/DesignNotificationCheck.cs` (`NotificationHeading` 47, the section scan 71-79), `Tools/bar_tier.ps1` (`$canvasShots` 46), `Tools/film_scope.tsv` (the `dryps3e` row 49), `docs/play/PLAY_PROTOCOL.md` (whole) and `docs/play/PLAY_SHEET.md` (the rows), `UiScreenshotDriver.cs` (the 89d/89e block 1334-1372, `StageDivisionWithContent` 1945-1972, `RecordCanvasTextAssert` 1702-1725, `_countryId` 65, the §630 re-seating 1545-1567), the film `G:/UNITY/Projects/PoliSim-captures/ps3f_1280_89f_signing_budget_contest.png` (2026-09-25 11:49, viewed) and its log `PoliSim-captures/logs/ps3f_1280.log` (lines 28022-28109, 28242, 28281, 30777), and `Reviews/2026-09-25_s632_budget_bill.md` for the shape and the standing notes.

---

## The reader

Adversarial review of §633 - Elias's three rulings on §632's budget bill path - working tree at G:/UNITY/Projects/PoliSim, read-only. Ruling (1) is built where it was ruled and nowhere else needed it: no other surface prints FOR/AGAINST beside a division's stamp (finding 1), the plate reads the two proposals with the adopted one stamped, and the frame is filmed real at 1280 with no canvas-text violation (finding 2). Two things stand against it: the seat map's inks paint the adopted alternative's seats in `Bad` (finding 3), and - a §632 residue the ruling makes visible - an OPPOSITION party that tabled the alternative is not made to vote for it (finding 4). Ruling (2) is right in its gates and its record (findings 5-8). Ruling (3) is built in the staging, but `PLAY_PROTOCOL.md` now says "M" and "the largest party" in the same table cell and "S, the largest" in its §1 (finding 9), the bar does not assert the seated party (finding 10), and the protocol does not say that a governing opening holds the clock on day one for the arrival budget (finding 10).

FINDINGS

1. OK - the ruling's reach: NO OTHER SURFACE prints FOR/AGAINST beside a verdict, and none reads `Contest`. The Parliament tab's DIVISION RECORDS (`GameController.cs:9847-9895`) prints "No. N · <title>", the date, the lean bar on `Alignment` and the CARRIED/REJECTED stamp on `Passed` - the title itself carries "the government's frames adopted, 176 to 173, over S's alternative", so the count is named without FOR/AGAINST; the calendar marker (`:3549-3552`) prints "(Carried)"; `QueueNewlyResolvedDivisions` (`:2284`) fires `BillPasses` because `ResolveGovernmentBudget` appends the frame decision `passed = true` either way (`SimulationManager.cs:1239`, §632's fix); Statistics' enactment ticks (`Statistics.cs:207`) read `Passed`; election night's standing budget act (`:6718-6722`) reads `Passed`, `Axis` and `Effects` - a contest division can be the act it cites, which is right (it is the enacted budget). Grep of `.Contest` outside the change: only `SigningScreen.cs`, the driver and the diagnostic. The ruling's "never FOR/AGAINST beside CARRIED" is therefore honoured everywhere by construction; the one thing the other surfaces carry from the old convention is the LEAN BAR on the Parliament list, which is the seat-weighted alignment toward the GOVERNMENT's concern (`:1240`): when the alternative is adopted the bar leans "against" beside a CARRIED stamp. Cosmetic, the same convention as finding 3, named not fixed.

2. OK - the plate reads the ruling, and it is filmed. `SigningScreen.cs:336-345` draws the seat map on (`VotesFor`, `Abstentions`, `VotesAgainst`), two `PlateBody` lines "<proposal> · <votes>[ · ADOPTED]" with the adopted one in `Good` and the other in `TextSecondary`, and "ABSTAINING n" as a caption; the citation (`:360-363`) reads "ADOPTED · <proposal>" in `Good` with no alignment figure and no CARRIED/LOST; the stances (`:398-402`) read "FOR <proposal>" / "ABSTAINS", the adopted side in `Good`, the other in `TextPrimary` - never `Bad`. All three sites test `record.Contest != null` first, so every pre-§633 record (and every ordinary division) takes the old branches unchanged; `ProposalAgainst` is built from `TabledBy`, which §632's reading showed reachable only through `TableShadowBudget` with a party seated, so no null reaches the plate. THE FILM: `ps3f_1280_89f_signing_budget_contest.png` exists (2026-09-25 11:49, Sweden at 1280, the `-batchmode` run in `logs/ps3f_1280.log` - "135 captured, 0 failed", `CANVAS TEXT [89f_signing_budget_contest]: 23 text(s) ... 0 violation(s)`). Viewed: the title "Annual budget: the government's frames adopted, 181 to 145, over S's alternative" on one line; the vote panel's three lines under the map, "THE GOVERNMENT'S FRAMES · 181 · ADOPTED" in green; the citation "ADOPTED · THE GOVERNMENT'S FRAMES" wrapped to two lines inside its 0.8-weight column, not clipped; the stances' government row "SD 72, M 67, KD 24, L 18 · FOR THE GOVERNMENT'S FRAMES" with its "+0.00" wrapped onto a second line inside the row (the `PlateBody` wraps, `:498`), the reason beneath it clear of the next row. So at 1280 the lengths hold. NOT filmed: 2560 - the canvas film rule (PF-13, `Tools/bar_tier.ps1:40-46`) owes a Canvas surface a REAL film at 1280 AND 2560 at a track's close. `FilmDiffCheck.cs:49-54` needs no row: 89f is captured after `WaitForCanvasSettle` (`:1380`), a settled frame like 89e, not the mid-rise class 89d is excluded for. `Tools/bar_tier.ps1:46`'s `$canvasShots` still names `'SigningScreen' = '89d_signing_entrance, 89e_signing_settled'` - that table is what tells a commit touching `SigningScreen.cs` which real frames it owes; 89f belongs in it. RISK (the table), NOTE (the 2560).

3. RISK - the seat map paints the ADOPTED alternative's seats in `Bad`. `CanvasPaint.SeatMap` inks the first count `Good`, the second `TextMuted`, the third `Bad` (`CanvasPaint.cs:31-33`); the contest branch passes (`VotesFor`, `Abstentions`, `VotesAgainst`) (`SigningScreen.cs:340`). When `AlternativeAdopted` is true the map shows the alternative's 180 seats in red and the government's 169 in green, directly above "S'S ALTERNATIVE · 180 · ADOPTED" in green ink and a citation stamped ADOPTED - the picture says the red side lost. The contest branch also drops the old "FOR n · UNDECIDED n · AGAINST n" caption, so nothing on the plate says which colour is which proposal; the reader pairs colour to line by order alone. Scenario: S tables an alternative the chamber prefers (the model's `forA > forG`); the ceremony's map is the 2021 picture inverted. Fix at `:340`: swap the two vote counts (or the ink pair) by `AlternativeAdopted` so the adopted proposal is always the `Good` count, or give `SeatMap` a neutral two-ink overload for a contest and say in a caption which ink is which proposal. The filmed frame cannot show this - the staging adopts the government's.

4. DEFECT (a §632 residue, made visible by this ruling) - an OPPOSITION party that tabled the alternative is NOT made to vote for it. `SimulationManager.cs:1232` sets `ownAlternative = role == Support && kv.Key == alternative.TabledBy`; for the player's party in OPPOSITION (S under Kristersson - the play's second sitting, and the sitting the whole §632 path was built for) the side is the alignment comparison at `:1234`: `g.Alignment >= a ? (g.Alignment > 0 ? 1 : 0) : (a > 0 ? -1 : 0)`. Where the alternative's concern gives S no more alignment than the government's bill does - an empty or near-empty draft tabled as it stands (the desk's own sentence invites "Your draft below can be tabled as the alternative"), `a` is 0 and S ABSTAINS on its own motion, or with `g.Alignment > 0` votes FOR THE GOVERNMENT'S FRAMES against it - and the signing screen now prints exactly that: "S · 99 · FOR THE GOVERNMENT'S FRAMES" two lines below "S'S ALTERNATIVE · n". The harness's staging forces S to −1 by name (`UiScreenshotDriver.cs:1400`), so the film cannot show it, and the diagnostic's part 1 asserts "the parties took sides" but never S's own side. The ruling's principle - a party votes its own alternative - is stated for the support party because that is where it costs (a break); it is no less true of the tabling party in opposition. Fix: `ownAlternative = kv.Key == alternative.TabledBy` for the side and the reason, with `breakBy` still gated on `Support` (`:1241`) so only the support party's vote is recorded as a break; the diagnostic's part 1 should assert `S`'s side `< 0`.

5. OK - `TableShadowBudget`'s order and the junior partner. The junior test (`:1187`) sits after the "already tabled" test (`:1186`); in the game the player's party never changes, so a junior partner can never have tabled and the order is unobservable - a tool that re-seats mid-countdown (the diagnostic does, `:79-82`) would read "ALREADY TABLED" for KD had S tabled first, which it had not (part 2's bill of 1 January 2027 stands with no alternative). The refusal reaches the desk through `DrawShadowBudgetAction`'s caption (§632). A SUPPORT party passes: Sweden's record at `StartDate` (18 January 2026, `WorldClock.cs:111` → the run-up before 13 September 2026) is Kristersson's, `CabinetSourced` with `{M, KD, L}` and support `{SD}` (`WorldClock.cs:235`), which `SeatedGovernment.TryAt` returns INSTALLED (`:60`) and `AtStart` stores with `PmParty` M (`GovernmentRecord.cs:69-74`) - so `RoleOf("KD")` is `JuniorPartner`, `RoleOf("SD")` is `Support`, as the diagnostic assumes. `PlayerMayIntroduce`'s junior-partner text (`:2428`) and this refusal now say the same thing in two registers ("YOUR PORTFOLIOS' LEVERS ARE NOT YET BUILT" / "YOUR BUDGET VOICE IS THE COALITION AGREEMENT"); consistent.

6. OK - the support party's own alternative is voted −1 whatever its alignment: `:1234` tests `governmentParty` (false for it, `:1233`) then `ownAlternative` BEFORE the alignment comparison; its recorded `Alignment` is `a`, its own toward its proposal (`:1236`); the reason is the break sentence (`:1235`); `abstaining` now sums the 0 sides (`:1234`). `breakBy` (`:1241`) is `RoleOf(TabledBy) == Support`, the same predicate as `ownAlternative`, so `Contest.BreakBy` and the `sides` cannot disagree; `country.Government` is null-guarded at `:1241` and dereferenced at `:1243` only when `breakBy` is non-null, which required it non-null. The break is appended whether or not the alternative carried - right: the break is the vote, not the outcome. NOTHING reads `Breaks` but the diagnostic (grep): the masthead, the desk and the Cabinet tab say nothing of a break - the field's own doc (`GovernmentRecord.cs:47`) defers the consequence to PS-3 part 5, and the record should say the reading is owed there too.

7. OK - the save. `Breaks` is a public field on `GovernmentRecord`, which rides `Country.Government` inside the World since v27; `Contest` a public field of `DivisionRecord` inside `DivisionLog.Entries`; the serializer is Json.NET with `MissingMemberHandling.Ignore` and `TypeNameHandling.None` (`SaveGameService.cs:64-77`), so both round-trip as their siblings (`Executive`, `Effects`) do, and a null `Contest` writes and reads as null. `DivisionContest` carries no `[Serializable]` where `DivisionRecord`/`DivisionSide`/`DivisionEffect` do (`DivisionRecord.cs:22, 108, 125`) - Json.NET does not need it; cosmetic. A new government's record (`FromView`, `WhatIfGoverning`) starts `Breaks` empty, and the election's verdict replaces the record (`GameController.cs:6622`), so the breaks belong to the government they were made against. The bump to 30 refuses every v29 save - the play save on disk stays v29 until `PlayProtocolStaging.Run` re-cuts it (`PlayProtocolCheck` cuts its own to a temp path and would pass; the file the protocol names would read "refused" in the game). NOT asserted anywhere: a serializer round trip of `Contest` or `Breaks` (the diagnostic reads them in memory only, `:89-90`); `SaveLoadRoundTripDiagnostic` seats the PM's party (`:200`) so no contest division is ever recorded in it. NOTE.

8. OK - the diagnostic's block (`GovernmentBudgetBillDiagnostic.cs:79-91`). It runs after part 2's bill is tabled on 1 January 2027 with S seated; no election resolves inside a bare `SimulationManager` (the verdict is `GameController.ResolveElectionVerdict`'s), so the record stays Kristersson's through 13 September 2026 and the roles in finding 5 hold. KD refused with the exact string; SD's alternative tabled; 25 ticks cover the 21-day countdown; `sdSide.Side < 0`, `Contest.BreakBy == "SD"`, `Breaks.Count + 1`; the sum `VotesFor + VotesAgainst + Abstentions == 349` holds on either chamber (`StancesOver` enumerates `PartySystems.For(Sweden)`, all eight, seats from `ParliamentSeats` - 2022's 107+73+68+24+24+19+18+16 = 349) and `Passed` true. The `F(...)` arguments use `?.` on `sdDivision`/`Contest` and the `Check` predicates short-circuit on the null tests first, so a missing division fails the check rather than throwing. Not asserted: SD's recorded `Alignment` and reason string, a serializer round trip (finding 7), and S's own side in part 1 (finding 4).

9. DEFECT - `docs/play/PLAY_PROTOCOL.md` contradicts itself on the seated party. Row 24 says "**seated as M leading the government of record - the first sitting's party (ruled §633); the second sitting is S in opposition ...**" and, in the SAME cell, still ends "Sweden, the player seated as the largest party of the seeded chamber (§558), a clean book"; §1 line 14 still says "the play's party (S, the largest)". The staging's own text says the old thing too: the class doc (`PlayProtocolStaging.cs:21-22`, "**The player is seated as the largest party of Sweden's seeded chamber**") and the §558 comment at `:63-65` above a `largest` loop (`:66-68`) that now serves only the null guard. Pre-existing staleness beside it, now sharper because the row was edited: §1's "The game starts on 1 October 2026 / The day-one government is **none**" against the row's 18 January 2026 (since §628, and finding 5's installed Kristersson record); §3 step 1's "4 February 2030" and step 2's "30 September 2030" against the row's dates; §2's "on the current format (24, ...)". Fix the row's tail and §1's parenthesis before the commit; the rest as the protocol's own housekeeping.

10. RISK - the staged save at M opens GOVERNING, and neither the bar nor the protocol says what that means. (a) THE BAR: `PlayProtocolStaging.CutAndVerify` asserts `PlayerPartyIndexForCampaign() >= 0` (`:101`) and `PlayProtocolCheck` adds the controller's adoption - neither asserts WHICH party is seated nor the role, so a regression to the largest party (or to S) passes the cheap bar; one line - `loaded.World`'s Sweden `PlayerPartyAbbrev == "M"` and `simB.PlayerGoverns(...)` - would hold the ruling. (b) DAY ONE: with M seated `PlayerGoverns` is true (`SimulationManager.cs:2412`), so the first CONTROLLER day tick takes `TryOpenBudgetProcess`'s old path (`:2229-2234`; the staging plays no day tick, `:73-78`, so `_incomingBudgetWindowUsed` is empty in the save): the arrival window opens, `_pendingBudgetProcessByCountry` gains Sweden, and `BudgetWindowHolds` (`GameController.cs:1396`) pauses the clock while `HoldOnBudgetWindow` - beside the campaign opening's own hold. The protocol's §3 step 1 ("The Desk opens ...; the rail's CAMPAIGN cell reads the run-up") does not say that the first sitting opens on the arrival budget's hold and that the player must introduce or defer a budget before the run-up's first day advances - under S it was the AI government's bill (no hold, §632). State it in step 1. (c) "the second sitting is S in opposition, seated from the same save by the picker": a LOAD never runs `SelectPlayerCountry` (§632's finding 12 - `PlayerPartyAbbrev` rides the save), so the picker cannot re-seat a loaded save; S's sitting is a NEW game on the same seed or a second staged save. Say which. The sheet (`PLAY_SHEET.md`) is generated from constants and names no party - unaffected.

11. OK - `MetaTextCheck`, the culture, the throws. The new UI literals in `SigningScreen.cs` - "ADOPTED · {0}", "ABSTAINING {0}", "FOR " + proposal, "ABSTAINS", " · ADOPTED" - match none of the banned patterns (`MetaTextCheck.cs:63-93`); the register pattern `\bS\d+\b` needs digits, so a runtime "S'S ALTERNATIVE" is not a hit and is not scanned anyway (it is built in `SimulationManager.cs`, outside the roots); "§633"/"PS-3f" appear in comments only in `DivisionRecord.cs` (a root) and `SigningScreen.cs` - the comment-only exemption (RL-1, s585). The three new `string.Format`s carry `CultureInfo.InvariantCulture`; the interpolated `{CurrentDate:yyyy-MM-dd}` in the break line (`:1243`) and the division titles ride the UI-thread culture that `UiCulture.Install` fixes (§568), as §632's strings do. Throws: every `Contest` reader guards null (finding 2); `Breaks` is initialised on the field and on every factory; `country.Government` is guarded at `:1187` and `:1241`. Nothing new can throw on an old record.

12. OK - compile and the staging's plausibility. `player.Divisions.Append(title, sim.CurrentDate, 0f, true, 0f, (int)BillAxis.Fiscal, sides)` matches the seven-argument overload (`DivisionRecord.cs:77`); `BillAxis` is `PoliSim.Simulation.BillAxis` (`ParliamentSystem.cs:14`), imported at the driver's `:9`; `PoliSim.Elections.PlayerRole` is fully qualified with the namespace also imported (`:8`) - legal; `PartySystems.For`/`PoliticalParty.ShortName`/`DivisionSide`/`DivisionContest` are in scope through `:7`; the film ran on this tree, so it compiled. The sides: the government's parties by `RoleOf` (+1), S/V/MP −1 at +0.40, the rest 0 - the 2021 pattern (the government bloc, the red-green bloc, C abstaining) on whatever government the film's world holds; the filmed record is 181 (SD 72, M 67, KD 24, L 18) to 145 (S 101, V 22, MP 22), C 23 abstaining - the post-election chamber the harness's warm-up holds the election on. Plausible and stated as staged in the log (`SHOT: staged the budget contest division - ...`).

13. RISK - the staging's party names are Sweden's. `StageBudgetContestDivision` names "S", "V", "MP" by literal (`:1397, :1400`); the harness's default `_countryId` is the USA (`:65`) and the 89 block runs for every filmed country. On a USA or Germany film no party matches, every non-government party abstains, and the frame reads "S'S ALTERNATIVE · 0" beneath the map with "ABSTAINING n" - a record no chamber could produce. Harmless to the film's assertions; guard the block on Sweden (as the `-shotsaves` staging does, `:1453`) or derive the alternative's party as the largest party outside the government.

14. NOTE - the stances' grouping key and the live record. The key is side|alignment|short-reason (`SigningScreen.cs:380`): the staging's four government parties share side +1, alignment 0.00 and the reason, so they group as ONE row (as filmed); the live path records each government party's own `g.Alignment` (`:1236`), so Sweden's four stand as four rows, each with a two-line reason floor (`PlateReason`, 32 px) - eight rows as 89e already had, but with the longer verdict "FOR THE GOVERNMENT'S FRAMES +0.12" that the film shows wrapping inside its row. The frame filmed is the STAGING's record, not the model's (the model's first contest lands on the 1 January 2027 bill, after the harness's warm-up); the real film owed at the track's close should be of a division the model recorded, at 1280 and 2560, with the alternative adopted at least once (finding 3).

15. NOTE - `CLAUDE_DESIGN_ASSET_REQUEST.md`'s new "## ⭐ NOTE FOR THE NEXT ROUND" heading (`:5`) sits above D-SP; `DesignNotificationCheck` scans from "TO DESIGN, WITH THE NEXT RETURN" (`:47`, the heading at line 91) to the next "## " (`:77`), so the note is outside its range and its rows are unaffected. The note's "filmed real as `89f_signing_budget_contest` at 1280" is true of `ps3f`; it does not claim 2560.

VERDICT: READY WITH ONE FIX AND ONE DOC LINE. Ruling (1) is built where it was ruled - the plate reads two proposals, the adopted one stamped, no FOR/AGAINST beside any verdict on any surface - and the frame is filmed real at 1280 with no canvas-text violation. Ruling (2) refuses the junior partner, votes the support party's own alternative before any alignment, records the break on the government and the contest on the division, and the diagnostic proves it. Ruling (3) seats M. Before the commit: make the OPPOSITION tabler vote its own alternative too (finding 4 - one predicate at `SimulationManager.cs:1232`, the break still gated on Support), and cut the "largest party (§558)" tail out of `PLAY_PROTOCOL.md` row 24 and the "(S, the largest)" of its §1 (finding 9). Then, as notes for the record: the seat map's inks when the alternative is adopted (3), `bar_tier.ps1`'s canvas table and the 2560 film owed at the close (2, 14), the bar's assertion of the seated party and the protocol's statement that a governing opening holds on the arrival budget (10), the staging's Swedish party names on other countries' films (13), a serializer round trip of `Contest`/`Breaks` and the play save's re-cut to v30 (7), and the break's reading owed to part 5 (6).

---

## Second reading

**Re-read (read-only).** `Assets/Scripts/Simulation/SimulationManager.cs` (`ResolveGovernmentBudget` 1228-1243: `ownAlternative` 1230, `governmentParty` 1231, the side 1232, the reason 1234, `breakBy` 1241; `IntroduceBudgetBill`'s process removal 1296-1300; `TableGovernmentBudget`'s 1172), `Assets/Editor/GovernmentBudgetBillDiagnostic.cs` (S's side 71-72, the SD block 91-93), `Assets/Scripts/UI/SigningScreen.cs` (the contest branch 336-347: the swapped counts 341-342, the two body lines 343-344, the caption 345; `PlateCaption` 473-478 unchanged), `Tools/bar_tier.ps1` (`$canvasShots` 46), `Assets/Scripts/Testing/UiScreenshotDriver.cs` (`CaptureFilmGaps` 1231-1389, section (d) 1375-1388 with the `yield break` at 1378, its one caller 1158, `StageBudgetContestDivision` 1392-1413), `Assets/Editor/PlayProtocolStaging.cs` (the class doc 15-22, the seat 65-70, the M/governs assertion 106-108), `docs/play/PLAY_PROTOCOL.md` (§1 line 14, row 24, the rest for "largest"), and the film's timestamp against the source's (`ps3f_1280_89f_signing_budget_contest.png` 11:49:57; `SigningScreen.cs` 11:59:39). Not re-run: no Unity.

1. Finding 4 CLOSED, with one line left behind. `:1230` reads `ownAlternative = kv.Key == alternative.TabledBy` - any tabler votes its own before the alignment test (`:1232`), and `breakBy` (`:1241`) stays gated on `Support`, so only a support party's vote is a break; the diagnostic's part 1 asserts `S`'s side `< 0` (`:71-72`). THE LINE LEFT BEHIND: the reason at `:1234` for `ownAlternative` is still the support party's sentence, "its own alternative over the frames of the government it supports - a break with the government" - for S in OPPOSITION that is false on both halves (it supports no government, it breaks nothing), and it is the reason line the plate prints beneath "S · 99 · FOR S'S ALTERNATIVE" (`SigningScreen.cs:409`). Scenario: the play's second sitting tables its alternative and the ceremony says S broke with a government it supports. Fix: `ownAlternative ? (role == Support ? "<the break sentence>" : "its own alternative, tabled against the government's frames") : ...`. DEFECT, one string.

2. Finding 3 CLOSED as to the inks, with a re-film owed and one order to note. `:341-342` computes `adoptedSeats`/`otherSeats` by `AlternativeAdopted` and `:342` passes (`adopted`, `Abstentions`, `other`), so the `Good` ink is always the adopted proposal's; the two body lines keep their fixed order (the government's first, `:343`; the alternative second, `:344`) with ADOPTED stamped on whichever carried. When the alternative carries, the map's green (left) is the SECOND line and the red (right) the FIRST - the caption at `:345` resolves it by naming the map's order ("THE MAP: THE ADOPTED, THE ABSTAINING, THE OTHER") rather than the lines', which is honest; a reader pairs by the stamp. Cosmetic. THE RE-FILM: the caption grew from "ABSTAINING 23" (13 characters) to "ABSTAINING 23 · THE MAP: THE ADOPTED, THE ABSTAINING, THE OTHER" (63) in the caption face at the vote panel's width (weight 1.2, no minimum, `:334`); `PlateCaption` wraps with a ONE-line floor (`:473-478`, 16 px), which is exactly the PF-13 compression class - a wrapped caption asks for more than its floor and the column may give it the floor. The film on disk (11:49:57) PREDATES this source (11:59:39), so the 0-violation line in `ps3f_1280.log` does not cover it. A real film of 89f at 1280 after this edit is owed before the commit is called filmed; if it wraps and compresses, `PlateReason`'s two-line floor is the shape of the fix. RISK.

3. Finding 2's table CLOSED: `Tools/bar_tier.ps1:46` names `89f_signing_budget_contest` beside 89d and 89e.

4. Finding 13 CLOSED, and the `yield break` is safe. `:1378` returns from `CaptureFilmGaps` when `player.Id != Sweden`, with the "skipped for this country, not missing" log line; section (d) is the LAST section of that coroutine (the method's closing brace is `:1389`, nothing follows the SIGN/settle at `:1386-1388`), and a `yield break` ends only the iterator it is in - the caller at `:1158` (`yield return CaptureFilmGaps(controller)`) resumes on the next line as it would after the method's natural end. The 89d/89e block above it (`:1334-1372`) is unaffected. Nothing is skipped on a non-Swedish film but 89f itself.

5. Finding 9 PARTLY CLOSED - one contradiction remains, inside row 24 itself. `PLAY_PROTOCOL.md` no longer says "largest" anywhere (grep), and §1 line 14 names M first and says S's sitting is "a NEW game from the picker on this seed, since a load keeps the save's party" - right (a load never runs `SelectPlayerCountry`). But row 24 still reads "**... the second sitting is S in opposition, seated from the same save by the picker**" - the very reading line 14 now denies, twelve lines apart. Cut "from the same save by the picker" to "a new game on the same seed" in the row. DEFECT, one phrase. The row's new tail - "M governs from day one - the arrival budget window opens on the first day tick and holds the clock until a budget bill is introduced (C-C2), beside the campaign hold" - is accurate to the code: only `IntroduceBudgetBill` removes the player's process (`SimulationManager.cs:1298`; `TableGovernmentBudget`'s `:1172` is the AI path), so the hold clears on the introduction and on nothing else while `HoldOnBudgetWindow` stands. The pre-existing staleness stands as noted in the first reading (§1's "none"/1 October 2026, §3's 2030 dates, §2's "format 24").

6. Finding 10(a) CLOSED. `CutAndVerify` (`:106-108`) fails unless the restored Sweden's `PlayerPartyAbbrev == "M"` and `simB.PlayerGoverns(restored)`; the failure text names the party found and the governs bit ("restored, the player is S and governs False - the first sitting is M leading the government of record"), which reads as a sentence and says what to look at. `PlayerGoverns` throws on a null `Government`, but the restored world carries the v27 record, and a throw here would fail the check with its own words rather than pass it. The class doc (`:21`) now says M, with the largest-party rule as history; its bold span closes mid-sentence ("... seeded chamber** (§558 - ...)") - cosmetic.

VERDICT: READY once two strings are cut - the opposition tabler's reason at `SimulationManager.cs:1234` (item 1) and "seated from the same save by the picker" in `PLAY_PROTOCOL.md` row 24 (item 5) - and 89f is re-filmed real at 1280 on the edited plate before the commit claims the film (item 2). The seat map's inks, the canvas table, the Sweden-only staging with its early return, the S-side assertion and the bar's M/governs assertion are closed at their sources.
