# Review — §642 who governs, one answer; the support agreements' templates the world's (2026-09-26)

The ultrareview of PR #1 (§629–§637) returned two findings that could not be posted to GitHub: (1) `ChamberVerdicts` goes stale when the
government record changes without a seat change, and the compass (`GovernmentFormation.Cabinet`) re-formed the cabinet from the seats while
the votes read the stored record; (2) `SupportAgreement.Templates` is a static, process-lifetime table. This pass fixes both. Two independent
readers, read-only (no build, no Unity run), on the staged diff and then on the fixes to it: `GovernmentRecord` (`Version`, `WithdrawSupport`,
`LeaveCabinet`, `Discharge`), `Assets/Scripts/Simulation/SimulationManager.cs` (the in-place writes routed through them), `ChamberVerdicts` (`Bind`, `ConcernKey`),
`GovernmentFormation` (`Cabinet`, `IsProvisional`), `World.SupportAgreementTemplates`, `SupportAgreement.Demand`, `GameController` (the world
passed), and the checks (`ChamberVerdictCacheCheck`, `SupportAgreementDiagnostic`, `OfficeTestDiagnostic`, `WorldClockDiagnostic`, the diagnostics
that now pass their world).

## First reading — READY WITH FIXES

1. **defect** — the compass legend's PROVISIONAL mark read the seeded table while its cabinet now comes from the record: France's what-if
   (`WhatIfGoverning`) printed "(PROVISIONAL)" over the player's own cabinet. **Fixed:** `GovernmentFormation.IsProvisional` reads the stored
   record's standing, the seated table's only where none is stored; `OfficeTestDiagnostic`'s probes of the seated rule call it directly, and
   the mark reading the record is asserted.
2. **ok** — no in-place write to who governs remains outside the record's methods; the stance model reads only the cabinet and the support.
3. **risk** — the version is a convention while the lists stay public. **Fixed as a tripwire:** `ChamberVerdictCacheCheck` scans the game's
   source for a write to a record's cabinet, support, prime minister or caretaker standing outside `GovernmentRecord.cs` (a formation's `view`
   lists exempt) - proved failing on a restored `g.Support.Remove(mover)`.
4. **risk, pre-existing** — `ChamberVerdicts.ConcernKey` omitted `GovernmentAuthored`, which the stance model reads. **Fixed:** the author is in
   the key; asserted by a member/government pair (the second reading's item 4), proved failing with the author taken out.
5. **risk** — `WorldClockDiagnostic`'s cabinet assertion became near-tautological. **Fixed:** it asserts the formation's installed branch
   (`ViewOf`) and the stored record apart.
6. **ok** — the templates are no longer process-static; the world is passed on every live path; passing it where null was passed changes no
   demand in practice (the null-world call hit the factory's world-scored template of the same key) and is a correction; serialization excludes
   the table and a loaded world builds its own. **nit** — the optional `world` parameters remain (stated, below); `TemplateKey` made private.
7. **risk** — the per-world scoring cost was unmeasured. **Measured:** `SupportAgreementDiagnostic` prints it (the default world's start
   governments' supporters, scored into a fresh table); the simulation bar's time holds within the day's range.
8. **ok with a risk** — the tests discriminate; the new check moved the random streams for later checks. **Fixed:** captured and restored.
   An early return no longer skips the next part (each part its own method).
9. **ok** — numeric inertness: no float on the play path changes.
10. **nit** — stale comments (`TryFormChamber`'s callers, `TryGovernment`'s doc, `ChamberVerdicts`' class note, the suite's registration,
    `CompassPositions`, the check's own doc transcribing the start's government). **Fixed**; `Demand`'s stacked summaries merged.

## Second reading (on the fixes) — READY

1. **ok** — `IsProvisional` from the record: no caller wrong; at the start the record's standing is the seated table's. **Stated:** after an
   election that forms no government the record stands, and so does its standing, where the table's rule had said false - consistent with the
   cabinet the votes read.
2. **nit, pre-existing** — the IMGUI pickers read the selector world's standing beside the start's rows. Inputs unchanged. **Stated.**
3. **nit** — `IsProvisional`'s summary. **Fixed.**
4. **risk** — no member/government pair pinned the author in the key. **Fixed** (above).
5. **risk** — the guard's false negatives (aliases, helpers, indexers, `RemoveRange`, a local named `view`, a method inside
   `GovernmentRecord.cs` that forgets the bump). **Stated:** a tripwire, not a proof.
6. **ok / nit** — the random restore is exact per stream; an unseeded process is restored as a tick seed. **Stated.**
7. **nit** — the guard read comments twice and ran outside a try. **Fixed** (`SourceText.ReadWithoutComments`; caught).
8. **risk** — the new part's cost in the cheap bar was not printed. **Fixed:** it prints its seconds.
9. **nit** — grammar; `Compass()` is a regression guard now. **Fixed / stated.**
10.–12. **ok** — `WorldClockDiagnostic`, `OfficeTestDiagnostic`, the cost measurement.
13. **nit** — a transcribed count ("the three methods"). **Fixed.**
14. **nit** — `LeaveCabinet` bumped after re-apportioning. **Fixed:** before.
15. **risk** — `world = null` remains the default on `AtStart`, `FromView` and `WhatIfGoverning`; a future caller that omits it drops the
    own-currency laws from the demands. **Stated:** every current caller passes it (the one that does not throws first).
16. **ok** — `Bind` on identity and version is sound.

## Films

The dry film (`dryps3n`) measured 132, three fewer than `dryps3m`: the Canvas selector's start panel and two pickers are filmed only when the
Canvas cover has come in (`CanvasSelectorActive`), and a fast batch run reaches them before it - a pre-existing race (`dryps3a`, `dryps3c`,
`dryps3e` and older films skip them too). The pickers this change touches are the IMGUI fallback, which no film captures; the Canvas picker
reads `WorldClock.PickerViewOf`, untouched. The real film (`ps3n_1280`) filmed the picker.
