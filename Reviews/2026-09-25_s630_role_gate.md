# Review: PS-3c - the role-gated levers (2026-09-25, s630)

**What was reviewed.** The working-tree change (read-only, `git diff` against HEAD) to `Assets/Scripts/Simulation/SimulationManager.cs` (`PlayerMayIntroduce` at 2289-2299 and the gate line at the head of the nine `Introduce*Bill` methods: 1172, 1263, 1322, 1381, 1470, 1523, 1830, 1894, 1983), `Assets/Scripts/UI/GameController.cs` (`DrawLeverLock` at 11922-11928, the eight lock sites at 4705, 9153, 10611, 10657, 11040, 11389, 11729, 11906 and the Fed dial at 3988), `Assets/Scripts/Testing/UiScreenshotDriver.cs` (the two re-seats at 1507 and 3037), `Assets/Editor/SaveLoadRoundTripDiagnostic.cs` (199-200), `Assets/Editor/BudgetWindowDiagnostic.cs` (97, 129) and `Assets/Editor/PlayerRoleDiagnostic.cs` (62-68). Read for the paths around the gate: `Assets/Scripts/Simulation/SimulationManager.cs` (`PlayerGoverns` 2271-2282, `TryOpenBudgetProcess` 2070-2078, `AdvanceCountryDayTick` 296-300, `ResolveCabinetDecision` 1094-1112, `ResolveForeignPolicyMeeting` 1141-1152, `QueueCampaignDecision` 2546, `QueueScandalResponse` 2524, the cabinet roll at 3135-3141, the AI ministries at 2865/2874, `IndexSpendingLines` 4303, the preview clones at 3158/3184/3461, `ApplyBudgetBillSpendingAndSwf` 1233/1239, `ApplyPartnerTariffOverrides` 3812, the bill countdowns at 1200-2002, the pause's only clears at 1180/2682), `Assets/Scripts/Simulation/CabinetSystem.cs` (`TryRollDecisions` 553-571), `Assets/Scripts/Simulation/TradeSystem.cs` (67-69), `Assets/Scripts/Simulation/ParliamentSystem.cs` (512, 831), `Assets/Scripts/Simulation/EnergyFleet.cs` (`Place` 115, `Withdraw` 147), `Assets/Scripts/UI/GameController.cs` (`BuildPlayerDecision` and the routing at 6318, `ResetPolicyInputs` 6519-6528, `ResolveElectionVerdict` 6600-6651, the turn block at 6236-6250, the Fed chair appointment 4230-4232, the Eurozone rate push 4013, the cabinet decision buttons 9689-9692, the reshuffle 10081-10086, the minister appointment 10127-10131, the trade override 10565-10589, `BuildTradeBillFromDrafts` 10702-10714, `BuildBudgetBillStatusText` 11053-11073, `DrawSentenceAction` 11940-11962, `CtaWidth`/`LedgerFamilyColumns` 4602-4619, the law pane's width at 8354-8359, `_calendarMetaStyle` 2947-2948/3271), `Assets/Scripts/UI/GameController.Desk.cs` (`DeskPx` 82-85, `DeskCaption` 99-110, `RoleTitleSuffix` 260-270), `Assets/Scripts/UI/GameController.Energy.cs` (the order chips 146-169, `EnergyFleet.For(_playerCountry)` 388), `Assets/Scripts/UI/GameController.LawsBoard.cs` (no introduce path), `Assets/Scripts/Elections/GovernmentRecord.cs` (`RoleOf` 52, `HeadParty` 101-110), `Assets/Scripts/Elections/WorldClock.cs` (Sweden's three government rows 221-227), `Assets/Editor/MetaTextCheck.cs` (the roots at 61, the banned and widened patterns 63-105), `Assets/Editor/TrajectoryBaselineDump.cs` and `Assets/Editor/NoPolicyCentury.cs` (61-68), `docs/specs/POLITICAL_SYSTEM_SPEC.md` §5.2 (84-97), and `Reviews/2026-09-25_s629_executives.md` for the shape. No Unity was run; the width finding is arithmetic, not a film.

---

## The reader

Adversarial review of PS-3c (working tree at G:/UNITY/Projects/PoliSim, read-only). The gate itself is sound: `PlayerMayIntroduce` is reached first in all nine `Introduce*Bill` methods, its null order is right (finding 12), the decision routing at 6318 already sends an opposition player's `PolicyDecision` to `None`, and the two dump tools never see it (finding 11). What the change does NOT close is every path that writes the player's book from the desk WITHOUT a bill - the cabinet's decisions, the foreign-policy meeting, the energy fleet's orders, a minister's or a chair's appointment, a reshuffle, a trade override - all of which an opposition player can still work while the masthead says "THE GOVERNMENT IS THE AI'S" (findings 1-3). And the election verdict's own sentence still tells the player the levers stay on their desk (finding 4).

FINDINGS

1. DEFECT - the cabinet and the foreign-policy meeting still land on the AI's book from the opposition benches, and BLOCK the opposition player's turn. `GameController.cs:9691` `_simulationManager.ResolveCabinetDecision(PlayerCountryId, ...)` → `SimulationManager.cs:1101` `CabinetSystem.ApplyDecisionOption(country, ...)` writes `State.ApprovalRating` and, through the option's `BudgetImpact`, `State.GovernmentDebt` (the debt-ledger line at 1103 records it). `GameController.cs:9769` `ResolveForeignPolicyMeeting` → `SimulationManager.cs:1147` `ForeignPolicySystem.ApplyMeetingOption(country, ...)` writes the same two. Neither asks `PlayerGoverns` or `PlayerMayIntroduce`. The meeting is rolled every day for the player's country regardless of role (`AdvanceCountryDayTick`, `SimulationManager.cs:298`; the controller calls the tick for `PlayerCountryId` only), and cabinet decisions are rolled at every turn boundary for every portfolio in `country.CabinetMinisters` (`CabinetSystem.cs:556`) - which an opposition player can still fill: `GameController.cs:10127-10129` `_playerCountry.CabinetMinisters[portfolio] = candidate` on "Appoint", ungated, and `:10081-10086` "Reshuffle" writes `_playerCountry.State.ApprovalRating` directly. Scenario: S opens in opposition (§628), the player appoints a finance minister on the Cabinet tab, a decision rolls at the boundary, `GameController.cs:6241-6249` adds "a Cabinet decision (Cabinet tab)" / "a Foreign Policy meeting" to the turn's blocking list - the AI's government cannot advance until the OPPOSITION answers its cabinet's question, and the answer moves the AI's debt stock. The spec's §5.2 row gives the opposition "the party's votes" and nothing of "appointments, the cabinet". Either gate the three (`ResolveCabinetDecision`, `ResolveForeignPolicyMeeting`, the appointment/reshuffle) on `PlayerGoverns` and stop rolling the meeting for a country the player does not govern, or state in the record that the cabinet tab is deferred with part 4 - but the turn block cannot stay: it is a deadlock of the AI's own government on the player's inaction.

2. DEFECT - energy fleet orders are placed from the desk with no gate, beside the AI energy ministry's. `GameController.Energy.cs:161` and `:165` call `EnergyFleet.Place(country, k, ±step, year, turn)` on the chip clicks; `country` is `_playerCountry` (`:388`, `using (EnergyFleet.For(_playerCountry))`). `DrawDeskChipButton`'s disabled argument is `!can` / `!can || full != null` (the technology's lead time and the connection queue), never the role. The AI energy ministry meanwhile decides for the same country at `SimulationManager.cs:2874` (`!PlayerGoverns(country)`). Scenario: S in opposition orders 3 GW of nuclear (BILLED capex, `EnergyFleet.CapexBill`, §539/§551) and the AI ministry orders its own - two hands on one fleet, the capex on the AI's book. `EnergyFleet.Withdraw` (`:147`) is the same. The Energy page's chips need the lock (a `DrawLeverLock`-style caption in the ORDER column, since the chips are rect-drawn) and `Place` itself a refusal, as the bills have.

3. RISK - three more direct writes from the desk, none through a bill. (a) `GameController.cs:4232` `_playerCountry.CurrentFedChair = candidate` - the central-bank appointment, which also blocks the turn (`:6236-6239`); in opposition the player still appoints the AI government's chair. (b) `GameController.cs:10573` `link.PlayerTariffOverride = Clamp(tariffOnOurImports, ...)` on "Set override" - the sentence says it "changes nothing until a Trade bill moves it", but `TradeSystem.cs:67-69` reads `PlayerTariffOverride` as the effective tariff the moment it is set (most-specific-wins), so the click PINS today's effective rate against any later base-rate change; small, but it is a live write with no gate and it survives the AI's tenure. (c) The Eurozone "National rate push" dial at `:4013` is not locked (only the independent-currency branch at 3988 is) - it draws and drags while `:6318` routes the decision to `None`, and the paragraph above it still says "This game deliberately hands you the central bank" to a player who does not hold it. The rest of the non-bill surfaces are clean: the SWF is created and dissolved only inside `ApplyBudgetBillSpendingAndSwf` (`SimulationManager.cs:1233/1239`), the pension age only at `ParliamentSystem.cs:512`, partner overrides' RATES only at `:3812` under a passed trade bill, the laws board introduces only through `IntroduceLawBill` at 9153, the policy web writes nothing, and the campaign and scandal queues (`:2546`, `:2524`) write the campaign record alone - the party's own machinery, which §5.2 keeps for every role. The three preview paths (`PreviewTurn` 3158, `PreviewTurnWithBudgetDraft` 3184, `PreviewTurnWithSectorDraft` 3461) apply to `ClonePreviewCountry` clones only; they still SHOW an estimate for a draft the player cannot introduce, which is honest as an estimate but the dials above a locked button keep drawing their amber hatch as if a bill were coming. Grep of `_playerCountry\.X =` / `.State.X =` in `GameController*.cs` finds exactly: 2214-2218 (seating), 4232, 6615 (the record), 10085, 10127 - (a), (c) and finding 1's two are the whole list.

4. DEFECT - the election verdict tells the player the opposite of §630. `GameController.cs:6648-6649`: "The run continues - losing office never ends it; until the roles are built, the government's levers stay on your desk." That sentence is shown on election night to the player who has just lost office, in the same frame the gate begins refusing every bill. It was §619's stated deviation and §630 removes the deviation; the sentence must go with it (and the `⚠ Stated deviation until PS-3` comment at 6640-6642).

5. RISK - the junior partner's lock says the wrong thing. `SimulationManager.cs:2296-2297`: a `JuniorPartner` reads "JUNIOR PARTNER · THE GOVERNMENT INTRODUCES BILLS · YOUR PARTY VOTES ON THEM". A partner's party IS in the cabinet (`GovernmentRecord.cs:52`, `Cabinet.Contains(abbrev)`); the spec's row gives it "the levers of the portfolios the party holds ... negotiate the budget in cabinet". Refusing everything until part 4 is stated and fine; saying the party only votes is false on screen. It is reachable today: after Sweden's 2026 election the formation stores M-led SD+M+KD+L (`PlayerRoleDiagnostic.cs:78`), so a KD or L player is a junior partner from election night. The masthead's " · JUNIOR PARTNER · THE GOVERNMENT IS THE AI'S" (`GameController.Desk.cs:267`, pre-existing) has the same problem. A word like "JUNIOR PARTNER · THE PRIME MINISTER'S PARTY LAYS THE BILLS · YOUR PORTFOLIOS ARE NOT YET YOURS" would say what the code does.

6. RISK - the lock's width at 1280 is not measured and the label is un-widthed. `DrawLeverLock` (`GameController.cs:11926`) draws `GUILayout.Label(lockedBecause, DeskCaption(9f, ..., bold: true))` with `wordWrap = false` (`Desk.cs:105`) and no `GUILayout.Width`. At 1280×720 `DeskPx(9f)` = max(9, round(9 × 720/720)) = 9 px in the Document face (`_calendarMetaStyle.font = PoliSimTheme.Document`, `:2948`); the opposition string is 62 characters, so ~0.6 em × 9 × 62 ≈ 335 px regular, and bold mono is WIDER at the floor (polisim-imgui-layout-facts) - call it 350-370 px. The sentence-row sites (4705, 10611, 10657, 11906) and the budget site (11040) sit in full-width ledgers, fine. The law pane (9153) gets `contentWidth = detailPaneWidth − scrollbar − 12` where `detailPaneWidth = lawsInnerWidth − listWidth − 10` (`:8354-8359`, the pane's "nominal 44% share"): at 1280 that is in the ~440 px region - it should fit, but with no margin to spare, and an un-widthed, unwrapped label that does NOT fit does not clip: it becomes the widest child and widens the scroll view's content (polisim-imgui-layout-facts, item 1) - exactly the class of defect the comment at 8335-8352 describes for this pane. Every other Desk caption goes through `PoliSimWidgets.MeasuredLabel` and its guard; this one bypasses it. Fix: `MeasuredLabel` into a `GetRect` row at the family's height, or `GUILayout.Width(contentWidth)` at the law site - and a 1280 film of the law pane and the Riksbank page before the close (the Desk is IMGUI, so a dry film is admissible here). The two rect sites (11389, 11729) draw "LOCKED" - 6 characters in an `actionRect` of at least 12 % of the row (`:4615`) - fits.

7. OK, with a cosmetic RISK - the eight sites' layout and `GUI.enabled`. `if (DrawLeverLock()) { } else if (...)` replaces one layout element (a `GetRect` row or a widthed button) with one `GUILayout.Label` - no element leaks, no group is left open. `DrawLeverLock` never touches `GUI.enabled`; every site that sets it restores it after the branch (9156, 11044, 11393, 11733; `DrawSentenceAction` restores inside itself and is skipped when locked). The rect sites: `GUI.Label(rect, ...)` consumes no control id where `PoliSimWidgets.Button(rect, ...)` consumed one, so the row's slider (control 2) takes a different id than in the governing state - safe, because the role flips only in `ResolveElectionVerdict`, which runs from `Update`, never inside `OnGUI`, so the sequence is stable within any frame and the one lost drag at an election is unobservable. The Fed dial: `if (!DrawLeverLock()) _interestRateChangeInput = DrawDialRow(...)` leaves `_interestRateChangeInput` at its last value while locked, but `ResetPolicyInputs` zeroes it every turn (`:6528`) and `:6318` routes the decision to `None` anyway. The cosmetic part: at 11040 the label is drawn under `GUI.enabled = ambientEnabled && pendingBill == null && budgetProcessOpen`, and for an opposition player `budgetProcessOpen` is ALWAYS false (`TryOpenBudgetProcess` returns at `SimulationManager.cs:2078`), so the Budget page's reason draws in Unity's disabled tint (half alpha) while the other seven draw in full ink; at 9153 the same happens whenever a law bill is pending. Draw the lock BEFORE the `GUI.enabled` line, or restore around it. Also `BuildBudgetBillStatusText` (`:11071-11072`) still says "The annual process opens on your country's own fiscal-year date." above the lock - it never opens for this player.

8. OK - a bill pending at the election belongs to the chamber. The countdowns (`SimulationManager.cs:1200`, 1291, 1350, 1400, 1489, 1559, 1849, 1919, 2002) run in the country's day tick with no role test, and `ApplyBillResult` applies on passage - so a bill the player laid while governing passes or fails on the chamber the election seated and lands on what is now the AI's book, and for at most `BillDurationDays` the AI finance ministry and the passing bill both write. That is the honest reading (the government that laid it is gone; the chamber still votes) and I would keep it. One latent hazard, not reachable today: the budget PAUSE is cleared only by `IntroduceBudgetBill` (`:1180`) or a load (`:2682`); if a pause were open on the day the role flips, the gate refuses the only action that clears it and `BudgetWindowHolds` (`GameController.cs:1396`) holds time forever. Sweden's fiscal-year window (1 January, or the arrival window) and its polling day (13 September) cannot coincide and the five AI countries hold no election, so no deadlock exists in this build - but `TryOpenBudgetProcess` should also CLOSE the pause when `PlayerGoverns` turns false, before AI elections arrive.

9. RISK - the harness's two re-seats change every later frame of those runs and are not undone. `UiScreenshotDriver.cs:1507` and `:3037` set `player.PlayerPartyAbbrev = player.Government.PmParty` when the sweep's largest party is not the prime minister's. For Sweden at the default epoch the record is "Ulf Kristersson (M), caretaker" (`WorldClock.cs:225`; `HeadParty` parses "(M)" → PmParty "M"), and the sweep seats S, so the re-seat FIRES on every Sweden run: from that line on the masthead reads no role suffix, the hemicycle's highlight, the HQ and the compass legend read M, `PlayerGoverns` turns TRUE so the AI finance ministry LEAVES the book and the budget process can open for any day-stepping that follows, and the dense save written after 1507 carries M as the player's party. Neither site restores S, and `PartyApprovalRating` (set from the largest at seating) is not re-derived. The 3037 site is "LAST, so no day ever ticks" and is the smaller change; the 1507 site is the dense-state builder and everything captured or saved from it is now a different game. Expected film diffs, but they should be named in the record and the driver should restore the seat after the bills are staged (or seat the PM's party for the whole run, stated).

10. OK - the Editor tools. `BudgetWindowDiagnostic.cs:97/129` names the country as the player with no party: `PlayerGoverns` returns true on the empty-party leg (`SimulationManager.cs:2280`), so `TryOpenBudgetProcess` opens and the two searches find their window again; a side effect is that the AI finance ministry is now OFF for that one country in this instrument (`:2865`), which does not move the calendar the instrument measures. `SaveLoadRoundTripDiagnostic.cs:200` seats `Government?.PmParty ?? roster[1]` on both determinism paths - consistent with itself; the AI ministry is now off the player's book there where §629's reading had it on. `PlayerRoleDiagnostic.cs:63-68` proves the gate both ways with a real `IntroduceLawBill` ("cash_bail_reform_act" is in `LawCatalog.cs`), and the M-seated introduction leaves a pending law bill in the sim that the save round trip at 70-73 then carries - harmless, but the assertions at 74-75 read the record only, so nothing checks the bill survived; NOTE.

11. OK - the dumps. `TrajectoryBaselineDump` sets no `PlayerCountryId`, so `PlayerMayIntroduce` returns at `!PlayerCountryId.HasValue` and `PlayerGoverns` at the same leg; `NoPolicyCentury.cs:68` names a player with no party → `PlayerGoverns` true → `PlayerMayIntroduce` true; neither calls any `Introduce*Bill` (grep), so the gate is never on their path and the families (ps3a = ps2cal) cannot move.

12. OK - the null order. `PlayerMayIntroduce` (`SimulationManager.cs:2293`) is `country == null || !PlayerCountryId.HasValue || PlayerCountryId.Value != countryId || PlayerGoverns(country)` - `PlayerGoverns` is reached only for the player's own non-null country, throws at `:2275` on `Government == null` before any `RoleOf`, and returns true at `:2280` for an empty `PlayerPartyAbbrev`, so the `RoleOf` at `:2295` never sees a null government or a null abbrev. Confirmed. It is called on every OnGUI frame from nine sites; `RoleOf` is two list scans - fine.

13. OK - meta-text. `MetaTextCheck` roots are `Assets/Scripts/UI`, `LawCatalog.cs` and `Assets/Scripts/Data` (`MetaTextCheck.cs:61`); the reason string is built in `SimulationManager.cs` (outside the roots) and by hand carries no `§`, no register id (`\bS\d+\b`, `PS-\d+` etc. do not match "IN OPPOSITION · THE GOVERNMENT INTRODUCES BILLS · YOUR PARTY VOTES ON THEM"), no backtick, no widened token; the UI's new literal is "LOCKED". The `§630`/`PS-3c` mentions sit in comments, the XML doc, `Debug.Log` lines (the `LEVERS:` refusals, the driver's `SHOT:` line - `Testing`, outside the roots and a diagnostic line either way). Clean.

VERDICT: NOT READY as it stands. The bills are gated correctly and the gate's own mechanics (null order, layout, `GUI.enabled`, the dumps, meta-text) hold, but "role-gated levers" is not yet true of the desk: an opposition player still answers the AI government's cabinet decisions and foreign-policy meetings (and BLOCKS the turn until they do), appoints its ministers and its central-bank chair, reshuffles its cabinet, orders and retires its power plants beside the AI energy ministry, and pins a trade override live (findings 1-3) - each a write to the AI's book with no bill and no gate; and the election-night sentence still promises the player the levers §630 has just taken (finding 4). Close 1, 2 and 4 before the commit (gate the three resolvers and the fleet's `Place`/`Withdraw` on `PlayerGoverns`, stop rolling the meeting and blocking the turn for a country the player does not govern, cut the sentence); rule the junior partner's words (5); measure the 62-character caption on a 1280 film of the law pane and give the label a width or the family's `MeasuredLabel` (6); and name the harness's re-seat in the record, with a restore (9). The pending-bill-at-the-election behaviour is acceptable as the chamber's (8), with the budget-pause close as a note for the AI elections.

---

## Second reading

**Re-read (read-only).** `Assets/Scripts/Simulation/SimulationManager.cs` (`AdvanceCountryDayTick` 296-298, the cabinet roll at 3135-3141, `PlayerMayIntroduce` 2289-2300 with the junior-partner reason, `ResolveCabinetDecision` 1094, `ResolveForeignPolicyMeeting` 1141), `Assets/Scripts/UI/GameController.cs` (`DrawLeverLock` 11923-11931, the fourteen lock sites: 3988, 4013, 4230, 4705, 9153, 10081, 10127, 10565, 10611, 10657, 11040, 11390, 11730, 11907; `DrawFedChairCandidateButton` 4221-4239, `DrawCabinetCandidateButton` 10117-10135, the reshuffle block 10070-10090, the override site 10555-10570, `ResolveElectionVerdict`'s sentence 6647-6649, `BuildBudgetBillStatusText` 11064-11075, `UpdateFedChairSelectionState`, the clock hold at 767-769 and the blocking list at 6228-6250), `Assets/Scripts/UI/GameController.Energy.cs` (140-175), `Assets/Scripts/Testing/UiScreenshotDriver.cs` (1507-1545 and 3039-3120: `seatedBefore`, the restore, `WritePlaytestSave`), `Assets/Editor/TrajectorySentinelCheck.cs` (the `BaselineLabel` bump `ps3a` to `ps3c`), `Tools/film_scope.tsv` (the `dryps3c` row), `Assets/Scripts/Data/WorldFactory.cs` (the seeded chairs at 962/992/997), `Assets/Scripts/Simulation/ElectionSystem.cs` (`IsElectionTurn` 32-35), `Assets/Editor/PlayerRoleDiagnostic.cs` (the opposition reason at 64, unchanged and still matching). Not re-run: no Unity.

1. Finding 1 CLOSED, with one new DEFECT it uncovers. `SimulationManager.cs:298` rolls the meeting only where `PlayerMayIntroduce(countryId)`; `:3141` rolls cabinet decisions likewise (an AI country passes the gate at the `PlayerCountryId.Value != countryId` leg, so the five AI countries roll exactly as before - and roll nothing, having no ministers). A decision or meeting pending from BEFORE the flip still blocks the turn (`GameController.cs:767`, `:6241-6249`) and is still resolvable (`ResolveCabinetDecision`/`ResolveForeignPolicyMeeting` are ungated, by the stated reason: the player's own government rolled it) - so the block clears by the player's answer and cannot persist; consistent. Appoint (10127), Reshuffle (10081) and the chair's Appoint (4230) are locked. **The new DEFECT is the chair's clock hold.** `UpdateFedChairSelectionState` returns true - and holds the clock at `:767` - whenever `CurrentFedChair != null` and the coming turn is a term turn (`ElectionSystem.IsElectionTurn`), generating candidates without asking the role; it is cleared only by the Appoint at 4232 (`_fedChairCandidates = null`), which `DrawLeverLock` now never draws for a locked player. Sweden and Poland seed a chair (`WorldFactory.cs:992/997`, the USA at 962), so an S player in opposition reaches turn `ElectionCycle - 1`, the blocking list says "a Sveriges Riksbank governor appointment (Politics tab)", the tab shows one lock line per candidate and no button, and the run cannot advance - ever. Fix: `UpdateFedChairSelectionState` returns false (and generates no candidates) where `!PlayerMayIntroduce(PlayerCountryId)`, and the AI's country keeps its sitting chair (or the AI appoints one - a later part); the blocking list's line follows. This is the same shape as finding 8's latent pause, but reachable today.

2. Finding 2 CLOSED. `GameController.Energy.cs:149` defines `fleetLocked` (and `fleetLockedWhy`) BEFORE its first use at 150 - confirmed, the order is right. Both chips take `!can || fleetLocked` (162, 166), so `EnergyFleet.Place` is unreachable from the desk while locked; the head caption carries the reason through `PoliSimWidgets.MeasuredLabel` at the order column's `width` (guarded, unlike finding 6's label). `Withdraw` has no desk caller (grep) - fine. Note only: the "+ BUILDS · - RETIRES · ONE STEP PER CLICK" caption at 142 still says the chips act, and the LEAD TIME column still prints years for a fleet the player cannot order - cosmetic.

3. Finding 3 CLOSED. The Eurozone push (4013) is locked in the same form as the independent dial; the override's "Set override"/"Reset draft" (10565) and both appointments are behind the lock. The "This game deliberately hands you the central bank" paragraph above 3988 still reads to a locked player (NOTE, the same class as the energy caption).

4. Finding 4 CLOSED. `GameController.cs:6649`: "the government's levers leave your desk, and your party votes on its bills." - true of the build. `BuildBudgetBillStatusText` (`:11071`) returns "The budget is the government's to lay - your party votes on its bill." when locked, ahead of the process test - so the false "opens on your country's own fiscal-year date" is no longer shown to them (the incoming-window branch at 11066 is unreachable for them: `TryOpenBudgetProcess` returns before it opens).

5. Finding 5 CLOSED. `SimulationManager.cs:2298`: a junior partner reads "JUNIOR PARTNER · THE PRIME MINISTER'S PARTY INTRODUCES BILLS · YOUR PORTFOLIOS' LEVERS ARE NOT YET BUILT" - true of the code and of the spec's part 4. `PlayerRoleDiagnostic.cs:64` asserts the opposition string, which is unchanged. The masthead's " · JUNIOR PARTNER · THE GOVERNMENT IS THE AI'S" (`Desk.cs:267`) stands as before - NOTE. By hand the new string carries no `MetaTextCheck` token; it lives in `Simulation`, outside the roots either way.

6. Finding 7 CLOSED; the five new sites checked. `DrawLeverLock` (`:11926-11929`) saves `GUI.enabled`, sets true, draws, restores - no leak, and the Budget page's reason is now full ink. Layout: each of the five new sites replaces exactly one button with one `GUILayout.Label` inside groups that open and close around it (the chair's `BeginVertical(_boxStyle)/BeginHorizontal/BeginVertical` at 4223-4238, the minister's at 10119-10134, the reshuffle's `EndVertical` at 10090) - no group is left open. To the question asked: YES, the chair and cabinet candidate lists draw ONE LOCK LINE PER CANDIDATE - `DrawFedChairCandidateButton` and `DrawCabinetCandidateButton` are called once per candidate and each calls `DrawLeverLock()` in place of its Appoint; with three candidates the same 62-character sentence repeats three times down the page. Correct, not wrong, but a locked candidate list should say it once at its head (the energy plate's form) and draw the candidates without a button - and for the chair's list, finding 1 above says the list should not be generated at all.

7. Finding 9 CLOSED. `UiScreenshotDriver.cs:1507/3039` save `seatedBefore`; `:1529/:3120` restore it after the bills are staged. At 1529 the restore precedes `WritePlaytestSave` at 1534 (the write at 1543), so the dense save carries the sweep's party (S) with the government's bills pending before the chamber - a state the game itself can produce (finding 8's reading), and the same on the loaded film. `PlayerGoverns` flips back to false with the restore, so the AI ministry is on the book for every later day of both runs as before; the re-seat now changes no captured frame outside the bill-staging window. `Tools/film_scope.tsv` adds `dryps3c` (Sweden@1280x720, s630) for the lock caption; the coordinator reports the dry film measured 438 px in 1202 px rows with 0 overflows.

8. Finding 6 - the width - answered, one site still open. The measured 438 px at 1280 (bold Document at 9 px is ~7.06 px per character, wider than my 0.6-em estimate - the memory's "bold mono is wider at the floor" holds) fits every full-width ledger row (1202 px) by a wide margin and settles those sites. The law pane is the one site not measured: its `contentWidth` at 1280 is ~440 px by the split at 8354-8359, so a 438 px un-widthed label is AT the pane's edge - within a pixel or two of widening the scroll content. I still want the width there: `GUILayout.Width(contentWidth)` on the law site's label (it is the one site whose siblings all carry it), or the law pane filmed once at 1280 with S in opposition and a law selected - either closes it. The candidate lists (item 6 above) are box-width labels too, unmeasured; the same width would serve them.

9. NOTE - `TrajectorySentinelCheck.cs` bumps `BaselineLabel` to `ps3c`. The dump sets no player, so neither `PlayerMayIntroduce` nor the gated rolls can move it (finding 11) - `ps3c` should be byte-identical to `ps3a` as `ps3a` was to `ps2cal`; the line's comment still narrates `ps3a` and should say so of `ps3c` when the family is written.

VERDICT: NOT READY on one point, READY on the rest. Findings 1-5, 7 and 9 of the first reading are closed as described - the cabinet and meeting rolls are the government's, the fleet's chips and every appointment, reshuffle and override are behind the lock, the verdict and the budget's sentence tell the truth, the junior partner's words match the build, the harness restores its seat before the dense save. What blocks the commit is the chair's term: `UpdateFedChairSelectionState` still generates candidates and holds the clock for a locked player, and the only clear is the Appoint that the lock removes - a Sweden or Poland player in opposition deadlocks at the chair's first term turn. Gate the selection state on `PlayerMayIntroduce` (no candidates, no hold, the sitting chair stays) and the change is ready; then the law pane's label wants `GUILayout.Width(contentWidth)` or one 1280 film, and the candidate lists one lock line at their head rather than one per candidate.

---

## Third reading

**Re-read (read-only).** `Assets/Scripts/UI/GameController.cs` (`UpdateFedChairSelectionState` 3819-3840, the candidate assignments at 337/1070-1071/3834-3835/4235, the clock hold at 767-769, the blocking list at 6236-6239, `DrawLeverLock` 11925-11933, the law site 9150-9156), `Assets/Editor/TrajectorySentinelCheck.cs` (97).

1. OK - the chair's hold. `UpdateFedChairSelectionState` (`GameController.cs:3822`) returns false FIRST where `!PlayerMayIntroduce(PlayerCountryId)`, before the chair test and before the generation at 3834, so a locked player raises no hold at 767 and no candidates are generated. A stale list cannot be left behind by the game's own path: candidates exist only between their generation (the year before a term turn, `:3834`) and the Appoint (`:4235`, `= null`), and while they exist the hold at 767 stops the day loop, so no polling day - and no role flip - can arrive while a list stands; a list generated while governing is therefore always appointed before the role can change. One residual: the blocking list at `:6236` reads `_fedChairCandidates != null && Count > 0` directly rather than the gated method, and a save carries the list (`:1070-1071`, restored on load) - so a list can only reach a locked player through a save whose role is already locked, which the same rule prevented from generating one. Unreachable today; belt-and-braces would clear `_fedChairCandidates` in the gated branch so the two readers cannot disagree. NOTE.

2. OK - the law site's width. `DrawLeverLock(Rect? rect = null, float width = 0f)` (`:11925`); the call `DrawLeverLock(null, actionWidth)` compiles (a `null` literal converts to `Rect?`; the second argument is a positional `float`). With `width > 0` the label takes `GUILayout.Width(width)` (`:11930`) - the same `actionWidth` the sibling button takes at 9155 (`contentWidth + _labelStyle.margin.horizontal - _lawActionButtonStyle.margin.horizontal`), so the two elements request the same width; the label's own margin is the skin label's (`DeskCaption` zeroes padding, not margin), so its outer extent can differ from the button's by that margin difference - at most a few pixels, inside the pane, and `wordWrap = false` with an explicit width now CLIPS rather than widening the scroll content. Closes finding 6. The rect sites and the un-widthed sites are unchanged; the per-candidate repetition stays as noted (second reading, item 6).

3. OK - `TrajectorySentinelCheck.cs:97` narrates `ps3c`: "`ps3c` is BYTE-IDENTICAL to `ps3a` (the dump passes no bill and sets no player, so the role gate is never asked)", ahead of the earlier chain. Matches finding 11.

VERDICT: READY. The chair's deadlock is closed at its source (no candidates and no hold for a locked player; a list can never outlive the government that generated it), the law pane's label is widthed to its sibling button, and the sentinel's comment says what the family is. Carry as notes: clear `_fedChairCandidates` in the gated branch so the blocking list and the hold read the same state; one lock line at the head of a candidate list rather than one per candidate; the "hands you the central bank" and "+ BUILDS · - RETIRES" captions still read to a locked player; the masthead's junior-partner suffix.
