# Review — §641 AI no-confidence motions (2026-09-25)

Two independent readers, read-only (no build, no Unity run), on the diff of `Assets/Scripts/Simulation/SimulationManager.cs`
(`TryAiMotion`, `AiMotionCandidates`, `AskToBeDischarged`, `CanOrderExtraElection`, the standing refusals in `DischargeAndRound`),
`Assets/Scripts/Elections/GovernmentFormation.cs` (`PayoffIn`, `RefusalLines`), `Assets/Scripts/Elections/GovernmentRecord.cs`
(`StandingRefusals`), `Assets/Scripts/UI/GameController.cs` (`DeclarationHolds`, the confidence block) and `Assets/Editor/ConfidenceDiagnostic.cs`.

## First reading

Correct: the refusals mirrored from the player's verb (the rules sourced, a caretaker, a pending declaration or extra election, the tenth);
the vote counts a supporter-mover for it before the withdrawal; the round an AI motion is weighed against is the discharge's round; the payoff
matches the formation's; deterministic; the dumps cannot reach it.

1. **defect** — an AI motion against the player's government opened no hold; the week, and the player's only answer, could pass unseen.
   **Fixed:** the clock holds on a declaration against the player's government until it is answered - an extra election (6 kap. 7 §) or the
   discharge asked for (6 kap. 8 §, `AskToBeDischarged`, the 2021 route); the fold's banner names it.
2. **defect** — the player's refusal line lasted one round; a later AI motion's round could hand back the prime minister the player brought down.
   **Fixed:** `GovernmentRecord.StandingRefusals`, carried into the government the round forms and read by every later round until the next election.
3. **defect (fit to the ruling)** — the natural mover cannot fire in unperturbed play. **Stated** for Elias (corrected by the second reading, below).
4. **defect** — a motion in the week before polling day was weighed against a round that never runs. **Fixed:** none is taken up in that week.
5.–8. **test depth** — (a) vacuous; no doomed case; the aggrieved gate, the player's exclusion and the candidate order untested; (c) not compared
   to the promised round; parts 1 and 3 silent. **Fixed:** (a) asserts its premise; (b), (c0), (c1), (d), (c), (e), (f) each assert both premises;
   (c) compares the formed cabinet to the promised one; parts 1 and 3 assert no motion.
9. **nit** — a carried guard, the round computed once, `now`'s meaning. **Fixed.**
10. **premise** — in an AI motion the player's party votes by the model's lines. **Stated.**
11. **stale** — the withdrawal's doc and break text, the desk sentence, the section header. **Fixed.**

## Second reading (on the fixes)

Correct: the hold can always be released (the discharge verb's guards are the hold's conditions, and the discharge sets the caretaker first);
both day-loop gates, the pause flag and the banner are wired; motions never reach the signing screen; the harness cannot deadlock (it drives
the manager directly, its player in opposition); the clones share the record by reference; an election's formation starts without refusals.

1. **defect (minor)** — refusals survived an election that formed no government. **Fixed:** any election clears them.
2. **stale (honesty)** — the stated finding's reasons were wrong: the start's government is the record of record, not the round's; leaving a
   cabinet or a refusal line does not make a motion possible; the formateur was not built. **Fixed:** as built, no reachable path fires an AI
   motion; it becomes live where an installed government differs from the round - the formateur's proposals and installed 2026 records.
3. **stale** — part 3's message. **Fixed.**
4.–6. **test** — (a)'s premise, the invariant (tautological), (e) without a positive control. **Fixed:** (a) enumerates its premise; the invariant
   reads the round before each day and asserts the mover is seated in it; (e+) moves eight days before polling day.
7. **UI copy** — the discharge date under the hold, and the extra-election verb drawn enabled where 3 kap. 11 § refuses it. **Fixed:**
   "THE CLOCK WAITS ON YOUR ANSWER"; the verb drawn disabled with its reason (`CanOrderExtraElection`).
8. **latent freeze** — the hold lacked the rules guard the block has. **Fixed.**
9. **save** — no round trip for the refusals. **Fixed.**
10. **fixture hygiene** — one `try` for the cases. **Kept** (a throw is counted, never silent).
11. **performance** — the round may run daily once a candidate carries. **Stated**, unmeasured.
12. **coverage** — no plain opposition mover. **Fixed:** (g).
13. **the hold's release is exercised by no bar** (the dry film's player is in opposition). **Stated.**
